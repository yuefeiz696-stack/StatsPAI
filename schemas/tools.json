[
  {
    "description": "OLS with absorbed high-dimensional fixed effects (reghdfe-style).",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Regressors *excluding* the absorbed FEs and the constant (the constant is absorbed by any FE dimension).",
          "type": "string"
        },
        "cluster": {
          "description": "One-way or multi-way cluster variables for robust SEs. If provided, returns cluster-robust SEs (one-way: Liang-Zeger sandwich; multi-way: inclusion-exclusion Cameron-Gelbach-Miller).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "drop_singletons": {
          "default": true,
          "description": "drop_singletons parameter (bool).",
          "type": "boolean"
        },
        "fe": {
          "description": "Fixed-effect columns.",
          "type": "string"
        },
        "maxiter": {
          "default": 10000,
          "description": "Demean convergence controls.",
          "type": "integer"
        },
        "return_absorber": {
          "default": false,
          "description": "If True, also return the ``Absorber`` object for reuse.",
          "type": "boolean"
        },
        "solver": {
          "default": "map",
          "description": "Within-transformation backend. See :class:`Absorber`.",
          "enum": [
            "map",
            "lsmr",
            "lsqr"
          ],
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Demean convergence controls.",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "fe",
        "y"
      ],
      "type": "object"
    },
    "name": "absorb_ols"
  },
  {
    "description": "Adjust p-values for multiple comparisons.",
    "input_schema": {
      "properties": {
        "method": {
          "default": "holm",
          "description": "Adjustment method. One of: - ``'bonferroni'`` -- Bonferroni correction. - ``'holm'`` -- Holm (1979) step-down. - ``'bh'`` or ``'fdr'`` -- Benjamini-Hochberg FDR. For Romano-Wolf or Westfall-Young adjustments (which require the original data and bootstrap), use :func:`romano_wolf` directly.",
          "type": "string"
        },
        "pvalues": {
          "description": "Unadjusted p-values.",
          "type": "number"
        }
      },
      "required": [
        "pvalues"
      ],
      "type": "object"
    },
    "name": "adjust_pvalues"
  },
  {
    "description": "Fit an Accelerated Failure Time model by MLE.",
    "input_schema": {
      "properties": {
        "family": {
          "default": "weibull",
          "description": "family parameter (AFTFamily).",
          "enum": [
            "exponential",
            "weibull",
            "lognormal",
            "loglogistic"
          ],
          "type": "string"
        },
        "formula": {
          "description": "``\"duration + event ~ x1 + x2\"``.",
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "aft"
  },
  {
    "description": "Aggregate Callaway-Sant'Anna group-time ATTs into interpretable summaries \u2014 overall ATT, event-study by relative time, group-specific ATT(g), or calendar-time ATT(t).  Inference uses the multiplier bootstrap on the pre-stored influence functions, so SEs are correct under clustering at the unit level. Validation: validated. Assumptions: Same identifying assumptions as the source estimator (parallel trends, no anticipation, SUTVA); For dynamic aggregation: balanced panel within the requested event-time window (use balance_e). Pre-conditions: result was produced by sp.callaway_santanna or sp.did with staggered=True; result.detail contains the per-(g, t) ATT estimates and their influence functions. Failure modes: result.detail is empty or missing influence functions \u2192 Re-run sp.callaway_santanna; aggte requires the per-(g,t) influence functions.; Empty event-time aggregation (no overlapping cohorts) \u2192 Widen the (min_e, max_e) window or drop balance_e.. Alternatives: sp.callaway_santanna, sp.sun_abraham, sp.did_imputation. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "balance_e": {
          "description": "For dynamic: cap event time at \u00b1balance_e for balanced panel",
          "type": "integer"
        },
        "boot_type": {
          "default": "multiplier",
          "description": "Bootstrap variant",
          "enum": [
            "multiplier"
          ],
          "type": "string"
        },
        "bstrap": {
          "default": true,
          "description": "bstrap parameter (bool).",
          "type": "boolean"
        },
        "cband": {
          "default": true,
          "description": "Uniform confidence band",
          "type": "boolean"
        },
        "max_e": {
          "default": Infinity,
          "description": "max_e parameter (float).",
          "type": "number"
        },
        "min_e": {
          "default": -Infinity,
          "description": "min_e parameter (float).",
          "type": "number"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "na_rm": {
          "default": true,
          "description": "Drop ATT(g,t) cells with missing / infinite SE before aggregating",
          "type": "boolean"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "result": {
          "description": "Output of sp.callaway_santanna or sp.did with staggered=True",
          "type": "string"
        },
        "type": {
          "default": "simple",
          "description": "Aggregation type",
          "enum": [
            "simple",
            "dynamic",
            "group",
            "calendar"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "aggte"
  },
  {
    "description": "Augmented inverse-probability weighting (AIPW) \u2014 the canonical doubly-robust ATE estimator.  Cross-fits an outcome regression and a propensity model and combines them via the efficient-influence-function formula, so the estimate is consistent if either nuisance is correctly specified (Robins, Rotnitzky & Zhao 1994). Validation: validated. Assumptions: Unconfoundedness conditional on covariates (Y(0), Y(1) \u22a5 D | X); Overlap / common support: 0 < e(X) < 1 for all X with positive density; SUTVA. Pre-conditions: binary treatment column with both arms present; covariates must contain all confounders for unconfoundedness; no perfect overlap violations (0 < propensity < 1 in support). Failure modes: Propensity scores cluster near 0 or 1 \u2192 Trim to overlap region with sp.trimming() or switch to overlap-weighted ATE.; Cross-fit estimate has very wide CI \u2192 Increase n_folds or reduce covariate dimension; check for near-empty propensity strata.. Alternatives: sp.ipw, sp.dml, sp.tmle, sp.matching. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Confounders to adjust for",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "ATE",
          "description": "Target estimand",
          "enum": [
            "ATE",
            "ATT",
            "ATC"
          ],
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds (>= 2)",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment (0/1)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "aipw"
  },
  {
    "description": "Anderson-Rubin confidence set \u2014 re-export of",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "anderson_rubin_ci"
  },
  {
    "description": "Anderson-Rubin (1949) test \u2014 size-correct under weak instruments.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "endog": {
          "description": "Endogenous regressor (single).",
          "type": "string"
        },
        "exog": {
          "description": "Included exogenous controls.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h0": {
          "default": 0,
          "description": "Null hypothesis value for the endogenous coefficient.",
          "type": "number"
        },
        "instruments": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vcov": {
          "default": "HC1",
          "description": "Variance estimator used for the Olea-Pflueger effective F reported alongside AR.",
          "enum": [
            "HC0",
            "HC1",
            "classic"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments",
        "y"
      ],
      "type": "object"
    },
    "name": "anderson_rubin_test"
  },
  {
    "description": "Fit ARIMA(p,d,q) or SARIMAX. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "auto": {
          "default": false,
          "description": "If True, select (p, d, q) by AICc grid search (ignores ``order``).",
          "type": "boolean"
        },
        "exog": {
          "description": "Exogenous regressors (ARIMAX).",
          "type": "string"
        },
        "max_d": {
          "default": 2,
          "description": "Bounds for the auto search.",
          "type": "integer"
        },
        "max_p": {
          "default": 5,
          "description": "Bounds for the auto search.",
          "type": "integer"
        },
        "max_q": {
          "default": 5,
          "description": "Bounds for the auto search.",
          "type": "integer"
        },
        "order": {
          "default": [
            1,
            0,
            0
          ],
          "description": "order parameter (Tuple[int, int, int]).",
          "type": "integer"
        },
        "seasonal_order": {
          "description": "seasonal_order parameter (Optional[Tuple[int, int, int, int]]).",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "arima"
  },
  {
    "description": "Comprehensive assumption audit for any estimated model.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for tests.",
          "type": "number"
        },
        "result": {
          "description": "Estimated model result.",
          "type": "string"
        },
        "verbose": {
          "default": true,
          "description": "Print summary automatically.",
          "type": "boolean"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "assumption_audit"
  },
  {
    "description": "Reviewer-grade audit on a result. Returns the literature checklist (parallel-trends test, honest-DID, Bacon decomposition, placebo, balance, \u2026) with status per item and the concrete suggest_function to call to fill any missing high-importance check.",
    "input_schema": {
      "properties": {
        "result_id": {
          "description": "Result handle. Required unless you also pass a fitted result via the result kwarg (programmatic use).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "audit"
  },
  {
    "description": "Reviewer-grade audit on a previously-fitted result. Pass the result_id returned by an earlier tool call (with as_handle=true). Returns the same checklist sp.audit() produces \u2014 every robustness check the literature expects for the design, with status='present|missing|run' and concrete suggested_function names for the missing ones.",
    "input_schema": {
      "properties": {
        "result_id": {
          "description": "Handle returned by an earlier estimator call. Must be in the server result cache (LRU-evicted; refit if missing).",
          "type": "string"
        }
      },
      "required": [
        "result_id"
      ],
      "type": "object"
    },
    "name": "audit_result"
  },
  {
    "description": "Augmented Synthetic Control with ridge bias correction (Ben-Michael et al. 2021). Validation: certified parity evidence. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "treated_unit parameter (str).",
          "type": "string"
        },
        "treatment_time": {
          "description": "treatment_time parameter (int).",
          "type": "integer"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "augsynth"
  },
  {
    "description": "Race several meta-learners and return a scored leaderboard + winner. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support \u2192 Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner).. Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for both learner confidence intervals and the BLP-beta1 acceptance region used by the selection rule.",
          "type": "number"
        },
        "cate_model": {
          "description": "Override the default gradient-boosting models used for nuisance and final CATE fitting.",
          "type": "string"
        },
        "covariates": {
          "description": "Effect-modifier columns used as features for every nuisance and CATE model.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "learners": {
          "default": [
            "s",
            "t",
            "x",
            "r",
            "dr"
          ],
          "description": "Short codes of the meta-learners to race. Duplicates are ignored.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Bootstrap iterations for ATE standard error on non-DR learners (passed through to ``metalearner``).",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of folds used for both the shared nuisance cross-fit and each learner's honest CATE prediction.",
          "type": "integer"
        },
        "outcome_model": {
          "description": "Override the default gradient-boosting models used for nuisance and final CATE fitting.",
          "type": "string"
        },
        "propensity_model": {
          "description": "Override the default gradient-boosting models used for nuisance and final CATE fitting.",
          "type": "string"
        },
        "random_state": {
          "default": 42,
          "description": "Seed for all K-fold splits.",
          "type": "integer"
        },
        "score": {
          "default": "r_loss",
          "description": "Currently only ``'r_loss'`` is implemented. Reserved for future expansion.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment column name (values in {0, 1}).",
          "type": "string"
        },
        "y": {
          "description": "Outcome column name.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "auto_cate"
  },
  {
    "description": "Optuna-tuned CATE learner race \u2014 nuisance, per-learner, or both. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support \u2192 Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner).. Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "learners": {
          "default": [
            "s",
            "t",
            "x",
            "r",
            "dr"
          ],
          "description": "learners parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_trials": {
          "default": 25,
          "description": "Budget for the nuisance-tuning study (ignored when ``tune == 'per_learner'``).",
          "type": "integer"
        },
        "n_trials_per_learner": {
          "description": "Budget for each per-learner study. Defaults to ``max(5, n_trials // 3)``.",
          "type": "integer"
        },
        "per_learner_search_space": {
          "description": "Override default spaces. See :data:`DEFAULT_SEARCH_SPACE` and :data:`DEFAULT_PER_LEARNER_SEARCH_SPACE`. Passed through / see :func:`auto_cate`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "sampler": {
          "description": "sampler parameter (Optional[Any]).",
          "type": "string"
        },
        "search_space": {
          "description": "Override default spaces. See :data:`DEFAULT_SEARCH_SPACE` and :data:`DEFAULT_PER_LEARNER_SEARCH_SPACE`. Passed through / see :func:`auto_cate`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "timeout": {
          "description": "Wall-clock limit per study (seconds).",
          "type": "number"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "tune": {
          "default": "nuisance",
          "description": "Tuning regime: - ``'nuisance'`` \u2014 tune the shared outcome / propensity GBMs against held-out R-loss, then hand them to ``auto_cate``. (v0.9.5 behaviour.) - ``'per_learner'`` \u2014 keep default nuisance models; for each learner, tune its final-stage CATE model against held-out R-loss. - ``'both'`` \u2014 run ``'nuisance'`` first, then ``'per_learner'`` using the tuned nuisance. Most expensive; most thorough.",
          "enum": [
            "nuisance",
            "per_learner",
            "both"
          ],
          "type": "string"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "auto_cate_tuned"
  },
  {
    "description": "Run several staggered-DiD estimators side by side.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for reported CIs.",
          "type": "number"
        },
        "g": {
          "description": "First-treatment-period column (0 or NaN for never-treated).",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "methods": {
          "description": "Subset of ``{'cs', 'sa', 'bjs'}``. Defaults to all three.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "select_by": {
          "default": "median",
          "description": "How to pick the winner. ``'median'`` returns the candidate with the median point estimate across successes. A method name returns that specific candidate (useful for forcing a baseline).",
          "enum": [
            "median",
            "first_success",
            "cs",
            "sa",
            "bjs"
          ],
          "type": "string"
        },
        "t": {
          "description": "Calendar time column.",
          "type": "string"
        },
        "x": {
          "description": "Covariates (used by CS / SA / BJS where supported).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "g",
        "i",
        "t",
        "y"
      ],
      "type": "object"
    },
    "name": "auto_did"
  },
  {
    "description": "Race 2SLS, LIML, and JIVE on a single-endogenous IV spec.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Forwarded to each estimator where supported.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "endog": {
          "description": "Single endogenous regressor column.",
          "type": "string"
        },
        "exog": {
          "description": "Exogenous controls (included in all estimators).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Instrument(s). A scalar is promoted to a one-element list.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "methods": {
          "description": "Subset of ``{'2sls', 'liml', 'jive'}``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "select_by": {
          "default": "median",
          "description": "select_by parameter (str).",
          "enum": [
            "median",
            "first_success",
            "2sls",
            "liml",
            "jive"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments",
        "y"
      ],
      "type": "object"
    },
    "name": "auto_iv"
  },
  {
    "description": "Return list of all registered decomposition method names.",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "available_methods"
  },
  {
    "description": "Aggregate CATE predictions into ATE/ATT/ATC/ATO targets. Assumptions: Unconfoundedness given the covariates; Overlap / positivity; Honesty: separate subsamples are used to choose splits and to estimate effects. Pre-conditions: Covariates, treatment, and outcome with enough data to grow an honest forest. Failure modes: Calibration test rejects \u2014 the forest's heterogeneity is not well calibrated \u2192 Increase the sample / number of trees, or fall back to a doubly-robust learner.. Alternatives: sp.dml, sp.auto_cate, sp.tmle. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "T": {
          "description": "T parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "target_sample": {
          "default": "all",
          "description": "target_sample parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "average_treatment_effect"
  },
  {
    "description": "Goodman-Bacon (2021) decomposition: breaks the two-way fixed-effects DID estimator into its 2x2 comparison weights.  Reveals whether treated-vs-treated comparisons (which can have negative weights) dominate the estimate. Run this before trusting a TWFE-DID point estimate.",
    "input_schema": {
      "properties": {
        "id": {
          "type": "string"
        },
        "time": {
          "type": "string"
        },
        "treat": {
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y",
        "treat",
        "time",
        "id"
      ],
      "type": "object"
    },
    "name": "bacon_decomposition"
  },
  {
    "description": "Scatter plot of Goodman-Bacon decomposition.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "bacon_result": {
          "description": "Output from ``bacon_decomposition()``. Must contain ``'decomposition'`` DataFrame and ``'beta_twfe'``.",
          "type": "object"
        },
        "colors": {
          "description": "Map comparison type \u2192 color. Defaults provided.",
          "type": "object"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "bacon_result"
      ],
      "type": "object"
    },
    "name": "bacon_plot"
  },
  {
    "description": "Unified balance diagnostics for matching and weighting estimators.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariates to audit.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "logit",
          "description": "Propensity-score model when ``ps`` is not supplied.",
          "enum": [
            "logit",
            "probit",
            "gbm"
          ],
          "type": "string"
        },
        "ps": {
          "description": "Propensity scores. If omitted, estimated with ``method``.",
          "type": "string"
        },
        "threshold": {
          "default": 0.1,
          "description": "Balance threshold for absolute standardized mean differences.",
          "type": "number"
        },
        "treatment": {
          "description": "Binary treatment indicator.",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights after matching/weighting. If omitted, ATE inverse-propensity weights are computed from ``ps``.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "balance_diagnostics"
  },
  {
    "description": "Balance a panel by keeping only units observed in every time period.",
    "input_schema": {
      "properties": {
        "entity": {
          "description": "Entity (unit) identifier column.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        }
      },
      "required": [
        "entity",
        "time"
      ],
      "type": "object"
    },
    "name": "balance_panel"
  },
  {
    "description": "Love plot: covariate balance visualization (SMD dot plot).",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "figsize": {
          "default": [
            8
          ],
          "description": "Height auto-scales with number of covariates if None.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Result from ``match()`` or ``ebalance()``.",
          "type": "string"
        },
        "threshold": {
          "default": 0.1,
          "description": "SMD threshold lines.",
          "type": "number"
        },
        "title": {
          "description": "title parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "balanceplot"
  },
  {
    "description": "Bartik / shift-share IV estimator (Ad\u00e3o-Koles\u00e1r-Morales 2019; Borusyak-Hull-Jaravel 2022). Uses pre-period industry / group shares \u00d7 exogenous shocks as an instrument for local outcome exposure. Validation: validated. Assumptions: Exogeneity of shocks conditional on pre-period exposure structure (Borusyak-Hull-Jaravel); Shock-level IV: shocks are independent of region-level unobserved trends; Asymptotic framework: many shocks (L \u2192 \u221e) \u2014 check via sp.ssaggregate Herfindahl. Pre-conditions: pre-period shares are pre-determined (measured strictly before the outcome window); shocks are as-good-as-random conditional on unit-level controls; \u2265 50 regions for AKM shift-share SE to be well-sized. Failure modes: Herfindahl of shares too concentrated (one industry dominates) \u2192 Shift-share SE unreliable \u2014 use Ad\u00e3o-Koles\u00e1r-Morales shock-level SE via sp.shift_share_se.; First-stage F < 10 \u2192 Shares don't predict exposure enough \u2014 report weak-IV-robust CI (sp.anderson_rubin_ci).; Shocks correlate with pre-trends \u2192 Shock exogeneity fails \u2014 drop the violating shock dimension or add trend controls.. Alternatives: sp.iv, sp.shift_share_se, sp.shift_share_political, sp.shift_share_political_panel. Typi\u2026",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "shares": {
          "description": "Pre-period share column (e.g. industry share)",
          "type": "string"
        },
        "shocks": {
          "description": "Shock column (e.g. industry-level change)",
          "type": "string"
        },
        "time": {
          "description": "Time column (panel)",
          "type": "string"
        },
        "unit": {
          "description": "Region / unit identifier",
          "type": "string"
        },
        "y": {
          "description": "Outcome (e.g. local wage growth)",
          "type": "string"
        }
      },
      "required": [
        "shares",
        "shocks",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "bartik"
  },
  {
    "description": "Basque Country terrorism dataset (simulated).",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "basque_terrorism"
  },
  {
    "description": "Bauer-Sinning (2008) nonlinear Oaxaca-Blinder decomposition with",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "model": {
          "default": "logit",
          "description": "Model variant or parameterisation to fit.",
          "enum": [
            "logit",
            "probit"
          ],
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "reference parameter (int).",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "variant": {
          "default": "yun",
          "description": "variant parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "bauer_sinning"
  },
  {
    "description": "Estimate heterogeneous treatment effects using Bayesian Causal Forest. Validation: validated. Assumptions: Unconfoundedness and overlap; The BART/forest priors are appropriate for the outcome scale. Pre-conditions: Covariates, treatment, and outcome; a propensity model is fit internally to limit regularization-induced confounding. Failure modes: MCMC diagnostics fail to converge, or estimates are sensitive to the prior \u2192 Increase draws/tuning, re-scale the outcome, and report posterior diagnostics.. Alternatives: sp.dml, sp.auto_cate, sp.causal_forest. Typical minimum N: 250.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Bootstrap iterations for uncertainty quantification.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds for propensity estimation.",
          "type": "integer"
        },
        "n_trees_mu": {
          "default": 200,
          "description": "Number of trees for the prognostic function mu(X).",
          "type": "integer"
        },
        "n_trees_tau": {
          "default": 50,
          "description": "Number of trees for the treatment effect function tau(X). Fewer trees = stronger shrinkage toward homogeneous effects.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "bcf"
  },
  {
    "description": "BCF on PCA-factor scores of a high-dimensional exposure vector (arXiv:2601.16595, 2026).  Compresses exposures via SVD or user-supplied loadings, then fits one BCF per factor. Validation: validated. Assumptions: Unconfoundedness and overlap; The BART/forest priors are appropriate for the outcome scale. Pre-conditions: Covariates, treatment, and outcome; a propensity model is fit internally to limit regularization-induced confounding. Failure modes: MCMC diagnostics fail to converge, or estimates are sensitive to the prior \u2192 Increase draws/tuning, re-scale the outcome, and report posterior diagnostics.. Alternatives: sp.dml, sp.auto_cate, sp.causal_forest. Typical minimum N: 250.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "binarize": {
          "default": "median",
          "description": "binarize parameter (str).",
          "enum": [
            "median",
            "zero",
            "none"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "exposures": {
          "description": "exposures parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "loadings": {
          "description": "loadings parameter (DataFrame).",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_factors": {
          "default": 3,
          "description": "Number of factors.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "exposures",
        "y"
      ],
      "type": "object"
    },
    "name": "bcf_factor_exposure"
  },
  {
    "description": "Hierarchical Bayesian Causal Forest for longitudinal data (BCFLong) \u2014 allows mu_t(X), tau_t(X) to evolve across time with unit-level random intercepts. Validation: validated. Assumptions: Unconfoundedness and overlap; The BART/forest priors are appropriate for the outcome scale. Pre-conditions: Covariates, treatment, and outcome; a propensity model is fit internally to limit regularization-induced confounding. Failure modes: MCMC diagnostics fail to converge, or estimates are sensitive to the prior \u2192 Increase draws/tuning, re-scale the outcome, and report posterior diagnostics.. Alternatives: sp.dml, sp.auto_cate, sp.causal_forest. Typical minimum N: 250.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_trees_mu": {
          "default": 200,
          "description": "Number of trees mu.",
          "type": "integer"
        },
        "n_trees_tau": {
          "default": 50,
          "description": "Number of trees tau.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "outcome",
        "time",
        "treatment",
        "unit"
      ],
      "type": "object"
    },
    "name": "bcf_longitudinal"
  },
  {
    "description": "Bayesian Causal Forest for ordered / dose-level treatment (Zorzetto et al. 2026).  Estimates cumulative dose-response curves via chained BCF between consecutive levels. Validation: validated. Assumptions: Unconfoundedness and overlap; The BART/forest priors are appropriate for the outcome scale. Pre-conditions: Covariates, treatment, and outcome; a propensity model is fit internally to limit regularization-induced confounding. Failure modes: MCMC diagnostics fail to converge, or estimates are sensitive to the prior \u2192 Increase draws/tuning, re-scale the outcome, and report posterior diagnostics.. Alternatives: sp.dml, sp.auto_cate, sp.causal_forest. Typical minimum N: 250.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "baseline parameter (str).",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "n_trees_mu": {
          "default": 200,
          "description": "Number of trees mu.",
          "type": "integer"
        },
        "n_trees_tau": {
          "default": 50,
          "description": "Number of trees tau.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "bcf_ordinal"
  },
  {
    "description": "Benjamini-Hochberg (1995) FDR correction.",
    "input_schema": {
      "properties": {
        "pvalues": {
          "description": "Unadjusted p-values.",
          "type": "number"
        }
      },
      "required": [
        "pvalues"
      ],
      "type": "object"
    },
    "name": "benjamini_hochberg"
  },
  {
    "description": "Beta regression (Ferrari & Cribari-Neto 2004).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "link": {
          "default": "logit",
          "description": "Link for mean: 'logit', 'probit', 'cloglog'.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors for the mean equation.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome in (0, 1).",
          "type": "string"
        },
        "z": {
          "description": "Regressors for the precision equation. If None, constant precision.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "betareg"
  },
  {
    "description": "Beyond-average LATE (Xie-Wu 2025). Identifies the entire treatment-effect distribution among compliers under incomplete compliance, not just its mean. Validation: validated. Assumptions: Selection-on-observables (unconfoundedness + overlap) or, for IV variants, instrument validity; For IV-QTE: rank invariance / rank similarity (monotonicity of the structural quantile function). Pre-conditions: Covariates, treatment, and outcome; for IV-quantile methods, a valid instrument; Enough data to estimate the outcome distribution across quantiles. Failure modes: Estimated conditional quantiles cross (non-monotone), or tail quantiles are unstable \u2192 Use rearrangement / monotonization and avoid extreme quantiles where data are sparse.. Alternatives: sp.qte, sp.iv, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "instrument": {
          "description": "instrument parameter (str).",
          "type": "string"
        },
        "quantiles": {
          "description": "Quantiles \u03c4 at which to evaluate QTE (default 0.1..0.9 step 0.1)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "beyond_average_late"
  },
  {
    "description": "Top-level structured citation for a fitted result.",
    "input_schema": {
      "properties": {
        "result": {
          "description": "Any fitted result object exposing a ``.cite()`` method.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "bib_for"
  },
  {
    "description": "Return verified BibTeX entries from paper.bib (StatsPAI's single source of truth for citations). Pass one or more bib keys (e.g. 'callaway2021difference'). NEVER invent citations \u2014 call this tool instead. Unknown keys return an empty entry plus a list of close matches.",
    "input_schema": {
      "properties": {
        "keys": {
          "description": "Bib keys to look up. Most estimators advertise their key in agent_card.reference.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "keys"
      ],
      "type": "object"
    },
    "name": "bibtex"
  },
  {
    "description": "Bidirectional proximal causal inference (Min, Zhang & Luo 2025). Solves for both outcome and treatment bridges simultaneously in a single two-way regression system. Validation: validated. Assumptions: The proxies are valid negative controls (relevant to the confounder, excluded from the causal channel); A bridge function exists (completeness conditions hold). Pre-conditions: Treatment-inducing and outcome-inducing proxy variables (negative controls) for the unobserved confounder. Failure modes: Proxies are weak or invalid \u2014 the bridge function is poorly identified \u2192 Test proxy relevance, select stronger proxies, or fall back to sensitivity analysis.. Alternatives: sp.select_pci_proxies, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_w": {
          "description": "proxy_w parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "proxy_z parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "proxy_w",
        "proxy_z",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "bidirectional_pci"
  },
  {
    "description": "Bivariate probit model.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x1": {
          "description": "Regressors for equation 1.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x2": {
          "description": "Regressors for equation 2. If None, same as x1.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y1": {
          "description": "First binary outcome.",
          "type": "string"
        },
        "y2": {
          "description": "Second binary outcome.",
          "type": "string"
        }
      },
      "required": [
        "x1",
        "y1",
        "y2"
      ],
      "type": "object"
    },
    "name": "biprobit"
  },
  {
    "description": "Borusyak, Jaravel & Spiess (2024) imputation DID estimator. Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) \u2192 Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates.; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights \u2192 Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage).. Alternatives: sp.callaway_santanna, sp.did, sp.honest_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable for cluster-robust standard errors. Defaults to ``group`` (unit-level clustering).",
          "type": "string"
        },
        "controls": {
          "description": "Additional control covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "Column indicating the period of first treatment. Use ``np.inf``, ``np.nan``, or ``0`` for never-treated units.",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "horizon": {
          "description": "Relative time periods for event study estimates, e.g. ``list(range(-5, 6))``. If ``None``, reports only the overall ATT (no event study disaggregation).",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "bjs"
  },
  {
    "description": "Cluster-bootstrap joint Wald test for BJS pre-treatment coefficients.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "first_treat parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "horizon": {
          "description": "If omitted, inferred from ``result.model_info['event_study']``.",
          "type": "integer"
        },
        "n_boot": {
          "default": 300,
          "description": "Cluster-bootstrap replications. Clusters are sampled with replacement; unit ids are reassigned in the resampled frame so BJS refits cleanly.",
          "type": "integer"
        },
        "result": {
          "description": "Output of :func:`did_imputation` on ``data`` with a non-trivial ``horizon`` that covers negative values. Only its ``model_info['event_study']`` frame is consulted, to look up the observed pre-period point estimates that we re-test with a covariance-aware statistic. Same arguments you passed to the original :func:`did_imputation` call. Needed to re-run BJS on each cluster-bootstrap resample.",
          "type": "string"
        },
        "seed": {
          "description": "RNG seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "result",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "bjs_pretrend_joint"
  },
  {
    "description": "Best Linear Predictor (BLP) test for CATE heterogeneity. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support \u2192 Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner).. Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_folds": {
          "default": 5,
          "description": "Folds for propensity cross-fitting.",
          "type": "integer"
        },
        "result": {
          "description": "Result from ``metalearner()``.",
          "type": "string"
        },
        "treat": {
          "description": "Outcome and treatment column names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and treatment column names.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "result",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "blp_test"
  },
  {
    "description": "Bonferroni correction: ``p_adj = min(p * S, 1)``.",
    "input_schema": {
      "properties": {
        "pvalues": {
          "description": "Unadjusted p-values.",
          "type": "number"
        }
      },
      "required": [
        "pvalues"
      ],
      "type": "object"
    },
    "name": "bonferroni"
  },
  {
    "description": "General bootstrap inference: nonparametric, cluster, block. Percentile/BCa/normal CIs. Validation: validated. Assumptions: Observations (or clusters/blocks) are exchangeable under the resampling scheme used; cluster= resamples whole clusters, block= preserves within-block ordering for serial dependence; The statistic callable is a smooth, well-defined functional of the data so its sampling distribution is approximable by resampling; BCa intervals further assume an estimable bias/acceleration; percentile/normal CIs assume approximate pivotality of the resampled distribution. Pre-conditions: statistic is a deterministic callable returning a finite scalar on any resampled DataFrame; Enough independent resampling units (rows, clusters, or blocks) to populate n_boot replicates; Specify cluster= or block= when data are clustered/serially dependent rather than iid. Failure modes: Few clusters yields anti-conservative CIs / wildly unstable bootstrap distribution \u2192 Use a wild cluster bootstrap with Rademacher weights when the number of clusters is small (<~40).; Statistic raises or returns NaN on a resample (e.g., a level drops out), aborting the loop \u2192 Make the statistic robust to missing\u2026",
    "input_schema": {
      "properties": {
        "ci_method": {
          "default": "percentile",
          "description": "CI method",
          "enum": [
            "percentile",
            "bca",
            "normal"
          ],
          "type": "string"
        },
        "cluster": {
          "description": "Cluster variable for cluster bootstrap",
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "statistic": {
          "description": "Function f(df) -> float",
          "type": "string"
        }
      },
      "required": [
        "statistic"
      ],
      "type": "object"
    },
    "name": "bootstrap"
  },
  {
    "description": "Borusyak, Jaravel & Spiess (2024) imputation DID estimator. Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) \u2192 Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates.; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights \u2192 Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage).; Pre-trend joint test p < 0.05 (or underpowered at 0.10) \u2192 Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna.. Alternatives: sp.callaway_santanna, sp.did, sp.honest_did, sp.sun_abraham. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable for cluster-robust standard errors. Defaults to ``group`` (unit-level clustering).",
          "type": "string"
        },
        "controls": {
          "description": "Additional control covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "Column indicating the period of first treatment. Use ``np.inf``, ``np.nan``, or ``0`` for never-treated units.",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "horizon": {
          "description": "Relative time periods for event study estimates, e.g. ``list(range(-5, 6))``. If ``None``, reports only the overall ATT (no event study disaggregation).",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "borusyak_jaravel_spiess"
  },
  {
    "description": "User-friendly alias for :func:`sp.rd2d` (boundary discontinuity design). Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "boundary_rd"
  },
  {
    "description": "Masten-Poirier (2021) breakdown frontier for qualitative conclusions. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative \u2192 Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds.. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "assumption": {
          "default": "parallel_trends",
          "description": "Label for the identifying assumption being relaxed. Currently supports a generic linear violation model applicable to ``'parallel_trends'``, ``'exclusion_restriction'``, or ``'selection_on_observables'``.",
          "type": "string"
        },
        "estimate": {
          "description": "Point estimate of the treatment effect.",
          "type": "number"
        },
        "max_violation": {
          "default": 0.1,
          "description": "Maximum magnitude of the assumption violation to explore.",
          "type": "number"
        },
        "n_grid": {
          "default": 100,
          "description": "Grid resolution for the frontier.",
          "type": "integer"
        },
        "se": {
          "description": "Standard error of the estimate.",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se"
      ],
      "type": "object"
    },
    "name": "breakdown_frontier"
  },
  {
    "description": "Compute the breakdown value of M. Validation: validated. Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) \u2192 Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates.; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights \u2192 Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage).. Alternatives: sp.callaway_santanna, sp.did, sp.honest_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "e": {
          "default": 0,
          "description": "Relative time period.",
          "type": "integer"
        },
        "method": {
          "default": "smoothness",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "result": {
          "description": "DID result with event study.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "breakdown_m"
  },
  {
    "description": "Unified dispatcher for six causal-inference bridging theorems (2025-2026): DiD\u2261SC (Shi-Athey), EWM\u2261CATE (Ferman), IPW\u2261DR\u2261CB (Zhao-Percival), Bunching\u2261RDD (Lu-Wang-Xie), DR-via-Calibration (Zhang), Long-term-surrogate\u2261PCI (Imbens-Kallus-Mao-Wang). Reports both path estimates + doubly-robust recommendation. Validation: validated. Assumptions: The two estimation paths being bridged target the same causal estimand under the chosen bridging theorem (e.g., DID = synthetic control for kind='did_sc'); Each path's own identification holds (parallel trends / SC convex-hull / proximal completeness, depending on kind), so a non-rejected agreement test is meaningful; The doubly-robust combined estimate is consistent if at least one of the two bridged paths is correctly specified. Pre-conditions: kind is one of the supported bridges and the matching per-bridge kwargs are supplied; Panel/data shape matches the chosen bridge (e.g., did_sc needs unit, time, treated_unit, treatment_time); Both paths are estimable on the same sample so the agreement test is well-defined. Failure modes: Agreement test rejects: the two paths give materially different estimates \u2192 Treat divergence as evidence one bridgi\u2026",
    "input_schema": {
      "properties": {
        "kind": {
          "default": "Which bridge to invoke",
          "description": "kind parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "name": "bridge"
  },
  {
    "description": "One-line agent-friendly brief for a fitted result. Cheaper than calling brief_result if you already have the result object in scope.",
    "input_schema": {
      "properties": {
        "result_id": {
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "brief"
  },
  {
    "description": "Return the one-line agent-friendly brief for a fitted result. Uses sp.brief(). Useful when an agent wants to summarise a chained workflow without paying for the full JSON payload again.",
    "input_schema": {
      "properties": {
        "result_id": {
          "description": "Handle to a previously-fitted result.",
          "type": "string"
        }
      },
      "required": [
        "result_id"
      ],
      "type": "object"
    },
    "name": "brief_result"
  },
  {
    "description": "Estimate bunching at a policy threshold. Assumptions: The counterfactual density would be smooth through the threshold absent the policy; Excess mass at the threshold reflects the behavioural elasticity of interest; No other discontinuity coincides with the threshold. Pre-conditions: A behavioural choice variable (earnings, hours, ...) with a known kink or notch in the budget/choice set; A visible empirical density of the running variable around the threshold. Failure modes: Round-number heaping or a coincident policy contaminates the bunching mass \u2192 Exclude heaping points, widen the excluded region, and test the counterfactual polynomial order.. Alternatives: sp.rdrobust, sp.rkd. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "bin_width": {
          "description": "Width of bins. If None, computed from data range / n_bins.",
          "type": "number"
        },
        "bunch_region": {
          "description": "(lower, upper) bounds of the bunching region. If None, uses [threshold - 2*bin_width, threshold + 2*bin_width].",
          "type": "number"
        },
        "design": {
          "default": "kink",
          "description": "'kink' or 'notch'.",
          "type": "string"
        },
        "dt": {
          "description": "Change in marginal tax rate at the kink (for elasticity). E.g., 0.10 for a 10pp increase.",
          "type": "number"
        },
        "exclude_region": {
          "description": "Same as bunch_region unless otherwise specified.",
          "type": "number"
        },
        "n_bins": {
          "default": 50,
          "description": "Number of bins on each side of the threshold.",
          "type": "integer"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Bootstrap iterations for standard errors.",
          "type": "integer"
        },
        "poly_order": {
          "default": 7,
          "description": "Order of the counterfactual polynomial.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "running_var": {
          "description": "Name of the running variable (e.g., income).",
          "type": "string"
        },
        "threshold": {
          "description": "Policy threshold (kink/notch point).",
          "type": "number"
        }
      },
      "required": [
        "running_var",
        "threshold"
      ],
      "type": "object"
    },
    "name": "bunching"
  },
  {
    "description": "Bayesian VAR with Minnesota (Litterman) prior.",
    "input_schema": {
      "properties": {
        "lags": {
          "default": 4,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "lambda1": {
          "default": 0.1,
          "description": "Overall tightness (smaller = stronger shrinkage toward RW).",
          "type": "number"
        },
        "lambda2": {
          "default": 0.5,
          "description": "Cross-variable shrinkage relative to own-lag.",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "bvar"
  },
  {
    "description": "Calibrate the strength of an unobserved confounder required to",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "observed_r2_outcome": {
          "description": "Partial-R\u00b2 of the observed covariate(s) with Y (resp. D). Used to benchmark \"1x as confounding as observed\" / \"2x\" etc.",
          "type": "number"
        },
        "observed_r2_treatment": {
          "description": "Partial-R\u00b2 of the observed covariate(s) with Y (resp. D). Used to benchmark \"1x as confounding as observed\" / \"2x\" etc.",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "target_estimate": {
          "default": 0.0,
          "description": "Effect value to explain away.",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "observed_r2_outcome",
        "observed_r2_treatment",
        "se"
      ],
      "type": "object"
    },
    "name": "calibrate_confounding_strength"
  },
  {
    "description": "BLP-of-CATE calibration test (Chernozhukov-Demirer-Duflo-Fernandez-Val 2020). Assumptions: Unconfoundedness given the covariates; Overlap / positivity; Honesty: separate subsamples are used to choose splits and to estimate effects. Pre-conditions: Covariates, treatment, and outcome with enough data to grow an honest forest. Failure modes: Calibration test rejects \u2014 the forest's heterogeneity is not well calibrated \u2192 Increase the sample / number of trees, or fall back to a doubly-robust learner.. Alternatives: sp.dml, sp.auto_cate, sp.tmle. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "T": {
          "description": "If not given, the forest's stored training arrays are used.",
          "type": "string"
        },
        "X": {
          "description": "If not given, the forest's stored training arrays are used.",
          "type": "string"
        },
        "Y": {
          "description": "If not given, the forest's stored training arrays are used.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for reported CIs.",
          "type": "number"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "calibration_test"
  },
  {
    "description": "California Proposition 99 tobacco control dataset.",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "california_prop99"
  },
  {
    "description": "California Proposition 99 tobacco dataset (simulated, extended).",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "california_tobacco"
  },
  {
    "description": "Staggered DID (Callaway-Sant'Anna 2021): group-time ATT with doubly-robust, IPW, or regression-adjusted estimators. Robust to heterogeneous treatment effects where TWFE fails. Requires a cohort column g (first-treatment period; 0 = never-treated).",
    "input_schema": {
      "properties": {
        "control_group": {
          "default": "nevertreated",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "estimator": {
          "default": "dr",
          "enum": [
            "dr",
            "ipw",
            "reg"
          ],
          "type": "string"
        },
        "g": {
          "description": "First-treatment-period cohort column (0 for never-treated).",
          "type": "string"
        },
        "i": {
          "description": "Unit ID column",
          "type": "string"
        },
        "t": {
          "description": "Time column",
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y",
        "g",
        "t",
        "i"
      ],
      "type": "object"
    },
    "name": "callaway_santanna"
  },
  {
    "description": "Cardinality matching \u2014 maximise the number of matched pairs subject Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap \u2014 extreme propensity scores or few acceptable matches \u2192 Trim or restrict to the common-support region and report the discarded units.; Covariate imbalance remains after matching/weighting \u2192 Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences.. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "smd_tolerance": {
          "default": 0.1,
          "description": "smd_tolerance parameter (float).",
          "type": "number"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "outcome",
        "treatment"
      ],
      "type": "object"
    },
    "name": "cardinality_match"
  },
  {
    "description": "Group-level average treatment effects. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support \u2192 Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner).. Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for CIs.",
          "type": "number"
        },
        "by": {
          "description": "Column name to group by, or 'cate' to group by CATE quartiles.",
          "type": "string"
        },
        "n_groups": {
          "default": 4,
          "description": "Number of quantile groups when ``by='cate'`` or when the grouping variable is continuous.",
          "type": "integer"
        },
        "result": {
          "description": "Result from ``metalearner()``.",
          "type": "string"
        }
      },
      "required": [
        "by",
        "result"
      ],
      "type": "object"
    },
    "name": "cate_by_group"
  },
  {
    "description": "Evaluate any CATE estimator via RATE / AUTOC / Qini (Yadlowsky 2025). Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support \u2192 Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner).. Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "T": {
          "description": "Observed outcome and binary treatment.",
          "type": "string"
        },
        "X": {
          "description": "Required if any of ``e_hat / m_hat / mu1_hat / mu0_hat`` is None; cross-fit nuisances are estimated with GBM defaults.",
          "type": "string"
        },
        "Y": {
          "description": "Observed outcome and binary treatment.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cate": {
          "description": "Estimated CATE :math:`\\hat\\tau(X_i)` from any estimator.",
          "type": "string"
        },
        "clip": {
          "default": 0.02,
          "description": "Propensity clip.",
          "type": "number"
        },
        "e_hat": {
          "description": "Pre-computed nuisance predictions (e.g., from the same estimator that produced ``cate``). If provided, no internal cross-fitting runs.",
          "type": "string"
        },
        "m_hat": {
          "description": "Pre-computed nuisance predictions (e.g., from the same estimator that produced ``cate``). If provided, no internal cross-fitting runs.",
          "type": "string"
        },
        "mu0_hat": {
          "description": "Pre-computed nuisance predictions (e.g., from the same estimator that produced ``cate``). If provided, no internal cross-fitting runs.",
          "type": "string"
        },
        "mu1_hat": {
          "description": "Pre-computed nuisance predictions (e.g., from the same estimator that produced ``cate``). If provided, no internal cross-fitting runs.",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "q_grid": {
          "default": 100,
          "description": "Grid of q values to evaluate.",
          "type": "integer"
        },
        "random_state": {
          "default": 0,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "target": {
          "default": "AUTOC",
          "description": "Which scalar headline to emit; both are computed and returned.",
          "enum": [
            "AUTOC",
            "QINI"
          ],
          "type": "string"
        }
      },
      "required": [
        "T",
        "Y",
        "cate"
      ],
      "type": "object"
    },
    "name": "cate_eval"
  },
  {
    "description": "Plot group-level CATEs with confidence intervals.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "color parameter (str).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group_df": {
          "description": "Output from ``cate_by_group()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "group_df"
      ],
      "type": "object"
    },
    "name": "cate_group_plot"
  },
  {
    "description": "Plot the CATE distribution.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "color parameter (str).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "default": "hist",
          "description": "'hist' for histogram, 'kde' for kernel density, 'both'.",
          "type": "string"
        },
        "result": {
          "description": "Result from ``metalearner()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "cate_plot"
  },
  {
    "description": "Descriptive statistics of the CATE distribution.",
    "input_schema": {
      "properties": {
        "result": {
          "description": "Result from ``metalearner()`` containing ``model_info['cate']``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "cate_summary"
  },
  {
    "description": "End-to-end causal workflow: diagnose -> recommend estimator -> fit -> run robustness -> return result.  The one-shot entry point that lets an agent analyse a dataset in a single call without orchestrating stages itself.",
    "input_schema": {
      "properties": {
        "cohort": {
          "type": "string"
        },
        "covariates": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "design": {
          "enum": [
            "did",
            "rd",
            "iv",
            "observational",
            "panel",
            "rct"
          ],
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "instrument": {
          "type": "string"
        },
        "running_var": {
          "type": "string"
        },
        "time": {
          "type": "string"
        },
        "treatment": {
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "causal"
  },
  {
    "description": "Causal-discovery dispatcher \u2014 article-facing alias.",
    "input_schema": {
      "properties": {
        "method": {
          "default": "notears",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "variables": {
          "description": "variables parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "causal_discovery"
  },
  {
    "description": "Causal deep Q-network (Li, Zhang, Bareinboim 2025, arXiv:2510.21110) for offline policy learning under unobserved confounding. Learns a confounding-robust Q-function via bootstrap data augmentation. Validation: validated. Assumptions: Sequential ignorability: no unobserved confounders of actions and outcomes; Positivity: the behaviour policy explores all evaluated actions; The environment satisfies the assumed (Markov) dynamics. Pre-conditions: Logged trajectories (states, actions, rewards) from a known or estimable behaviour policy. Failure modes: Poor behaviour-policy coverage \u2014 the target policy queries unseen state-action regions \u2192 Use offline-safe / pessimistic methods and report effective sample size of the importance weights.. Alternatives: sp.offline_safe_policy, sp.policy_value. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "action": {
          "description": "action parameter (str).",
          "type": "string"
        },
        "gamma": {
          "default": 0.95,
          "description": "gamma parameter (float).",
          "type": "number"
        },
        "n_episodes": {
          "default": 50,
          "description": "Number of episodes.",
          "type": "integer"
        },
        "next_states": {
          "description": "next_states parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "reward": {
          "description": "reward parameter (str).",
          "type": "string"
        },
        "states": {
          "description": "states parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "terminal": {
          "description": "terminal parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "action",
        "reward",
        "states"
      ],
      "type": "object"
    },
    "name": "causal_dqn"
  },
  {
    "description": "Causal Forest for heterogeneous treatment effect estimation (CATE). Validation: certified parity evidence. Assumptions: Unconfoundedness: Y(d) \u22a5 D | X; Overlap: 0 < P(D=1 | X) < 1 for the estimand support; Honest splitting: splits and estimates use disjoint samples (enforced by default). Pre-conditions: formula uses pipe separator: 'y ~ treatment | x_1 + x_2 + ...'; treatment is binary 0/1 (use sp.multi_arm_forest for multi-valued); covariates are numeric; encode categoricals beforehand. Failure modes: Calibration test (sp.calibration_test) rejects \u2192 CATE predictions are miscalibrated \u2014 increase n_trees, add variables, or switch to a DR-Learner.; Variance of CATE estimates too large to be useful \u2192 Need more observations or narrower conditioning set; consider GATE on discrete subgroups.; Extreme propensity scores in part of the covariate space \u2192 Trim to overlap region via sp.trimming or restrict estimand to overlap support.. Alternatives: sp.metalearner, sp.dml, sp.multi_arm_forest, sp.iv_forest. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "'y ~ treatment | x1 + x2' (pipe separates covariates)",
          "type": "string"
        },
        "n_trees": {
          "default": 100,
          "description": "Number of trees.",
          "type": "integer"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "causal_forest"
  },
  {
    "description": "Bayesian structural time series for causal impact analysis. Validation: validated. Assumptions: No simultaneous shocks affect treated and control series differently at intervention; Pre-period relationship extrapolates into the post-period absent treatment. Pre-conditions: Observed time series has a clearly defined intervention date; Pre-intervention period is long enough to fit the counterfactual model. Failure modes: Poor pre-period fit or unstable posterior predictive interval \u2192 Add controls, lengthen the pre-period, or use synthetic control as a robustness check.. Alternatives: sp.synth, sp.sequential_sdid, sp.local_projections. Typical minimum N: 30.",
    "input_schema": {
      "properties": {
        "intervention_time": {
          "description": "Date/index of intervention",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "intervention_time",
        "outcome"
      ],
      "type": "object"
    },
    "name": "causal_impact"
  },
  {
    "description": "Generate a synthetic causal-RL benchmark dataset. Assumptions: Sequential ignorability: no unobserved confounders of actions and outcomes; Positivity: the behaviour policy explores all evaluated actions; The environment satisfies the assumed (Markov) dynamics. Pre-conditions: Logged trajectories (states, actions, rewards) from a known or estimable behaviour policy. Failure modes: Poor behaviour-policy coverage \u2014 the target policy queries unseen state-action regions \u2192 Use offline-safe / pessimistic methods and report effective sample size of the importance weights.. Alternatives: sp.offline_safe_policy, sp.policy_value. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "confounding_strength": {
          "default": 0.5,
          "description": "Magnitude of unmeasured confounding U \u2192 (action, reward).",
          "type": "number"
        },
        "n_episodes": {
          "default": 1000,
          "description": "Number of episodes.",
          "type": "integer"
        },
        "name": {
          "default": "confounded_bandit",
          "description": "'confounded_targeting', 'confounded_routing'}",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "causal_rl_benchmark"
  },
  {
    "description": "Covariate-Balancing Propensity Score estimator (Imai-Ratkovic 2014). Validation: validated. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap \u2014 extreme propensity scores or few acceptable matches \u2192 Trim or restrict to the common-support region and report the discarded units.; Covariate imbalance remains after matching/weighting \u2192 Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences.. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "add_intercept": {
          "default": true,
          "description": "Prepend a constant to the covariate matrix.",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates entering the logit score.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "ATE",
          "description": "estimand parameter (Literal['ATE', 'ATT']).",
          "enum": [
            "ATE",
            "ATT"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary 0/1 treatment column.",
          "type": "string"
        },
        "trim": {
          "default": 0.0,
          "description": "Optional pscore clip for stability.",
          "type": "number"
        },
        "variant": {
          "default": "over",
          "description": "'exact': just-identified CBPS (only balance moments). 'over': over-identified CBPS (MLE + balance, solved via two-step GMM).",
          "enum": [
            "exact",
            "over"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "cbps"
  },
  {
    "description": "Chernozhukov-Fern\u00e1ndez-Val-Melly (2013) counterfactual decomposition.",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "ks_test": {
          "default": true,
          "description": "ks_test parameter (bool).",
          "type": "boolean"
        },
        "n_thresh": {
          "default": 40,
          "description": "Number of thresh.",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "Same convention as ``machado_mata`` / ``melly_decompose``: ``reference=0`` builds the counterfactual from A's distribution regression coefficients applied to B's X (F_{Y<0|1>}), opposite to the reweighting convention in ``dfl_decompose``.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "tau_grid": {
          "description": "Grid of tau values to evaluate.",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "cfm_decompose"
  },
  {
    "description": "Design-level identification diagnostics: bad controls, overlap, cohort sizes, IV first-stage F, clustering.  Run BEFORE fitting any estimator to surface design problems.",
    "input_schema": {
      "properties": {
        "cohort": {
          "type": "string"
        },
        "covariates": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "design": {
          "enum": [
            "did",
            "rd",
            "iv",
            "observational",
            "panel",
            "rct",
            "cross-section"
          ],
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "instrument": {
          "type": "string"
        },
        "running_var": {
          "type": "string"
        },
        "strict": {
          "default": false,
          "description": "Raise IdentificationError on BLOCKERS.",
          "type": "boolean"
        },
        "time": {
          "type": "string"
        },
        "treatment": {
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "check_identification"
  },
  {
    "description": "Chilean-style household income with urban/rural gap.",
    "input_schema": {
      "properties": {
        "n": {
          "default": 2500,
          "description": "n parameter (int).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "chilean_households"
  },
  {
    "description": "Changes-in-Changes (Athey & Imbens 2006). Nonparametric quantile DiD that identifies the full counterfactual outcome distribution for treated units, not just the mean. Reports quantile treatment effects (QTE) via empirical-CDF transformation; bootstrap SE. Validation: validated. Assumptions: Rank-invariance of untreated potential outcomes across periods; Time-invariant group-level production technology (distributional DiD); SUTVA. Pre-conditions: continuous-ish outcome with sufficient support overlap between treated and control; 2 periods, 2 groups. Failure modes: Discrete outcome with few support points \u2192 CIC quantile transformation degenerates; use sp.qte or sp.drdid for mean effects.. Alternatives: sp.qte, sp.drdid, sp.did_2x2. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "group": {
          "description": "Treatment-group indicator (0/1)",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_grid": {
          "default": 100,
          "description": "Grid size for inverse-CDF mapping",
          "type": "integer"
        },
        "quantiles": {
          "description": "Quantile grid (default: deciles)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Period indicator (0=pre, 1=post)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "cic"
  },
  {
    "description": "McFadden's conditional (fixed-effect) logit for choice data. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``\"chosen ~ price + quality\"``.",
          "type": "string"
        },
        "group": {
          "description": "Variable identifying the choice set / decision-maker.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Alternative-specific (and/or individual-specific interacted with alternative dummies) covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Binary indicator: 1 = chosen, 0 = not chosen.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "clogit"
  },
  {
    "description": "Complementary log-log regression via maximum likelihood.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "at_values": {
          "description": "Variable values for ``marginal_effects='at'``.",
          "type": "object"
        },
        "cluster": {
          "description": "Column name for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Formula like ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "marginal_effects": {
          "description": "``'average'`` (AME), ``'mean'`` (MEM), or ``'at'`` (MER).",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum Newton-Raphson iterations.",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'nonrobust'`` for MLE SE, ``'hc1'`` / ``'robust'`` for sandwich SE.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance on log-likelihood change.",
          "type": "number"
        },
        "weights": {
          "description": "Column name for frequency/analytic weights.",
          "type": "string"
        },
        "x": {
          "description": "Regressor names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "cloglog"
  },
  {
    "description": "Cluster-based CATE estimator. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support \u2192 Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner).. Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_clusters": {
          "default": 4,
          "description": "Number of K-means clusters.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "cluster_cate"
  },
  {
    "description": "Cluster-randomised trial under cross-cluster interference (Ding et al. 2025). Estimates direct + spillover effects when treatment of one cluster affects outcomes in adjacent clusters. Validation: validated. Assumptions: Partial interference: spillovers operate within clusters but not across cluster boundaries; Exposure mapping correctly captured by neighbour_treat_share (user-precomputed share of treated neighbours); Cluster-level treatment is binary and randomized (cluster RCT). Pre-conditions: Cluster identifier column plus individual-level outcome; Cluster-level binary treatment column; Precomputed neighbour_treat_share column from spatial/network adjacency. Failure modes: neighbour_treat_share missing or not a valid 0-1 share \u2192 Precompute the treated-neighbour share per cluster from your adjacency matrix before calling.; Too few clusters for cluster-robust inference \u2192 Increase the number of clusters or use a design with weaker cluster requirements.. Alternatives: sp.inward_outward_spillover, sp.network_hte, sp.interference, sp.spillover. Typical minimum N: 30.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "exposure": {
          "description": "Column with neighbours' treatment share",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "exposure",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "cluster_cross_interference"
  },
  {
    "description": "Return cluster-robust standard errors (diagonal sqrt of vcov).",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "clusters": {
          "description": "Cluster labels for clustered inference.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "resid": {
          "description": "resid parameter (np.ndarray).",
          "type": "string"
        }
      },
      "required": [
        "X",
        "clusters",
        "resid"
      ],
      "type": "object"
    },
    "name": "cluster_robust_se"
  },
  {
    "description": "Cohort-anchored event study. Instead of averaging across cohorts at each relative-time bin (which can contaminate leads / lags with other cohorts' dynamics), estimates separate event-study paths per cohort and then aggregates with cohort weights. Successor to the Rambachan-Roth sensitivity-friendly specification. Validation: validated. Assumptions: Parallel trends hold within each treatment cohort relative to never-treated units (cohort-anchored, not pooled TWFE); No anticipation: outcomes in pre-event periods unaffected by future treatment; Treatment is an absorbing first-treatment event (staggered adoption, treat encodes first period, 0 = never-treated). Pre-conditions: Long-format balanced/unbalanced panel with id and time columns; treat column gives first-treatment period per unit (0 = never-treated), with at least one never-treated cohort; Enough pre/post periods to fill the requested leads/lags event-time window. Failure modes: All units treated in same period / no never-treated cohort to anchor against \u2192 Supply a never-treated comparison group or switch to a not-yet-treated control design; Requested leads/lags exceed available pre/post periods so event-time cells are empty\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "lags": {
          "default": 5,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "leads": {
          "default": 3,
          "description": "leads parameter (int).",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "cohort_anchored_event_study"
  },
  {
    "description": "Per-cohort event study plot (overlay).",
    "input_schema": {
      "properties": {
        "aggregate_color": {
          "default": "#2C3E50",
          "description": "Color for aggregate line.",
          "type": "string"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "ci": {
          "default": true,
          "description": "Show confidence intervals for each cohort.",
          "type": "boolean"
        },
        "ci_alpha": {
          "default": 0.08,
          "description": "CI band transparency.",
          "type": "number"
        },
        "figsize": {
          "default": [
            12,
            7
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "palette": {
          "description": "Colors for each cohort. Auto-generated if None.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Result from ``callaway_santanna()`` or ``did(method='cs')``. Must have ``detail`` with 'group', 'relative_time', 'att' columns, and ``model_info['event_study']`` for aggregate.",
          "type": "string"
        },
        "show_aggregate": {
          "default": true,
          "description": "Overlay the aggregate event study line.",
          "type": "boolean"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "cohort_event_study_plot"
  },
  {
    "description": "Run multiple estimators on the same data and compare.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Panel unit ID.",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (str).",
          "type": "string"
        },
        "method_hints": {
          "description": "Per-method keyword overrides, merged with the shared kwargs when dispatching each estimator. Structure:: {'proximal': {'proxy_z': ['z'], 'proxy_w': ['w']}, 'msm': {'time_varying': ['L_lag']}, 'principal_strat': {'strata': 's'}} **Collision rule** (docs/ROADMAP.md \u00a76): per-method hints take precedence over the shared kwargs for the method they name. If the top-level ``covariates=['age']`` disagrees with ``method_hints={'proximal': {'covariates': ['age', 'educ']}}``, proximal uses the hint and every other method uses the shared arg. A ``UserWarning`` fires on conflict so the override is visible in the log.",
          "type": "object"
        },
        "methods": {
          "description": "Estimators to compare. Default auto-selects based on data. Classical options: ``'ols'``, ``'matching'``, ``'ipw'``, ``'aipw'``, ``'dml'``, ``'g_computation'``, ``'causal_forest'``, ``'did'``, ``'panel_fe'``. Hint-driven Sprint-B options (require ``method_hints``): ``'proximal'``, ``'msm'``, ``'principal_strat'``, ``'mediate'``, ``'mediate_interventional'``, ``'front_door'``. Each needs method-specific kwargs the shared signature does not expose (proxy_z/proxy_w, time_varying, strata, mediator, etc.) \u2014 pass them through ``method_hints``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time variable.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment variable (binary).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "compare_estimators"
  },
  {
    "description": "Fit multiple meta-learners and compare their ATE estimates. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support \u2192 Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner).. Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate / effect modifier variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "learners": {
          "description": "Which learners to compare. Default: all five ('s','t','x','r','dr'). Additional arguments passed to ``metalearner()``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "compare_metalearners"
  },
  {
    "description": "Moreira (2003) CLR confidence set \u2014 re-export of",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "conditional_lr_ci"
  },
  {
    "description": "Unified conformal causal inference dispatcher. kind= selects the estimator: 'cate' / 'counterfactual' / 'ite' (Lei-Cand\u00e8s 2021 base) / 'weighted' (TBCR 2019 primitive) / 'density' / 'multidp' / 'debiased' / 'fair' (2025-2026 frontier) / 'continuous' (dose-response) / 'interference' (cluster-exchangeable).  Kwargs pass through to the target function; see sp.conformal_family guide. Validation: validated. Assumptions: Exchangeability of calibration and test points (base case); For kind='weighted': known or estimable density ratio between calibration and test; For kind='cate' / 'ite': selection-on-observables with correct propensity / outcome model. Pre-conditions: calibration sample disjoint from training sample (auto-split or user-supplied); exchangeability between calibration and test distributions (weighted variants for covariate shift); for CATE / ITE variants: unconfoundedness + overlap on covariates. Failure modes: Calibration and test distributions differ (covariate shift) \u2192 Use kind='weighted' with estimated density ratios.; Calibration set too small \u2014 intervals wide \u2192 Increase calibration sample or raise alpha; coverage gets loose below ~100.; Miscalibrated nuisance (propens\u2026",
    "input_schema": {
      "properties": {
        "kind": {
          "default": "cate",
          "description": "Conformal estimator \u2014 call sp.conformal_available_kinds() for the full list.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "conformal"
  },
  {
    "description": "Counterfactual-fair conformal prediction for ITE (2025). Wraps standard conformal ITE intervals with a demographic-parity adjustment, giving distribution-free coverage under protected-attribute shifts. Validation: validated. Assumptions: Exchangeability for conformal coverage, applied within protected-group strata; Counterfactual fairness: protected attribute excluded from the outcome regression (used only for stratified calibration); ITE ignorability / overlap so the treated and control nuisances are identified. Pre-conditions: DataFrame with y, treat, predictive covariates, and a categorical protected column; Calibration set per protected stratum for group-wise conformal coverage; Optional test_data to emit fair ITE intervals on. Failure modes: A protected stratum has too few calibration rows for valid group-wise quantiles \u2192 Collapse sparse protected categories or pool strata before calibration; protected column accidentally leaks into covariates, breaking counterfactual fairness \u2192 Remove the protected attribute from covariates; pass it only via the protected argument. Alternatives: sp.conformal, sp.conformal_continuous. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.1,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "protected": {
          "description": "Protected-attribute column",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "protected",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "conformal_fair_ite"
  },
  {
    "description": "Conformal ITE \u2014 article alias for :func:`conformal_cate`.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "d",
        "y"
      ],
      "type": "object"
    },
    "name": "conformal_ite"
  },
  {
    "description": "Conformal inference for synthetic control. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "grid_range": {
          "description": "(min, max) of the hypothesis grid. If None, auto-determined from pre-treatment residual scale.",
          "type": "number"
        },
        "grid_size": {
          "default": 101,
          "description": "Number of points in the hypothesis grid for CI inversion.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty (used when scm_method='ridge').",
          "type": "number"
        },
        "scm_method": {
          "default": "classic",
          "description": "Which SCM variant to use for weight estimation. Currently supports 'classic' (constrained) and 'ridge'.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "conformal_synth"
  },
  {
    "description": "Compute Conley (1999) spatial HAC standard errors.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "dist_cutoff": {
          "description": "Distance cutoff *h* in kilometres. Pairs farther apart than this receive zero weight.",
          "type": "number"
        },
        "kernel": {
          "default": "uniform",
          "description": "Kernel function: ``\"uniform\"`` (indicator) or ``\"bartlett\"`` (linearly declining weight).",
          "type": "string"
        },
        "lat": {
          "description": "Column name for latitude.",
          "type": "string"
        },
        "lon": {
          "description": "Column name for longitude.",
          "type": "string"
        },
        "result": {
          "description": "Fitted OLS result. Must have ``data_info`` containing ``'X'`` (design matrix), ``'y'`` (response), and ``'residuals'``.",
          "type": "string"
        }
      },
      "required": [
        "dist_cutoff",
        "lat",
        "lon",
        "result"
      ],
      "type": "object"
    },
    "name": "conley"
  },
  {
    "description": "DiD with continuous treatment intensity. Four modes: (i) 'twfe' TWFE with dose\u00d7post interaction; (ii) 'att_gt' dose-quantile group-time ATT versus the untreated (dose=0) arm with bootstrap SE (heuristic); (iii) 'dose_response' local-linear regression of \u0394Y=Y_post\u2212Y_pre on baseline dose; (iv) 'cgs' Callaway-Goodman-Bacon-Sant'Anna (2024) ATT(d|g,t) MVP \u2014 2-period design, OR only, bootstrap SE, [\u5f85\u6838\u9a8c] markers on paper formulas. Full CGS parity (cohort aggregation, DR/IPW, analytical IF variance) is on the roadmap \u2014 see docs/rfc/continuous_did_cgs.md. Validation: validated. Known limitations: method='cgs' is an MVP \u2014 2-period design, OR only, bootstrap SE; full CGS parity (cohort aggregation, DR/IPW, analytical IF variance) is on the roadmap (see docs/rfc/continuous_did_cgs.md). Other modes (twfe / att_gt / dose_response) are stable.. Assumptions: Parallel trends in potential outcomes across dose levels; No anticipation of treatment; Strong parallel trends (CGS 2024) required for ATT(d|g,t) interpretation in att_gt mode. Pre-conditions: panel data with unit \u00d7 time \u00d7 outcome \u00d7 continuous dose; at least one unit with dose == 0 acts as untreated control (or the lowest dose quantile is us\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for SE (TWFE mode)",
          "type": "string"
        },
        "controls": {
          "description": "Control variables",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dose": {
          "description": "Continuous treatment / dose variable",
          "type": "string"
        },
        "id": {
          "description": "Unit identifier",
          "type": "string"
        },
        "method": {
          "default": "att_gt",
          "description": "Estimation mode",
          "enum": [
            "att_gt",
            "twfe",
            "dose_response",
            "cgs"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications for SE",
          "type": "integer"
        },
        "n_quantiles": {
          "default": 5,
          "description": "Number of dose quantiles for discretisation",
          "type": "integer"
        },
        "post": {
          "description": "Binary post-treatment indicator (inferred from t_pre / t_post if omitted)",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "t_post": {
          "description": "First post-treatment period",
          "type": "integer"
        },
        "t_pre": {
          "description": "Last pre-treatment period",
          "type": "integer"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "dose",
        "id",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "continuous_did"
  },
  {
    "description": "LATE with a continuous instrument (Xie et al. 2025). Estimates the LATE on the maximal complier class via quantile-bin Wald ratios, weighted by the bin-pair with the largest first-stage response. Validation: validated. Assumptions: Instrument relevance (non-zero first stage); Exclusion restriction: the instrument affects the outcome only through the treatment; Independence/exogeneity of the instrument; for LATE, monotonicity (no defiers). Pre-conditions: An instrument plausibly affecting treatment, an endogenous treatment, and an outcome; A strong first stage (assess instrument strength before interpreting estimates); formula includes the (endog ~ instruments) parenthesised block. Failure modes: Weak first stage \u2014 biased point estimates and unreliable conventional SEs \u2192 Report first-stage F / effective F and use weak-IV-robust inference (Anderson-Rubin).; First-stage F < 10 (Stock-Yogo 5% bias) \u2192 Use weak-IV-robust inference (Anderson-Rubin) or LIML.; Over-identification test rejects (sp.estat 'overid') \u2192 At least one instrument is invalid; drop instruments or switch to just-identified LIML.. Alternatives: sp.iv, sp.anderson_rubin_ci, sp.dml, sp.deepiv. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "instrument": {
          "description": "Continuous instrument",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_quantiles": {
          "default": 4,
          "description": "Number of instrument quantile bins",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "continuous_iv_late"
  },
  {
    "description": "Compute contrasts of predictive margins across levels of a variable.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "method": {
          "default": "r",
          "description": "Contrast type: - ``'r'`` (reference): each level vs *reference* level. - ``'ar'`` (adjacent): each level vs the previous level. - ``'gw'`` (grand-mean weighted): each level vs the weighted grand mean of all levels.",
          "type": "string"
        },
        "reference": {
          "description": "Reference level when ``method='r'``. Defaults to the smallest observed level.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model result.",
          "type": "string"
        },
        "variable": {
          "description": "Categorical variable whose levels are contrasted.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "variable"
      ],
      "type": "object"
    },
    "name": "contrast"
  },
  {
    "description": "Gaussian-copula sensitivity to unobserved confounding.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "estimate": {
          "description": "estimate parameter (float).",
          "type": "number"
        },
        "rho_grid": {
          "description": "Correlation grid. Defaults to ``np.linspace(-0.5, 0.5, 21)``.",
          "type": "number"
        },
        "se": {
          "description": "se parameter (float).",
          "type": "number"
        },
        "sigma_u": {
          "default": 1.0,
          "description": "Standard deviations of the latent confounder and the outcome. With default values the bias coefficient is numerically equal to ``rho``, matching Chernozhukov-Cinelli-Hazlett's \"percentile scaling.\"",
          "type": "number"
        },
        "sigma_y": {
          "default": 1.0,
          "description": "Standard deviations of the latent confounder and the outcome. With default values the bias coefficient is numerically equal to ``rho``, matching Chernozhukov-Cinelli-Hazlett's \"percentile scaling.\"",
          "type": "number"
        }
      },
      "required": [
        "estimate",
        "se"
      ],
      "type": "object"
    },
    "name": "copula_sensitivity"
  },
  {
    "description": "Cox Proportional Hazards model via partial likelihood. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Column name for cluster-robust SE.",
          "type": "string"
        },
        "duration": {
          "description": "Column name for follow-up time (overrides formula LHS).",
          "type": "string"
        },
        "event": {
          "description": "Column name for event indicator (1 = event, 0 = censored).",
          "type": "string"
        },
        "formula": {
          "description": "Formula of the form ``'duration ~ x1 + x2'``. If given, ``duration`` is inferred from the LHS.",
          "type": "string"
        },
        "hazard_ratio": {
          "default": true,
          "description": "If True, report hazard ratios in the summary alongside coefficients.",
          "type": "boolean"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'hc0'`` for sandwich SE.",
          "type": "string"
        },
        "strata": {
          "description": "Column name for stratification variable.",
          "type": "string"
        },
        "ties": {
          "default": "efron",
          "description": "Tie-handling method: ``'efron'`` or ``'breslow'``.",
          "type": "string"
        },
        "x": {
          "description": "Covariate column names (overrides formula RHS).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "cox"
  },
  {
    "description": "Cox proportional hazards with shared gamma frailty.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Column identifying clusters (e.g. hospital, site).",
          "type": "string"
        },
        "formula": {
          "description": "``\"duration + event ~ x1 + x2\"`` (like R's ``Surv(time, event) ~ x``).",
          "type": "string"
        },
        "maxiter": {
          "default": 50,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "tol": {
          "default": 1e-06,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        }
      },
      "required": [
        "cluster",
        "formula"
      ],
      "type": "object"
    },
    "name": "cox_frailty"
  },
  {
    "description": "CPS-style wage data with a gender gap.",
    "input_schema": {
      "properties": {
        "n": {
          "default": 3000,
          "description": "n parameter (int).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "cps_wage"
  },
  {
    "description": "CR2 bias-corrected cluster-robust standard errors (Bell & McCaffrey 2002).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Name of the cluster variable in ``data``.",
          "type": "string"
        },
        "result": {
          "description": "A fitted regression result from ``sp.regress()``.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "result"
      ],
      "type": "object"
    },
    "name": "cr2_se"
  },
  {
    "description": "CR3 cluster-jackknife variance (delete-one-cluster).",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "cluster",
        "y"
      ],
      "type": "object"
    },
    "name": "cr3_jackknife_vcov"
  },
  {
    "description": "One-call staggered-DID workflow: estimate \u2192 aggregate \u2192 sensitivity.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "anticipation": {
          "default": 0,
          "description": "anticipation parameter (int).",
          "type": "integer"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "control_group parameter (str).",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "data_or_result": {
          "description": "Either a long-format panel (then ``y, g, t, i`` are required and :func:`callaway_santanna` is run first), or an already-fitted :func:`callaway_santanna` result.",
          "type": "string"
        },
        "estimator": {
          "default": "dr",
          "description": "estimator parameter (str).",
          "enum": [
            "dr",
            "ipw",
            "reg"
          ],
          "type": "string"
        },
        "g": {
          "description": "Outcome / cohort / time / unit id columns (required when ``data_or_result`` is a DataFrame).",
          "type": "string"
        },
        "i": {
          "description": "Outcome / cohort / time / unit id columns (required when ``data_or_result`` is a DataFrame).",
          "type": "string"
        },
        "max_e": {
          "default": Infinity,
          "description": "Event-time window passed to the dynamic aggregation.",
          "type": "number"
        },
        "min_e": {
          "default": -Infinity,
          "description": "Event-time window passed to the dynamic aggregation.",
          "type": "number"
        },
        "n_boot": {
          "default": 1000,
          "description": "Multiplier-bootstrap replications for :func:`aggte`.",
          "type": "integer"
        },
        "random_state": {
          "default": 0,
          "description": "Seed for the bootstrap (set to ``None`` for non-reproducibility).",
          "type": "integer"
        },
        "rr_method": {
          "default": "smoothness",
          "description": "Sensitivity restriction handed to :func:`breakdown_m`.",
          "enum": [
            "smoothness",
            "relative_magnitude"
          ],
          "type": "string"
        },
        "save_to": {
          "description": "When set, treats the value as a *path prefix* and writes the report in every supported format in one call: - ``<prefix>.txt`` \u2014 fixed-width plain-text report - ``<prefix>.md`` \u2014 GitHub-flavoured Markdown - ``<prefix>.tex`` \u2014 booktabs LaTeX fragment - ``<prefix>.xlsx`` \u2014 multi-sheet workbook - ``<prefix>.png`` \u2014 2\u00d72 summary figure (only if matplotlib is installed; silently skipped otherwise) Missing parent directories are created on the fly.",
          "type": "string"
        },
        "t": {
          "description": "Outcome / cohort / time / unit id columns (required when ``data_or_result`` is a DataFrame).",
          "type": "string"
        },
        "verbose": {
          "default": true,
          "description": "If ``True``, print the report before returning.",
          "type": "boolean"
        },
        "x": {
          "description": "Covariates for conditional parallel trends.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome / cohort / time / unit id columns (required when ``data_or_result`` is a DataFrame).",
          "type": "string"
        }
      },
      "required": [
        "data_or_result"
      ],
      "type": "object"
    },
    "name": "cs_report"
  },
  {
    "description": "CUSUM test for parameter stability.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "cusum_test"
  },
  {
    "description": "Declare a causal DAG and perform identification analysis: backdoor/frontdoor adjustment sets, d-separation, path enumeration, bad controls detection, variable role classification, do-operator. Validation: validated. Assumptions: The graph is acyclic and contains the relevant common causes; Adjustment-set validity depends on the supplied graph being substantively correct. Pre-conditions: Nodes and directed edges encode a substantive causal model; Treatment and outcome nodes are named consistently. Failure modes: No valid adjustment set or cycle detected \u2192 Inspect graph structure, remove cycles, or use sensitivity analysis for unobserved common causes.. Alternatives: sp.identify, sp.dag_recommend_estimator, sp.swig. Typical minimum N: 1.",
    "input_schema": {
      "properties": {
        "spec": {
          "description": "Edge spec: \"Z -> X; Z -> Y; X -> Y\"",
          "type": "string"
        }
      },
      "required": [
        "spec"
      ],
      "type": "object"
    },
    "name": "dag"
  },
  {
    "description": "Load a classic textbook DAG: confounding, collider, mediation, discrimination, movie_star, police, frontdoor, bad_control_earnings, m_bias.",
    "input_schema": {
      "properties": {
        "name": {
          "description": "Example name, e.g. 'discrimination'",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "dag_example"
  },
  {
    "description": "Das Gupta (1993) multi-factor decomposition.",
    "input_schema": {
      "properties": {
        "data_a": {
          "description": "Each row contributes the factor value. The aggregate for each group is computed as \u03a3_i \u220f_f factor_{f,i}. For single-row DataFrames (one population, no stratification) the aggregate is simply \u220f_f factor_f.",
          "type": "string"
        },
        "data_b": {
          "description": "Each row contributes the factor value. The aggregate for each group is computed as \u03a3_i \u220f_f factor_{f,i}. For single-row DataFrames (one population, no stratification) the aggregate is simply \u220f_f factor_f.",
          "type": "string"
        },
        "factor_names": {
          "description": "factor_names parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "data_a",
        "data_b",
        "factor_names"
      ],
      "type": "object"
    },
    "name": "das_gupta"
  },
  {
    "description": "Triple Differences (DDD) estimator. Adds a within-treatment-group subgroup that is unaffected by treatment as an additional control dimension, relaxing parallel trends from 'same trend across groups' to 'same differential trend across subgroups within groups'. Validation: validated. Assumptions: Parallel trends in the DDD differential (weaker than DID PT); No anticipation; SUTVA. Pre-conditions: treat \u00d7 time \u00d7 subgroup variation exists; subgroup is binary and meaningful within treatment group. Failure modes: Staggered adoption with heterogeneous effects \u2192 Textbook DDD can have negative weights with staggered timing. The Olden-M\u00f8en (2022) / Strezhnev (2023) heterogeneity-robust DDD is on the roadmap (see docs/rfc/did_roadmap_gap_audit.md \u00a74).. Alternatives: sp.did_2x2, sp.callaway_santanna. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "robust": {
          "default": true,
          "description": "Robust standard-error or covariance estimator option.",
          "type": "boolean"
        },
        "subgroup": {
          "description": "Within-group subgroup (1=affected, 0=not)",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Primary treatment indicator",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "subgroup",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "ddd"
  },
  {
    "description": "Heterogeneity-robust triple differences (DDD) for staggered adoption. Decomposes DDD into per-(cohort, time) cells via a Callaway-Sant'Anna-style aggregation, with the unaffected subgroup's DID as a placebo. Avoids the negative-weight issue that textbook TWFE DDD inherits from TWFE DID (Goodman-Bacon 2021 analogue). Validation: validated. Assumptions: Parallel trends relaxed to: same differential trend across treated vs never-treated, within both affected and unaffected subgroups; No anticipation; SUTVA. Pre-conditions: staggered adoption panel with \u2265 1 never-treated unit; binary within-group subgroup (affected vs unaffected); \u2265 1 pre-treatment period per cohort. Failure modes: No never-treated units \u2192 First-cut implementation requires never-treated controls; not-yet-treated variant is on the roadmap.; placebo_joint_test rejects \u2014 DDD parallel-trends assumption violated \u2192 Inspect per-(g, t) did_placebo values in the detail DataFrame; add controls or apply sp.honest_did sensitivity to the affected arm.. Alternatives: sp.ddd, sp.callaway_santanna, sp.wooldridge_did. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cohort": {
          "description": "First-treatment period (never_value = never-treated)",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "never_value": {
          "default": 0,
          "description": "Value in cohort for never-treated units",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "subgroup": {
          "description": "Binary within-group subgroup indicator (1=affected, 0=placebo)",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cohort",
        "subgroup",
        "time",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "ddd_heterogeneous"
  },
  {
    "description": "Unified entry point for all decomposition methods. Validation: certified parity evidence. Assumptions: Overlapping support of covariates across groups (reweighting / RIF methods are invalid outside overlap); Linearity assumption holds for Oaxaca-Blinder-type decompositions; non-linear methods (FFL/DFL/Machado-Mata) relax this; Conditional independence of group membership for causal interpretation (otherwise: descriptive decomposition only). Pre-conditions: Data contains a binary or categorical group indicator with both groups represented; Outcome and covariates are numeric (or properly encoded) and finite; Sample sizes per group are large enough to estimate group-specific moments (rule of thumb: each group >= 100). Failure modes: Trimming warning at common-support boundaries \u2192 Inspect propensity-score support; restrict the analysis sample or use bounds.; RIF coefficients explode at distribution tails \u2192 Use higher-bandwidth kernel density, restrict quantile range, or switch to FFL.. Alternatives: sp.dfl_decompose, sp.ffl_decompose, sp.oaxaca, sp.rif_decomposition. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "method": {
          "description": "One of the methods listed in ``available_methods()``. Aliases are supported (e.g. 'mm' \u2192 'machado_mata').",
          "type": "string"
        }
      },
      "required": [
        "method"
      ],
      "type": "object"
    },
    "name": "decompose"
  },
  {
    "description": "Return the within-transformed ``x`` and the singleton keep mask.",
    "input_schema": {
      "properties": {
        "drop_singletons": {
          "default": true,
          "description": "drop_singletons parameter (bool).",
          "type": "boolean"
        },
        "fe": {
          "description": "fe parameter (Union[pd.DataFrame, np.ndarray]).",
          "type": "string"
        },
        "maxiter": {
          "default": 10000,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "solver": {
          "default": "map",
          "description": "solver parameter (str).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        }
      },
      "required": [
        "fe",
        "x"
      ],
      "type": "object"
    },
    "name": "demean"
  },
  {
    "description": "De-meaned / De-trended Synthetic Control Method. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Additional covariates to match on.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty on weights.",
          "type": "number"
        },
        "placebo": {
          "default": true,
          "description": "Run in-space placebo inference.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "variant": {
          "default": "demeaned",
          "description": "* ``'demeaned'`` \u2014 subtract unit-level pre-treatment means. * ``'detrended'`` \u2014 subtract unit-level linear time trends.",
          "enum": [
            "demeaned",
            "detrended"
          ],
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "demeaned_synth"
  },
  {
    "description": "Design-robust event study with explicit negative-weight diagnostics per cohort \u00d7 relative-time cell. Reports which event-study coefficients receive negative weights in TWFE and flags the affected horizons. Validation: validated. Assumptions: Parallel trends across cohorts; treatment effects may be heterogeneous across cohort and time; No anticipation prior to the event time; Implicit TWFE comparison weights are non-negative (negative-weight contamination is diagnosed, not assumed away). Pre-conditions: Long-format panel with y, treat, time, id (same conventions as callaway_santanna); Staggered/variable treatment timing so the per-(cohort, time) weight diagnostic is meaningful; Event-time window (leads, lags) contained within observed pre/post coverage. Failure modes: model_info weights show large negative TWFE weights flagging forbidden comparisons \u2192 Drop already-treated controls and use a heterogeneity-robust staggered estimator instead of TWFE; Too few treated units per cohort-time cell to identify weights or SEs \u2192 Coarsen the event-time window or pool cohorts to raise per-cell counts. Alternatives: sp.sun_abraham, sp.bacon_decomposition, sp.cohort_anchored_event_study, sp.calla\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "lags": {
          "default": 5,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "leads": {
          "default": 3,
          "description": "leads parameter (int).",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "design_robust_event_study"
  },
  {
    "description": "Auto-detect the study design (panel / cross-section / RD / IV-style) from column shapes and types. Returns the guessed design plus the columns that drove the inference. Call this BEFORE recommend() when the user pastes a CSV with no context.",
    "input_schema": {
      "properties": {
        "id_col_hint": {
          "type": "string"
        },
        "time_col_hint": {
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "detect_design"
  },
  {
    "description": "DFL (1996) reweighting decomposition at a chosen distributional statistic. Validation: certified parity evidence. Assumptions: DiNardo-Fortin-Lemieux reweighting: ignorable group assignment given covariates; Propensity-score model is correctly specified for the reweighting kernel; Common support across groups (no extrapolation beyond observed covariate range). Pre-conditions: Binary group indicator with sufficient overlap on covariates; Outcome distribution to decompose is continuous (typically log-wage). Failure modes: Extreme propensity-score weights inflate variance \u2192 Trim or stabilize weights, or restrict to the common-support region.. Alternatives: sp.ffl_decompose, sp.oaxaca, sp.machado_mata. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "inference": {
          "default": "analytical",
          "description": "inference parameter (str).",
          "enum": [
            "bootstrap",
            "analytical"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 299,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantile_grid": {
          "description": "If provided, also compute quantile-process decomposition on this grid.",
          "type": "number"
        },
        "reference": {
          "default": 0,
          "description": "- 0: reweight Group B to look like A's X (default). The counterfactual is F_{Y<1|0>} \u2014 A's X distribution with B's outcome structure. - 1: reweight Group A to look like B's X. The counterfactual is F_{Y<0|1>} \u2014 B's X distribution with A's outcome structure. .. warning:: ``reference`` has different economic semantics across method families. In DFL, ``reference=0`` yields cf = *A's X, B's \u03b2* (reweighting approach). In ``machado_mata`` / ``melly`` / ``cfm``, ``reference=0`` yields cf = *A's \u03b2, B's X* (coefficient-substitution approach). These are **opposite** counterfactual constructions. Within each method labels are internally consistent (DFL structure = A \u2212 cf; MM composition = A \u2212 cf). When comparing estimates across methods, read the per-method docstrings carefully.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "stat": {
          "default": "mean",
          "description": "stat parameter (str).",
          "enum": [
            "mean",
            "variance",
            "std",
            "quantile",
            "iqr",
            "gini",
            "log_var"
          ],
          "type": "string"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile level or target treatment-effect index.",
          "type": "number"
        },
        "trim": {
          "default": 0.001,
          "description": "trim parameter (float).",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "dfl_decompose"
  },
  {
    "description": "Comprehensive regression diagnostics in one call.",
    "input_schema": {
      "properties": {
        "print_results": {
          "default": true,
          "description": "Print formatted output.",
          "type": "boolean"
        },
        "x": {
          "description": "Independent variable names (excluding constant).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "diagnose"
  },
  {
    "description": "Method-aware diagnostic battery: auto-selects tests by model type (OLS/DID/RDD/IV/SCM). Validation: validated. Assumptions: The result object carries a recognizable method_type so the correct diagnostic battery can be routed; Each sub-check (e.g. parallel-trends, weak-IV, overid, balance) is only valid under that method's own identifying assumptions; Tests use the supplied alpha as the significance threshold; p-values are interpreted, not corrected for multiplicity. Pre-conditions: A fitted EconometricResults or CausalResult from a StatsPAI estimator; The estimator must expose enough fitted internals (residuals, first-stage, design info) for its checks. Failure modes: Passed a raw DataFrame, dict, or estimate float instead of a fitted result object \u2192 Fit an estimator first and pass the returned result object, not the input data.; Method type is unrecognized so no diagnostic battery applies and 'checks' comes back empty \u2192 Run the method-appropriate standalone diagnostic directly instead of the router.. Alternatives: sp.unified_sensitivity, sp.sensemakr, sp.oster_bounds, sp.spec_curve. Typical minimum N: 30.",
    "input_schema": {
      "properties": {
        "result": {
          "description": "Fitted result from any StatsPAI estimator",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "diagnose_result"
  },
  {
    "description": "Fit a classic 2-period 2-group difference-in-differences. Pass treatment / time / post column names. For staggered adoption across many cohorts use callaway_santanna instead.",
    "input_schema": {
      "properties": {
        "post": {
          "description": "Binary post-treatment period indicator",
          "type": "string"
        },
        "time": {
          "description": "Time column",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment-group indicator",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "y",
        "treat",
        "time"
      ],
      "type": "object"
    },
    "name": "did"
  },
  {
    "description": "Gardner (2021) two-stage DID estimator. Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) \u2192 Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates.; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights \u2192 Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage).; Pre-trend joint test p < 0.05 (or underpowered at 0.10) \u2192 Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna.. Alternatives: sp.callaway_santanna, sp.did, sp.honest_did, sp.sun_abraham. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Two-sided CI level.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for Stage-2 SEs. Defaults to ``group``.",
          "type": "string"
        },
        "controls": {
          "description": "Additional covariates included in both stages.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "event_study": {
          "default": false,
          "description": "If True, Stage 2 reports coefficients by relative time ``k = t - first_treat_i``.",
          "type": "boolean"
        },
        "first_treat": {
          "description": "First-treatment-period column. Never-treated units should be encoded as ``0``, ``NaN``, or ``+inf``.",
          "type": "string"
        },
        "group": {
          "description": "Unit (panel-id) column.",
          "type": "string"
        },
        "horizon": {
          "description": "Relative-time leads/lags to report when ``event_study=True``; defaults to ``range(-5, 6)`` intersected with available support.",
          "type": "integer"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column name.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "did_2stage"
  },
  {
    "description": "Canonical 2\u00d72 DID: two groups (treated / control) \u00d7 two periods (pre / post). Point estimate via either group-means differencing or OLS on the treat \u00d7 post interaction; optional covariates, robust / cluster SE, and sample weights. Validation: validated. Assumptions: Parallel trends; No anticipation; SUTVA (no spillovers). Pre-conditions: data has exactly two time periods (pre, post); treat is 0/1 constant within unit (unit-level, not time-varying); at least a handful of treated and control units. Failure modes: Staggered timing (> 2 periods with varying treat start) \u2192 Use sp.callaway_santanna / sp.sun_abraham / sp.did_imputation.; Very few clusters at the group level \u2192 Use wild cluster bootstrap via sp.wild_cluster_bootstrap.; Pre-trend joint test p < 0.05 (or underpowered at 0.10) \u2192 Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna.. Alternatives: sp.drdid, sp.did_analysis, sp.callaway_santanna, sp.sun_abraham. Typical minimum N: 30.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Column for cluster-robust SE (defaults to treat)",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates included additively; for DR use sp.drdid",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "robust": {
          "default": true,
          "description": "Heteroskedasticity-robust SE when no cluster provided",
          "type": "boolean"
        },
        "time": {
          "description": "Time / period indicator",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment-group indicator (0/1)",
          "type": "string"
        },
        "weights": {
          "description": "Optional column name for sampling weights",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "did_2x2"
  },
  {
    "description": "Workflow wrapper that runs a full DiD pipeline: auto-detects 2\u00d72 vs. staggered, runs the right estimator (CS by default), optionally runs Bacon decomposition, event study, and Rambachan-Roth sensitivity, and aggregates into a DIDAnalysis report object. Validation: validated. Assumptions: Parallel trends between treated and control (2x2) or across cohorts (staggered) after any covariate adjustment; No anticipation: pre-treatment outcomes unaffected by future treatment; SUTVA / no interference across units; correct design auto-detection (2x2 vs staggered). Pre-conditions: Panel or repeated cross-section with y, treat, time; id required for staggered designs; treat is binary 0/1 for 2x2, or first-treatment period (0 = never-treated) for staggered; event_window within observed periods when run_event_study is enabled. Failure modes: Staggered design detected but no id column supplied, so cohorts cannot be formed \u2192 Pass the unit identifier via id= so first-treatment cohorts can be inferred; Event-study pre-trend test rejects parallel trends in the bundled report \u2192 Inspect the included honest_did sensitivity output and report bounds rather than the point ATT; Requested method incompatibl\u2026",
    "input_schema": {
      "properties": {
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "description": "Estimator selection",
          "enum": [
            "auto",
            "2x2",
            "cs",
            "sa",
            "sdid"
          ],
          "type": "string"
        },
        "run_bacon": {
          "default": true,
          "description": "run_bacon parameter (bool).",
          "type": "boolean"
        },
        "run_event_study": {
          "default": true,
          "description": "run_event_study parameter (bool).",
          "type": "boolean"
        },
        "run_sensitivity": {
          "default": true,
          "description": "run_sensitivity parameter (bool).",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment or first-treat column",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "did_analysis"
  },
  {
    "description": "Bayesian Causal Forests DiD. Fits a BART-style ensemble with treatment and prognostic terms on the DiD residuals, providing heterogeneous treatment-effect posterior draws per unit. Useful for machine-learning DiD with covariates. Validation: validated. Assumptions: Parallel trends: treated and control groups would have followed the same trajectory absent treatment; No anticipation: outcomes in pre-treatment periods are unaffected by future treatment; SUTVA: no spillovers between units. Pre-conditions: data is panel or repeated cross-section with a time column; treat column is binary (0/1) for 2x2, or first-treatment-period (int) for staggered; at least one pre-treatment period (\u2265 2 periods for 2x2; \u2265 3 recommended for event study). Failure modes: Pre-trend joint test p < 0.05 (or underpowered at 0.10) \u2192 Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna.; Staggered treatment timing with TWFE method \u2192 TWFE can give negative weights; use Callaway-Sant'Anna, Sun-Abraham, or BJS imputation.; Pre-trend test underpowered (Roth 2022) \u2192 Check sp.pretrends_power \u2014 if low, report honest CI via sp.sensitivity_rr.. Alternatives: sp.did_imputation, sp.drdid, s\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "n_trees": {
          "default": 200,
          "description": "Number of trees.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "did_bcf"
  },
  {
    "description": "R-style alias: ``synthdid::did_estimate``. Assumptions: Parallel trends in the absence of treatment, after the synthetic/DiD weighting; No anticipation and no interference between units (SUTVA); The control pool's outcome process is stable around the intervention. Pre-conditions: Panel with treated and control units and a clear treatment date; Pre-treatment periods available to assess comparability of trends. Failure modes: Weighted pre-treatment trends still diverge between treated and synthetic control \u2192 Inspect the unit/time weights and pre-trend fit; consider event-study DiD with honest bounds.. Alternatives: sp.synth, sp.augsynth, sp.callaway_santanna, sp.gardner_did. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat_time",
        "treat_unit",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "did_estimate"
  },
  {
    "description": "Borusyak-Jaravel-Spiess (2024) imputation DiD. Fits a TWFE model on untreated observations only, imputes counterfactual Y(0) for treated obs, and averages the imputation residuals. Efficient under no-anticipation + parallel trends; analytical SE via bjs_inference. Validation: certified parity evidence. Assumptions: Parallel trends in absolute levels; No anticipation (no pre-treatment reaction); SUTVA. Pre-conditions: panel with unit \u00d7 time \u00d7 outcome; first_treat encodes cohort (first treated period or 0); \u2265 1 never-treated unit OR \u2265 1 late-treated cohort. Failure modes: All units treated (no untreated observations to fit) \u2192 Impossible to impute Y(0); use sp.did_multiplegt if on/off switching.; Pre-trend joint test p < 0.05 (or underpowered at 0.10) \u2192 Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna.; Staggered treatment timing with TWFE method \u2192 TWFE can give negative weights; use Callaway-Sant'Anna, Sun-Abraham, or BJS imputation.. Alternatives: sp.callaway_santanna, sp.sun_abraham, sp.gardner_did, sp.wooldridge_did. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "First-treatment period; 0 = never-treated",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "horizon": {
          "description": "Relative-time leads / lags (default: all available)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "did_imputation"
  },
  {
    "description": "Staggered DiD robust to treatment-timing misclassification and anticipation. Adjusts the CS-style aggregation for a user-supplied misclassification probability pi_misclass and a known anticipation horizon. Use when first-treat dates are noisy (e.g., survey-reported). Validation: validated. Assumptions: Parallel trends: treated and control groups would have followed the same trajectory absent treatment; No anticipation: outcomes in pre-treatment periods are unaffected by future treatment; SUTVA: no spillovers between units. Pre-conditions: pi_misclass is between 0 and 0.5 (else identification flips); Known anticipation horizon; data is panel or repeated cross-section with a time column. Failure modes: Pre-trend joint test p < 0.05 (or underpowered at 0.10) \u2192 Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna.; Staggered treatment timing with TWFE method \u2192 TWFE can give negative weights; use Callaway-Sant'Anna, Sun-Abraham, or BJS imputation.; Pre-trend test underpowered (Roth 2022) \u2192 Check sp.pretrends_power \u2014 if low, report honest CI via sp.sensitivity_rr.. Alternatives: sp.callaway_santanna, sp.sun_abraham, sp.did_imputation, sp.sdid. Typical min\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "anticipation_periods": {
          "default": 0,
          "description": "anticipation_periods parameter (int).",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "id": {
          "description": "Unit, subject, or panel identifier column.",
          "type": "string"
        },
        "pi_misclass": {
          "default": 0.0,
          "description": "P(observed treat \u2260 true treat) \u2014 between 0 and 1",
          "type": "number"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "First-treatment period (possibly noisy)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "did_misclassified"
  },
  {
    "description": "de Chaisemartin & D'Haultf\u0153uille (2020) DID_M estimator. Weighted average of consecutive-period DID cells where treatment 'switchers' are compared to 'stayers'. Handles treatments that switch on AND off (unlike Callaway-Sant'Anna which assumes staggered adoption). Supports placebo lags, dynamic horizons, cluster bootstrap SE, joint placebo test and average-cumulative-effect summary from dCDH (2024). The heteroskedastic-weights variant and full dCDH (2024) intertemporal event-study (did_multiplegt_dyn Stata) are on the roadmap \u2014 see docs/rfc/multiplegt_dyn.md. Validation: validated. Assumptions: Parallel trends between switchers and stayers; Stable treatment effects across consecutive periods (for the DID_M weighted average interpretation); No anticipation. Pre-conditions: long-format panel with one row per unit \u00d7 period; treatment is binary (0/1) and may vary over time within a unit; at least two periods observed per unit so a first difference can be computed. Failure modes: No switching cells (nobody changes treatment) \u2192 did_multiplegt identifies effects only from treatment switches. Fall back to callaway_santanna if the design is staggered adoption.; Joint placebo test rejects \u2014\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for bootstrap (defaults to group)",
          "type": "string"
        },
        "controls": {
          "description": "Controls residualised via first differences",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dynamic": {
          "default": 0,
          "description": "Number of post-treatment dynamic horizons",
          "type": "integer"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_boot": {
          "default": 100,
          "description": "Cluster-bootstrap replications",
          "type": "integer"
        },
        "placebo": {
          "default": 0,
          "description": "Number of pre-treatment placebo lags",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "treatment": {
          "description": "Binary current-treatment indicator (may switch on and off)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "group",
        "time",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "did_multiplegt"
  },
  {
    "description": "[experimental] dCDH (2024) intertemporal event-study DiD (MVP \u2014 see docs/rfc/multiplegt_dyn.md). At each horizon l \u2208 {-placebo, ..., dynamic}, compares Y_{F+l} \u2212 Y_{F-1} between units first switching at F and a not-yet-treated or never-treated control set held stable across the horizon. **MVP caveats**: analytical influence-function variance [\u5f85\u6838\u9a8c] is not yet implemented (SE via cluster bootstrap); switch-off events are ignored; heteroskedastic-weights variant pending. Validation: experimental. Known limitations: switch-on only \u2014 switch-off events are silently dropped; SE is cluster bootstrap; the paper's analytical influence-function variance is not yet implemented; heteroskedastic-weights variant (dCDH 2023 EJ survey) is not implemented. Assumptions: Parallel trends between switchers and controls per horizon; No anticipation prior to F; Stable control treatment across horizon window. Pre-conditions: long-format panel with binary time-varying treatment; at least some units switching on from d=0 to d=1; enough horizons pre/post to compute long differences. Failure modes: No units switch from 0 to 1 \u2192 did_multiplegt_dyn identifies from switch-on events. Use sp.callaway_santanna if d\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster column (defaults to group)",
          "type": "string"
        },
        "control": {
          "default": "not_yet_treated",
          "description": "Control group",
          "enum": [
            "not_yet_treated",
            "never_treated"
          ],
          "type": "string"
        },
        "dynamic": {
          "default": 3,
          "description": "Number of post-treatment dynamic horizons",
          "type": "integer"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "placebo": {
          "default": 0,
          "description": "Number of pre-treatment placebo horizons",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment (0/1), switch-on only in MVP",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "time",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "did_multiplegt_dyn"
  },
  {
    "description": "Classic DID diagram showing treatment effect with counterfactual.",
    "input_schema": {
      "properties": {
        "annotate_effect": {
          "default": true,
          "description": "Annotate the treatment effect arrow on the plot.",
          "type": "boolean"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "colors": {
          "description": "(treatment, control, counterfactual) colors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "labels": {
          "description": "Custom labels: ``{'treat': ..., 'control': ..., 'counterfactual': ...}``.",
          "type": "object"
        },
        "show_counterfactual": {
          "default": true,
          "description": "Draw the dashed counterfactual line.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment group indicator (0/1).",
          "type": "string"
        },
        "treat_time": {
          "description": "Treatment onset time. If None, inferred as the midpoint.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "did_plot"
  },
  {
    "description": "One-call comprehensive DID report: fits all methods + exports every format.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "first_treat parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "include_sensitivity": {
          "default": true,
          "description": "Whether to run Rambachan-Roth breakdown M*. Defaults to ``True`` in ``did_report`` (vs ``False`` in ``did_summary``) because a report is expected to be comprehensive.",
          "type": "boolean"
        },
        "methods": {
          "default": "auto",
          "description": "methods parameter (Union[str, List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "plot_sort_by": {
          "default": "estimate",
          "description": "Sort the forest plot by estimate ascending.",
          "type": "string"
        },
        "save_to": {
          "description": "Directory path. Created if it does not exist.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "verbose": {
          "default": false,
          "description": "Print progress lines.",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "save_to",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "did_report"
  },
  {
    "description": "One-call method-robustness comparison for staggered DID.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for SE (defaults to ``group`` in each sub-method).",
          "type": "string"
        },
        "controls": {
          "description": "Time-varying covariates passed to methods that support them.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "First-treatment period per unit; NaN (or 0) for never-treated.",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier.",
          "type": "string"
        },
        "include_sensitivity": {
          "default": false,
          "description": "If ``True`` and ``'cs'`` is among the methods fit, compute the Rambachan\u2013Roth (2023) *breakdown M\\** \u2014 the largest relative violation of parallel trends under which the treatment effect is still significantly different from zero. The value is added to ``model_info['breakdown_m']`` and to the ``breakdown_m`` column of ``detail`` (CS row only; other methods leave ``NaN``).",
          "type": "boolean"
        },
        "methods": {
          "default": "auto",
          "description": "Methods to run. Valid keys: ``'cs'``, ``'sa'``, ``'bjs'``, ``'etwfe'``, ``'stacked'``, or ``'all'`` / ``'auto'`` for all.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time / period variable (integer-valued).",
          "type": "string"
        },
        "verbose": {
          "default": false,
          "description": "Print progress for each method.",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "did_summary"
  },
  {
    "description": "Forest plot of DID method-robustness summary.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Existing axes to draw on. If ``None`` a new figure is created.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "Color for point estimates and CIs.",
          "type": "string"
        },
        "figsize": {
          "default": [
            9,
            5
          ],
          "description": "Figure size when creating a new figure.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "highlight_color": {
          "default": "#C0392B",
          "description": "Color for the cross-method mean line.",
          "type": "string"
        },
        "reference": {
          "description": "Horizontal reference value (e.g. 0 for 'no effect'). Defaults to ``0``.",
          "type": "number"
        },
        "result": {
          "description": "Output of :func:`did_summary`. Must have a ``detail`` DataFrame with columns ``estimate``, ``ci_low``, ``ci_high``, and either ``method`` or ``estimator``.",
          "type": "string"
        },
        "sort_by": {
          "description": "If ``'estimate'``, sort methods by point estimate ascending. Otherwise keep the order in ``result.detail``.",
          "type": "string"
        },
        "title": {
          "description": "Plot title. Defaults to ``\"DID Method-Robustness Summary\"``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "did_summary_plot"
  },
  {
    "description": "Render a :func:`did_summary` result as a publication-ready LaTeX",
    "input_schema": {
      "properties": {
        "caption": {
          "default": "DID method-robustness summary.",
          "description": "LaTeX caption.",
          "type": "string"
        },
        "digits": {
          "default": 4,
          "description": "Decimal precision.",
          "type": "integer"
        },
        "include_breakdown": {
          "default": true,
          "description": "Include the Rambachan-Roth breakdown M* column when sensitivity was requested.",
          "type": "boolean"
        },
        "include_ci": {
          "default": true,
          "description": "Include the 95 % CI column.",
          "type": "boolean"
        },
        "label": {
          "default": "tab:did_summary",
          "description": "LaTeX label for the table.",
          "type": "string"
        },
        "result": {
          "description": "Output of :func:`did_summary`.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "did_summary_to_latex"
  },
  {
    "description": "Render a :func:`did_summary` result as a GitHub-Flavoured Markdown table.",
    "input_schema": {
      "properties": {
        "digits": {
          "default": 4,
          "description": "Decimal precision for numeric columns.",
          "type": "integer"
        },
        "include_breakdown": {
          "default": true,
          "description": "Include the Rambachan-Roth breakdown M* column (CS row only, blank for others). Ignored if sensitivity was not requested.",
          "type": "boolean"
        },
        "include_ci": {
          "default": true,
          "description": "Include the 95 % CI column.",
          "type": "boolean"
        },
        "result": {
          "description": "Output of :func:`did_summary`.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "did_summary_to_markdown"
  },
  {
    "description": "DiD with time-varying covariates frozen at baseline (Caetano, Callaway, Payne & Rodrigues 2022 [\u5f85\u6838\u9a8c]). Avoids the bad-controls bias that arises when treatment affects the covariates: freezes X at period g + baseline_offset (default g-1) per cohort and uses the frozen values as controls in a per-(g, t) outcome-regression DiD. Aggregates via cohort-size weights. Validation: validated. Assumptions: Conditional parallel trends given frozen baseline X; No anticipation; SUTVA. Pre-conditions: staggered adoption with \u2265 1 never-treated unit; covariates column(s) exist for the baseline period per cohort; integer-valued time column. Failure modes: No observation at baseline period for some units \u2192 Fallback uses the first observed period; review detail coverage.; Covariate measured with error or missing \u2192 Impute (sp.mice_impute) or restrict to a complete sub-sample before calling.; Pre-trend joint test p < 0.05 (or underpowered at 0.10) \u2192 Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna.. Alternatives: sp.callaway_santanna, sp.drdid, sp.wooldridge_did, sp.sun_abraham. Typical minimum N: 150.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline_offset": {
          "default": -1,
          "description": "Offset relative to first-treatment period for freezing",
          "type": "integer"
        },
        "cohort": {
          "description": "First-treatment period (never_value = never-treated)",
          "type": "string"
        },
        "covariates": {
          "description": "Time-varying covariates to freeze at baseline",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "never_value": {
          "default": 0,
          "description": "never_value parameter.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cohort",
        "covariates",
        "time",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "did_timevarying_covariates"
  },
  {
    "description": "Distributional Synthetic Controls (Gunsilius 2023). Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "method": {
          "default": "mixture",
          "description": "``'mixture'``: constrained (\u03c9 \u2265 0, \u03a3\u03c9 = 1) \u2014 minimises the L\u2082-Wasserstein distance between quantile functions. ``'quantile'``: unconstrained quantile-on-quantile regression.",
          "enum": [
            "mixture",
            "quantile"
          ],
          "type": "string"
        },
        "n_quantiles": {
          "default": 100,
          "description": "Number of quantile grid points on (0, 1).",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run in-space placebo permutation tests for inference.",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed (currently unused; reserved for bootstrap extensions).",
          "type": "integer"
        },
        "time": {
          "description": "Time period column name.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Value in *unit* that identifies the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First period of treatment (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column name.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "discos"
  },
  {
    "description": "Visualise distributional synthetic control results.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Pre-existing axes for the plot.",
          "type": "string"
        },
        "ci_alpha": {
          "default": 0.2,
          "description": "Transparency for CI band.",
          "type": "number"
        },
        "color": {
          "default": "#2C3E50",
          "description": "Primary plot colour.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "Figure size.",
          "type": "integer"
        },
        "result": {
          "description": "Output from ``discos()`` or ``qqsynth()``.",
          "type": "string"
        },
        "title": {
          "description": "Plot title override.",
          "type": "string"
        },
        "type": {
          "default": "quantile_effect",
          "description": "default 'quantile_effect' ``'quantile_effect'``: treatment effect \u0394(\u03c4) across quantiles with CIs. ``'quantile_comparison'``: overlay treated vs. counterfactual quantile functions. ``'gap'``: gap plot (treated \u2212 synthetic) over time. ``'weights'``: horizontal bar chart of donor weights.",
          "enum": [
            "quantile_effect",
            "quantile_comparison",
            "gap",
            "weights"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "discos_plot"
  },
  {
    "description": "Test for distributional treatment effects. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "result": {
          "description": "Output from ``discos()`` or ``qqsynth()``.",
          "type": "string"
        },
        "test": {
          "default": "ks",
          "description": "``'ks'``: two-sample Kolmogorov-Smirnov test comparing treated and counterfactual quantile functions. ``'cvm'``: Cram\u00e9r-von Mises test statistic (permutation-based). ``'stochastic_dominance'``: first-order stochastic dominance test.",
          "enum": [
            "ks",
            "cvm",
            "stochastic_dominance"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "discos_test"
  },
  {
    "description": "Jackson & VanderWeele (2018) causal disparity decomposition.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "mediator": {
          "description": "mediator parameter (str).",
          "type": "string"
        },
        "target_level": {
          "description": "Value at which to fix mediator for the \"initial\" counterfactual.",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "mediator",
        "y"
      ],
      "type": "object"
    },
    "name": "disparity_decompose"
  },
  {
    "description": "Synthetic disparity panel with treatment, mediator, outcome.",
    "input_schema": {
      "properties": {
        "n": {
          "default": 3000,
          "description": "n parameter (int).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "disparity_panel"
  },
  {
    "description": "Distributional IV: LATE at each quantile of Y. Assumptions: Selection-on-observables (unconfoundedness + overlap) or, for IV variants, instrument validity; For IV-QTE: rank invariance / rank similarity (monotonicity of the structural quantile function); Relevance: instruments predict the endogenous regressor (first-stage F \u2265 10 rule of thumb). Pre-conditions: Covariates, treatment, and outcome; for IV-quantile methods, a valid instrument; Enough data to estimate the outcome distribution across quantiles; formula includes the (endog ~ instruments) parenthesised block. Failure modes: Estimated conditional quantiles cross (non-monotone), or tail quantiles are unstable \u2192 Use rearrangement / monotonization and avoid extreme quantiles where data are sparse.; First-stage F < 10 (Stock-Yogo 5% bias) \u2192 Use weak-IV-robust inference (Anderson-Rubin) or LIML.; Over-identification test rejects (sp.estat 'overid') \u2192 At least one instrument is invalid; drop instruments or switch to just-identified LIML.. Alternatives: sp.qte, sp.iv, sp.dml, sp.deepiv. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument": {
          "description": "instrument parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "Defaults to (0.1, 0.25, 0.5, 0.75, 0.9).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "dist_iv"
  },
  {
    "description": "Estimate distributional treatment effects. Assumptions: Selection-on-observables (unconfoundedness + overlap) or, for IV variants, instrument validity; For IV-QTE: rank invariance / rank similarity (monotonicity of the structural quantile function). Pre-conditions: Covariates, treatment, and outcome; for IV-quantile methods, a valid instrument; Enough data to estimate the outcome distribution across quantiles. Failure modes: Estimated conditional quantiles cross (non-monotone), or tail quantiles are unstable \u2192 Use rearrangement / monotonization and avoid extreme quantiles where data are sparse.. Alternatives: sp.qte, sp.iv, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "method": {
          "default": "ipw",
          "description": "Estimator or algorithm variant to use.",
          "enum": [
            "ipw",
            "dr",
            "cic"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_grid": {
          "default": 100,
          "description": "Number of grid.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (Optional[List[float]]).",
          "type": "number"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "0-3 group encoding for CiC).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "distributional_te"
  },
  {
    "description": "Neural-net propensity score estimator (arXiv:2404.04794, 2024). Validation: validated. Assumptions: Unconfoundedness / selection on observables: treatment is conditionally independent of potential outcomes given covariates; Overlap (positivity): 0 < e(X) < 1 for all units (scores are clipped to [0.02, 0.98]); Covariates are pre-treatment and the MLP correctly approximates e(X) = P(T=1 | X). Pre-conditions: Cross-sectional or pooled DataFrame with a binary treatment column and numeric covariate columns; No missing values in the covariate matrix passed to the network; Enough observations per treatment arm to fit a small MLP without overfitting. Failure modes: Heavy clipping at 0.02/0.98 indicates near-deterministic treatment and positivity violation \u2192 Trim or restrict to the region of common support before using the scores in a weighted estimator; torch absent and sklearn MLP fails to converge in max_iter with lbfgs \u2192 Increase max_iter, scale covariates, or shrink hidden_sizes for a simpler network. Alternatives: sp.overlap_weighted_did, sp.drdid, sp.did. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "hidden_sizes": {
          "description": "hidden_sizes parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "dl_propensity_score"
  },
  {
    "description": "Double/Debiased Machine Learning for treatment effect estimation. Supports partially linear (PLR), interactive regression (IRM, binary D), partially linear IV (PLIV), and interactive IV (IIVM, binary D/binary Z \u2192 LATE). Validation: certified parity evidence. Assumptions: Unconfoundedness: Y(d) \u22a5 D | X (conditional ignorability); Overlap: 0 < P(D=1 | X) < 1 for the estimand support (strong for IRM); Nuisance-function estimators converge at op(n^{-1/4}) \u2014 fast enough that orthogonal moments give \u221an CATE. Pre-conditions: data is tabular (DataFrame); covariates include all confounders conditional on which unconfoundedness holds; cross-fitting folds \u2265 2 (default 5) \u2014 more folds \u2192 lower variance, higher compute; for irm / iivm: treatment (and for iivm: instrument) is binary 0/1. Failure modes: Extreme propensity scores (\u2248 0 or 1) \u2192 Trim sample to 0.05 < e(x) < 0.95 or use overlap weights (sp.overlap_weights).; Nuisance models cross-val R\u00b2 near zero \u2192 Nuisances not learnable \u2014 DML bias guarantees don't apply; re-featurize or pick a different model family.; Large Monte-Carlo variance across folds (n_rep > 1) \u2192 Increase n_rep to 10+ and aggregate by median; check for leakage.. Alternatives\u2026",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "List of control variable names",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument": {
          "description": "Instrument (required for pliv/iivm)",
          "type": "string"
        },
        "model": {
          "default": "plr",
          "description": "DML model family",
          "enum": [
            "plr",
            "irm",
            "pliv",
            "iivm"
          ],
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds",
          "type": "integer"
        },
        "n_rep": {
          "default": 1,
          "description": "Repeated cross-fitting splits (median aggregation)",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment variable",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "dml"
  },
  {
    "description": "Build a :class:`DMLDiagnostics` report from a DML CausalResult. Assumptions: Unconfoundedness (IRM/PLR) or instrument validity (IIVM/PLIV) given the covariates; Overlap / positivity; Neyman-orthogonal score plus cross-fitting; nuisance estimators converge fast enough (o(n^-1/4)). Pre-conditions: Covariates, treatment, and outcome with enough data for cross-fitted machine-learning nuisances; For instrumented variants (PLIV / IIVM): an instrument as well. Failure modes: Propensity scores near 0/1 \u2014 overlap failure inflates variance and bias \u2192 Trim extreme scores, restrict the estimand to the overlap region, or report sensitivity (dml_sensitivity).. Alternatives: sp.tmle, sp.auto_cate, sp.causal_forest. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "clip": {
          "default": 0.02,
          "description": "For IRM-style overlap: count units with propensity within ``[0, clip] \u222a [1-clip, 1]`` as overlap-violating.",
          "type": "number"
        },
        "result": {
          "description": "Result returned by :func:`statspai.dml.dml`. Must include the post-fit residuals (``model_info['_y_resid']``, ``model_info['_d_resid']``); for IRM, additionally the propensity ``model_info['diagnostics']['pscore_min']`` etc. are surfaced.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "dml_diagnostics"
  },
  {
    "description": "Model-averaging DML (PLR) per Ahrens et al. (2025, JAE). Fits DML-PLR under multiple candidate nuisance learners and reports a risk-weighted (or equal/single-best) average of their \u03b8 estimates with a covariance-adjusted SE. Validation: validated. Assumptions: Unconfoundedness (IRM/PLR) or instrument validity (IIVM/PLIV) given the covariates; Overlap / positivity; Neyman-orthogonal score plus cross-fitting; nuisance estimators converge fast enough (o(n^-1/4)). Pre-conditions: Covariates, treatment, and outcome with enough data for cross-fitted machine-learning nuisances; For instrumented variants (PLIV / IIVM): an instrument as well. Failure modes: Propensity scores near 0/1 \u2014 overlap failure inflates variance and bias \u2192 Trim extreme scores, restrict the estimand to the overlap region, or report sensitivity (dml_sensitivity).. Alternatives: sp.tmle, sp.auto_cate, sp.causal_forest. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "candidates": {
          "description": "List of (ml_g, ml_m, label) sklearn triples; defaults to Lasso/Ridge/RF/GBM",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariate columns X",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment column",
          "type": "string"
        },
        "weight_rule": {
          "default": "inverse_risk",
          "description": "Weighting of candidate estimators",
          "enum": [
            "inverse_risk",
            "equal",
            "single_best"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "dml_model_averaging"
  },
  {
    "description": "Long-panel Double/Debiased ML for static panel models with fixed effects (Clarke & Polselli 2025, simplified). Absorbs unit (and optional time) fixed effects via within-transform, cross-fits ML nuisance learners with folds that split units, and reports cluster-robust SE at the unit level. PLR moment (continuous or binary treatment). Validation: validated. Assumptions: Conditional unconfoundedness within unit: E[\u03b5_it | X_it, \u03b1_i, \u03bb_t] = 0; Strict exogeneity conditional on covariates (weaker than standard FE); Nuisance learners converge fast enough (op(n^{-1/4})) after within-transform. Pre-conditions: long panel: at least unit and outcome columns; include_time_fe=True needs time column; enough units (clusters) for cluster-robust SE \u2014 \u2265 30 ideally; enough periods per unit for within-transform to leave variation in the treatment. Failure modes: Few units (< 30) \u2014 cluster-robust SE under-coverage \u2192 Use wild cluster bootstrap (sp.wild_cluster_bootstrap) or CR3 jackknife.; Within-unit variation in treatment is near zero \u2192 Unit FE absorbs almost all treatment variation \u2014 switch to between estimator or cross-section.; Nuisance cross-val R\u00b2 near zero on demeaned outcomes \u2192 ML nuisances not\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "binary_treatment": {
          "default": false,
          "description": "binary_treatment parameter (bool).",
          "type": "boolean"
        },
        "covariates": {
          "description": "Covariate columns X_it",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "include_time_fe": {
          "default": false,
          "description": "Whether to include time fe.",
          "type": "boolean"
        },
        "ml_g": {
          "description": "Outcome nuisance learner",
          "type": "string"
        },
        "ml_m": {
          "description": "Treatment nuisance learner",
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time column (required if include_time_fe)",
          "type": "string"
        },
        "treat": {
          "description": "Treatment column",
          "type": "string"
        },
        "unit": {
          "description": "Unit ID column (FE + clustering)",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "dml_panel"
  },
  {
    "description": "Compute DML-OVB sensitivity for a fitted DML CausalResult. Assumptions: Unconfoundedness (IRM/PLR) or instrument validity (IIVM/PLIV) given the covariates; Overlap / positivity; Neyman-orthogonal score plus cross-fitting; nuisance estimators converge fast enough (o(n^-1/4)). Pre-conditions: Covariates, treatment, and outcome with enough data for cross-fitted machine-learning nuisances; For instrumented variants (PLIV / IIVM): an instrument as well. Failure modes: Propensity scores near 0/1 \u2014 overlap failure inflates variance and bias \u2192 Trim extreme scores, restrict the estimand to the overlap region, or report sensitivity (dml_sensitivity).. Alternatives: sp.tmle, sp.auto_cate, sp.causal_forest. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "benchmark_covariates": {
          "description": "Subset of the original covariates to benchmark against. For each ``X_k``, the benchmark sets ``cf_y_bench, cf_d_bench`` to the partial R\u00b2 that ``X_k`` itself contributes (multiplied by ``k_y, k_d`` to express \"what if a confounder were k\u00d7 as strong as ``X_k``?\").",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cf_d": {
          "description": "Hypothesized partial-R\u00b2 of an unobserved confounder with the residualised outcome and treatment. If both are given, the report includes a bias bound and adjusted-estimate range.",
          "type": "number"
        },
        "cf_y": {
          "description": "Hypothesized partial-R\u00b2 of an unobserved confounder with the residualised outcome and treatment. If both are given, the report includes a bias bound and adjusted-estimate range.",
          "type": "number"
        },
        "k_d": {
          "default": 1.0,
          "description": "Multipliers for the benchmark strengths.",
          "type": "number"
        },
        "k_y": {
          "default": 1.0,
          "description": "Multipliers for the benchmark strengths.",
          "type": "number"
        },
        "q": {
          "default": 1.0,
          "description": "Bias threshold as a fraction of |\u03b8\u0302|. ``q=1`` \u21d2 confounder needed to shrink estimate to zero; ``q=0.5`` \u21d2 half the estimate.",
          "type": "number"
        },
        "result": {
          "description": "result parameter.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "dml_sensitivity"
  },
  {
    "description": "Dose-response function for a continuous treatment under unconfoundedness. Uses generalised propensity-score weighting or double ML for the conditional expectation E[Y(d)]. Validation: validated. Assumptions: Weak unconfoundedness: Y(d) \u22a5 D | X for each d; Generalised overlap: positive conditional density of D at each evaluated dose; Smoothness of dose-response function (for local-polynomial / kernel smoothing). Pre-conditions: treat is continuous (numeric, not binary); covariates comprise the confounding set; n \u2265 1000 for stable dose-response curves. Failure modes: Sparse data at extreme doses \u2192 Narrow dose_range; CIs at tails will be wide and uninformative.; Heavy-tailed generalised propensity weights \u2192 Use stabilised weights or restrict to common-support dose window.. Alternatives: sp.dml, sp.metalearner, sp.causal_forest. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dose_range": {
          "description": "(lo, hi) over which to evaluate dose-response",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_dose_points": {
          "default": 20,
          "description": "Number of dose points.",
          "type": "integer"
        },
        "treat": {
          "description": "Continuous treatment / dose",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "dose_response"
  },
  {
    "description": "Doubly-robust DiD (Sant'Anna & Zhao 2020). Combines outcome regression with IPW; consistent if either model is correct. Primary estimator for 2\u00d72 DiD with covariates. Validation: certified parity evidence. Assumptions: Conditional parallel trends given X; Overlap / positivity: 0 < P(D=1|X) < 1; Correct specification of at least one nuisance model. Pre-conditions: panel or repeated cross-section with 2 periods; group is a binary unit-level treatment indicator; covariates have non-zero variance and overlap. Failure modes: Propensity score near 0/1 (overlap violation) \u2192 Trim extreme propensity scores or use sp.ipw_trim.. Alternatives: sp.did_2x2, sp.callaway_santanna, sp.wooldridge_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates X",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group": {
          "description": "Unit-level treatment indicator (0/1)",
          "type": "string"
        },
        "method": {
          "default": "dr",
          "description": "Estimation method",
          "enum": [
            "dr",
            "or",
            "ipw",
            "reg",
            "stdipw"
          ],
          "type": "string"
        },
        "n_boot": {
          "description": "Bootstrap replications (None \u2192 analytical SE)",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "drdid"
  },
  {
    "description": "Hainmueller (2012) entropy balancing.  Targets the ATT by exactly balancing covariate means across treatment groups. No propensity-score model specification needed.",
    "input_schema": {
      "properties": {
        "covariates": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "moments": {
          "default": 1,
          "description": "Max moment balanced (1=means, 2=vars).",
          "type": "integer"
        },
        "treat": {
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y",
        "treat",
        "covariates"
      ],
      "type": "object"
    },
    "name": "ebalance"
  },
  {
    "description": "Olea-Pflueger (2013) robust effective F statistic for weak instruments.",
    "input_schema": {
      "properties": {
        "endog": {
          "description": "Endogenous regressor (single endogenous variable).",
          "type": "string"
        },
        "exog": {
          "description": "Included exogenous controls (a constant is added automatically).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "vcov": {
          "default": "HC1",
          "description": "Variance estimator for the first-stage residuals: - ``'classic'`` \u2014 homoskedastic; F_eff equals first-stage F. - ``'HC0'`` \u2014 White heteroskedasticity-robust. - ``'HC1'`` \u2014 HC0 with small-sample correction ``n/(n-k)``.",
          "enum": [
            "HC0",
            "HC1",
            "classic"
          ],
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments"
      ],
      "type": "object"
    },
    "name": "effective_f_test"
  },
  {
    "description": "Engle-Granger (1987) two-step cointegration test.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "lags": {
          "description": "Lags for ADF test. If None, uses AIC selection.",
          "type": "integer"
        },
        "trend": {
          "default": "c",
          "description": "trend parameter (str).",
          "type": "string"
        },
        "variables": {
          "description": "Variables to test (first is dependent).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "engle_granger"
  },
  {
    "description": "Enhanced event study plot with pre/post shading and significance coloring.",
    "input_schema": {
      "properties": {
        "alpha_level": {
          "default": 0.05,
          "description": "alpha_level parameter (float).",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "ci_alpha": {
          "default": 0.15,
          "description": "Confidence band transparency.",
          "type": "number"
        },
        "color": {
          "default": "#2C3E50",
          "description": "Default color for estimates.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "marker": {
          "default": "o",
          "description": "marker parameter (str).",
          "type": "string"
        },
        "markersize": {
          "default": 6,
          "description": "markersize parameter (int).",
          "type": "integer"
        },
        "post_color": {
          "default": "#FDEDEC",
          "description": "Post-treatment shading color.",
          "type": "string"
        },
        "pre_color": {
          "default": "#EBF5FB",
          "description": "Pre-treatment shading color.",
          "type": "string"
        },
        "result": {
          "description": "DID result with event study in ``model_info['event_study']``.",
          "type": "string"
        },
        "shade_post": {
          "default": true,
          "description": "Shade post-treatment region.",
          "type": "boolean"
        },
        "shade_pre": {
          "default": true,
          "description": "Shade pre-treatment region.",
          "type": "boolean"
        },
        "show_zero": {
          "default": true,
          "description": "Show horizontal zero line.",
          "type": "boolean"
        },
        "sig_color": {
          "default": "#E74C3C",
          "description": "Color for significant estimates. None disables coloring.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "enhanced_event_study_plot"
  },
  {
    "description": "Unified post-estimation diagnostics dispatcher.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for interpretation strings.",
          "type": "number"
        },
        "lags": {
          "default": 1,
          "description": "Number of lags for the Breusch-Godfrey test.",
          "type": "integer"
        },
        "powers": {
          "default": 3,
          "description": "Highest power of y-hat for the RESET test.",
          "type": "integer"
        },
        "print_results": {
          "default": true,
          "description": "If True, print a formatted table to stdout.",
          "type": "boolean"
        },
        "result": {
          "description": "A fitted result object with ``params``, ``data_info``, etc.",
          "type": "string"
        },
        "test": {
          "default": "all",
          "description": "Name of the diagnostic test. One of ``'hettest'``, ``'white'``, ``'reset'``, ``'ovtest'``, ``'bgodfrey'``, ``'dwatson'``, ``'vif'``, ``'ic'``, ``'linktest'``, ``'normality'``, ``'leverage'``, ``'endogenous'``, ``'overid'``, ``'firststage'``, ``'all'``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "estat"
  },
  {
    "description": "Display a pyfixest-style regression table for StatsPAI results.",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "etable"
  },
  {
    "description": "Endogenous treatment effects model.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "mle",
          "description": "'mle' or 'twostep' (Heckman-style).",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "treatment": {
          "description": "Binary treatment variable.",
          "type": "string"
        },
        "x": {
          "description": "Exogenous regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "z": {
          "description": "Instruments for the selection equation.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "treatment",
        "x",
        "y",
        "z"
      ],
      "type": "object"
    },
    "name": "etregress"
  },
  {
    "description": "Extended Two-Way Fixed Effects (Wooldridge 2021). Explicit API mirroring the R etwfe package. Alias for sp.wooldridge_did; same numerical output. Validation: certified parity evidence. Known limitations: cgroup='nevertreated' combined with panel=False (repeated cross-sections) is not yet supported; pass either panel=True with cgroup='nevertreated' or panel=False with cgroup='notyet'. Alternatives: sp.wooldridge_did, sp.callaway_santanna, sp.did_imputation. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cgroup": {
          "default": "notyet",
          "description": "Control group: 'notyet' (not-yet-treated) or 'nevertreated'. The latter is only supported when panel=True.",
          "enum": [
            "notyet",
            "nevertreated"
          ],
          "type": "string"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "first_treat parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "panel": {
          "default": true,
          "description": "If False, treat data as repeated cross-section",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "xvar": {
          "description": "R-style alias for controls",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "etwfe"
  },
  {
    "description": "R ``etwfe::emfx``-style aggregated marginal effects for an ETWFE fit. Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) \u2192 Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates.; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights \u2192 Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage).. Alternatives: sp.callaway_santanna, sp.did, sp.honest_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "include_leads": {
          "default": false,
          "description": "For ``type='event'`` and ``type='calendar'``, whether to include pre-treatment relative times (``rel_time < 0``) in the output. These coefficients identify pre-trends and are informative for parallel-trends inspection. Default ``False`` for backward compatibility with earlier versions; set ``True`` for full event-study output matching the R ``etwfe::emfx(type='event')`` default. ``rel_time = -1`` is always the reference category and is excluded.",
          "type": "boolean"
        },
        "result": {
          "description": "Output of :func:`etwfe` or :func:`wooldridge_did`.",
          "type": "string"
        },
        "type": {
          "default": "simple",
          "description": "Aggregation type.",
          "enum": [
            "simple",
            "group",
            "event",
            "calendar"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "etwfe_emfx"
  },
  {
    "description": "Compute E-value for sensitivity to unmeasured confounding. Validation: certified parity evidence. Assumptions: VanderWeele-Ding (2017) E-value: quantifies the minimum confounder-exposure and confounder-outcome risk ratios that could explain away an observed association; The estimate is expressed (or convertible) to a risk-ratio scale; OR/HR are mapped to RR, with the rare-outcome approximation only valid when the outcome is uncommon; The E-value bounds joint confounding but assumes no other bias (selection, measurement, model misspecification). Pre-conditions: A point estimate on a supported scale (RR/OR/HR/diff/RD/SMD); Either an SE or an explicit CI to obtain the CI E-value. Failure modes: Ratio estimate <= 0 supplied for measure RR/OR/HR, or risk difference outside [-1, 1] \u2192 Pass a positive ratio for RR/OR/HR or a difference within [-1, 1], matching the measure argument.; Reported E-value near 1.0, i.e. trivially weak confounding overturns the result \u2192 Treat the finding as fragile to unmeasured confounding and corroborate with a regression-based sensitivity analysis.. Alternatives: sp.sensemakr, sp.oster_bounds, sp.unified_sensitivity. Typical minimum N: 1.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for CI if se is provided but ci is not.",
          "type": "number"
        },
        "ci": {
          "description": "Confidence interval (lower, upper). If not provided, computed from estimate +/- z * se.",
          "type": "number"
        },
        "estimate": {
          "description": "Point estimate of the treatment effect. - For measure='RR': risk ratio (must be > 0). - For measure='OR': odds ratio (must be > 0). - For measure='HR': hazard ratio (must be > 0). - For measure='diff': risk difference (between -1 and 1). - For measure='SMD': standardised mean difference. - For measure='RD': same as 'diff'.",
          "type": "number"
        },
        "measure": {
          "default": "RR",
          "description": "Type of effect measure: 'RR', 'OR', 'HR', 'diff', 'RD', 'SMD'.",
          "type": "string"
        },
        "rare_outcome": {
          "default": false,
          "description": "If True and measure='OR', uses the rare disease approximation (OR approx RR). Otherwise converts OR to RR.",
          "type": "boolean"
        },
        "se": {
          "description": "Standard error of the estimate (used to compute CI if ci is not provided).",
          "type": "number"
        }
      },
      "required": [
        "estimate"
      ],
      "type": "object"
    },
    "name": "evalue"
  },
  {
    "description": "Compute E-value from a StatsPAI CausalResult object.",
    "input_schema": {
      "properties": {
        "measure": {
          "default": "SMD",
          "description": "How to interpret the estimate. For most causal estimators producing ATE/ATT on continuous outcomes, 'SMD' is appropriate.",
          "type": "string"
        },
        "rare_outcome": {
          "default": false,
          "description": "rare_outcome parameter (bool).",
          "type": "boolean"
        },
        "result": {
          "description": "Result from any StatsPAI causal estimator.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "evalue_from_result"
  },
  {
    "description": "E-value computed directly from a risk ratio and its CI bounds.",
    "input_schema": {
      "properties": {
        "rare_outcome": {
          "default": false,
          "description": "rare_outcome parameter (bool).",
          "type": "boolean"
        },
        "rr": {
          "description": "rr parameter (float).",
          "type": "number"
        },
        "rr_lower": {
          "description": "rr_lower parameter (Optional[float]).",
          "type": "number"
        },
        "rr_upper": {
          "description": "rr_upper parameter (Optional[float]).",
          "type": "number"
        }
      },
      "required": [
        "rr"
      ],
      "type": "object"
    },
    "name": "evalue_rr"
  },
  {
    "description": "Traditional OLS event-study with entity and time FEs. Generates relative-time dummies around the treatment date, omits a reference period, and estimates via TWFE + optional clustered SE. Exposed for users who want the classical specification alongside CS / SA / BJS; not robust to staggered-effect heterogeneity \u2014 use sp.sun_abraham for that. Validation: validated. Assumptions: Parallel trends across event time; No anticipation beyond window lead; SUTVA. Pre-conditions: panel with unit \u00d7 time \u00d7 outcome; treat_time column gives first-treatment period (or 0/NaN). Failure modes: Staggered heterogeneity \u2014 TWFE event-study biased \u2192 Use sp.sun_abraham for contamination-robust event-study coefficients.. Alternatives: sp.sun_abraham, sp.callaway_santanna, sp.did_imputation. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ref_period": {
          "default": -1,
          "description": "Reference relative-time period to omit",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "First-treatment period column",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier",
          "type": "string"
        },
        "window": {
          "default": [
            -4,
            4
          ],
          "description": "(lead, lag) horizons",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat_time",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "event_study"
  },
  {
    "description": "Adapter that turns an event-study fit into a regtable input.",
    "input_schema": {
      "properties": {
        "include_reference": {
          "default": false,
          "description": "Whether to render the reference period row (typically ``t=-1``) where the estimate is identically zero.",
          "type": "boolean"
        },
        "label_fmt": {
          "default": "t={t}",
          "description": "Format string for the row label of each event-time bin. The ``{t}`` placeholder receives the integer relative time.",
          "type": "string"
        },
        "regex": {
          "description": "Pattern with one capture group that matches the relative time in coefficient names. Required when the CausalResult fast path is not applicable. Examples: ``r\"^tau_(-?\\d+)$\"``, ``r\"^lag(\\d+)$\"``, ``r\"::(-?\\d+)$\"``.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model. The CausalResult fast path is used automatically when ``model_info['event_study']`` is present.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "event_study_table"
  },
  {
    "description": "Return runnable code examples + registry metadata for a function.",
    "input_schema": {
      "properties": {
        "name": {
          "description": "Canonical StatsPAI function name (e.g. ``\"did\"``, ``\"regress\"``, ``\"callaway_santanna\"``). Lower-cased and stripped before lookup.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "examples"
  },
  {
    "description": "Fairlie (2005) nonlinear decomposition for binary outcomes.",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "model": {
          "default": "logit",
          "description": "Model variant or parameterisation to fit.",
          "enum": [
            "logit",
            "probit"
          ],
          "type": "string"
        },
        "n_sim": {
          "default": 500,
          "description": "Number of sim.",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "reference parameter (int).",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "enum": [
            "0",
            "1"
          ],
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "fairlie"
  },
  {
    "description": "Run FCI. Returns a :class:`FCIResult` with the learned PAG. Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges \u2014 faithfulness or sample size is the likely culprit \u2192 Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible.. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for CI tests.",
          "type": "number"
        },
        "ci_test": {
          "default": "fisherz",
          "description": "Only Fisher-Z partial-correlation test is supported; extensions (kernel / chi-square) can be added later.",
          "type": "string"
        },
        "max_cond_size": {
          "description": "Max size of conditioning set.",
          "type": "integer"
        },
        "variables": {
          "description": "Columns to use; defaults to all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "fci"
  },
  {
    "description": "Estimate GLM (logit, probit, Gaussian) with high-dimensional fixed effects.",
    "input_schema": {
      "properties": {
        "family": {
          "default": "gaussian",
          "description": "GLM family: ``\"gaussian\"``, ``\"logit\"``, ``\"probit\"``.",
          "type": "string"
        },
        "fml": {
          "description": "pyfixest formula.",
          "type": "string"
        },
        "vcov": {
          "description": "Variance-covariance estimator. Additional arguments passed to ``pyfixest.feglm()``.",
          "type": "object"
        }
      },
      "required": [
        "fml"
      ],
      "type": "object"
    },
    "name": "feglm"
  },
  {
    "description": "Estimate OLS / IV with high-dimensional fixed effects via pyfixest. Validation: certified parity evidence. Assumptions: Strict exogeneity conditional on the absorbed fixed effects; No perfect collinearity after FE absorption (within-transformation rank); Cluster structure for `vcov={'CRV1': '...'}` matches the relevant dependence. Pre-conditions: Data is a long-format DataFrame; FE columns are categorical or convertible; Every absorbed FE level has more than one observation (singleton dropping behaviour controlled by `drop_singletons`); Optional IV stage: instruments are at least as many as endogenous regressors. Failure modes: Singleton groups dropped warning \u2192 Aggregate small categories or accept the drop; verify estimand is unchanged.; Slow convergence on > 3 high-cardinality FEs \u2192 Reduce FE dimension, switch to Rust HDFE backend, or simplify the model.. Alternatives: sp.panel, sp.regress, sp.hdfe_ols. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "collin_tol": {
          "default": 1e-06,
          "description": "Collinearity tolerance.",
          "type": "number"
        },
        "fixef_rm": {
          "default": "none",
          "description": "How to handle singleton fixed effects: ``\"none\"`` (keep) or ``\"singleton\"`` (drop).",
          "type": "string"
        },
        "fml": {
          "description": "A pyfixest-style formula. Examples: - ``\"Y ~ X1 + X2\"`` \u2014 plain OLS - ``\"Y ~ X1 | firm + year\"`` \u2014 two-way fixed effects - ``\"Y ~ 1 | firm | X1 ~ Z1\"`` \u2014 IV with fixed effects - ``\"Y ~ X1 | csw0(firm, year)\"`` \u2014 multiple estimations",
          "type": "string"
        },
        "lean": {
          "default": false,
          "description": "If True, drop large intermediate arrays to save memory. Additional arguments passed to ``pyfixest.feols()``.",
          "type": "boolean"
        },
        "ssc": {
          "description": "Small-sample correction via ``pyfixest.ssc()``.",
          "type": "string"
        },
        "vcov": {
          "description": "Variance-covariance estimator. - ``\"iid\"`` \u2014 classical - ``\"HC1\"``, ``\"HC2\"``, ``\"HC3\"`` \u2014 heteroskedasticity-robust - ``{\"CRV1\": \"firm\"}`` \u2014 cluster-robust - ``{\"CRV1\": \"firm + year\"}`` \u2014 two-way clustering",
          "type": "object"
        },
        "weights": {
          "description": "Column name for regression weights.",
          "type": "string"
        }
      },
      "required": [
        "fml"
      ],
      "type": "object"
    },
    "name": "feols"
  },
  {
    "description": "Estimate Poisson regression with high-dimensional fixed effects via pyfixest. Assumptions: Conditional mean exponential link: E[y | x, alpha] = exp(x'beta + alpha); Strict exogeneity conditional on the absorbed fixed effects (PPML consistency). Pre-conditions: Outcome is a non-negative count or non-negative continuous variable; Fixed effects columns are categorical; absorbed groups exist. Failure modes: Convergence failure or extreme exponentiated predictions \u2192 Drop large-magnitude regressors, rescale, or switch to OLS on log(1+y) (with caveats).; Separation: some FE level perfectly predicts zero outcomes \u2192 Drop perfectly-predicted groups and rerun; document the restriction.. Alternatives: sp.feols, sp.regress, sp.panel. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "collin_tol": {
          "default": 1e-06,
          "description": "Collinearity tolerance.",
          "type": "number"
        },
        "fixef_rm": {
          "default": "none",
          "description": "Singleton fixed effect handling.",
          "type": "string"
        },
        "fml": {
          "description": "pyfixest formula. E.g. ``\"Y ~ X1 | firm\"``.",
          "type": "string"
        },
        "iwls_maxiter": {
          "default": 25,
          "description": "Max IWLS iterations. Additional arguments passed to ``pyfixest.fepois()``.",
          "type": "integer"
        },
        "iwls_tol": {
          "default": 1e-08,
          "description": "IWLS convergence tolerance.",
          "type": "number"
        },
        "ssc": {
          "description": "Small-sample correction.",
          "type": "string"
        },
        "vcov": {
          "description": "Variance-covariance estimator.",
          "type": "object"
        },
        "weights": {
          "description": "Column name for regression weights.",
          "type": "string"
        }
      },
      "required": [
        "fml"
      ],
      "type": "object"
    },
    "name": "fepois"
  },
  {
    "description": "Firpo-Fortin-Lemieux two-step detailed distributional decomposition. Assumptions: Firpo-Fortin-Lemieux RIF regression: small perturbations to the covariate distribution induce small changes in the distributional statistic; Linear approximation of the recentered influence function is locally valid. Pre-conditions: Outcome is continuous (e.g. log earnings) with adequate distributional support; Covariates explain a non-trivial share of outcome variation across groups. Failure modes: RIF instability at extreme quantiles \u2192 Avoid quantiles below ~0.05 or above ~0.95; widen the kernel bandwidth.. Alternatives: sp.dfl_decompose, sp.oaxaca, sp.rif_decomposition. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "enum": [
            "0",
            "1"
          ],
          "type": "string"
        },
        "inference": {
          "default": "analytical",
          "description": "inference parameter (str).",
          "enum": [
            "analytical",
            "bootstrap"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 299,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "0: B reweighted to look like A's X (composition = effect of A's X on B's outcomes relative to observed B)",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "stat": {
          "default": "quantile",
          "description": "'log_var', 'theil_t', 'theil_l', 'atkinson'}",
          "type": "string"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile level or target treatment-effect index.",
          "type": "number"
        },
        "trim": {
          "default": 0.001,
          "description": "trim parameter (float).",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "ffl_decompose"
  },
  {
    "description": "Fisher's exact randomization test with enhanced features.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the Hodges-Lehmann confidence interval.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable for cluster-level randomization (permute by cluster).",
          "type": "string"
        },
        "controls": {
          "description": "Control variables for covariate-adjusted inference. When provided, the test statistic is computed on residuals from regressing Y on controls.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_perm": {
          "default": 10000,
          "description": "Number of random permutations.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "statistic": {
          "default": "ate",
          "description": "Test statistic to use: - ``'ate'``: Average treatment effect (difference in means). - ``'ks'``: Kolmogorov-Smirnov statistic. - ``'rank_sum'``: Wilcoxon rank-sum statistic.",
          "type": "string"
        },
        "stratify": {
          "description": "Variable for stratified permutation (permute within strata).",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment variable name (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "fisher_exact"
  },
  {
    "description": "Functional doubly-robust CATE estimator. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support \u2192 Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner).. Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "test_data": {
          "description": "Defaults to ``data``.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y_columns": {
          "description": "Outcome columns; len = number of function points.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y_columns"
      ],
      "type": "object"
    },
    "name": "focal_cate"
  },
  {
    "description": "Return overlap and CATE-distribution diagnostics for a fitted forest. Assumptions: Unconfoundedness given the covariates; Overlap / positivity; Honesty: separate subsamples are used to choose splits and to estimate effects. Pre-conditions: Covariates, treatment, and outcome with enough data to grow an honest forest. Failure modes: Calibration test rejects \u2014 the forest's heterogeneity is not well calibrated \u2192 Increase the sample / number of trees, or fall back to a doubly-robust learner.. Alternatives: sp.dml, sp.auto_cate, sp.tmle. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "T": {
          "description": "T parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "propensity_bounds": {
          "default": [
            0.05,
            0.95
          ],
          "description": "propensity_bounds parameter (Tuple[float, float]).",
          "type": "number"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "forest_diagnostics"
  },
  {
    "description": "Fortified proximal causal inference (Yu, Shi & Tchetgen Tchetgen 2025). Adds a bridge-function stability constraint that gives robust ATT under mild misspecification of the outcome/treatment bridge. Validation: validated. Assumptions: The proxies are valid negative controls (relevant to the confounder, excluded from the causal channel); A bridge function exists (completeness conditions hold). Pre-conditions: Treatment-inducing and outcome-inducing proxy variables (negative controls) for the unobserved confounder. Failure modes: Proxies are weak or invalid \u2014 the bridge function is poorly identified \u2192 Test proxy relevance, select stronger proxies, or fall back to sensitivity analysis.. Alternatives: sp.select_pci_proxies, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_w": {
          "description": "Outcome-side proxies",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "Treatment-side proxies",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "proxy_w",
        "proxy_z",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "fortified_pci"
  },
  {
    "description": "Fractional response model (Papke & Wooldridge 1996).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for clustered SE.",
          "type": "string"
        },
        "link": {
          "default": "logit",
          "description": "Link function: 'logit' or 'probit'.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "robust",
          "description": "Always use robust SE (quasi-MLE).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable in [0, 1].",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "fracreg"
  },
  {
    "description": "Translate a single R / fixest / felm / did expression to a verified StatsPAI tool-call payload. Returns the same shape as from_stata. Supported callables: feols / felm / lm / att_gt / did. Pass ONE expression \u2014 no assignment, no piping.",
    "input_schema": {
      "properties": {
        "expression": {
          "description": "One R expression, e.g. 'feols(y ~ x | id^year, data=df, cluster=\"id\")'.",
          "type": "string"
        }
      },
      "required": [
        "expression"
      ],
      "type": "object"
    },
    "name": "from_r"
  },
  {
    "description": "Translate a single Stata command to a verified StatsPAI tool-call payload. Returns ``python_code`` (string for chat replies) AND ``arguments`` (ready-to-dispatch JSON-RPC for tools/call). Tier-1 commands: regress / xtreg / reghdfe / ivreg2 / csdid / did_imputation / synth / rdrobust; count-panel commands include nbreg / xtnbreg / ppmlhdfe. Unrecognised commands return close-match suggestions instead of guessing.",
    "input_schema": {
      "properties": {
        "command": {
          "description": "One Stata command, e.g. 'reghdfe y x, absorb(id year) cluster(id)'. Multi-command lines must be split by the caller.",
          "type": "string"
        }
      },
      "required": [
        "command"
      ],
      "type": "object"
    },
    "name": "from_stata"
  },
  {
    "description": "Pearl's front-door adjustment: identifies ATE with unmeasured confounding when a mediator fully transmits the effect of D on Y. Supports binary or continuous mediator; integrate_by controls Pearl (marginal) vs Fulcher et al. (conditional) aggregation. Validation: validated. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) \u2192 Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE).. Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Pre-treatment covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "integrate_by": {
          "default": "marginal",
          "description": "MC integration formulation (continuous M only)",
          "enum": [
            "marginal",
            "conditional"
          ],
          "type": "string"
        },
        "mediator": {
          "description": "Fully-transmitting mediator",
          "type": "string"
        },
        "mediator_type": {
          "default": "auto",
          "description": "Mediator model",
          "enum": [
            "auto",
            "binary",
            "continuous"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment (0/1)",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "mediator",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "front_door"
  },
  {
    "description": "Front-door adjustment \u2014 article-friendly alias for",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "m": {
          "description": "m parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "d",
        "m",
        "y"
      ],
      "type": "object"
    },
    "name": "frontdoor"
  },
  {
    "description": "Parametric g-formula (standardization) estimator. ATE/ATT for binary D, or dose-response curve for continuous D. Consistent under correctly-specified outcome model; not doubly robust. Validation: validated. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) \u2192 Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE).. Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Baseline covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "ATE",
          "description": "Target estimand",
          "enum": [
            "ATE",
            "ATT",
            "dose_response"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications for SE",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment variable",
          "type": "string"
        },
        "treat_values": {
          "description": "Dose grid (required for dose_response)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "g_computation"
  },
  {
    "description": "G-estimation for a multi-stage dynamic treatment regime. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) \u2192 Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE).. Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates_by_stage": {
          "description": "Covariates (tailoring variables) available at each stage. covariates_by_stage[k] are the variables available when deciding treatment k.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "propensity_covariates": {
          "description": "Covariates for propensity model at each stage. If None, uses covariates_by_stage.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatments": {
          "description": "Treatment variables at each stage, in temporal order. E.g., ['A1', 'A2'] for a two-stage DTR.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Final outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "covariates_by_stage",
        "treatments",
        "y"
      ],
      "type": "object"
    },
    "name": "g_estimation"
  },
  {
    "description": "Lundberg (2021) gap-closing estimator.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "inference": {
          "default": "analytical",
          "description": "inference parameter (str).",
          "enum": [
            "analytical",
            "bootstrap"
          ],
          "type": "string"
        },
        "method": {
          "default": "aipw",
          "description": "AIPW is doubly robust (recommended).",
          "enum": [
            "regression",
            "ipw",
            "aipw"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 299,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "target_dist": {
          "default": 1,
          "description": "- 1: shift Group A's covariate distribution to match Group B's - 0: shift Group B's to match Group A's",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "trim": {
          "default": 0.001,
          "description": "trim parameter (float).",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "gap_closing"
  },
  {
    "description": "Fit GARCH(p,q) by conditional Gaussian MLE.",
    "input_schema": {
      "properties": {
        "mean": {
          "default": true,
          "description": "Estimate a constant mean \u03bc; if False, \u03bc = 0.",
          "type": "boolean"
        },
        "p": {
          "default": 1,
          "description": "Number of GARCH (lagged \u03c3\u00b2) terms.",
          "type": "integer"
        },
        "q": {
          "default": 1,
          "description": "Number of ARCH (lagged \u03b5\u00b2) terms.",
          "type": "integer"
        },
        "y": {
          "description": "Return series (or log-return, etc.).",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "garch"
  },
  {
    "description": "Gardner (2021) two-stage DID. Stage-1 fits two-way FEs on untreated observations; Stage-2 regresses the residualised outcome on treatment dummies (ATT or event study). Numerically close to Borusyak-Jaravel-Spiess imputation with unit-clustered SEs. Validation: validated. Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) \u2192 Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates.; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights \u2192 Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage).. Alternatives: sp.callaway_santanna, sp.did, sp.honest_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for Stage-2 SEs (defaults to group)",
          "type": "string"
        },
        "controls": {
          "description": "Additional covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "event_study": {
          "default": false,
          "description": "If True, report coefficients by relative time k = t - first_treat",
          "type": "boolean"
        },
        "first_treat": {
          "description": "First-treatment-period column; 0/NaN/inf = never treated",
          "type": "string"
        },
        "group": {
          "description": "Unit/panel-id column",
          "type": "string"
        },
        "horizon": {
          "description": "Relative-time leads/lags to report (default range(-5, 6))",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time column",
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "gardner_did"
  },
  {
    "description": "Test for significant heterogeneity across GATE (Group ATE) groups. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support \u2192 Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner).. Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "by": {
          "description": "Column name to group by, or 'cate' for CATE quartiles.",
          "type": "string"
        },
        "n_groups": {
          "default": 4,
          "description": "Number of groups.",
          "type": "integer"
        },
        "result": {
          "description": "Result from ``metalearner()``.",
          "type": "string"
        }
      },
      "required": [
        "by",
        "result"
      ],
      "type": "object"
    },
    "name": "gate_test"
  },
  {
    "description": "Gelbach (2016) decomposition of omitted variable bias.",
    "input_schema": {
      "properties": {
        "added_x": {
          "description": "Variables added to obtain the full (long) specification.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "base_x": {
          "description": "Variables in the base (short) specification.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "var_of_interest": {
          "description": "Which base variable's coefficient change to decompose. Defaults to the first element of ``base_x``.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "added_x",
        "base_x",
        "y"
      ],
      "type": "object"
    },
    "name": "gelbach"
  },
  {
    "description": "High-order bunching design with bias correction. Assumptions: The counterfactual density would be smooth through the threshold absent the policy; Excess mass at the threshold reflects the behavioural elasticity of interest; No other discontinuity coincides with the threshold. Pre-conditions: A behavioural choice variable (earnings, hours, ...) with a known kink or notch in the budget/choice set; A visible empirical density of the running variable around the threshold. Failure modes: Round-number heaping or a coincident policy contaminates the bunching mass \u2192 Exclude heaping points, widen the excluded region, and test the counterfactual polynomial order.. Alternatives: sp.rdrobust, sp.rkd. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "default": 1.0,
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "bin_width": {
          "description": "Defaults to bandwidth / 25.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "polynomial_order": {
          "default": 4,
          "description": "Order of the counterfactual polynomial fit.",
          "type": "integer"
        },
        "running": {
          "description": "Running variable (e.g. earnings).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [
        "running"
      ],
      "type": "object"
    },
    "name": "general_bunching"
  },
  {
    "description": "Genetic Matching for ATT estimation. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap \u2014 extreme propensity scores or few acceptable matches \u2192 Trim or restrict to the common-support region and report the discarded units.; Covariate imbalance remains after matching/weighting \u2192 Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences.. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "generations": {
          "default": 20,
          "description": "generations parameter (int).",
          "type": "integer"
        },
        "k": {
          "default": 1,
          "description": "Number of matches per treated unit.",
          "type": "integer"
        },
        "mutation_rate": {
          "default": 0.2,
          "description": "mutation_rate parameter (float).",
          "type": "number"
        },
        "population_size": {
          "default": 40,
          "description": "population_size parameter (int).",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment indicator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "genmatch"
  },
  {
    "description": "User-friendly alias for :func:`sp.rdms` (multi-score RD). Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "geographic_rd"
  },
  {
    "description": "German reunification dataset (simulated).",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "german_reunification"
  },
  {
    "description": "Greedy Equivalence Search. Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges \u2014 faithfulness or sample size is the likely culprit \u2192 Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible.. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "max_iter": {
          "default": 500,
          "description": "Maximum total edge additions + removals.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "ges"
  },
  {
    "description": "Plot an ``aggte()`` result, mirroring R :func:`did::ggdid`.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "band_color": {
          "default": "#F18F01",
          "description": "Colours for the pointwise estimate and the uniform band.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "point_color": {
          "default": "#2E86AB",
          "description": "Colours for the pointwise estimate and the uniform band.",
          "type": "string"
        },
        "result": {
          "description": "Output of :func:`aggte`.",
          "type": "string"
        },
        "show_pointwise": {
          "default": true,
          "description": "Draw pointwise CI lines.",
          "type": "boolean"
        },
        "show_uniform": {
          "default": true,
          "description": "Draw uniform band (shaded region).",
          "type": "boolean"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "ggdid"
  },
  {
    "description": "Fit a Generalized Linear Model.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable name for clustered standard errors.",
          "type": "string"
        },
        "exposure": {
          "description": "Variable name for exposure (``log(exposure)`` is added as offset).",
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "Distribution family. One of ``\"gaussian\"``, ``\"binomial\"``, ``\"poisson\"``, ``\"gamma\"``, ``\"inverse_gaussian\"``, ``\"negative_binomial\"``.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula (e.g. ``\"y ~ x1 + x2\"``).",
          "type": "string"
        },
        "link": {
          "description": "Link function. If ``None`` the canonical link for the chosen family is used. Options: ``\"identity\"``, ``\"log\"``, ``\"logit\"``, ``\"probit\"``, ``\"inverse\"``, ``\"cloglog\"``, ``\"power\"``, ``\"sqrt\"``.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum number of IRLS iterations.",
          "type": "integer"
        },
        "offset": {
          "description": "Variable name for offset.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard-error type (``\"nonrobust\"``, ``\"hc0\"``-``\"hc3\"``, ``\"hac\"``).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance on the relative change in deviance.",
          "type": "number"
        },
        "weights": {
          "description": "Variable name for observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Names of independent variables (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Name of the dependent variable (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "glm"
  },
  {
    "description": "General GMM estimator for arbitrary moment conditions.",
    "input_schema": {
      "properties": {
        "W": {
          "description": "Weighting matrix (q x q). If None, uses identity (one-step) then optimal (two-step).",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "twostep",
          "description": "'onestep', 'twostep', 'iterative', 'cue' (continuously updated).",
          "type": "string"
        },
        "moment_fn": {
          "description": "Function g(theta, data) -> np.ndarray of shape (n, q) returning moment conditions for each observation.",
          "type": "string"
        },
        "param_names": {
          "description": "Names for parameters.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "se": {
          "default": "robust",
          "description": "'robust' (HAC-type), 'unadjusted'.",
          "type": "string"
        },
        "theta0": {
          "description": "Initial parameter vector.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        }
      },
      "required": [
        "moment_fn",
        "theta0"
      ],
      "type": "object"
    },
    "name": "gmm"
  },
  {
    "description": "Granger causality test.",
    "input_schema": {
      "properties": {
        "caused": {
          "description": "Variable being tested for causation.",
          "type": "string"
        },
        "causing": {
          "description": "Variable hypothesized to cause.",
          "type": "string"
        },
        "lags": {
          "description": "Number of lags (if fitting new VAR).",
          "type": "integer"
        },
        "var_result": {
          "description": "Pre-estimated VAR model.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "granger_causality"
  },
  {
    "description": "Plot group-time ATT estimates from Callaway-Sant'Anna.",
    "input_schema": {
      "properties": {
        "alpha_level": {
          "default": 0.05,
          "description": "Significance threshold.",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "Default color for dot plot.",
          "type": "string"
        },
        "figsize": {
          "default": [
            12,
            7
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "insig_color": {
          "default": "#BDC3C7",
          "description": "Color for insignificant estimates.",
          "type": "string"
        },
        "plot_type": {
          "default": "dot",
          "description": "'dot' or 'heatmap'.",
          "type": "string"
        },
        "result": {
          "description": "Result from ``callaway_santanna()`` or ``did(method='cs')``. Must have ``detail`` DataFrame with 'group', 'time', 'att' columns.",
          "type": "string"
        },
        "sig_color": {
          "default": "#E74C3C",
          "description": "Color for significant estimates.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "group_time_plot"
  },
  {
    "description": "Generalized Synthetic Control via interactive fixed effects. Validation: certified parity evidence. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Additional time-varying covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cv_folds": {
          "default": 5,
          "description": "Cross-validation folds for factor selection.",
          "type": "integer"
        },
        "max_factors": {
          "default": 5,
          "description": "Maximum factors to try during CV.",
          "type": "integer"
        },
        "n_factors": {
          "description": "Number of latent factors. If None, selected by cross-validation.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run placebo inference.",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "gsynth"
  },
  {
    "description": "TMLE with Highly Adaptive Lasso (HAL) nuisance learners (Qian & van der Laan 2025). The stable 'delta' variant plugs HAL into standard TMLE. The reserved 'projection' variant raises NotImplementedError until the Riesz-projection targeting step has reference parity. Validation: validated. Known limitations: variant='projection' raises NotImplementedError \u2014 the Riesz-projection targeting step from Li-Qiu-Wang-vdL (2025) \u00a73.2 is not yet ported (the v1.11.x code path was a no-op on the point estimate; see CHANGELOG). The implementation roadmap and parity-test gates are in docs/rfc/hal_tmle_projection.md. Assumptions: Unconfoundedness (sequential exchangeability for longitudinal/LTMLE); Positivity / overlap of treatment given history; At least one nuisance (outcome or treatment) is estimated consistently; the targeting step gives double robustness. Pre-conditions: Covariates, treatment, and outcome (for survival/longitudinal variants: time-to-event and time-varying covariates); Enough data to fit a Super Learner / HAL nuisance library. Failure modes: Near-positivity violations create extreme clever-covariate weights \u2192 Truncate weights, restrict the estimand, or report a positivity diag\u2026",
    "input_schema": {
      "properties": {
        "C_propensity": {
          "default": 1.0,
          "description": "Inverse L1 penalty for HAL propensity classifier",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "ATE",
          "description": "Estimand",
          "enum": [
            "ATE",
            "ATT"
          ],
          "type": "string"
        },
        "lambda_outcome": {
          "description": "Outcome L1 penalty; None \u2192 5-fold CV",
          "type": "number"
        },
        "max_anchors_per_col": {
          "default": 40,
          "description": "Column name for max anchors per.",
          "type": "integer"
        },
        "n_folds": {
          "default": 5,
          "description": "Number of cross-fitting or cross-validation folds.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "variant": {
          "default": "delta",
          "description": "HAL-TMLE variant",
          "enum": [
            "delta",
            "projection"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "hal_tmle"
  },
  {
    "description": "Harvest every valid 2\u00d72 DID comparison from a staggered panel and aggregate them via precision-weighted / simple / cohort-weighted averages. Agnostic to cohort structure; useful for robustness comparisons against CS / SA / BJS. Validation: validated. Assumptions: Parallel trends hold for every valid 2x2 sub-comparison being harvested and aggregated; No anticipation before each cohort's first treated period (reference horizon -1 is clean); Independence across units within each cohort (unit-level cluster-robust SEs); cross-horizon covariance ignored. Pre-conditions: Long-format panel with unit, time, outcome columns; Either a binary treat indicator or a precomputed cohort (first-treatment) column with a never_value marker; Multiple cohorts and overlapping periods so valid 2x2 comparisons exist across the requested horizons. Failure modes: No clean (never-treated or not-yet-treated) controls so no valid 2x2 cells can be harvested \u2192 Add never-treated units or restrict horizons to periods with available clean controls; Precision weighting dominated by one tiny high-variance comparison distorts the aggregate \u2192 Switch weighting to 'equal' or 'n_treated' to down-weight unstable cells. Alt\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cohort": {
          "description": "First-treat cohort column (for static harvesting)",
          "type": "string"
        },
        "horizons": {
          "description": "horizons parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "never_value": {
          "default": 0,
          "description": "never_value parameter.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "reference": {
          "default": "pre",
          "description": "Reference period convention",
          "enum": [
            "pre",
            "first_treat"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Time-varying treat indicator (for dynamic harvesting)",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "harvest_did"
  },
  {
    "description": "Hausman test for FE vs RE in panel data.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "id": {
          "description": "Unit identifier.",
          "type": "string"
        },
        "time": {
          "description": "Time period identifier.",
          "type": "string"
        },
        "x": {
          "description": "Independent variables (time-varying).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "hausman_test"
  },
  {
    "description": "reghdfe-style OLS with high-dimensional fixed effects. Validation: certified parity evidence. Assumptions: Linear conditional mean after absorbing high-dimensional fixed effects (Frisch-Waugh-Lovell); Exogeneity of regressors conditional on the absorbed fixed-effect dimensions; Clustered SEs require enough clusters for asymptotics; multiway cluster needs each dimension well-populated. Pre-conditions: Continuous outcome; Fixed-effect factor variable(s) in the '| fe1 + fe2' part of the formula. Failure modes: Singleton groups absorb their own observations and bias clustered SEs \u2192 Keep drop_singletons=True so singletons are removed before estimation; Alternating-projections absorber does not converge \u2192 Raise maxiter / loosen tol, or reduce the number of FE dimensions; Too few clusters make wild/cluster inference unreliable \u2192 Use a wild-cluster bootstrap or cluster at a coarser level. Alternatives: sp.feols, sp.regress, sp.qreg. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "One-way or multi-way cluster column(s).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "drop_singletons": {
          "default": true,
          "description": "drop_singletons parameter (bool).",
          "type": "boolean"
        },
        "formula": {
          "description": "``\"y ~ x1 + x2 | fe1 + fe2 + fe3\"``. The ``| fe...`` part is optional.",
          "type": "string"
        },
        "maxiter": {
          "default": 10000,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "se_type": {
          "description": "Override automatic inference of SE type. Usually inferred from ``cluster`` / ``wild``.",
          "enum": [
            "iid",
            "cluster",
            "multiway_cluster",
            "wild_cluster"
          ],
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "weights": {
          "description": "Observation weights. Column name or raw array.",
          "type": "string"
        },
        "wild": {
          "default": false,
          "description": "If True (and ``cluster`` is given), return wild-cluster-bootstrap p-values / CIs alongside classical cluster SE. Applied variable- by-variable. Only supported with a single cluster column.",
          "type": "boolean"
        },
        "wild_n_boot": {
          "default": 999,
          "description": "Bootstrap replications.",
          "type": "integer"
        },
        "wild_seed": {
          "description": "wild_seed parameter (Optional[int]).",
          "type": "integer"
        },
        "wild_weight_type": {
          "default": "webb",
          "description": "wild_weight_type parameter (str).",
          "enum": [
            "rademacher",
            "webb",
            "mammen"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "hdfe_ols"
  },
  {
    "description": "Heckman two-step selection model correcting for sample selection bias. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "Outcome equation formula",
          "type": "string"
        },
        "select_formula": {
          "description": "Selection equation formula",
          "type": "string"
        }
      },
      "required": [
        "formula",
        "select_formula"
      ],
      "type": "object"
    },
    "name": "heckman"
  },
  {
    "description": "Breusch-Pagan test for heteroskedasticity.",
    "input_schema": {
      "properties": {
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "het_test"
  },
  {
    "description": "Holm (1979) step-down correction.",
    "input_schema": {
      "properties": {
        "pvalues": {
          "description": "Unadjusted p-values.",
          "type": "number"
        }
      },
      "required": [
        "pvalues"
      ],
      "type": "object"
    },
    "name": "holm"
  },
  {
    "description": "Rambachan-Roth (2023) 'honest' DID sensitivity analysis. Takes an existing DID / event-study estimate and returns honest confidence intervals under varying degrees of parallel-trends violation (smoothness or relative-magnitude restrictions).  Call this when a pre-trend test rejects at low power instead of abandoning the design.",
    "input_schema": {
      "properties": {
        "betas": {
          "description": "Event-study coefficients.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "m_bar": {
          "description": "Bound on deviation magnitude.",
          "type": "number"
        },
        "method": {
          "default": "SD",
          "description": "SD = smoothness deviation; RM = relative magnitude.",
          "enum": [
            "SD",
            "RM"
          ],
          "type": "string"
        },
        "num_post_periods": {
          "type": "integer"
        },
        "num_pre_periods": {
          "type": "integer"
        },
        "sigma": {
          "description": "Covariance matrix of betas (square 2-D array).",
          "type": "array"
        }
      },
      "required": [
        "betas",
        "sigma",
        "num_pre_periods",
        "num_post_periods"
      ],
      "type": "object"
    },
    "name": "honest_did"
  },
  {
    "description": "Rambachan-Roth (2023) honest CIs on a fitted DID / event-study result. Auto-extracts betas + sigma + pre/post-period counts from the result; the LLM never ferries arrays.",
    "input_schema": {
      "properties": {
        "e": {
          "default": 0,
          "description": "Relative event time to audit.",
          "type": "integer"
        },
        "m_bar": {
          "description": "Bound on deviation magnitude (optional).",
          "type": "number"
        },
        "method": {
          "default": "SD",
          "description": "SD = smoothness deviation (Rambachan-Roth default); RM = relative magnitude.",
          "enum": [
            "SD",
            "RM"
          ],
          "type": "string"
        },
        "result_id": {
          "description": "Handle to a DID / event-study result.",
          "type": "string"
        }
      },
      "required": [
        "result_id"
      ],
      "type": "object"
    },
    "name": "honest_did_from_result"
  },
  {
    "description": "Half-sample bootstrap variance of the ATE/GATE estimate. Assumptions: Unconfoundedness given the covariates; Overlap / positivity; Honesty: separate subsamples are used to choose splits and to estimate effects. Pre-conditions: Covariates, treatment, and outcome with enough data to grow an honest forest. Failure modes: Calibration test rejects \u2014 the forest's heterogeneity is not well calibrated \u2192 Increase the sample / number of trees, or fall back to a doubly-robust learner.. Alternatives: sp.dml, sp.auto_cate, sp.tmle. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "n_splits": {
          "default": 25,
          "description": "Number of random half-sample draws.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "honest_variance"
  },
  {
    "description": "Horowitz-Manski (2000) bounds conditioning on covariates. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative \u2192 Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds.. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates to condition on (discretised via quartiles for continuous variables).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "y_lower": {
          "description": "Known lower bound of Y. Defaults to observed min.",
          "type": "number"
        },
        "y_upper": {
          "description": "Known upper bound of Y. Defaults to observed max.",
          "type": "number"
        }
      },
      "required": [
        "covariates",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "horowitz_manski"
  },
  {
    "description": "Hurdle (two-part) model for count data.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "count_model": {
          "default": "poisson",
          "description": "Count distribution: \"poisson\" or \"negbin\".",
          "type": "string"
        },
        "formula": {
          "description": "Patsy-style formula.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors (used for both hurdle and count parts).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "hurdle"
  },
  {
    "description": "Intra-class correlation for a fitted mixed model.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "component": {
          "default": "_cons",
          "description": "component parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 0,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "result": {
          "description": "result parameter.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "icc"
  },
  {
    "description": "Causal Impact visualization (Google-style 3-panel plot).",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Only for single-panel types.",
          "type": "string"
        },
        "figsize": {
          "default": [
            12,
            9
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Result from ``causal_impact()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (str).",
          "type": "string"
        },
        "type": {
          "default": "all",
          "description": "'all': 3-panel (original + pointwise + cumulative). 'original': actual vs counterfactual. 'pointwise': pointwise causal effect. 'cumulative': cumulative effect.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "impactplot"
  },
  {
    "description": "Compute a single inequality index.",
    "input_schema": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "eps": {
          "default": 1.0,
          "description": "eps parameter (float).",
          "type": "number"
        },
        "index": {
          "default": "theil_t",
          "description": "index parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "inequality_index"
  },
  {
    "description": "Interactive fixed effects estimator (Bai 2009).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "id": {
          "default": "id",
          "description": "Unit identifier.",
          "type": "string"
        },
        "maxiter": {
          "default": 1000,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "iterative",
          "description": "'iterative' (Bai 2009 CCE-type), 'pca' (principal components).",
          "type": "string"
        },
        "n_factors": {
          "default": 1,
          "description": "Number of interactive factors (r).",
          "type": "integer"
        },
        "robust": {
          "default": true,
          "description": "Robust standard-error or covariance estimator option.",
          "type": "boolean"
        },
        "time": {
          "default": "time",
          "description": "Time identifier.",
          "type": "string"
        },
        "tol": {
          "default": 1e-06,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "interactive_fe"
  },
  {
    "description": "Unified interference / spillover dispatcher. design= selects the estimator: 'partial' (Hudgens-Halloran cluster) / 'network_exposure' (Aronow-Samii HT) / 'peer_effects' (Manski / Bramoull\u00e9 linear-in-means) / 'network_hte' (Wu & Yuan 2025 orthogonal, arXiv:2509.18484) / 'inward_outward' (directed network; Fang, Airoldi & Forastiere 2025, arXiv:2506.06615) / 'cluster_matched_pair' (Bai 2022) / 'cluster_cross' (Ding et al. 2025) / 'cluster_staggered' (Zhou et al. 2025) / 'dnc_gnn' (Zhao et al. 2026).  Kwargs pass through to the target function; see sp.interference_family guide. Validation: validated. Assumptions: Partial interference (within-cluster spillover only) OR an explicit exposure mapping; SUTVA modulo the declared spillover structure; Correctly specified exposure function (e.g. fraction-treated, neighbour-share). Pre-conditions: clustered data OR network / adjacency matrix; treatment varies within cluster (or exposure is well-defined on the network); enough clusters (\u2265 30) for cluster-robust inference. Failure modes: Few clusters (< 30) with cluster-level inference \u2192 Use wild cluster bootstrap or permutation; CR3 jackknife for < 50.; Very few treated per cluster \u2192 Saturation\u2026",
    "input_schema": {
      "properties": {
        "design": {
          "default": "partial",
          "description": "Interference design \u2014 call sp.interference_available_designs() for the full list.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "interference"
  },
  {
    "description": "Natural-language interpretation of a fitted result. When the connected MCP client advertised sampling, this REUSES the agent's own model (no API key) to explain the estimate, its uncertainty, and what the design does / does not identify \u2014 optionally focused by a `question` and tuned for an `audience`. With no sampling available it falls back to a deterministic structured brief: it NEVER fabricates a narrative. Every claim is grounded in the result's own numbers \u2014 the model is told not to invent estimates. Pass the result_id from an earlier as_handle=true call.",
    "input_schema": {
      "properties": {
        "audience": {
          "default": "researcher",
          "description": "Tone / depth: 'researcher' (precise, names identification assumptions), 'policymaker' (plain, decision-focused), 'general' (no jargon).",
          "enum": [
            "researcher",
            "policymaker",
            "general"
          ],
          "type": "string"
        },
        "question": {
          "description": "Optional specific question to focus the interpretation (e.g. 'is the effect economically meaningful?').",
          "type": "string"
        },
        "result_id": {
          "description": "Handle to a previously-fitted result.",
          "type": "string"
        }
      },
      "required": [
        "result_id"
      ],
      "type": "object"
    },
    "name": "interpret_result"
  },
  {
    "description": "Inverse Probability Weighting for ATE/ATT/ATC with propensity score trimming. Validation: validated. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) \u2192 Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE).. Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "ATE",
          "description": "Target estimand",
          "enum": [
            "ATE",
            "ATT",
            "ATC"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "trim": {
          "default": 0.0,
          "description": "Propensity score trimming threshold",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "ipw"
  },
  {
    "description": "Compute impulse response functions from VAR.",
    "input_schema": {
      "properties": {
        "impulse": {
          "description": "Impulse variable (if None, all).",
          "type": "string"
        },
        "orthogonal": {
          "default": true,
          "description": "Orthogonalized (Cholesky) IRF.",
          "type": "boolean"
        },
        "periods": {
          "default": 20,
          "description": "Number of periods for IRF.",
          "type": "integer"
        },
        "response": {
          "description": "Response variable (if None, all).",
          "type": "string"
        },
        "var_result": {
          "description": "Estimated VAR model.",
          "type": "string"
        }
      },
      "required": [
        "var_result"
      ],
      "type": "object"
    },
    "name": "irf"
  },
  {
    "description": "Unified IV estimation: 2SLS, LIML, Fuller, GMM, JIVE. Includes first-stage F, Sargan/Hansen J, and Hausman diagnostics. Validation: certified parity evidence. Assumptions: Relevance: instruments predict the endogenous regressor (first-stage F \u2265 10 rule of thumb); Exclusion: instruments affect outcome only through the endogenous regressor; Monotonicity (for LATE interpretation under heterogeneous effects). Pre-conditions: formula includes the (endog ~ instruments) parenthesised block; at least as many instruments as endogenous regressors (order condition); instruments are not themselves endogenous in the outcome equation. Failure modes: First-stage F < 10 (Stock-Yogo 5% bias) \u2192 Use weak-IV-robust inference (Anderson-Rubin) or LIML.; Over-identification test rejects (sp.estat 'overid') \u2192 At least one instrument is invalid; drop instruments or switch to just-identified LIML.; Hausman endogeneity test fails to reject \u2192 OLS may be consistent and more efficient; report both.. Alternatives: sp.deepiv, sp.bartik, sp.proximal, sp.regress. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Column name for cluster-robust SEs",
          "type": "string"
        },
        "formula": {
          "description": "IV formula: 'y ~ (endog ~ instruments) + exog'",
          "type": "string"
        },
        "fuller_alpha": {
          "default": 1.0,
          "description": "Fuller constant (method='fuller' only)",
          "type": "number"
        },
        "method": {
          "default": "2sls",
          "description": "Estimation method",
          "enum": [
            "2sls",
            "liml",
            "fuller",
            "gmm",
            "jive"
          ],
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard error type",
          "enum": [
            "nonrobust",
            "hc0",
            "hc1",
            "hc2",
            "hc3"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "iv"
  },
  {
    "description": "Nevo-Rosen (2012) bounds for LATE under imperfect instruments. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative \u2192 Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds.. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "assumption": {
          "default": "monotone_iv",
          "description": "- ``'monotone_iv'``: instrument has same-sign direct effect as through the treatment (Nevo-Rosen Proposition 2). - ``'less_than_late'``: direct effect of Z on Y is weakly less than the indirect effect (tighter).",
          "type": "string"
        },
        "controls": {
          "description": "Control variables (residualized out via OLS).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument": {
          "description": "Instrument variable (binary).",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Endogenous treatment (binary).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "iv_bounds"
  },
  {
    "description": "Run several k-class / JIVE estimators on the same IV specification and return a one-row-per-method comparison DataFrame (estimate, SE, CI, first-stage F). Useful as a sensitivity sanity check before reporting. Validation: validated. Assumptions: Instrument relevance (non-zero first stage); Exclusion restriction: the instrument affects the outcome only through the treatment; Independence/exogeneity of the instrument; for LATE, monotonicity (no defiers). Pre-conditions: An instrument plausibly affecting treatment, an endogenous treatment, and an outcome; A strong first stage (assess instrument strength before interpreting estimates). Failure modes: Weak first stage \u2014 biased point estimates and unreliable conventional SEs \u2192 Report first-stage F / effective F and use weak-IV-robust inference (Anderson-Rubin).. Alternatives: sp.iv, sp.anderson_rubin_ci, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "endog_name": {
          "description": "Override endogenous-coefficient name lookup",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "methods": {
          "default": "('2sls','liml','fuller','jive')",
          "description": "methods parameter (tuple[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "iv_compare"
  },
  {
    "description": "Modern IV reporting bundle (R `ivDiag` analogue). Combines 2SLS point estimate, analytic + pairs/wild bootstrap SEs, Olea-Pflueger effective F, Lee-McCrary-Moreira-Porter (2022) tF-corrected critical value, Anderson-Rubin / CLR / K weak-IV-robust confidence sets, Kleibergen-Paap rk LM, Conley-Hansen-Rossi (2012) plausibly-exogenous LTZ sensitivity, and a Blandhol-Bonney-Mogstad-Torgovitsky (2022/2025) / S\u0142oczy\u0144ski (2024) `TSLS-as-LATE` caveat into a single IVDiagResult. Validation: validated. Assumptions: Instrument relevance (non-zero first stage); Exclusion restriction: the instrument affects the outcome only through the treatment; Independence/exogeneity of the instrument; for LATE, monotonicity (no defiers). Pre-conditions: An instrument plausibly affecting treatment, an endogenous treatment, and an outcome; A strong first stage (assess instrument strength before interpreting estimates). Failure modes: Weak first stage \u2014 biased point estimates and unreliable conventional SEs \u2192 Report first-stage F / effective F and use weak-IV-robust inference (Anderson-Rubin).. Alternatives: sp.iv, sp.anderson_rubin_ci, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "boot_methods": {
          "default": "('pairs',)",
          "description": "Subset of {'pairs','wild'}",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cluster": {
          "description": "Cluster column for cluster-robust SE / cluster bootstrap",
          "type": "string"
        },
        "endog": {
          "description": "Single endogenous regressor",
          "type": "string"
        },
        "exog": {
          "description": "Optional included exogenous controls",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h0": {
          "default": 0.0,
          "description": "Null hypothesis for AR/CLR/K",
          "type": "number"
        },
        "include_clr_ci": {
          "default": false,
          "description": "Whether to include clr ci.",
          "type": "boolean"
        },
        "include_k_ci": {
          "default": false,
          "description": "Whether to include k ci.",
          "type": "boolean"
        },
        "instruments": {
          "description": "instruments parameter (list[str] | str).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ltz_gamma_sd": {
          "description": "Standard deviation of CHR (2012) LTZ Gaussian prior on \u03b3",
          "type": "number"
        },
        "n_boot": {
          "default": 1000,
          "description": "Bootstrap replications (0 to skip)",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "vcov": {
          "default": "HC1",
          "description": "Heteroskedasticity-robust covariance type",
          "enum": [
            "HC0",
            "HC1",
            "classic"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments",
        "y"
      ],
      "type": "object"
    },
    "name": "iv_diag"
  },
  {
    "description": "Instrumental-variable quantile regression (Chernozhukov-Hansen).",
    "input_schema": {
      "properties": {
        "add_constant": {
          "default": true,
          "description": "add_constant parameter (bool).",
          "type": "boolean"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "bootstrap": {
          "default": 0,
          "description": "Number of pairs-bootstrap replications for standard errors. ``0`` disables bootstrap; asymptotic rank-test inversion is not implemented in this MVP.",
          "type": "integer"
        },
        "endog": {
          "description": "Endogenous regressor(s) ``D``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "exog": {
          "description": "Exogenous controls ``X`` (may be empty).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Instrument(s) ``Z``. Must be at least as many as ``endog``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_grid": {
          "default": 41,
          "description": "Grid resolution for the profile search over ``\u03b1`` (scalar case) \u2014 ignored when ``endog`` is multi-dimensional.",
          "type": "integer"
        },
        "random_state": {
          "default": 1234,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "refine": {
          "default": true,
          "description": "After the grid search, refine ``\u03b1\u0302`` with a local optimizer.",
          "type": "boolean"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile(s) of interest in ``(0, 1)``.",
          "type": "number"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments",
        "y"
      ],
      "type": "object"
    },
    "name": "ivqreg"
  },
  {
    "description": "2SLS instrumental-variables regression with robust or clustered SEs and first-stage F diagnostics. Formula syntax: 'y ~ x_exog + (d_endog ~ z_instrument)'.",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "'y ~ x + (d ~ z)' style.",
          "type": "string"
        },
        "robust": {
          "default": "hc1",
          "enum": [
            "hc1",
            "hc2",
            "hc3",
            "nonrobust"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "ivreg"
  },
  {
    "description": "Leave-one-cluster-out jackknife standard errors.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Name of the cluster variable in ``data``.",
          "type": "string"
        },
        "result": {
          "description": "A fitted regression result from ``sp.regress()``.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "result"
      ],
      "type": "object"
    },
    "name": "jackknife_se"
  },
  {
    "description": "Jackknife Instrumental Variables Estimation (JIVE).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "variant": {
          "default": "jive1",
          "description": "'jive1' (Angrist et al. 1999) or 'jive2' (alternative).",
          "type": "string"
        },
        "x_endog": {
          "description": "x_endog parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_exog": {
          "description": "x_exog parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "z": {
          "description": "Instrument, proxy, or auxiliary variable used by this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x_endog",
        "y"
      ],
      "type": "object"
    },
    "name": "jive"
  },
  {
    "description": "Johansen (1991) cointegration test.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "lags": {
          "default": 1,
          "description": "Number of lags in the VECM.",
          "type": "integer"
        },
        "test": {
          "default": "trace",
          "description": "'trace' or 'maxeig' (maximum eigenvalue).",
          "type": "string"
        },
        "trend": {
          "default": "c",
          "description": "'n' (none), 'c' (constant), 'ct' (constant + trend).",
          "type": "string"
        },
        "variables": {
          "description": "Variables to test.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "johansen"
  },
  {
    "description": "KAN-Powered D-IV-LATE (Shaw 2025, arXiv 2506.12765). Assumptions: Selection-on-observables (unconfoundedness + overlap) or, for IV variants, instrument validity; For IV-QTE: rank invariance / rank similarity (monotonicity of the structural quantile function). Pre-conditions: Covariates, treatment, and outcome; for IV-quantile methods, a valid instrument; Enough data to estimate the outcome distribution across quantiles. Failure modes: Estimated conditional quantiles cross (non-monotone), or tail quantiles are unstable \u2192 Use rearrangement / monotonization and avoid extreme quantiles where data are sparse.. Alternatives: sp.qte, sp.iv, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument": {
          "description": "instrument parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 200,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "kan_dlate"
  },
  {
    "description": "Kaplan-Meier non-parametric survival function estimator.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals (Greenwood formula).",
          "type": "number"
        },
        "duration": {
          "description": "Column name for duration / follow-up time.",
          "type": "string"
        },
        "event": {
          "description": "Column name for event indicator (1 = event, 0 = censored).",
          "type": "string"
        },
        "group": {
          "description": "Column name for group variable (stratification).",
          "type": "string"
        }
      },
      "required": [
        "duration",
        "event"
      ],
      "type": "object"
    },
    "name": "kaplan_meier"
  },
  {
    "description": "Kernel IV regression with uniform confidence bands (Lob et al. 2025). Estimates the structural function h*(d) = E[Y | do(D=d)] via kernel-weighted local averaging under a continuous instrument Z, with wild-bootstrap uniform SEs. Validation: validated. Assumptions: Instrument relevance (non-zero first stage); Exclusion restriction: the instrument affects the outcome only through the treatment; Independence/exogeneity of the instrument; for LATE, monotonicity (no defiers). Pre-conditions: An instrument plausibly affecting treatment, an endogenous treatment, and an outcome; A strong first stage (assess instrument strength before interpreting estimates); formula includes the (endog ~ instruments) parenthesised block. Failure modes: Weak first stage \u2014 biased point estimates and unreliable conventional SEs \u2192 Report first-stage F / effective F and use weak-IV-robust inference (Anderson-Rubin).; First-stage F < 10 (Stock-Yogo 5% bias) \u2192 Use weak-IV-robust inference (Anderson-Rubin) or LIML.; Over-identification test rejects (sp.estat 'overid') \u2192 At least one instrument is invalid; drop instruments or switch to just-identified LIML.. Alternatives: sp.iv, sp.anderson_rubin_ci, sp.dml, sp.de\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Silverman default",
          "type": "number"
        },
        "grid": {
          "description": "Grid of d-values (default 30 quantile-evenly spaced)",
          "type": "string"
        },
        "instrument": {
          "description": "Continuous instrument Z",
          "type": "string"
        },
        "n_boot": {
          "default": 100,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "ridge": {
          "default": 0.001,
          "description": "Tikhonov regularisation",
          "type": "number"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Continuous treatment D",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "kernel_iv"
  },
  {
    "description": "Run RDD + RKD + Bunching on the same data. Assumptions: The counterfactual density would be smooth through the threshold absent the policy; Excess mass at the threshold reflects the behavioural elasticity of interest; No other discontinuity coincides with the threshold. Pre-conditions: A behavioural choice variable (earnings, hours, ...) with a known kink or notch in the budget/choice set; A visible empirical density of the running variable around the threshold. Failure modes: Round-number heaping or a coincident policy contaminates the bunching mass \u2192 Exclude heaping points, widen the excluded region, and test the counterfactual polynomial order.. Alternatives: sp.rdrobust, sp.rkd. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "bin_width": {
          "description": "bin_width parameter (Optional[float]).",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "polynomial_order": {
          "default": 2,
          "description": "polynomial_order parameter (int).",
          "type": "integer"
        },
        "running": {
          "description": "Running variable (also forms density for Bunching).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable (used by RDD/RKD).",
          "type": "string"
        }
      },
      "required": [
        "running",
        "y"
      ],
      "type": "object"
    },
    "name": "kink_unified"
  },
  {
    "description": "Kitagawa (1955) two-factor rate decomposition.",
    "input_schema": {
      "properties": {
        "by": {
          "description": "Category variable(s) defining cells.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group": {
          "description": "Binary group indicator.",
          "type": "string"
        },
        "normalize": {
          "default": "symmetric",
          "description": "- 'a': rate effect evaluated at A's composition - 'b': rate effect evaluated at B's composition - 'symmetric': average (default)",
          "enum": [
            "symmetric",
            "a",
            "b"
          ],
          "type": "string"
        },
        "rate": {
          "description": "Column holding the category-specific rate (or 0/1 outcome at the individual level).",
          "type": "string"
        },
        "weights": {
          "description": "Cell population weights. If None, each row treated as individual-level data (weight = 1).",
          "type": "string"
        }
      },
      "required": [
        "by",
        "group",
        "rate"
      ],
      "type": "object"
    },
    "name": "kitagawa_decompose"
  },
  {
    "description": "Kitagawa (2015) specification test for the validity of LATE.",
    "input_schema": {
      "properties": {
        "instrument": {
          "description": "Binary instrument variable (Z).",
          "type": "string"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications for the p-value.",
          "type": "integer"
        },
        "n_grid": {
          "default": 100,
          "description": "Number of grid points for evaluating the CDF conditions.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "treatment": {
          "description": "Endogenous binary treatment variable (D).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "instrument",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "kitagawa_test"
  },
  {
    "description": "LASSO-selected instrumental variables. Assumptions: Relevance: instruments predict the endogenous regressor (first-stage F \u2265 10 rule of thumb); Exclusion: instruments affect outcome only through the endogenous regressor; Monotonicity (for LATE interpretation under heterogeneous effects). Pre-conditions: formula includes the (endog ~ instruments) parenthesised block; at least as many instruments as endogenous regressors (order condition); instruments are not themselves endogenous in the outcome equation. Failure modes: First-stage F < 10 (Stock-Yogo 5% bias) \u2192 Use weak-IV-robust inference (Anderson-Rubin) or LIML.; Over-identification test rejects (sp.estat 'overid') \u2192 At least one instrument is invalid; drop instruments or switch to just-identified LIML.; Hausman endogeneity test fails to reject \u2192 OLS may be consistent and more efficient; report both.. Alternatives: sp.deepiv, sp.bartik, sp.proximal, sp.regress. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "penalty": {
          "default": "bic",
          "description": "Instrument selection criterion: 'bic', 'aic', 'cv'.",
          "type": "string"
        },
        "robust": {
          "default": "robust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "x_endog": {
          "description": "x_endog parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_exog": {
          "description": "x_exog parameter (List[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "z": {
          "description": "Full set of candidate instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x_endog",
        "y"
      ],
      "type": "object"
    },
    "name": "lasso_iv"
  },
  {
    "description": "LASSO-based variable selection.",
    "input_schema": {
      "properties": {
        "eps": {
          "default": 0.001,
          "description": "Ratio of lambda_min / lambda_max.",
          "type": "number"
        },
        "max_iter": {
          "default": 1000,
          "description": "Maximum coordinate descent iterations per \u03bb.",
          "type": "integer"
        },
        "method": {
          "default": "cv",
          "description": "How to choose the regularisation parameter \u03bb.",
          "enum": [
            "cv",
            "bic",
            "aic"
          ],
          "type": "string"
        },
        "n_folds": {
          "default": 10,
          "description": "Number of cross-validation folds (only for ``method=\"cv\"``).",
          "type": "integer"
        },
        "n_lambda": {
          "default": 100,
          "description": "Number of \u03bb values in the grid.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for CV fold assignment.",
          "type": "integer"
        },
        "tol": {
          "default": 1e-06,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "verbose": {
          "default": true,
          "description": "Print progress.",
          "type": "boolean"
        },
        "x": {
          "description": "Candidate independent variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable column.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "lasso_select"
  },
  {
    "description": "Compute Lee (2009) bounds for ATE under sample selection. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative \u2192 Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds.. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Not used in basic Lee bounds, reserved for conditional bounds.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Bootstrap iterations for inference.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "selection": {
          "description": "Binary selection/retention indicator (1 = observed, 0 = missing).",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable (may have NaN for selected-out units).",
          "type": "string"
        }
      },
      "required": [
        "selection",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "lee_bounds"
  },
  {
    "description": "Limited Information Maximum Likelihood (LIML) estimator. Validation: validated. Assumptions: Relevance: instruments predict the endogenous regressor (first-stage F \u2265 10 rule of thumb); Exclusion: instruments affect outcome only through the endogenous regressor; Monotonicity (for LATE interpretation under heterogeneous effects). Pre-conditions: formula includes the (endog ~ instruments) parenthesised block; at least as many instruments as endogenous regressors (order condition); instruments are not themselves endogenous in the outcome equation. Failure modes: First-stage F < 10 (Stock-Yogo 5% bias) \u2192 Use weak-IV-robust inference (Anderson-Rubin) or LIML.; Over-identification test rejects (sp.estat 'overid') \u2192 At least one instrument is invalid; drop instruments or switch to just-identified LIML.; Hausman endogeneity test fails to reject \u2192 OLS may be consistent and more efficient; report both.. Alternatives: sp.deepiv, sp.bartik, sp.proximal, sp.regress. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "fuller": {
          "description": "Fuller's constant (typically 1 or 4). If None, pure LIML.",
          "type": "number"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "x_endog": {
          "description": "Endogenous regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_exog": {
          "description": "Exogenous regressors (included instruments).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "z": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "liml"
  },
  {
    "description": "Estimate a linear combination of coefficients with inference.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "expression": {
          "description": "Linear combination. Examples: - ``\"x1 + x2\"`` \u2014 beta_x1 + beta_x2 - ``\"x1 - x2\"`` \u2014 beta_x1 - beta_x2 - ``\"2*x1 + 3*x2\"`` \u2014 2*beta_x1 + 3*beta_x2",
          "type": "string"
        },
        "result": {
          "description": "Fitted model.",
          "type": "string"
        }
      },
      "required": [
        "expression",
        "result"
      ],
      "type": "object"
    },
    "name": "lincom"
  },
  {
    "description": "Fit DirectLiNGAM (Shimizu 2011). Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges \u2014 faithfulness or sample size is the likely culprit \u2192 Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible.. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "standardize": {
          "default": true,
          "description": "Zero-mean / unit-variance each variable before the algorithm.",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "lingam"
  },
  {
    "description": "List all available replication datasets and guides.",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "list_replications"
  },
  {
    "description": "Propose a candidate DAG from variable names + domain description. Assumptions: LLM-proposed graphs / priors are hypotheses to validate, not statistical identification; Human review or data-driven falsification is required before any causal claim. Pre-conditions: Domain context and a bounded list of candidate variables; A configured, logged LLM provider for reproducibility. Failure modes: Proposals are unstable across runs or include hallucinated variables/edges \u2192 Fix the model release and seed, add constraints, and cross-check with constraint-based discovery.. Alternatives: sp.llm_dag_constrained, sp.causal_discovery, sp.dag. Typical minimum N: 1.",
    "input_schema": {
      "properties": {
        "client": {
          "description": "An LLM client implementing ``.complete(prompt: str) -> str``. If ``None``, use the deterministic heuristic backend.",
          "type": "string"
        },
        "domain": {
          "default": "",
          "description": "Free-text domain description (e.g. \"labor economics, education and earnings\"). Helps the LLM but ignored by the heuristic backend.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "variables": {
          "description": "Names of variables in the dataset.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "variables"
      ],
      "type": "object"
    },
    "name": "llm_dag_propose"
  },
  {
    "description": "Propose sensitivity-analysis priors for the substantive setting. Assumptions: LLM-proposed graphs / priors are hypotheses to validate, not statistical identification; Human review or data-driven falsification is required before any causal claim. Pre-conditions: Domain context and a bounded list of candidate variables; A configured, logged LLM provider for reproducibility. Failure modes: Proposals are unstable across runs or include hallucinated variables/edges \u2192 Fix the model release and seed, add constraints, and cross-check with constraint-based discovery.. Alternatives: sp.llm_dag_constrained, sp.causal_discovery, sp.dag. Typical minimum N: 1.",
    "input_schema": {
      "properties": {
        "client": {
          "description": "LLM client with ``.complete(prompt: str) -> str``.",
          "type": "string"
        },
        "domain": {
          "default": "health",
          "description": "domain parameter (str).",
          "enum": [
            "health",
            "education",
            "labor",
            "policy",
            "marketing"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "treatment"
      ],
      "type": "object"
    },
    "name": "llm_sensitivity_priors"
  },
  {
    "description": "Enumerate plausible unobserved confounders for a study. Assumptions: LLM-proposed graphs / priors are hypotheses to validate, not statistical identification; Human review or data-driven falsification is required before any causal claim. Pre-conditions: Domain context and a bounded list of candidate variables; A configured, logged LLM provider for reproducibility. Failure modes: Proposals are unstable across runs or include hallucinated variables/edges \u2192 Fix the model release and seed, add constraints, and cross-check with constraint-based discovery.. Alternatives: sp.llm_dag_constrained, sp.causal_discovery, sp.dag. Typical minimum N: 1.",
    "input_schema": {
      "properties": {
        "client": {
          "description": "LLM client with ``.complete(prompt: str) -> str``.",
          "type": "string"
        },
        "domain": {
          "default": "health",
          "description": "domain parameter (str).",
          "enum": [
            "health",
            "education",
            "labor",
            "policy",
            "marketing"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Free-text descriptions (used by LLM, ignored by heuristic).",
          "type": "string"
        },
        "point_estimate_rr": {
          "default": 1.5,
          "description": "Observed risk ratio; suggested E-values are scaled relative to this so the user can read \"to nullify a RR of X you'd need an unobserved RR of Y\".",
          "type": "number"
        },
        "treatment": {
          "description": "Free-text descriptions (used by LLM, ignored by heuristic).",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "treatment"
      ],
      "type": "object"
    },
    "name": "llm_unobserved_confounders"
  },
  {
    "description": "Estimate impulse responses via Jord\u00e0 (2005) local projections. Validation: certified parity evidence. Assumptions: Outcome and shock series are (covariance-)stationary so horizon-h projections are not spurious; Shock is conditionally exogenous at t given controls and the auto-added lags (no contemporaneous feedback from y_t to shock_t); Newey-West truncation lag is adequate for the moving-average serial correlation induced by overlapping horizons. Pre-conditions: Single time-ordered series in a DataFrame with the outcome and shock columns; Length comfortably exceeds horizons + max lag so the deepest horizon regression retains enough usable rows. Failure modes: Confidence bands explode or flip sign at long horizons after passing already-lagged controls together with auto_lag=True \u2192 Set auto_lag=False for a bare specification, or drop your manual lags so collinear duplicate-lag columns are not formed; Series too short: the longest-horizon regression has fewer observations than regressors and OLS cannot be solved \u2192 Reduce horizons or nw_lags, or supply a longer sample so each horizon retains degrees of freedom. Alternatives: sp.var, sp.arima, sp.iv. Typical minimum N: 80.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the confidence band.",
          "type": "number"
        },
        "auto_lag": {
          "default": true,
          "description": "If ``True`` (the legacy default), also adds ``y_{t-1}`` and ``shock_{t-1}`` as automatic regressors. Set ``False`` for a bare ``y_{t+h} ~ const + shock_t + controls`` specification. These two auto-controls were silent in the pre-1.16 docstring.",
          "type": "boolean"
        },
        "controls": {
          "description": "Additional regressors taken **verbatim** from ``data``: the column values at time t are used directly, without re-lagging. If you want the lag of a control, lag it yourself before passing it in (e.g. ``df[\"unemp_lag\"] = df[\"unemp\"].shift(1)`` and then ``controls=[\"unemp_lag\"]``). The pre-1.16 behaviour silently re-lagged controls a second time on top of an auto- added ``y_{t-1}``, producing collinear columns and surprising impulse responses; see ``MIGRATION.md`` for context.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cumulative": {
          "default": false,
          "description": "If ``True``, return the cumulative response ``y_{t+h} - y_{t-1}``. Default (False) returns ``y_{t+h}`` directly.",
          "type": "boolean"
        },
        "horizons": {
          "default": 20,
          "description": "Number of horizons h = 0, 1, \u2026, H to estimate.",
          "type": "integer"
        },
        "nw_lags": {
          "description": "Newey-West truncation lag. Defaults to ``round(1.5 * horizons)`` per Kilian & Kim (2011) recommendation.",
          "type": "integer"
        },
        "outcome": {
          "description": "Column name of the outcome variable y.",
          "type": "string"
        },
        "shock": {
          "description": "Column name of the shock / treatment variable.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "shock"
      ],
      "type": "object"
    },
    "name": "local_projections"
  },
  {
    "description": "Logit (logistic) regression via maximum likelihood.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "at_values": {
          "description": "Variable values for ``marginal_effects='at'``.",
          "type": "object"
        },
        "cluster": {
          "description": "Column name for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Formula like ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "marginal_effects": {
          "description": "``'average'`` (AME), ``'mean'`` (MEM), or ``'at'`` (MER).",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum Newton-Raphson iterations.",
          "type": "integer"
        },
        "odds_ratio": {
          "default": false,
          "description": "Report odds ratios instead of log-odds coefficients.",
          "type": "boolean"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'nonrobust'`` for MLE SE, ``'hc1'`` / ``'robust'`` for sandwich SE.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance on log-likelihood change.",
          "type": "number"
        },
        "weights": {
          "description": "Column name for frequency/analytic weights.",
          "type": "string"
        },
        "x": {
          "description": "Regressor names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "logit"
  },
  {
    "description": "Log-rank test for equality of survival distributions across groups.",
    "input_schema": {
      "properties": {
        "duration": {
          "description": "Column names.",
          "type": "string"
        },
        "event": {
          "description": "Column names.",
          "type": "string"
        },
        "group": {
          "description": "Column names.",
          "type": "string"
        }
      },
      "required": [
        "duration",
        "event",
        "group"
      ],
      "type": "object"
    },
    "name": "logrank_test"
  },
  {
    "description": "Love plot: dot plot of standardized mean differences before/after.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            7
          ],
          "description": "(width, height). Height defaults to 0.4 * n_covariates + 1.",
          "type": "number"
        },
        "ps_method": {
          "default": "logit",
          "description": "PS estimation method for balance computation.",
          "type": "string"
        },
        "threshold": {
          "default": 0.1,
          "description": "SMD threshold for the vertical dashed line (default 0.1).",
          "type": "number"
        },
        "title": {
          "default": "Covariate Balance (Love Plot)",
          "description": "Plot title.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column.",
          "type": "string"
        },
        "weights": {
          "description": "IPW or matching weights. If None, inverse-PS weights are computed.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "love_plot"
  },
  {
    "description": "Local-Projections DiD (Dube-Girardi-Jord\u00e0-Taylor 2023). At each event-time horizon h \u2208 {-P, ..., H}, runs a separate OLS of Y_{t+h} \u2212 Y_{t-1} on the treatment change \u0394d_{t} with time FE and cluster-robust SE, using 'not-yet-treated' or 'never-treated' units as controls. Event-study \u03b2_h paths are returned in ``model_info['event_study']``. Validation: validated. Assumptions: Parallel trends across event time (standard DiD); No anticipation within the pre-treatment horizon; SUTVA. Pre-conditions: long-format panel with consecutive integer time; treatment is binary 0/1 and time-varying; horizons feasible: enough periods for Y_{t-1} and Y_{t+H}. Failure modes: Horizon-0 n_obs is tiny because few units switch on in the clean-control window \u2192 Widen clean_controls='never_treated' \u2192 'not_yet_treated' or shorten horizons.; Placebo CIs don't cover zero \u2014 parallel trends suspect \u2192 Apply sp.honest_did to the event-study paths for Rambachan-Roth sensitivity bounds.. Alternatives: sp.callaway_santanna, sp.sun_abraham, sp.did_imputation, sp.gardner_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "clean_controls": {
          "default": "not_yet_treated",
          "description": "Control selection",
          "enum": [
            "not_yet_treated",
            "never_treated"
          ],
          "type": "string"
        },
        "cluster": {
          "description": "Cluster variable (defaults to unit)",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "horizons": {
          "default": [
            -3,
            5
          ],
          "description": "(min, max) event-time horizons to estimate",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Integer period (consecutive)",
          "type": "string"
        },
        "time_fe": {
          "default": true,
          "description": "time_fe parameter (bool).",
          "type": "boolean"
        },
        "treatment": {
          "description": "Binary time-varying treatment (0/1)",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treatment",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "lp_did"
  },
  {
    "description": "Likelihood-ratio test comparing a *restricted* and a *full* model.",
    "input_schema": {
      "properties": {
        "boundary": {
          "description": "boundary parameter ('bool | None').",
          "type": "boolean"
        },
        "full": {
          "description": "full parameter.",
          "type": "string"
        },
        "restricted": {
          "description": "restricted parameter.",
          "type": "string"
        }
      },
      "required": [
        "full",
        "restricted"
      ],
      "type": "object"
    },
    "name": "lrtest"
  },
  {
    "description": "LTMLE for a discrete-time survival outcome under dynamic regimes. Assumptions: Unconfoundedness (sequential exchangeability for longitudinal/LTMLE); Positivity / overlap of treatment given history; At least one nuisance (outcome or treatment) is estimated consistently; the targeting step gives double robustness. Pre-conditions: Covariates, treatment, and outcome (for survival/longitudinal variants: time-to-event and time-varying covariates); Enough data to fit a Super Learner / HAL nuisance library. Failure modes: Near-positivity violations create extreme clever-covariate weights \u2192 Truncate weights, restrict the estimand, or report a positivity diagnostic.. Alternatives: sp.dml, sp.ipw, sp.g_computation. Typical minimum N: 400.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline": {
          "description": "Time-invariant baseline covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "censoring": {
          "description": "``1`` if the subject is observed *through* interval k, ``0`` if right-censored at or before k. If omitted, no censoring.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates_time": {
          "description": "Time-varying covariates at each interval.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "event_indicators": {
          "description": "Column names for the per-interval event indicator ``T_k`` (``1`` if the event occurs *in* interval k, ``0`` otherwise).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "propensity_bounds": {
          "default": [
            0.01,
            0.99
          ],
          "description": "propensity_bounds parameter (Tuple[float, float]).",
          "type": "number"
        },
        "regime_control": {
          "description": "Treatment regimes, same semantics as :func:`ltmle`.",
          "type": "string"
        },
        "regime_treated": {
          "description": "Treatment regimes, same semantics as :func:`ltmle`.",
          "type": "string"
        },
        "treatments": {
          "description": "Treatment column per interval.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "covariates_time",
        "event_indicators",
        "treatments"
      ],
      "type": "object"
    },
    "name": "ltmle_survival"
  },
  {
    "description": "Machado-Mata (2005) quantile decomposition.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "inference": {
          "default": "none",
          "description": "inference parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 199,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_sim": {
          "default": 500,
          "description": "Number of sim.",
          "type": "integer"
        },
        "n_tau_qr": {
          "default": 99,
          "description": "Number of tau qr.",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "0: use Group A's coefficients with Group B's X. The counterfactual is F_{Y<0|1>} \u2014 A's \u03b2 on B's X. 1: use Group B's coefficients with Group A's X. .. warning:: Opposite convention to ``dfl_decompose``. In DFL, ``reference=0`` means *A's X, B's \u03b2* (reweighting). Here, ``reference=0`` means *A's \u03b2, B's X* (coefficient swap). See ``dfl_decompose`` docstring for the full convention map.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "tau_grid": {
          "description": "\u03c4 grid for reporting (default: deciles 0.1..0.9)",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "machado_mata"
  },
  {
    "description": "Compute Manski (1990) worst-case bounds on ATE. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative \u2192 Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds.. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "assumption": {
          "default": "none",
          "description": "Additional assumption: - 'none': no assumptions (widest bounds) - 'mtr': Monotone Treatment Response (Y(1) >= Y(0) for all) - 'mts': Monotone Treatment Selection (selection on levels)",
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment variable (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "y_lower": {
          "description": "Known lower bound of the outcome. If None, uses observed min.",
          "type": "number"
        },
        "y_upper": {
          "description": "Known upper bound of the outcome. If None, uses observed max.",
          "type": "number"
        }
      },
      "required": [
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "manski_bounds"
  },
  {
    "description": "Compute marginal effects from a fitted model.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "at": {
          "description": "Fix covariates at specific values for conditional margins. E.g., ``{'age': 30, 'female': 1}``.",
          "type": "object"
        },
        "eps": {
          "default": 1e-05,
          "description": "Step size for numerical differentiation.",
          "type": "number"
        },
        "method": {
          "default": "ame",
          "description": "- 'ame': Average Marginal Effect (average dy/dx across all obs) - 'mem': Marginal Effect at the Mean (dy/dx at mean of X)",
          "type": "string"
        },
        "result": {
          "description": "Fitted model result (must have ``.params`` and associated data).",
          "type": "string"
        },
        "variables": {
          "description": "Variables to compute dy/dx for. Default: all regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "margins"
  },
  {
    "description": "Compute predictive margins at specific covariate values.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "at": {
          "description": "Mapping of variable names to lists/arrays of values. If multiple variables are given, the Cartesian product of all value lists is used. Example:: at={\"experience\": [1, 5, 10], \"female\": [0, 1]} produces 6 grid points.",
          "type": "object"
        },
        "result": {
          "description": "Fitted model result (must have ``.params`` and associated data).",
          "type": "string"
        }
      },
      "required": [
        "at",
        "result"
      ],
      "type": "object"
    },
    "name": "margins_at"
  },
  {
    "description": "Plot predictive margins from ``margins_at()`` with confidence bands.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "by": {
          "description": "Variable to produce separate lines for (legend grouping).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "margins_at_df": {
          "description": "Output from ``margins_at()``.",
          "type": "string"
        },
        "palette": {
          "description": "Colours for each ``by`` group.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "x": {
          "description": "Variable to place on the x-axis. If *None*, inferred as the at-variable with the most unique values.",
          "type": "string"
        },
        "xlabel": {
          "description": "xlabel parameter (Optional[str]).",
          "type": "string"
        },
        "ylabel": {
          "default": "Predicted Value",
          "description": "ylabel parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "margins_at_df"
      ],
      "type": "object"
    },
    "name": "margins_at_plot"
  },
  {
    "description": "Marginal-effects result that pipes straight into ``sp.regtable``.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "at": {
          "description": "at parameter (Optional[Dict[str, Any]]).",
          "type": "object"
        },
        "eps": {
          "default": 1e-05,
          "description": "eps parameter (float).",
          "type": "number"
        },
        "method": {
          "default": "ame",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "result": {
          "description": "Fitted model \u2014 same input ``sp.margins`` accepts. Forwarded verbatim to :func:`margins`.",
          "type": "string"
        },
        "variables": {
          "description": "variables parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "margins_table"
  },
  {
    "description": "Plot marginal effects with confidence intervals.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "color parameter (str).",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "margins_df": {
          "description": "Output from ``margins()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "margins_df"
      ],
      "type": "object"
    },
    "name": "marginsplot"
  },
  {
    "description": "Propensity score and covariate matching for treatment effect estimation. Validation: validated. Assumptions: Unconfoundedness / CIA: Y(d) \u22a5 D | X; Overlap / common support: treated X-values are in the control X-support; SUTVA: no interference between matched units. Pre-conditions: binary treatment 0/1; covariates are pre-treatment (temporally prior to D); enough control units for each treated unit under the chosen method (k:1 matching). Failure modes: Covariate imbalance after matching (max |SMD| > 0.1) \u2192 Re-match with stricter caliper, add interactions, or switch to sp.ebalance (entropy balancing).; Poor propensity score overlap (density plots, treated mass where controls are sparse) \u2192 Apply sp.trimming (Crump 2009) or redefine the estimand to the overlap region.; Too few matched controls per treated unit \u2192 Relax caliper, allow with-replacement, or use entropy balancing / overlap weights.. Alternatives: sp.ebalance, sp.cbps, sp.optimal_match, sp.sbw. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "nearest",
          "description": "Matching method",
          "enum": [
            "nearest",
            "caliper",
            "mahalanobis"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "outcome",
        "treatment"
      ],
      "type": "object"
    },
    "name": "match"
  },
  {
    "description": "Matrix-completion causal panel estimator (Athey et al., 2021).",
    "input_schema": {
      "properties": {
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "d",
        "time",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "matrix_completion"
  },
  {
    "description": "Estimate treatment effects using matrix completion. Assumptions: The relationship between the treated unit and controls is stable absent the intervention (causal_impact); Matrix-completion: the untreated potential outcomes follow a low-rank factor structure with treatment as the missingness pattern; No concurrent intervention affects the controls. Pre-conditions: A treated unit with a pre-period and a set of control series, or a panel with a low-rank structure. Failure modes: Pre-period fit is poor or controls are themselves affected by the intervention \u2192 Re-select controls, lengthen the pre-period, or use synthetic-control / DiD diagnostics.. Alternatives: sp.synth, sp.sdid, sp.gsynth. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "lambda_reg": {
          "description": "Nuclear norm regularisation parameter. If None, estimated via the universal threshold: lambda = sigma * sqrt(n).",
          "type": "number"
        },
        "max_iter": {
          "default": 1000,
          "description": "Maximum iterations for the proximal gradient algorithm.",
          "type": "integer"
        },
        "max_rank": {
          "description": "Maximum rank for the completed matrix. If None, no constraint.",
          "type": "integer"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Bootstrap iterations for standard errors.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "tol": {
          "default": 1e-05,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "treat": {
          "description": "Binary treatment indicator (0/1). Can be staggered.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "mc_panel"
  },
  {
    "description": "Matrix Completion Synthetic Control Method. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "covariates": {
          "description": "Time-varying covariates to partial out before matrix completion.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cv_folds": {
          "default": 5,
          "description": "Number of CV folds for automatic lambda selection.",
          "type": "integer"
        },
        "lambda_reg": {
          "description": "Nuclear norm penalty. If ``None`` (default), selected automatically via cross-validation on observed entries.",
          "type": "number"
        },
        "max_iter": {
          "default": 500,
          "description": "Maximum Soft-Impute iterations.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run placebo (permutation) inference by treating each control unit as if it were treated.",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "tol": {
          "default": 1e-06,
          "description": "Convergence tolerance (relative change in Frobenius norm).",
          "type": "number"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "mc_synth"
  },
  {
    "description": "McCrary (2008) density test for manipulation of the running variable at the cutoff in regression-discontinuity designs.  A significant discontinuity in the density of x at c is direct evidence that units are sorting around the cutoff (e.g. test-taking strategy, income manipulation), invalidating local randomisation. Validation: validated. Assumptions: Smooth density of x at c under the null of no manipulation; Local-linear density estimator captures the shape near c. Pre-conditions: x is continuous with mass on both sides of c; no extreme heaping at c (rounded data invalidates the local-linear density estimate). Failure modes: Test rejects (p < alpha) \u2014 manipulation evidence \u2192 Switch to donut-hole RD (sp.rdrobust(donut=\u03b4)) or partial-identification bounds (sp.rdrbounds).; Heaped data near c (e.g. integer-rounded scores) \u2192 The density-test statistic is unreliable on heaped data; consider Frandsen (2017) integer-RD adjustment.. Alternatives: sp.rddensity, sp.rdrbounds. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bw": {
          "description": "Bandwidth; auto if None",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "Cutoff value",
          "type": "number"
        },
        "n_bins": {
          "description": "Histogram bins; auto if None",
          "type": "integer"
        },
        "x": {
          "description": "Running variable",
          "type": "string"
        }
      },
      "required": [
        "x"
      ],
      "type": "object"
    },
    "name": "mccrary_test"
  },
  {
    "description": "Mediation analysis (Imai-Keele-Tingley 2010). Decomposes the total effect into natural direct effect (NDE) and natural indirect effect (NIE) via an interventional or sequential-ignorability identification strategy. Validation: certified parity evidence. Assumptions: Sequential ignorability: (Y(t,m), M(t)) \u22a5 T | X; Y(t,m) \u22a5 M | T, X; No post-treatment confounder of the mediator-outcome relationship (classical Imai-Keele-Tingley); SUTVA on both mediator and outcome. Pre-conditions: binary treatment 0/1; mediator is a post-treatment variable causally between treat and y; pre-treatment covariates capture confounding for T\u2013Y, M\u2013Y, T\u2013M. Failure modes: NDE + NIE do not sum to total effect (difference vs product decomposition) \u2192 Nonlinear / interactive mediator model \u2014 use sp.mediate_interventional or four-way decomposition.; Sensitivity to unobserved T-M / M-Y confounder unknown \u2192 Always report sp.mediate_sensitivity (Imai-Keele-Yamamoto \u03c1 bound).; Post-treatment confounder L suspected \u2192 Use sp.four_way_decomposition (VanderWeele 2014) which handles L.. Alternatives: sp.mediate_sensitivity, sp.mediate_interventional, sp.four_way_decomposition, sp.proximal. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Pre-treatment confounders",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mediator": {
          "description": "Mediator variable",
          "type": "string"
        },
        "n_sim": {
          "default": 1000,
          "description": "Monte Carlo sims for NDE/NIE",
          "type": "integer"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "mediator",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "mediate"
  },
  {
    "description": "Interventional (in)direct effects (VanderWeele, Vansteelandt, Robins 2014). Identifies mediation effects in the presence of treatment-induced mediator-outcome confounders where natural (in)direct effects are not identified. Validation: validated. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) \u2192 Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE).. Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Baseline covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mediator": {
          "description": "Mediator variable",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "tv_confounders": {
          "description": "Treatment-induced M-Y confounders",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "mediator",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "mediate_interventional"
  },
  {
    "description": "Causal-mediation analysis \u2014 article-facing alias for Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "m": {
          "description": "m parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "d",
        "m",
        "y"
      ],
      "type": "object"
    },
    "name": "mediation"
  },
  {
    "description": "Linear nested-models mediation decomposition (VanderWeele 2014",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "inference": {
          "default": "analytical",
          "description": "inference parameter (str).",
          "enum": [
            "analytical",
            "bootstrap"
          ],
          "type": "string"
        },
        "mediator": {
          "description": "mediator parameter (str).",
          "type": "string"
        },
        "n_boot": {
          "default": 299,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "mediator",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "mediation_decompose"
  },
  {
    "description": "Random-effects Gamma GLMM with log link (Stata ``meglm`` ``family(gamma)``).",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "megamma"
  },
  {
    "description": "Fit a generalised linear mixed model.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cov_type": {
          "default": "unstructured",
          "description": "Covariance estimator type.",
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "family parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "maxiter": {
          "default": 300,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "tol": {
          "default": 1e-06,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "trials": {
          "description": "trials parameter (Optional[str]).",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "meglm"
  },
  {
    "description": "Melly (2005) quantile decomposition.",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "n_tau_qr": {
          "default": 99,
          "description": "Number of tau qr.",
          "type": "integer"
        },
        "reference": {
          "default": 0,
          "description": "Same convention as ``machado_mata``: ``reference=0`` uses A's \u03b2 on B's X (coefficient-swap counterfactual F_{Y<0|1>}), opposite to ``dfl_decompose`` whose ``reference=0`` uses A's X with B's \u03b2.",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "tau_grid": {
          "description": "Grid of tau values to evaluate.",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "melly_decompose"
  },
  {
    "description": "Random-effects logistic regression (Stata ``melogit``). Validation: certified parity evidence. Assumptions: Binary outcome with logit link conditional on random effects; Cluster-level random intercepts (and slopes) are normally distributed; Random effects independent of covariates (no correlated-effects endogeneity). Pre-conditions: Binary (0/1) outcome; Grouping variable for the random effects. Failure modes: Adaptive quadrature likelihood does not converge \u2192 Increase quadrature points or simplify the random-effects structure to a single intercept; Estimated random-effect variance near zero (no clustering) \u2192 Drop the random effect and fit ordinary logit; Perfect separation in a sparse cluster \u2192 Collapse sparse categories or add a weak penalty/prior. Alternatives: sp.mixed, sp.regress. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "trials": {
          "description": "trials parameter (Optional[str]).",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "melogit"
  },
  {
    "description": "Random-effects negative-binomial regression (Stata ``menbreg``).",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "menbreg"
  },
  {
    "description": "Random-effects ordinal logit (Stata ``meologit``, R ``ordinal::clmm``).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cov_type": {
          "default": "unstructured",
          "description": "Covariance estimator type.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "maxiter": {
          "default": 300,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "tol": {
          "default": 1e-06,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "meologit"
  },
  {
    "description": "Random-effects Poisson regression (Stata ``mepoisson``).",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "nAGQ": {
          "default": 1,
          "description": "nAGQ parameter (int).",
          "type": "integer"
        },
        "offset": {
          "description": "Offset term or offset column.",
          "type": "string"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "mepoisson"
  },
  {
    "description": "Meta-learner framework for CATE: S-, T-, X-, R-, DR-Learner. Validation: validated. Assumptions: Unconfoundedness: Y(d) \u22a5 D | X; Overlap: 0 < P(D=1 | X) < 1; For R-Learner / DR-Learner: orthogonality between treatment residual and outcome residual. Pre-conditions: binary treatment (0/1); covariates numeric; categoricals encoded; enough treated AND control to train separate outcome models (T/X/DR-Learner). Failure modes: Large divergence across learner types \u2192 Use sp.compare_metalearners to identify which learner is biased; DR-Learner is safest under model misspecification.; S-Learner estimates near zero regardless of true effect \u2192 S-Learner regularization smooths treatment coefficient toward zero; use T/X/DR instead.; X-Learner fails when treated group is very small \u2192 X-Learner needs well-identified control-outcome model; fall back to T-Learner or weighted T-Learner.. Alternatives: sp.causal_forest, sp.dml, sp.tmle, sp.bcf. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "t",
          "description": "Learner type",
          "enum": [
            "s",
            "t",
            "x",
            "r",
            "dr"
          ],
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "metalearner"
  },
  {
    "description": "Two-period Mincer wage distribution with a structural shift.",
    "input_schema": {
      "properties": {
        "n": {
          "default": 5000,
          "description": "n parameter (int).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "mincer_wage_panel"
  },
  {
    "description": "Fit a linear mixed-effects model. Validation: certified parity evidence. Assumptions: Continuous outcome, linear in fixed effects; Random effects (intercepts/slopes) normally distributed with the chosen covariance structure; Residuals normal and homoscedastic conditional on random effects; random effects independent of covariates. Pre-conditions: Continuous outcome; Grouping variable (or nested list of grouping levels) for random effects. Failure modes: REML/ML optimizer fails to converge with rich random-slope covariance \u2192 Switch cov_type to 'diagonal' or 'identity', or drop random slopes; Singular covariance (boundary variance estimate) \u2192 Simplify the random-effects covariance or remove the offending random term; Binary or count outcome passed to a linear model \u2192 Use a generalized mixed model for the appropriate family. Alternatives: sp.regress, sp.melogit, sp.feols. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cov_type": {
          "default": "unstructured",
          "description": "Covariance estimator type.",
          "type": "string"
        },
        "group": {
          "description": "Group or cohort identifier.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 400,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "reml",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "tol": {
          "default": 1e-07,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x_fixed": {
          "description": "x_fixed parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "x_random parameter ('Sequence[str] | None').",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x_fixed",
        "y"
      ],
      "type": "object"
    },
    "name": "mixed"
  },
  {
    "description": "Mixed Logit (random-coefficient MNL) via simulated maximum likelihood.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "alt": {
          "description": "Alternative identifier \u2014 accepted for API compatibility with ``statsmodels.MNLogit`` / Stata conventions, but the ordering of alternatives is taken directly from the DataFrame's row order within each ``chid`` group.",
          "type": "string"
        },
        "chid": {
          "description": "Choice-situation identifier. All rows with the same ``chid`` form one choice set.",
          "type": "string"
        },
        "correlated": {
          "default": false,
          "description": "If True, estimate a full Cholesky factor of ``cov(beta_random)``; otherwise only diagonal standard deviations.",
          "type": "boolean"
        },
        "halton_seed": {
          "default": 1234,
          "description": "halton_seed parameter (int).",
          "type": "integer"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "n_draws": {
          "default": 500,
          "description": "Number of Halton draws per individual. Rule-of-thumb: use ``>= 1000`` for correlated models or precise inference.",
          "type": "integer"
        },
        "panel_id": {
          "description": "Individual identifier. When provided, the SAME draws of the random coefficients are used for every choice of the individual (Train 2009 \u00a76.5). Omit for cross-sectional data.",
          "type": "string"
        },
        "random_dist": {
          "description": "Per-random-variable distribution \u2014 one of ``'normal'`` (default), ``'lognormal'``, ``'triangular'``.",
          "type": "object"
        },
        "robust": {
          "default": true,
          "description": "Report OPG-sandwich robust SEs. ``False`` \u2192 classical inverse Hessian.",
          "type": "boolean"
        },
        "tol": {
          "default": 1e-06,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        },
        "x_fixed": {
          "description": "Columns entering with fixed (non-random) coefficients.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_random": {
          "description": "Columns entering with random coefficients (at least one required).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Column name with the 0/1 chosen indicator.",
          "type": "string"
        }
      },
      "required": [
        "chid",
        "y"
      ],
      "type": "object"
    },
    "name": "mixlogit"
  },
  {
    "description": "ML-enhanced partial-identification bounds on the ATE. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative \u2192 Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds.. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the band.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates X used for the outcome / propensity regressions.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "custom_learner": {
          "description": "Any ``.fit()`` / ``.predict()``-compatible regressor.",
          "type": "string"
        },
        "learner": {
          "default": "random_forest",
          "description": "\"random_forest\" Outcome-regression learner. Ignored if ``custom_learner`` is set.",
          "enum": [
            "random_forest",
            "gradient_boosting"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 200,
          "description": "Non-parametric bootstrap replicates for the 2-sided frequentist band. Set to 0 to return the raw plug-in bounds only.",
          "type": "integer"
        },
        "n_splits": {
          "default": 5,
          "description": "Number of cross-fitting folds.",
          "type": "integer"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Outcome and 0/1 treatment column names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and 0/1 treatment column names.",
          "type": "string"
        },
        "y_max": {
          "description": "A priori bounds on Y. Defaults to the empirical min/max. **Tighter** external bounds (e.g. if Y is a probability, use ``[0, 1]``) give tighter ML bounds.",
          "type": "number"
        },
        "y_min": {
          "description": "A priori bounds on Y. Defaults to the empirical min/max. **Tighter** external bounds (e.g. if Y is a probability, use ``[0, 1]``) give tighter ML bounds.",
          "type": "number"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "ml_bounds"
  },
  {
    "description": "Multinomial logit for J > 2 unordered categories via MLE. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "base": {
          "default": 0,
          "description": "Base / reference category (index into sorted unique values).",
          "type": "integer"
        },
        "cluster": {
          "description": "Cluster variable for clustered SE.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``\"robust\"`` / ``\"HC1\"`` for Huber-White sandwich SE.",
          "type": "string"
        },
        "rrr": {
          "default": false,
          "description": "Report Relative Risk Ratios (exp(beta)) instead of coefficients.",
          "type": "boolean"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable (categorical, integer-coded).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "mlogit"
  },
  {
    "description": "Model-averaging / stacking DML-PLR estimator.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Two-sided CI level.",
          "type": "number"
        },
        "candidates": {
          "description": "Candidate nuisance learners. ``ml_g`` regresses ``y`` on ``X``; ``ml_m`` regresses ``treat`` on ``X``. Defaults to a Lasso/Ridge/ RandomForest/GradientBoosting roster.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariate columns ``X``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fitting folds per candidate.",
          "type": "integer"
        },
        "sample_weight": {
          "description": "Per-observation weights. If supplied, every nuisance fit uses ``sample_weight=`` (with a graceful fallback warning if the learner does not accept it), the CLS stacking objective becomes weighted least squares, and the PLR moment + sandwich variance use weighted sums. The MSE used for ``inverse_risk`` / ``single_best`` weighting is also the weighted MSE.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Continuous-or-binary treatment column.",
          "type": "string"
        },
        "weight_rule": {
          "default": "short_stacking",
          "description": "How to combine candidate nuisance predictions or estimates. * ``\"short_stacking\"`` *(default; Ahrens et al. 2025 eq. 7)* \u2014 solve constrained least squares on cross-fitted predictions for each nuisance separately (``\u0177`` and ``D\u0302``), produce stacked nuisances, plug into the PLR moment equation. * ``\"single_best\"`` \u2014 Ahrens et al. (2025, fn. 8): pick the candidate with lowest joint nuisance MSE. * ``\"inverse_risk\"`` \u2014 :math:`w_k \\propto 1/(\\text{MSE}_g + \\text{MSE}_m)`. Convenience baseline; **not** in the paper. * ``\"equal\"`` \u2014 :math:`w_k = 1/K`. Convenience baseline; **not** in the paper. For the non-stacking rules (``inverse_risk`` / ``equal`` / ``single_best``) the function computes per-candidate :math:`\\hat\\theta_k` and reports the weighted average with a between-candidate-covariance-corrected SE; for ``\"short_stacking\"`` it reports the standard PLR sandwich SE on the stacked-nuisance score (Neyman orthogonality is preserved).",
          "enum": [
            "short_stacking",
            "single_best",
            "inverse_risk",
            "equal"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "model_averaging_dml"
  },
  {
    "description": "Unified Mendelian Randomization dispatcher. method= selects the estimator: 'ivw' / 'egger' / 'median' / 'penalized_median' / 'mode' / 'all' (runs IVW+Egger+Median together) / 'mvmr' / 'mediation' / 'bma' (multi-exposure) / 'presso' / 'radial' / 'leave_one_out' / 'steiger' / 'heterogeneity' / 'pleiotropy_egger' / 'f_statistic' (diagnostics).  Kwargs are passed through to the target function unchanged; see sp.mendelian_family guide. Validation: validated. Assumptions: Relevance: SNPs predict exposure (F-statistic \u2265 10 per SNP or set-F); Independence: SNPs \u22a5 confounders of exposure-outcome; Exclusion restriction: SNPs affect outcome only through exposure (InSIDE for Egger; \u2265 50% valid for median; modal for mode-based). Pre-conditions: SNP-summary statistics for exposure and outcome aligned by SNP; beta_exposure / beta_outcome / se_exposure / se_outcome arrays of equal length; \u2265 10 genetic instruments for reliable IVW/median/mode; \u2265 20 for robust Egger intercept. Failure modes: Egger intercept p < 0.05 \u2014 directional pleiotropy \u2192 Use weighted-median or mode-based estimator; report Egger intercept + I\u00b2 as pleiotropy diagnostic.; Q-statistic rejects homogeneity (Cochran's Q p < 0.05) \u2192 H\u2026",
    "input_schema": {
      "properties": {
        "method": {
          "default": "ivw",
          "description": "MR estimator / diagnostic \u2014 call sp.mr_available_methods() for the full list.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "mr"
  },
  {
    "description": "Marginal Structural Models for time-varying treatments with time-varying confounders. Uses stabilized IPTW and cluster-robust inference. Handles binary or continuous treatment; exposure summary can be current, cumulative, or ever. Validation: validated. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) \u2192 Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE).. Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "baseline": {
          "description": "Baseline covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "exposure": {
          "default": "cumulative",
          "description": "Exposure summary",
          "enum": [
            "cumulative",
            "current",
            "ever"
          ],
          "type": "string"
        },
        "family": {
          "default": "gaussian",
          "description": "Outcome family",
          "enum": [
            "gaussian",
            "binomial"
          ],
          "type": "string"
        },
        "id": {
          "description": "Unit identifier",
          "type": "string"
        },
        "time": {
          "description": "Period identifier",
          "type": "string"
        },
        "time_varying": {
          "description": "Time-varying confounders (pre-treatment)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Time-varying treatment",
          "type": "string"
        },
        "trim": {
          "default": 0.01,
          "description": "Weight truncation quantile",
          "type": "number"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "time_varying",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "msm"
  },
  {
    "description": "User-friendly alias for :func:`sp.rdmc` (multi-cutoff RD). Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "multi_cutoff_rd"
  },
  {
    "description": "Multiple Outcomes Synthetic Control Method (Sun 2023). Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and joint test.",
          "type": "number"
        },
        "method": {
          "default": "concatenated",
          "description": "Weight-estimation strategy. * ``'concatenated'`` -- stack all K standardised outcome panels vertically and solve one quadratic programme. * ``'averaged'`` -- standardise each outcome, average across K, then solve SCM on the mean series.",
          "enum": [
            "concatenated",
            "averaged"
          ],
          "type": "string"
        },
        "outcomes": {
          "description": "Column names for the K outcome variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge-type penalty added to the diagonal of the donor cross-product matrix (``penalization * I``). Helps when donors are collinear.",
          "type": "number"
        },
        "placebo": {
          "default": true,
          "description": "Run in-space placebo permutations for inference (each donor is pretended to be treated in turn).",
          "type": "boolean"
        },
        "standardize": {
          "default": true,
          "description": "Standardise each outcome to zero mean / unit variance before stacking or averaging (strongly recommended when outcome scales differ).",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Value identifying the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcomes",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "multi_outcome_synth"
  },
  {
    "description": "User-friendly alias for :func:`sp.rd_multi_score`. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {},
      "required": [],
      "type": "object"
    },
    "name": "multi_score_rd"
  },
  {
    "description": "Effects of multi-valued (3+ level) treatments via AIPW. Returns pairwise contrasts versus a reference level. Validation: validated. Assumptions: Generalised unconfoundedness: Y(a) \u22a5 T | X for all a; Generalised overlap: 0 < P(T=a | X) < 1 for each arm a; SUTVA across arms. Pre-conditions: treat is integer-valued with \u2265 2 distinct levels; covariates comprise the confounding set; enough units per treatment arm (\u2265 50 per arm). Failure modes: Some arm has near-zero propensity in the data \u2192 Violates overlap \u2014 drop that arm or use bounds.; Tiny treatment cells (< 30) \u2192 Collapse sparse arms or use regularised multinomial propensity.. Alternatives: sp.multi_arm_forest, sp.dml, sp.metalearner. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "reference": {
          "description": "Reference treatment level (defaults to 0 / smallest)",
          "type": "integer"
        },
        "treat": {
          "description": "Multi-valued treatment (int)",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "multi_treatment"
  },
  {
    "description": "Compute N-way cluster-robust variance of an OLS coefficient vector.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Design matrix used in the regression.",
          "type": "string"
        },
        "clusters": {
          "description": "One or more cluster variables, one per dimension. Non-numeric labels are supported.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "df_adjust": {
          "default": true,
          "description": "If True, apply the G/(G-1) * (n-1)/(n-k) CR1 finite-sample correction per component variance. If False, uses raw sandwich (useful when the caller has already degreed-freedom adjusted).",
          "type": "boolean"
        },
        "n_params": {
          "description": "Override for the ``k`` used in DOF adjustment; useful when FEs have been absorbed (pass total absorbed DOF here).",
          "type": "integer"
        },
        "psd_correct": {
          "default": true,
          "description": "Project V onto PSD cone by zeroing negative eigenvalues.",
          "type": "boolean"
        },
        "resid": {
          "description": "OLS residuals.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "clusters",
        "resid"
      ],
      "type": "object"
    },
    "name": "multiway_cluster_vcov"
  },
  {
    "description": "Fit a negative-binomial count model. Use this for overdispersed non-negative count outcomes; formulas may include explicit fixed effects with 'y ~ x | id' for moderate panels.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Column name for cluster-robust SEs.",
          "type": "string"
        },
        "dispersion": {
          "default": "mean",
          "description": "NB2 mean dispersion or NB1 constant dispersion.",
          "enum": [
            "mean",
            "constant"
          ],
          "type": "string"
        },
        "exposure": {
          "description": "Positive exposure column; log(exposure) is used as offset.",
          "type": "string"
        },
        "formula": {
          "description": "R-style formula, e.g. 'count ~ x1 + x2 | id'",
          "type": "string"
        },
        "irr": {
          "default": false,
          "description": "Report incidence-rate ratios instead of log coefficients.",
          "type": "boolean"
        },
        "offset": {
          "description": "Column containing a log offset.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "enum": [
            "nonrobust",
            "robust",
            "hc0",
            "hc1"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "nbreg"
  },
  {
    "description": "Aronow-Samii Horvitz-Thompson estimator for arbitrary interference via a user-supplied exposure mapping. Handles Bernoulli randomisation designs with simulated conservative variance. Validation: validated. Known limitations: design='complete' is reserved but not implemented; passing it raises NotImplementedError. Use design='bernoulli' with p_treat=K/N as an approximation only if that matches the assignment mechanism you are willing to assume. Assumptions: Exposure mapping is correctly specified (as4 / as3 / as2 \u2014 Aronow-Samii hierarchy); Positivity: every exposure level has positive probability under the design; Network adjacency is fixed / known (measurement error in ties introduces bias). Pre-conditions: adjacency is a binary n \u00d7 n matrix encoding network ties; Y, Z have same length n; randomisation design is known (bernoulli with p_treat, or complete). Failure modes: Some exposure level has < 5 observed units \u2192 Switch to a coarser mapping (as4 \u2192 as3) or increase sample size.; Variance estimate extremely conservative (wide CI) \u2192 HT-style variance is conservative by design \u2014 use sp.spillover for cluster case.. Alternatives: sp.spillover, sp.peer_effects, sp.cluster_matched_pair.\u2026",
    "input_schema": {
      "properties": {
        "Y": {
          "description": "Outcome vector",
          "type": "string"
        },
        "Z": {
          "description": "Treatment vector (0/1)",
          "type": "string"
        },
        "adjacency": {
          "description": "Adjacency matrix (n x n) or sparse",
          "type": "string"
        },
        "design": {
          "default": "bernoulli",
          "description": "Randomisation design",
          "enum": [
            "bernoulli",
            "complete"
          ],
          "type": "string"
        },
        "mapping": {
          "default": "as4",
          "description": "Exposure mapping",
          "enum": [
            "as4",
            "as3",
            "as2",
            "custom"
          ],
          "type": "string"
        },
        "n_sim": {
          "default": 2000,
          "description": "Number of sim.",
          "type": "integer"
        },
        "p_treat": {
          "description": "Marginal treatment probability",
          "type": "number"
        }
      },
      "required": [
        "Y",
        "Z",
        "adjacency"
      ],
      "type": "object"
    },
    "name": "network_exposure"
  },
  {
    "description": "Alias for ``icp(..., method='nonlinear')`` -- Heinze-Deml et al. 2018. Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges \u2014 faithfulness or sample size is the likely culprit \u2192 Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible.. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "environment": {
          "description": "environment parameter.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "environment",
        "y"
      ],
      "type": "object"
    },
    "name": "nonlinear_icp"
  },
  {
    "description": "Bunching at Notches estimator (Kleven & Waseem 2013). Assumptions: The counterfactual density would be smooth through the threshold absent the policy; Excess mass at the threshold reflects the behavioural elasticity of interest; No other discontinuity coincides with the threshold. Pre-conditions: A behavioural choice variable (earnings, hours, ...) with a known kink or notch in the budget/choice set; A visible empirical density of the running variable around the threshold. Failure modes: Round-number heaping or a coincident policy contaminates the bunching mass \u2192 Exclude heaping points, widen the excluded region, and test the counterfactual polynomial order.. Alternatives: sp.rdrobust, sp.rkd. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "bin_width": {
          "default": 500,
          "description": "Bin width for the histogram.",
          "type": "number"
        },
        "exclude_range": {
          "description": "Range around the notch to exclude from the counterfactual fit. If None, defaults to (notch_point - 3*bin_width, notch_point + 5*bin_width).",
          "type": "number"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications for standard errors.",
          "type": "integer"
        },
        "notch_point": {
          "description": "Location of the notch in the running variable.",
          "type": "number"
        },
        "notch_size": {
          "description": "Size of the discontinuous jump (delta-tau). If provided, a structural elasticity is estimated.",
          "type": "number"
        },
        "poly_order": {
          "default": 7,
          "description": "Polynomial order for the counterfactual distribution.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable name (e.g., 'income').",
          "type": "string"
        }
      },
      "required": [
        "notch_point",
        "x"
      ],
      "type": "object"
    },
    "name": "notch"
  },
  {
    "description": "Learn a DAG from data using NOTEARS (Zheng et al. 2018). Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges \u2014 faithfulness or sample size is the likely culprit \u2192 Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible.. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "h_tol": {
          "default": 1e-08,
          "description": "Convergence threshold for acyclicity constraint h(W).",
          "type": "number"
        },
        "lambda1": {
          "default": 0.1,
          "description": "L1 penalty weight for sparsity. Higher = sparser graph.",
          "type": "number"
        },
        "max_iter": {
          "default": 100,
          "description": "Maximum augmented Lagrangian iterations.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "rho_max": {
          "default": 1e+16,
          "description": "Maximum penalty parameter rho.",
          "type": "number"
        },
        "variables": {
          "description": "Column names to use. If None, uses all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "w_threshold": {
          "default": 0.3,
          "description": "Threshold for pruning small edge weights. Edges with |W_ij| < w_threshold are removed.",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "notears"
  },
  {
    "description": "Oaxaca-Blinder decomposition of mean outcome gaps. Validation: certified parity evidence. Assumptions: Linearity of conditional mean within each group; Constant returns to covariates within group (no interactions ignored); Reference-group choice does not change interpretive sign of explained vs. unexplained gaps. Pre-conditions: Binary group indicator with both groups represented; Linear specification of outcome on covariates within each group. Failure modes: Detailed decomposition signs flip when reference group changes \u2192 Report aggregated decomposition only, or use pooled reference (Neumark / Cotton).. Alternatives: sp.ffl_decompose, sp.dfl_decompose, sp.rif_decomposition. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for p-values.",
          "type": "number"
        },
        "detail": {
          "default": true,
          "description": "If True, compute variable-level contributions to the explained component.",
          "type": "boolean"
        },
        "group": {
          "description": "Binary group indicator (0 = Group A, 1 = Group B).",
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "Reference coefficient vector beta*: - ``0`` \u2014 Group A coefficients (beta_A). The \"explained\" part uses Group A's returns as the benchmark. - ``1`` \u2014 Group B coefficients (beta_B). - ``'pooled'`` \u2014 Pooled OLS (Neumark 1988). - ``'cotton'`` \u2014 Sample-size weighted average (Cotton 1988). - ``'reimers'`` \u2014 Equal-weighted average (Reimers 1983).",
          "enum": [
            "pooled",
            "cotton",
            "reimers"
          ],
          "type": "integer"
        },
        "x": {
          "description": "Covariate names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "oaxaca"
  },
  {
    "description": "Safe offline policy learning with a cost-constraint. Assumptions: Sequential ignorability: no unobserved confounders of actions and outcomes; Positivity: the behaviour policy explores all evaluated actions; The environment satisfies the assumed (Markov) dynamics. Pre-conditions: Logged trajectories (states, actions, rewards) from a known or estimable behaviour policy. Failure modes: Poor behaviour-policy coverage \u2014 the target policy queries unseen state-action regions \u2192 Use offline-safe / pessimistic methods and report effective sample size of the importance weights.. Alternatives: sp.offline_safe_policy, sp.policy_value. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "action": {
          "description": "Column names. state and action must be discrete.",
          "type": "string"
        },
        "cost": {
          "description": "Column names. state and action must be discrete.",
          "type": "string"
        },
        "cost_threshold": {
          "default": 0.5,
          "description": "Max allowed expected cost per step.",
          "type": "number"
        },
        "discount": {
          "default": 0.95,
          "description": "discount parameter (float).",
          "type": "number"
        },
        "n_iter": {
          "default": 100,
          "description": "Number of iter.",
          "type": "integer"
        },
        "reward": {
          "description": "Column names. state and action must be discrete.",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "state": {
          "description": "Column names. state and action must be discrete.",
          "type": "string"
        }
      },
      "required": [
        "action",
        "cost",
        "reward",
        "state"
      ],
      "type": "object"
    },
    "name": "offline_safe_policy"
  },
  {
    "description": "Ordered logit (proportional odds) model via MLE. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Ordered categorical dependent variable.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "ologit"
  },
  {
    "description": "Ordered probit model via MLE. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Ordered categorical dependent variable.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "oprobit"
  },
  {
    "description": "Optimal 1:1 matching via the Hungarian algorithm. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap \u2014 extreme propensity scores or few acceptable matches \u2192 Trim or restrict to the common-support region and report the discarded units.; Covariate imbalance remains after matching/weighting \u2192 Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences.. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "caliper": {
          "description": "Drop any pair with distance greater than ``caliper``.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "metric": {
          "default": "mahalanobis",
          "description": "metric parameter (str).",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "outcome",
        "treatment"
      ],
      "type": "object"
    },
    "name": "optimal_match"
  },
  {
    "description": "Oster (2019) sensitivity to selection on unobservables \u2014 computes the bounding coefficient under the assumption that selection on unobservables (proportional to delta x selection on observables) brings the explained variance to r_max.  The breakdown delta tells you how strong unobserved selection has to be to overturn your result. Validation: validated. Assumptions: Selection on unobservables is proportional (by factor delta) to selection on observables; r_max upper-bounds the explained variance achievable with all confounders included; Linear functional form for y on (treat, controls). Pre-conditions: you have fitted both a short (treatment-only) and long (treatment + controls) regression of y; long-regression R^2 is meaningfully larger than short-regression R^2. Failure modes: breakdown delta < 1.0 (weak unobservables overturn the result) \u2192 The result is fragile; report the breakdown delta alongside the point estimate.; r2_long \u2248 r2_short (controls add no explanatory power) \u2192 Oster's identified set degenerates when long and short R^2 are nearly equal; use sp.evalue or sp.sensemakr instead.. Alternatives: sp.evalue, sp.sensemakr, sp.rosenbaum_bounds. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "beta_long": {
          "description": "beta_long parameter (float).",
          "type": "number"
        },
        "beta_short": {
          "description": "Short-regression coefficient; if None, fit from data",
          "type": "number"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "delta": {
          "default": 1.0,
          "description": "Ratio of unobserved-to-observed selection (1.0 = equally strong)",
          "type": "number"
        },
        "r2_long": {
          "description": "r2_long parameter (float).",
          "type": "number"
        },
        "r2_short": {
          "description": "r2_short parameter (float).",
          "type": "number"
        },
        "r_max": {
          "description": "Hypothetical R^2 from a regression that includes all unobserved confounders; default 1.3*R^2_long",
          "type": "number"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome (alternative to passing beta_short/long directly)",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "oster_bounds"
  },
  {
    "description": "Oster (2019) coefficient stability bounds and delta* computation. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative \u2192 Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds.. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "delta_range": {
          "default": [
            -2.0,
            2.0
          ],
          "description": "Range of proportional selection parameter delta.",
          "type": "number"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "n_grid": {
          "default": 200,
          "description": "Grid points for delta in the identified set computation.",
          "type": "integer"
        },
        "r_max": {
          "default": 1.3,
          "description": "Maximum R-squared assumption. Oster recommends 1.3 * R-squared from the fully controlled regression. If <= 0, it is set to 1.3 * R_full automatically.",
          "type": "number"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "x_base": {
          "description": "Key treatment/variable(s) of interest.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x_controls": {
          "description": "Additional controls whose inclusion tightens identification.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x_base",
        "x_controls",
        "y"
      ],
      "type": "object"
    },
    "name": "oster_delta"
  },
  {
    "description": "Mirrored density plot of propensity scores by treatment group.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Axes to plot on. If None, a new figure is created.",
          "type": "string"
        },
        "covariates": {
          "description": "Covariates for PS estimation (ignored if *ps* supplied).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            8,
            4
          ],
          "description": "Figure size (width, height).",
          "type": "number"
        },
        "method": {
          "default": "logit",
          "description": "PS estimation method if *ps* is None.",
          "type": "string"
        },
        "ps": {
          "description": "Pre-estimated propensity scores.",
          "type": "string"
        },
        "title": {
          "default": "Propensity Score Overlap",
          "description": "Plot title.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "overlap_plot"
  },
  {
    "description": "Overlap-weighted 2\u00d72 DiD. Weights observations by e(X)(1-e(X)), where e(X) is the estimated propensity score, placing highest weight on units with the most overlap between treated and control covariate distributions. Useful when overlap is poor at the tails. Validation: validated. Assumptions: Overlap weights target the sub-population with positive overlap; Correct PS model OR outcome model for DR variant; Conditional parallel trends given covariates X after overlap weighting. Pre-conditions: 2 periods, binary treat; covariates with variation; Two-period panel with a binary treat indicator and a binary pre/post time indicator. Failure modes: Propensity scores pile up near 0/1 giving near-zero overlap weights and unstable ATT \u2192 Trim extreme-PS units or simplify the covariate set to restore common support; ps_model='dl' requested but the deep-learning propensity backend fails to converge \u2192 Fall back to ps_model='logit' or 'gbm' for a more stable propensity estimate; More than two time periods passed to this 2x2-only estimator \u2192 Collapse to a single pre/post contrast or use a staggered/multi-period estimator. Alternatives: sp.drdid, sp.did_2x2, sp.did, sp.callaway_santanna. Typical m\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates X",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ps_model": {
          "default": "logit",
          "description": "Propensity score model",
          "enum": [
            "logit",
            "rf",
            "gbm",
            "dl"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment indicator",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "overlap_weighted_did"
  },
  {
    "description": "Overlap-weight (ATO) treatment effect estimator. Validation: validated. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap \u2014 extreme propensity scores or few acceptable matches \u2192 Trim or restrict to the common-support region and report the discarded units.; Covariate imbalance remains after matching/weighting \u2192 Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences.. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates for the logistic propensity-score model.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "estimand": {
          "default": "ATO",
          "description": "Which generalized-weight scheme to use. All follow Li-Li-Li (2019) Table 1; 'ATO' uses the overlap weights; 'matching' uses the ``min(e, 1-e)`` weight; 'entropy' uses ``-e\u00b7log(e) - (1-e)\u00b7log(1-e)``; 'ATE/ATT/ATC' reduce to standard IPW for comparison.",
          "enum": [
            "ATO",
            "ATE",
            "ATT",
            "ATC",
            "matching",
            "entropy"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Paired-sample bootstrap replications for SE.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treat": {
          "description": "Binary 0/1 treatment column.",
          "type": "string"
        },
        "trim": {
          "default": 0.0,
          "description": "Optional clip of pscore to ``[trim, 1-trim]``. For overlap weights this is rarely needed \u2014 set to 0 by default.",
          "type": "number"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "overlap_weights"
  },
  {
    "description": "Unified panel regression: FE, RE, between, FD, pooled OLS, two-way FE, Mundlak/Chamberlain CRE, Arellano-Bond, Blundell-Bond system GMM. Results include built-in diagnostics: .hausman_test(), .bp_lm_test(), .f_test_effects(), .pesaran_cd_test(), .compare(method). Validation: certified parity evidence. Assumptions: Static FE: strict exogeneity of regressors conditional on unit fixed effects (E[u_it | x_i, alpha_i] = 0); Random effects: unit effect uncorrelated with regressors; relax with Mundlak / Chamberlain; Dynamic GMM: weak exogeneity and no second-order serial correlation in differenced errors. Pre-conditions: Data is a long-format panel keyed by (entity, time) with at least 2 time periods per entity; Outcome and regressors are numeric or properly encoded; Method-specific structure satisfied (e.g. dynamic GMM needs T moderate, system GMM needs initial-condition validity). Failure modes: Hausman test rejects RE \u2192 Switch to fixed effects (method='fe') or correlated random effects (method='mundlak').; Few clusters (< 30) inflate Type I error with cluster-robust SEs \u2192 Use wild-cluster bootstrap or CR2/CR3 small-sample corrections.; High-dimensional fixed effects make the design si\u2026",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Cluster variable: entity, time, or twoway",
          "type": "string"
        },
        "entity": {
          "description": "Unit identifier column",
          "type": "string"
        },
        "formula": {
          "description": "Regression formula: 'y ~ x1 + x2'",
          "type": "string"
        },
        "gmm_lags": {
          "default": "(2, 5)",
          "description": "GMM instrument lag range",
          "type": "string"
        },
        "lags": {
          "default": 1,
          "description": "AR lags for dynamic panel (ab/system)",
          "type": "integer"
        },
        "method": {
          "default": "fe",
          "description": "Estimation method",
          "enum": [
            "fe",
            "re",
            "be",
            "fd",
            "pooled",
            "twoway",
            "mundlak",
            "cre",
            "chamberlain",
            "ab",
            "system"
          ],
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard errors: nonrobust, robust, kernel, driscoll-kraay",
          "type": "string"
        },
        "time": {
          "description": "Time column",
          "type": "string"
        },
        "twostep": {
          "default": false,
          "description": "Two-step GMM",
          "type": "boolean"
        }
      },
      "required": [
        "entity",
        "formula",
        "time"
      ],
      "type": "object"
    },
    "name": "panel"
  },
  {
    "description": "Estimate the same model with multiple panel methods and return a side-by-side comparison table.",
    "input_schema": {
      "properties": {
        "entity": {
          "description": "Panel entity identifier column.",
          "type": "string"
        },
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "methods": {
          "description": "List of methods to compare, default: pooled/fe/re/twoway/mundlak",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        }
      },
      "required": [
        "entity",
        "formula",
        "time"
      ],
      "type": "object"
    },
    "name": "panel_compare"
  },
  {
    "description": "Panel FGLS (Feasible Generalized Least Squares).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "corr": {
          "default": "independent",
          "description": "Within-panel correlation: 'independent', 'ar1' (panel-specific AR(1)), 'psar1' (common AR(1)).",
          "type": "string"
        },
        "id": {
          "default": "id",
          "description": "Panel identifier.",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "panels": {
          "default": "heteroskedastic",
          "description": "Error structure across panels: 'homoskedastic', 'heteroskedastic', 'correlated' (cross-sectional).",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Time identifier.",
          "type": "string"
        },
        "tol": {
          "default": 1e-06,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "panel_fgls"
  },
  {
    "description": "Panel logit model.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Column for cluster-robust SEs.",
          "type": "string"
        },
        "id": {
          "default": "id",
          "description": "Unit and time identifier columns.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "Maximum optimizer iterations.",
          "type": "integer"
        },
        "method": {
          "default": "fe",
          "description": "'fe' (conditional FE logit), 're' (random effects), 'cre' (Mundlak).",
          "type": "string"
        },
        "n_quadrature": {
          "default": 12,
          "description": "Gauss-Hermite quadrature points (RE/CRE only).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "'nonrobust' or 'robust'.",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Unit and time identifier columns.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Gradient tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Binary dependent variable (0/1).",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "panel_logit"
  },
  {
    "description": "Panel probit model.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Column for cluster-robust SEs.",
          "type": "string"
        },
        "id": {
          "default": "id",
          "description": "Unit and time identifier columns.",
          "type": "string"
        },
        "maxiter": {
          "default": 200,
          "description": "Maximum optimizer iterations.",
          "type": "integer"
        },
        "method": {
          "default": "re",
          "description": "'re' (random effects) or 'cre' (Mundlak). FE probit not supported (incidental parameters problem).",
          "type": "string"
        },
        "n_quadrature": {
          "default": 12,
          "description": "Gauss-Hermite quadrature points.",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "'nonrobust' or 'robust'.",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Unit and time identifier columns.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Gradient tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Binary dependent variable (0/1).",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "panel_probit"
  },
  {
    "description": "Panel unit root test.",
    "input_schema": {
      "properties": {
        "id": {
          "default": "id",
          "description": "Unit identifier.",
          "type": "string"
        },
        "lags": {
          "description": "Number of ADF lags. If None, uses AIC selection.",
          "type": "integer"
        },
        "test": {
          "default": "ips",
          "description": "Test type: 'llc' (Levin-Lin-Chu), 'ips' (Im-Pesaran-Shin), 'fisher' (Fisher-type ADF), 'hadri' (stationarity test).",
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Time identifier.",
          "type": "string"
        },
        "trend": {
          "default": "c",
          "description": "'n' (none), 'c' (constant), 'ct' (constant + trend).",
          "type": "string"
        },
        "variable": {
          "description": "Variable to test.",
          "type": "string"
        }
      },
      "required": [
        "variable"
      ],
      "type": "object"
    },
    "name": "panel_unitroot"
  },
  {
    "description": "Plot raw outcome means over time for treatment and control groups.",
    "input_schema": {
      "properties": {
        "agg": {
          "default": "mean",
          "description": "Aggregation function: 'mean' or 'median'.",
          "type": "string"
        },
        "ax": {
          "description": "Existing axes to plot on.",
          "type": "string"
        },
        "ci": {
          "default": true,
          "description": "Show 95% confidence intervals (\u00b11.96 SE of mean).",
          "type": "boolean"
        },
        "colors": {
          "description": "Colors for (treatment, control). Default: ('#E74C3C', '#2C3E50').",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "Figure size.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit identifier (for panel data).",
          "type": "string"
        },
        "labels": {
          "description": "Custom labels, e.g. ``{'treat': 'New Jersey', 'control': 'Pennsylvania'}``.",
          "type": "object"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "title": {
          "description": "Plot title.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment group indicator. Binary (0/1) for 2\u00d72, or first-treatment-period for staggered (0 = never treated).",
          "type": "string"
        },
        "treat_time": {
          "description": "Treatment onset time. Draws a vertical line if provided.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "parallel_trends_plot"
  },
  {
    "description": "Partial-correlation p-value for H0: ``X \u27c2 Y | Z``.",
    "input_schema": {
      "properties": {
        "Z": {
          "description": "Instrument matrix or auxiliary covariate matrix.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "partial_corr_pvalue"
  },
  {
    "description": "Partial identification of ATE \u2014 article alias for the ``bounds`` module. Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative \u2192 Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds.. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "assumptions": {
          "description": "assumptions parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "instrument": {
          "description": "instrument parameter (Optional[str]).",
          "type": "string"
        },
        "method": {
          "default": "manski",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "selection": {
          "description": "selection parameter (Optional[str]).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "d",
        "y"
      ],
      "type": "object"
    },
    "name": "partial_identification"
  },
  {
    "description": "Estimate the Population Average Treatment Effect (PATE).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the confidence interval.",
          "type": "number"
        },
        "covariates": {
          "description": "Shared covariates present in both datasets.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "data_experiment": {
          "description": "Experimental/study sample. Must contain *y*, *treatment*, and all *covariates*.",
          "type": "string"
        },
        "data_target": {
          "description": "Target population sample. Must contain all *covariates*. Need not contain *y* or *treatment*.",
          "type": "string"
        },
        "method": {
          "default": "ipw",
          "description": "Estimation strategy: * ``'ipw'`` -- Inverse probability of sampling weights. * ``'aipw'`` -- Augmented IPW (doubly robust). * ``'calibration'`` -- Entropy balancing on covariate moments.",
          "enum": [
            "ipw",
            "aipw",
            "calibration"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications for standard-error estimation.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "treatment": {
          "description": "Binary treatment indicator (only in data_experiment).",
          "type": "string"
        },
        "trim": {
          "default": 0.01,
          "description": "Trimming threshold for participation propensities (values below *trim* or above 1 - *trim* are clipped).",
          "type": "number"
        },
        "y": {
          "description": "Outcome variable (only used from data_experiment).",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "data_experiment",
        "data_target",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "pate"
  },
  {
    "description": "Learn causal structure using the PC algorithm. Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges \u2014 faithfulness or sample size is the likely culprit \u2192 Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible.. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for conditional independence tests. Lower alpha = sparser graph (fewer edges).",
          "type": "number"
        },
        "ci_test": {
          "default": "fisherz",
          "description": "Conditional independence test: 'fisherz' (partial correlation) or 'hsic' (kernel-based, non-linear).",
          "type": "string"
        },
        "forbidden": {
          "description": "Background knowledge: edges that must NOT appear in the final graph (treated as undirected \u2014 both ``(a, b)`` and ``(b, a)`` are forbidden when either is given). The skeleton phase keeps these absent regardless of CI test outcomes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_cond_size": {
          "description": "Maximum conditioning set size. If None, goes up to d-2.",
          "type": "integer"
        },
        "required": {
          "description": "Background knowledge: directed edges ``a -> b`` that must appear in the CPDAG. The skeleton phase preserves them regardless of CI rejection, and the orientation phase pins their direction.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "variables": {
          "description": "Column names to use. If None, uses all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "pc_algorithm"
  },
  {
    "description": "Proximal causal inference for modified treatment policies (Park & Ying 2025). Estimates the effect of a policy that shifts the treatment distribution (e.g., raises the dose by 10%) under unobserved confounding identified by PCI. Validation: validated. Assumptions: The proxies are valid negative controls (relevant to the confounder, excluded from the causal channel); A bridge function exists (completeness conditions hold). Pre-conditions: Treatment-inducing and outcome-inducing proxy variables (negative controls) for the unobserved confounder. Failure modes: Proxies are weak or invalid \u2014 the bridge function is poorly identified \u2192 Test proxy relevance, select stronger proxies, or fall back to sensitivity analysis.. Alternatives: sp.select_pci_proxies, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "policy": {
          "description": "Function D \u2192 D_shifted",
          "type": "string"
        },
        "proxy_w": {
          "description": "proxy_w parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "proxy_z parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "policy",
        "proxy_w",
        "proxy_z",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "pci_mtp"
  },
  {
    "description": "PCMCI causal discovery for stationary time-series. Assumptions: Causal Markov condition and faithfulness (PC/GES/FCI); Causal sufficiency for PC/GES (no latent confounders); FCI relaxes this; Acyclicity; LiNGAM additionally assumes a linear non-Gaussian model. Pre-conditions: Constraint-/score-based discovery needs i.i.d. observational data with enough samples for reliable conditional-independence tests; Invariance-based discovery (ICP) needs data labelled by environment / intervention. Failure modes: Unstable skeleton / many undirected edges \u2014 faithfulness or sample size is the likely culprit \u2192 Increase the sample, relax the CI-test threshold, or switch to FCI if latent confounders are plausible.. Alternatives: sp.pc_algorithm, sp.fci, sp.ges, sp.lingam. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "ci_test": {
          "description": "Custom CI test ``(x, y, Z) -> p_value``. Defaults to :func:`partial_corr_pvalue`.",
          "type": "number"
        },
        "max_conds_dim": {
          "description": "Hard cap on the conditioning-set size during PC1. ``None`` means no cap (the algorithm stops automatically when no predictors remain).",
          "type": "integer"
        },
        "mci_alpha": {
          "description": "Significance threshold for the final MCI adjacency. Defaults to ``pc_alpha``.",
          "type": "number"
        },
        "pc_alpha": {
          "default": 0.05,
          "description": "Significance threshold used during the PC1 selection stage.",
          "type": "number"
        },
        "tau_max": {
          "default": 3,
          "description": "Maximum lag to consider for parent candidates.",
          "type": "integer"
        },
        "variables": {
          "description": "Columns to use. Defaults to all columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "verbose": {
          "default": false,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "pcmci"
  },
  {
    "description": "End-to-end DID workflow: preflight \u2192 did/CS estimator \u2192 audit \u2192 honest-DID sensitivity \u2192 bacon decomposition \u2192 brief. Returns one markdown report + the primary result_id. Use this when the user pastes a DID dataset and asks 'is the effect real?' \u2014 the pipeline runs every diagnostic the literature expects.",
    "input_schema": {
      "properties": {
        "cohort": {
          "description": "First-treatment cohort column. When supplied, dispatches callaway_santanna instead of classic 2x2 did.",
          "type": "string"
        },
        "covariates": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit id (panel) \u2014 required for staggered-DID.",
          "type": "string"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment indicator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "y",
        "treat",
        "time"
      ],
      "type": "object"
    },
    "name": "pipeline_did"
  },
  {
    "description": "End-to-end IV workflow: ivreg \u2192 first-stage F (effective + Olea-Pflueger) \u2192 Anderson-Rubin CI \u2192 e-value. Returns one markdown report + result_id.",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "'y ~ x_exog + (d_endog ~ z_instrument)' style.",
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "pipeline_iv"
  },
  {
    "description": "End-to-end RD workflow: rdrobust \u2192 rdplot (PNG image) \u2192 rddensity (McCrary) \u2192 rdsensitivity (bandwidth). Returns one markdown report + result_id + an image content block.",
    "input_schema": {
      "properties": {
        "c": {
          "default": 0.0,
          "description": "Cutoff value.",
          "type": "number"
        },
        "fuzzy": {
          "description": "Treatment column for fuzzy RD (optional).",
          "type": "string"
        },
        "x": {
          "description": "Running variable column.",
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y",
        "x"
      ],
      "type": "object"
    },
    "name": "pipeline_rd"
  },
  {
    "description": "Render the canonical diagnostic plot for a fitted result and return it as an inline PNG image content block. MCP clients with vision (Claude Desktop, vision-capable agents) get the plot for free; clients that don't support image content see only the JSON metadata. Plot kind is auto-selected from the result type: event-study for DID, rdplot for RD, gap plot for synth, balance plot for matching, ROC for classification, etc.",
    "input_schema": {
      "properties": {
        "figsize": {
          "description": "Width, height in inches (default [8,5]).",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "kind": {
          "description": "Override the auto-detected plot kind. Common values: 'event_study', 'rdplot', 'synth_gap', 'love_plot', 'coef_plot'.",
          "type": "string"
        },
        "result_id": {
          "description": "Handle to a fitted result.",
          "type": "string"
        }
      },
      "required": [
        "result_id"
      ],
      "type": "object"
    },
    "name": "plot_from_result"
  },
  {
    "description": "Poisson regression via MLE (IRLS).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable name for clustered standard errors.",
          "type": "string"
        },
        "exposure": {
          "description": "Exposure variable (will be logged and used as offset).",
          "type": "string"
        },
        "formula": {
          "description": "Model formula, e.g. \"y ~ x1 + x2\".",
          "type": "string"
        },
        "irr": {
          "default": false,
          "description": "If True, report Incidence Rate Ratios (exp(beta)) instead of raw coefficients.",
          "type": "boolean"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum IRLS iterations.",
          "type": "integer"
        },
        "offset": {
          "description": "Offset variable (log of exposure already computed).",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard error type: \"nonrobust\", \"robust\"/\"hc0\", \"hc1\".",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "weights": {
          "description": "Frequency/analytic weight variable.",
          "type": "string"
        },
        "x": {
          "description": "Independent variable names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "poisson"
  },
  {
    "description": "Doubly-robust policy-tree \u2014 article-facing alias.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (Optional[str]).",
          "type": "string"
        },
        "depth": {
          "description": "depth parameter (Optional[int]).",
          "type": "integer"
        },
        "max_depth": {
          "description": "max_depth parameter (Optional[int]).",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "policy_tree"
  },
  {
    "description": "Evaluate the expected value of a treatment policy. Assumptions: The relationship between the treated unit and controls is stable absent the intervention (causal_impact); Matrix-completion: the untreated potential outcomes follow a low-rank factor structure with treatment as the missingness pattern; No concurrent intervention affects the controls. Pre-conditions: A treated unit with a pre-period and a set of control series, or a panel with a low-rank structure. Failure modes: Pre-period fit is poor or controls are themselves affected by the intervention \u2192 Re-select controls, lengthen the pre-period, or use synthetic-control / DiD diagnostics.. Alternatives: sp.synth, sp.sdid, sp.gsynth. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "policy": {
          "description": "Binary policy recommendations (0 or 1).",
          "type": "string"
        },
        "scores": {
          "description": "Doubly robust scores (AIPW pseudo-outcomes for treatment). Positive scores indicate the individual benefits from treatment.",
          "type": "string"
        }
      },
      "required": [
        "policy",
        "scores"
      ],
      "type": "object"
    },
    "name": "policy_value"
  },
  {
    "description": "Return the post-estimation actions supported by *result*.",
    "input_schema": {
      "properties": {
        "include_diagnostics": {
          "default": true,
          "description": "Include scalar diagnostics from ``model_info`` / ``diagnostics``.",
          "type": "boolean"
        },
        "result": {
          "description": "Fitted StatsPAI result, estimator, or compatible object.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "postestimation_contract"
  },
  {
    "description": "Return the post-estimation actions supported by *result*.",
    "input_schema": {
      "properties": {
        "include_diagnostics": {
          "default": true,
          "description": "Include scalar diagnostics from ``model_info`` / ``diagnostics``.",
          "type": "boolean"
        },
        "result": {
          "description": "Fitted StatsPAI result, estimator, or compatible object.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "postestimation_report"
  },
  {
    "description": "Pseudo-Poisson Maximum Likelihood with high-dimensional fixed effects. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "absorb": {
          "description": "Fixed effects to absorb, e.g. ``\"origin + destination + year\"``. Overrides any FE specification in the formula.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Variable name for clustered standard errors (recommended for gravity models, e.g. cluster on country-pair).",
          "type": "string"
        },
        "formula": {
          "description": "Model formula. Fixed effects can be specified via ``|``: ``\"trade ~ dist + contig | origin + destination + year\"``",
          "type": "string"
        },
        "maxiter": {
          "default": 1000,
          "description": "Maximum IRLS iterations.",
          "type": "integer"
        },
        "robust": {
          "default": "robust",
          "description": "Default is robust SE (as in Stata's ppmlhdfe). Options: \"robust\"/\"hc0\", \"hc1\", \"nonrobust\".",
          "type": "string"
        },
        "separation": {
          "default": true,
          "description": "If True, check for separation (perfect prediction of zeros) and warn. Observations causing separation are not dropped automatically.",
          "type": "boolean"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "weights": {
          "description": "Weight variable name.",
          "type": "string"
        },
        "x": {
          "description": "Independent variable names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "ppmlhdfe"
  },
  {
    "description": "Predict CATE on new (out-of-sample) data. Assumptions: Unconfoundedness given the covariates; Overlap / positivity across the covariate space; Nuisance functions are estimated consistently; cross-fitting controls overfitting bias. Pre-conditions: Covariates, a treatment indicator, and an outcome for each unit; Enough data to fit flexible nuisance models with sample-splitting / cross-fitting. Failure modes: CATE estimates are unstable or extrapolate beyond the covariate support \u2192 Restrict to the overlap region, increase data, or use a doubly-robust learner (DR-/R-learner).. Alternatives: sp.dml, sp.causal_forest, sp.tmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "new_data": {
          "description": "New data with the same covariate columns used in estimation.",
          "type": "string"
        },
        "result": {
          "description": "Result from ``metalearner()`` containing a fitted estimator.",
          "type": "string"
        }
      },
      "required": [
        "new_data",
        "result"
      ],
      "type": "object"
    },
    "name": "predict_cate"
  },
  {
    "description": "Run pre-fit identification checks for a chosen method on a DataFrame. Verdict in {PASS, WARN, FAIL}. ALWAYS call this before fitting on an unfamiliar dataset to surface design problems (overlap, cohort sizes, IV first-stage F, running-variable density at the cutoff).",
    "input_schema": {
      "properties": {
        "cohort": {
          "type": "string"
        },
        "covariates": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit id column.",
          "type": "string"
        },
        "instrument": {
          "type": "string"
        },
        "method": {
          "description": "Estimator name: 'did', 'rd', 'iv', 'synth', 'matching', 'dml', \u2026",
          "type": "string"
        },
        "running_var": {
          "type": "string"
        },
        "time": {
          "type": "string"
        },
        "treatment": {
          "type": "string"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "method"
      ],
      "type": "object"
    },
    "name": "preflight"
  },
  {
    "description": "Power of the pre-trend test against a hypothesised violation.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level of the pre-trend test.",
          "type": "number"
        },
        "delta": {
          "description": "Hypothesised trend violation in the pre-period (length = number of pre-periods). Default: linear trend ``delta[k] = (k+1) * min(|SE|)`` -- a violation equal to one SE at the furthest lag, declining linearly to near-zero.",
          "type": "string"
        },
        "result": {
          "description": "Event-study result with pre-treatment estimates and SEs.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "pretrends_power"
  },
  {
    "description": "Print a combined pre-trends diagnostic report.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "delta": {
          "description": "Passed to ``pretrends_power``.",
          "type": "string"
        },
        "result": {
          "description": "Event-study result.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "pretrends_summary"
  },
  {
    "description": "Joint Wald test of pre-treatment ATTs (or event-study leads) against zero \u2014 the canonical sanity check for the parallel-trends assumption in DiD designs.  Failing to reject is necessary but not sufficient evidence for parallel trends; always pair with sp.honest_did / sp.sensitivity_rr for design-robust inference. Validation: validated. Assumptions: The test asks whether the pre-period ATTs *jointly* differ from zero; Failing to reject is consistent with parallel trends but does NOT prove it (low power problem \u2014 Roth 2022). Pre-conditions: result has at least one pre-treatment period coefficient and its variance; covariance between pre-period coefficients is available (cluster-robust SE recommended). Failure modes: Single pre-period (no pretrends to test) \u2192 Pretrends test needs >= 2 pre-treatment periods; widen the panel or drop the test.; High-power study rejects but visual pretrends look flat \u2192 Use sp.honest_did + sp.sensitivity_rr to bound the bias; reporting *both* is standard practice.. Alternatives: sp.sensitivity_rr, sp.honest_did, sp.event_study. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "result": {
          "description": "DiD or event-study result with pre-period coefficients",
          "type": "string"
        },
        "type": {
          "default": "wald",
          "description": "Test statistic",
          "enum": [
            "wald"
          ],
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "pretrends_test"
  },
  {
    "description": "Principal Stratification (Frangakis & Rubin 2002). 'monotonicity' method identifies the complier PCE (= LATE) and reports Zhang-Rubin sharp bounds on the always-survivor SACE. 'principal_score' uses Ding-Lu covariate weighting to point-identify stratum-specific effects under principal ignorability. Validation: validated. Known limitations: Always-survivor SACE under encouragement design (Mealli & Pacini 2013, partial identification) is not yet implemented; only AIR / Wald LATE point estimates (\u03c4_Y on outcome, \u03c4_S on the post-treatment stratum) are reported when an instrument is supplied. Assumptions: Monotonicity (no defiers) for method='monotonicity'; Principal ignorability for method='principal_score' (strata \u22a5 Y(d) | X); SUTVA and exclusion restriction for the never-takers / always-takers interpretation. Pre-conditions: binary treatment; binary post-treatment stratum variable (compliance, survival, employment, \u2026); covariates required when method='principal_score' (for Ding-Lu weighting). Failure modes: Zhang-Rubin bounds include 0 and both signs \u2192 Strata partition too weak for point identification \u2014 add covariates and use method='principal_score'.; Complier share near zero \u2192 Lo\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "CI level (e.g. 0.05 for 95% CIs)",
          "type": "number"
        },
        "covariates": {
          "description": "Baseline covariates (required for principal_score)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instrument": {
          "description": "Binary instrument column. When supplied, switches to the AIR / Wald LATE estimator: under random Z, monotonicity, and exclusion, reports two LATEs among Z-compliers \u2014 \u03c4_Y for the effect of the treatment on the outcome, and \u03c4_S for the effect on the post-treatment stratum variable. method= is ignored on this path.",
          "type": "string"
        },
        "method": {
          "default": "monotonicity",
          "description": "Identification strategy",
          "enum": [
            "monotonicity",
            "principal_score"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Bootstrap replications",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible bootstrap draws",
          "type": "integer"
        },
        "strata": {
          "description": "Binary post-treatment variable",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "strata",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "principal_strat"
  },
  {
    "description": "Probit regression via maximum likelihood. Validation: certified parity evidence.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "at_values": {
          "description": "Variable values for ``marginal_effects='at'``.",
          "type": "object"
        },
        "cluster": {
          "description": "Column name for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Formula like ``\"y ~ x1 + x2\"``.",
          "type": "string"
        },
        "marginal_effects": {
          "description": "``'average'`` (AME), ``'mean'`` (MEM), or ``'at'`` (MER).",
          "type": "string"
        },
        "maxiter": {
          "default": 100,
          "description": "Maximum Newton-Raphson iterations.",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "``'nonrobust'`` for MLE SE, ``'hc1'`` / ``'robust'`` for sandwich SE.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance on log-likelihood change.",
          "type": "number"
        },
        "weights": {
          "description": "Column name for frequency/analytic weights.",
          "type": "string"
        },
        "x": {
          "description": "Regressor names (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "probit"
  },
  {
    "description": "Estimate propensity scores P(D=1|X). Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap \u2014 extreme propensity scores or few acceptable matches \u2192 Trim or restrict to the common-support region and report the discarded units.; Covariate imbalance remains after matching/weighting \u2192 Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences.. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "logit",
          "description": "Estimation method. ``'logit'`` uses IRLS (no sklearn needed). ``'probit'`` uses scipy.optimize. ``'gbm'`` tries sklearn GradientBoostingClassifier, falling back to logit with interactions.",
          "enum": [
            "logit",
            "probit",
            "gbm"
          ],
          "type": "string"
        },
        "treatment": {
          "description": "Name of binary treatment column (0/1).",
          "type": "string"
        },
        "trimming": {
          "description": "If ``'crump'``, apply Crump et al. (2009) trimming after estimation. Trimmed observations receive ``NaN`` scores.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "propensity_score"
  },
  {
    "description": "Proximal Causal Inference via linear 2SLS on the outcome bridge. Identifies ATE with unmeasured confounding using two proxy variables: a treatment-side Z (instrument for W) and an outcome-side W (endogenous bridge regressor). Validation: validated. Assumptions: Existence of an outcome bridge function h(w, a, x) that recovers E[Y(a) | U, X]; Z and W are conditionally independent given U and (A, X); Z \u22a5 Y | U, A, X (exclusion on Z). Pre-conditions: at least one treatment-side proxy Z (independent of outcome given U, X); at least one outcome-side proxy W (independent of treatment given U, X); proxy_z and proxy_w measure the same unmeasured confounder U from different angles. Failure modes: First-stage (Z \u2192 W) too weak \u2192 Try richer Z or more proxies; without first-stage strength the bridge is underidentified.; Proxies collapse to nearly-constant \u2192 Proxy variation insufficient \u2014 redesign measurement or fall back to sensitivity (sp.sensemakr).; Estimate highly sensitive to bridge specification \u2192 Report multiple bridge families; compare with sp.negative_control_outcome / _exposure.. Alternatives: sp.negative_control_outcome, sp.negative_control_exposure, sp.double_negative_control, sp.iv\u2026",
    "input_schema": {
      "properties": {
        "bridge": {
          "default": "linear",
          "description": "Bridge function family",
          "enum": [
            "linear"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Baseline covariates",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 0,
          "description": "Bootstrap SE replications",
          "type": "integer"
        },
        "proxy_w": {
          "description": "Outcome-side proxies (endogenous)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_z": {
          "description": "Treatment-side proxies (instruments for W)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "proxy_w",
        "proxy_z",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "proximal"
  },
  {
    "description": "Compute comprehensive propensity score balance table.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate columns to assess balance for.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "logit",
          "description": "PS estimation method ('logit', 'probit', 'gbm').",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column.",
          "type": "string"
        },
        "weights": {
          "description": "IPW or matching weights. If None, inverse-PS weights are computed automatically from estimated propensity scores.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "ps_balance"
  },
  {
    "description": "Propensity-score matching \u2014 article alias for :func:`match` Validation: certified parity evidence. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap \u2014 extreme propensity scores or few acceptable matches \u2192 Trim or restrict to the common-support region and report the discarded units.; Covariate imbalance remains after matching/weighting \u2192 Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences.. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "method": {
          "default": "nn",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "d",
        "y"
      ],
      "type": "object"
    },
    "name": "psm"
  },
  {
    "description": "Propensity score distribution plot (common support diagnostic).",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "colors": {
          "default": [
            "#3498DB",
            "#E74C3C"
          ],
          "description": "Colors for (control, treated).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariates used to estimate the propensity score.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "labels": {
          "default": [
            "Control",
            "Treated"
          ],
          "description": "Labels for (control, treated).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_bins": {
          "default": 40,
          "description": "Number of histogram bins.",
          "type": "integer"
        },
        "title": {
          "description": "title parameter (str).",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment column.",
          "type": "string"
        },
        "trim": {
          "description": "If set, draw vertical lines at (trim, 1-trim) to show the recommended trimming region.",
          "type": "number"
        }
      },
      "required": [
        "covariates",
        "treat"
      ],
      "type": "object"
    },
    "name": "psplot"
  },
  {
    "description": "Publication readiness checklist.",
    "input_schema": {
      "properties": {
        "design": {
          "description": "Research design: 'rct', 'did', 'rd', 'iv', 'observational'.",
          "type": "string"
        },
        "has_balance": {
          "default": false,
          "description": "Already have balance table.",
          "type": "boolean"
        },
        "has_heterogeneity": {
          "default": false,
          "description": "Already have subgroup analysis.",
          "type": "boolean"
        },
        "has_mht": {
          "default": false,
          "description": "Already have MHT correction.",
          "type": "boolean"
        },
        "has_placebo": {
          "default": false,
          "description": "Already have placebo tests.",
          "type": "boolean"
        },
        "has_pretrends": {
          "default": false,
          "description": "Already have pre-trend tests.",
          "type": "boolean"
        },
        "has_robustness": {
          "default": false,
          "description": "Already have robustness checks.",
          "type": "boolean"
        },
        "has_sensitivity": {
          "default": false,
          "description": "Already have sensitivity analysis.",
          "type": "boolean"
        },
        "results": {
          "description": "List of estimated result objects.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "venue": {
          "default": "top5_econ",
          "description": "Target venue: 'top5_econ', 'aej_applied', 'rct'.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "pub_ready"
  },
  {
    "description": "Pairwise comparisons of predictive margins across all levels.",
    "input_schema": {
      "properties": {
        "adjust": {
          "default": "none",
          "description": "P-value adjustment method: - ``'none'``: unadjusted. - ``'bonferroni'``: Bonferroni correction. - ``'sidak'``: Sidak correction. - ``'holm'``: Holm step-down procedure.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for (adjusted) confidence intervals.",
          "type": "number"
        },
        "result": {
          "description": "Fitted model result.",
          "type": "string"
        },
        "variable": {
          "description": "Categorical variable whose levels are compared pairwise.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "variable"
      ],
      "type": "object"
    },
    "name": "pwcompare"
  },
  {
    "description": "Quantile Difference-in-Differences (Athey & Imbens 2006 CIC). Estimates QTE at multiple quantiles via changes-in-changes on a 2\u00d72 design with bootstrap SE. Validation: validated. Assumptions: CIC rank invariance: the quantile rank in the untreated distribution is stable across groups; Continuous outcome support covering both groups in both periods; SUTVA (no cross-group spillovers). Pre-conditions: panel or repeated cross-section; group is binary 0/1; time is binary 0/1 (pre / post). Failure modes: Outcome heavily discrete / zero-inflated \u2192 CIC rank-matching is unstable on discrete supports \u2014 use QTE regression (sp.qte) or Firpo-RIF.; Bootstrap CI across quantiles varies wildly \u2192 Thin tails at extreme quantiles \u2014 restrict to [0.2, 0.8] or raise n_boot to 2000.. Alternatives: sp.qte, sp.did, sp.rifreg. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "group": {
          "description": "Binary treated / control group",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "Quantiles to estimate, defaults to [0.1, ..., 0.9]",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "time": {
          "description": "Binary pre / post indicator",
          "type": "string"
        },
        "y": {
          "description": "Outcome",
          "type": "string"
        }
      },
      "required": [
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "qdid"
  },
  {
    "description": "Quantile Synthetic Control (alias for DiSCo with ``method='quantile'``). Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "n_quantiles": {
          "default": 100,
          "description": "Number of quantile grid points.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run placebo permutation inference.",
          "type": "boolean"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "qqsynth"
  },
  {
    "description": "Quantile regression at specified quantile(s). Validation: certified parity evidence. Assumptions: Conditional quantile is linear in covariates; Outcome continuous (or finely discretized) so the check-function minimization is well-posed; Powell (1991) sandwich SEs require a consistent kernel density of the conditional density at zero. Pre-conditions: Continuous outcome variable; Quantile tau strictly in (0,1). Failure modes: Sparse data in tail quantiles gives unstable density estimate and wide SEs \u2192 Estimate a more central quantile or pool more data near the tail; Crossing/degenerate fit when regressors are collinear \u2192 Drop or combine collinear regressors before refitting. Alternatives: sp.regress, sp.feols. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "'y ~ x1 + x2'",
          "type": "string"
        },
        "q": {
          "default": 0.5,
          "description": "Quantile (0-1)",
          "type": "number"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "qreg"
  },
  {
    "description": "Quantile Treatment Effect via quantile regression or IPW weighting. Returns QTE at supplied quantiles with bootstrap SE. Validation: validated. Assumptions: For 'quantile_regression': unconfoundedness conditional on controls; For 'ipw': unconfoundedness + overlap 0 < e(x) < 1; Correct parametric quantile model (sensitivity tested via multiple quantiles). Pre-conditions: binary or continuous treatment (method='quantile_regression' supports both; 'ipw' needs binary); continuous outcome; controls cover the confounding set (for 'quantile_regression'). Failure modes: Large IPW weights (method='ipw') \u2192 Extreme propensities \u2014 trim (sp.trimming) or switch to doubly-robust DR-QTE.; Quantile crossing \u2192 Use rearrangement (Chernozhukov-Fernandez-Val-Galichon) or monotone constraints.. Alternatives: sp.qdid, sp.rifreg, sp.cic, sp.metalearner. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "quantile_regression",
          "description": "Estimation method",
          "enum": [
            "quantile_regression",
            "ipw"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "quantiles": {
          "description": "quantiles parameter (list).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "qte"
  },
  {
    "description": "High-dimensional panel QTE via LASSO-selected controls. Assumptions: Selection-on-observables (unconfoundedness + overlap) or, for IV variants, instrument validity; For IV-QTE: rank invariance / rank similarity (monotonicity of the structural quantile function). Pre-conditions: Covariates, treatment, and outcome; for IV-quantile methods, a valid instrument; Enough data to estimate the outcome distribution across quantiles. Failure modes: Estimated conditional quantiles cross (non-monotone), or tail quantiles are unstable \u2192 Use rearrangement / monotonization and avoid extreme quantiles where data are sparse.. Alternatives: sp.qte, sp.iv, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "High-dim candidate control set.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "lasso_alpha": {
          "default": 0.01,
          "description": "lasso_alpha parameter (float).",
          "type": "number"
        },
        "quantiles": {
          "description": "Defaults to (0.1, 0.25, 0.5, 0.75, 0.9).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "time",
        "treat",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "qte_hd_panel"
  },
  {
    "description": "Rank-Average Treatment Effect (Yadlowsky et al. 2023). Assumptions: Unconfoundedness given the covariates; Overlap / positivity; Honesty: separate subsamples are used to choose splits and to estimate effects. Pre-conditions: Covariates, treatment, and outcome with enough data to grow an honest forest. Failure modes: Calibration test rejects \u2014 the forest's heterogeneity is not well calibrated \u2192 Increase the sample / number of trees, or fall back to a doubly-robust learner.. Alternatives: sp.dml, sp.auto_cate, sp.tmle. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "T": {
          "description": "If omitted, falls back to the forest's stored training arrays.",
          "type": "string"
        },
        "X": {
          "description": "If omitted, falls back to the forest's stored training arrays.",
          "type": "string"
        },
        "Y": {
          "description": "If omitted, falls back to the forest's stored training arrays.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        },
        "q_grid": {
          "default": 100,
          "description": "Number of quantile grid points used to report the TOC curve. Does not affect the point estimate or SE (those are computed from ranks exactly).",
          "type": "integer"
        },
        "seed": {
          "description": "Ignored; kept for API backwards compatibility.",
          "type": "integer"
        },
        "target": {
          "default": "AUTOC",
          "description": "target parameter (str).",
          "enum": [
            "AUTOC",
            "QINI"
          ],
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "rate"
  },
  {
    "description": "2D boundary regression discontinuity estimation. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "approach": {
          "default": "distance",
          "description": "``'distance'``: project onto signed distance to boundary, then apply univariate local polynomial RD. ``'location'``: fit bivariate local polynomial on each side of the boundary at evaluation points.",
          "type": "string"
        },
        "boundary": {
          "description": "Function ``f(x1) -> x2`` defining the boundary curve. If None, the boundary is the vertical line ``x1 = 0``.",
          "type": "string"
        },
        "bwselect": {
          "default": "mserd",
          "description": "Bandwidth selection method (used when ``h`` is None).",
          "type": "string"
        },
        "eval_points": {
          "description": "Shape ``(k, 2)`` array of boundary evaluation points. If None, points are automatically selected along the boundary.",
          "type": "string"
        },
        "h": {
          "description": "Manual bandwidth. If None, MSE-optimal bandwidth is selected.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function: 'triangular', 'uniform', or 'epanechnikov'.",
          "type": "string"
        },
        "n_eval": {
          "default": 1,
          "description": "Number of evaluation points when ``eval_points`` is None. Use 1 for a single pooled effect.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order for point estimation (1 = local linear).",
          "type": "integer"
        },
        "treatment": {
          "description": "Binary treatment indicator (1 = treated, 0 = control).",
          "type": "string"
        },
        "x1": {
          "description": "First running variable (score dimension 1).",
          "type": "string"
        },
        "x2": {
          "description": "Second running variable (score dimension 2).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "x1",
        "x2",
        "y"
      ],
      "type": "object"
    },
    "name": "rd2d"
  },
  {
    "description": "Bandwidth selection for 2D boundary RD.",
    "input_schema": {
      "properties": {
        "approach": {
          "default": "distance",
          "description": "'distance' or 'location'.",
          "type": "string"
        },
        "boundary": {
          "description": "Boundary function f(x1) -> x2. None implies x1 = 0.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order.",
          "type": "integer"
        },
        "treatment": {
          "description": "Binary treatment indicator.",
          "type": "string"
        },
        "x1": {
          "description": "Running variable names.",
          "type": "string"
        },
        "x2": {
          "description": "Running variable names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "x1",
        "x2",
        "y"
      ],
      "type": "object"
    },
    "name": "rd2d_bw"
  },
  {
    "description": "2D boundary RD visualization.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Pre-existing axes to draw on.",
          "type": "string"
        },
        "boundary": {
          "description": "Boundary function f(x1) -> x2. None implies x1 = 0.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            8
          ],
          "description": "Figure size.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "plot_type": {
          "default": "scatter",
          "description": "``'scatter'``: 2D scatter of (x1, x2) colored by treatment status, with boundary curve and optional bandwidth region. ``'heatmap'``: outcome values displayed as a heatmap with boundary overlay. ``'boundary_effects'``: treatment effect estimates along the boundary (requires ``result`` with multiple eval points).",
          "type": "string"
        },
        "result": {
          "description": "Result from ``rd2d()``, used for bandwidth and effect info.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment indicator.",
          "type": "string"
        },
        "x1": {
          "description": "Running variable names.",
          "type": "string"
        },
        "x2": {
          "description": "Running variable names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "treatment",
        "x1",
        "x2",
        "y"
      ],
      "type": "object"
    },
    "name": "rd2d_plot"
  },
  {
    "description": "Bayesian RDD allowing CATE to depend on covariates. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "n_draws": {
          "default": 2000,
          "description": "Number of draws.",
          "type": "integer"
        },
        "running": {
          "description": "running parameter (str).",
          "type": "string"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "running",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_bayes_hte"
  },
  {
    "description": "Bias-aware confidence interval for fuzzy RD via Anderson-Rubin test inversion (Noack-Rothe 2024 Econometrica).  Robust to weak first stages and avoids the power asymmetry of conventional 2SLS-style fuzzy RD CIs (Kaliski-Keane-Neal 2025). Validation: validated. Assumptions: Continuity of potential outcomes in x at c (Hahn, Todd, van der Klaauw 2001); No manipulation of x at c (McCrary density test); Local randomization only in a neighborhood of c \u2014 extrapolation away from c is not identified. Pre-conditions: running variable x is continuous with support on both sides of c; treatment assignment is determined by the cutoff c (sharp) or probabilistically at c (fuzzy); sufficient mass of observations within the optimal bandwidth. Failure modes: McCrary density test p < 0.05 \u2192 Use donut-hole RD (donut=<\u03b4>) or partial-identification bounds.; Covariate imbalance at cutoff (sp.rdbalance rejects) \u2192 Include covariates as controls, narrow bandwidth, or report as caveat.; Effect unstable across bandwidth halvings \u2192 Report sp.rdbwsensitivity and sp.rd_honest (Armstrong-Koles\u00e1r honest CI).. Alternatives: sp.rdrobust, sp.rd_honest, sp.rdrbounds, sp.bounds. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "M_d": {
          "description": "Bound on |g_D''|; auto if None",
          "type": "number"
        },
        "M_y": {
          "description": "Bound on |g_Y''|; auto if None",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "fuzzy": {
          "description": "Treatment indicator column",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "enum": [
            "triangular",
            "epanechnikov",
            "uniform"
          ],
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "fuzzy",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_bias_aware_fuzzy"
  },
  {
    "description": "Gradient Boosting for RD \u2014 flexible CATE estimation.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names for heterogeneity.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Bandwidth (auto-selected if None).",
          "type": "number"
        },
        "learning_rate": {
          "default": 0.1,
          "description": "Shrinkage factor.",
          "type": "number"
        },
        "max_depth": {
          "default": 3,
          "description": "Maximum tree depth per round.",
          "type": "integer"
        },
        "n_estimators": {
          "default": 200,
          "description": "Number of boosting rounds.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_boost"
  },
  {
    "description": "Run multiple ML-RD methods and compare CATE estimates.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariates for heterogeneity / selection.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Bandwidth (shared across methods).",
          "type": "number"
        },
        "methods": {
          "description": "Subset of ``['forest', 'boost', 'lasso']``. Default: all three.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_cate_summary"
  },
  {
    "description": "Compare multiple RD estimators on the same data.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Confidence level forwarded to each estimator that accepts ``alpha``.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "fuzzy": {
          "description": "Fuzzy treatment column passed through to all methods that accept it.",
          "type": "string"
        },
        "method_kwargs": {
          "description": "Per-method extra kwargs, e.g. ``{'rdrobust': {'kernel': 'uniform'}}``.",
          "type": "object"
        },
        "methods": {
          "default": [
            "rdrobust",
            "honest",
            "randinf"
          ],
          "description": "Method aliases recognised by the :data:`sp.rd._RD_METHOD_ALIASES` dispatcher. Default: ``('rdrobust', 'honest', 'randinf')``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_compare"
  },
  {
    "description": "Four-panel RD diagnostic dashboard.",
    "input_schema": {
      "properties": {
        "bw_grid": {
          "description": "Bandwidths to evaluate in the sensitivity panel. If None, uses ``[0.5, 0.75, 1.0, 1.25, 1.5, 2.0] \u00d7 h_mse``.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "Cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariates for the balance panel. If None, the panel shows the density's binomial near-cutoff test instead.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "figsize": {
          "default": [
            12,
            9
          ],
          "description": "figsize parameter (Tuple[float, float]).",
          "type": "number"
        },
        "fuzzy": {
          "description": "Fuzzy treatment column (passed through to RD plot/sensitivity).",
          "type": "string"
        },
        "h": {
          "description": "Reference bandwidth used for plotting and as the basis for ``bw_grid``. If None, MSE-optimal bandwidth from rdrobust.",
          "type": "number"
        },
        "save": {
          "description": "If a path is given, also save the figure (extension determines format).",
          "type": "string"
        },
        "title": {
          "description": "Suptitle.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_dashboard"
  },
  {
    "description": "Honest CI for RD when the running variable takes only a moderate number of distinct values (Koles\u00e1r-Rothe 2018 AER).  Uses bounded second derivative or bounded misspecification smoothness classes; robust to the loss of asymptotics that affects rdrobust under sparse mass points. Validation: validated. Assumptions: Continuity of potential outcomes in x at c (Hahn, Todd, van der Klaauw 2001); No manipulation of x at c (McCrary density test); Local randomization only in a neighborhood of c \u2014 extrapolation away from c is not identified. Pre-conditions: running variable x is continuous with support on both sides of c; treatment assignment is determined by the cutoff c (sharp) or probabilistically at c (fuzzy); sufficient mass of observations within the optimal bandwidth. Failure modes: McCrary density test p < 0.05 \u2192 Use donut-hole RD (donut=<\u03b4>) or partial-identification bounds.; Covariate imbalance at cutoff (sp.rdbalance rejects) \u2192 Include covariates as controls, narrow bandwidth, or report as caveat.; Effect unstable across bandwidth halvings \u2192 Report sp.rdbwsensitivity and sp.rd_honest (Armstrong-Koles\u00e1r honest CI).. Alternatives: sp.rdrobust, sp.rd_honest, sp.rdrbounds, sp.bounds.\u2026",
    "input_schema": {
      "properties": {
        "K": {
          "description": "Bound on per-side linear-approximation bias; auto if None (BM method)",
          "type": "number"
        },
        "M": {
          "description": "Bound on |g''|; auto if None (BSD method)",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "h": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "method": {
          "default": "bsd",
          "description": "Smoothness class",
          "enum": [
            "bsd",
            "bm"
          ],
          "type": "string"
        },
        "x": {
          "description": "Discrete running variable",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_discrete"
  },
  {
    "description": "Distribution-valued sharp RDD. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial; running variable x is continuous with support on both sides of c. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.; McCrary density test p < 0.05 \u2192 Use donut-hole RD (donut=<\u03b4>) or partial-identification bounds.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect, sp.rd_honest. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "quantiles": {
          "description": "Defaults to (0.1, 0.25, 0.5, 0.75, 0.9).",
          "type": "string"
        },
        "running": {
          "description": "running parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "running",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_distribution"
  },
  {
    "description": "Joint RDD + RKD on the conditional distribution of Y. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "quantiles": {
          "description": "quantiles parameter (Optional[np.ndarray]).",
          "type": "string"
        },
        "running": {
          "description": "running parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "running",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_distributional_design"
  },
  {
    "description": "Diagnostic assessment of RD external validity. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names for overlap and CI testing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "target_x_range": {
          "description": "Running variable range ``(x_low, x_high)`` defining the target population. Defaults to the full data range.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_external_validity"
  },
  {
    "description": "Angrist-Rokkanen (2015) extrapolation of RD effects away from the cutoff. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial; running variable x is continuous with support on both sides of c. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.; McCrary density test p < 0.05 \u2192 Use donut-hole RD (donut=<\u03b4>) or partial-identification bounds.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect, sp.rd_honest. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names for conditional independence. Required.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "eval_points": {
          "description": "Running variable values at which to extrapolate the CATE. If None, ``n_eval`` equally spaced points spanning the data range.",
          "type": "string"
        },
        "h_local": {
          "description": "Bandwidth for local RD estimate at the cutoff (for comparison). If None, the MSE-optimal bandwidth from ``rdrobust`` is used.",
          "type": "number"
        },
        "method": {
          "default": "ols",
          "description": "Estimation method: ``'ols'``, ``'ipw'``, or ``'doubly_robust'``.",
          "type": "string"
        },
        "n_eval": {
          "default": 20,
          "description": "Number of evaluation points when ``eval_points`` is None.",
          "type": "integer"
        },
        "treatment": {
          "description": "Treatment variable for fuzzy RD. If None, sharp design assumed (D = 1{X >= c}).",
          "type": "string"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_extrapolate"
  },
  {
    "description": "RD with flexible covariate adjustment via cross-fit ML residualisation (Noack-Olma-Rothe 2025).  Reduces variance of \u03c4\u0302 at the cutoff by subtracting an ML estimate of E[Y|W] before running rdrobust; consistent under free-of-cutoff continuity of \u03b7, asymptotically efficient when \u03b7\u0302 converges to E[Y|X=c, W]. Validation: validated. Assumptions: Continuity-based RD identification at the cutoff: potential outcomes are continuous in the running variable except for the treatment jump; Cross-fit ML residualisation of the outcome (and treatment, when fuzzy) on covariates only removes outcome variance and does not bias the cutoff estimate, requiring honest K-fold cross-fitting; Covariates predict the outcome well enough to shorten CIs relative to plain rdrobust; covariates are pre-determined (not affected by treatment). Pre-conditions: data has continuous running variable with adequate mass on both sides of the cutoff; Covariates list valid pre-treatment columns (or is empty/None to fall back to rdrobust); n_folds>=2 for genuine cross-fitting. Failure modes: Sparse data near the cutoff makes the local fit and learner unstable \u2192 Widen the bandwidth via bwselect or collect more mass around the\u2026",
    "input_schema": {
      "properties": {
        "W": {
          "description": "Covariates used by the flexible adjustment",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "fuzzy": {
          "description": "fuzzy parameter (str).",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "enum": [
            "triangular",
            "epanechnikov",
            "uniform"
          ],
          "type": "string"
        },
        "learner": {
          "default": "boost",
          "description": "Built-in learner",
          "enum": [
            "boost",
            "forest",
            "ridge",
            "lasso"
          ],
          "type": "string"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-fit folds (1 disables CV)",
          "type": "integer"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_flex"
  },
  {
    "description": "Causal Forest for RD \u2014 heterogeneous treatment effect estimation. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names used as features for heterogeneity detection. Must not include the running variable *x*.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Bandwidth (uses IK-style automatic selection if None).",
          "type": "number"
        },
        "honesty": {
          "default": true,
          "description": "Split-sample (honest) estimation: half the data for tree construction, the other half for leaf predictions.",
          "type": "boolean"
        },
        "min_leaf": {
          "default": 20,
          "description": "Minimum leaf size (larger \u2192 more regularisation).",
          "type": "integer"
        },
        "n_trees": {
          "default": 500,
          "description": "Number of trees in each forest.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_forest"
  },
  {
    "description": "Armstrong-Koles\u00e1r (2018) honest confidence intervals for sharp regression discontinuity \u2014 the only RD inference procedure with provable finite-sample coverage without bandwidth-selection bias.  M is the upper bound on the second derivative of E[Y|X] near the cutoff; smaller M means tighter CIs but riskier coverage if the true curvature is larger. Validation: validated. Assumptions: E[Y|X] has bounded second derivative |E[Y|X]''| <= M near c; Continuity of potential outcomes at c (Hahn-Todd-van der Klaauw 2001); No manipulation of x at c (run sp.mccrary_test alongside). Pre-conditions: x is continuous with support on both sides of c; Sample mass within the optimal bandwidth on each side; User-supplied M (or willingness to estimate it from data). Failure modes: M estimated from data and effective sample tiny \u2192 Pass an explicit M based on theory or sensitivity analysis (M_grid in Armstrong-Koles\u00e1r 2018 \u00a74).; Honest CI much wider than rdrobust CI \u2192 rd_honest is *honest* by construction (covers under any |f''| <= M); rdrobust trades coverage for precision. Reporting both is recommended.; McCrary density test p < 0.05 \u2192 Use donut-hole RD (donut=<\u03b4>) or partial-identification bounds.. Al\u2026",
    "input_schema": {
      "properties": {
        "M": {
          "description": "Upper bound on |E[Y|X]''| near c; if None, estimated from data",
          "type": "number"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0.0,
          "description": "Cutoff value",
          "type": "number"
        },
        "h": {
          "description": "Bandwidth; auto-selected by opt_criterion if None",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Local-linear kernel",
          "enum": [
            "triangular",
            "uniform",
            "epanechnikov"
          ],
          "type": "string"
        },
        "opt_criterion": {
          "default": "mse",
          "description": "Bandwidth optimization criterion",
          "enum": [
            "mse",
            "fwer"
          ],
          "type": "string"
        },
        "x": {
          "description": "Running variable",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_honest"
  },
  {
    "description": "Sharp RDD with network interference (Cabrelli-Marconi 2024). Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Defaults to IQR of own running variable.",
          "type": "number"
        },
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "neighbour_running": {
          "description": "Average running variable across neighbours (precomputed).",
          "type": "string"
        },
        "running": {
          "description": "Own running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [
        "neighbour_running",
        "running",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_interference"
  },
  {
    "description": "LASSO-assisted RD via post-double-selection. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Candidate covariates (can be large set).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cv_folds": {
          "default": 5,
          "description": "Cross-validation folds for LASSO penalty selection.",
          "type": "integer"
        },
        "h": {
          "description": "Bandwidth (auto-selected if None).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel for local linear regression ('triangular', 'uniform', 'epanechnikov').",
          "type": "string"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_lasso"
  },
  {
    "description": "Multi-cutoff RD extrapolation (Cattaneo, Keele, Titiunik, Vazquez-Bare 2021). Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "cutoffs": {
          "description": "Cutoff values. Must contain at least 2 cutoffs.",
          "type": "number"
        },
        "eval_points": {
          "description": "Running variable values at which to predict tau(x). Defaults to 30 equally spaced points spanning the data range.",
          "type": "string"
        },
        "method": {
          "default": "linear",
          "description": "Interpolation method: - ``'linear'``: tau(x) = a + b*x - ``'polynomial'``: polynomial of degree min(len(cutoffs)-1, 3) - ``'weighted'``: inverse-variance weighted local linear",
          "type": "string"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "cutoffs",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_multi_extrapolate"
  },
  {
    "description": "Multi-score RDD: treatment if all running variables exceed cutoffs.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Defaults to median IQR across running vars.",
          "type": "number"
        },
        "cutoffs": {
          "description": "One cutoff per running variable (same length).",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "running_vars": {
          "description": "Multiple running variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome.",
          "type": "string"
        }
      },
      "required": [
        "cutoffs",
        "running_vars",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_multi_score"
  },
  {
    "description": "Sweep over (kernel, bwselect, polynomial, donut) and return a",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bwselects": {
          "default": [
            "mserd",
            "cerrd",
            "msetwo"
          ],
          "description": "Specification grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "c": {
          "default": 0.0,
          "description": "c parameter (float).",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "covs": {
          "description": "covs parameter (Optional[List[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "donuts": {
          "default": [
            0.0
          ],
          "description": "Specification grid.",
          "type": "number"
        },
        "fuzzy": {
          "description": "fuzzy parameter (Optional[str]).",
          "type": "string"
        },
        "kernels": {
          "default": [
            "triangular",
            "epanechnikov",
            "uniform"
          ],
          "description": "Specification grid.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "polynomials": {
          "default": [
            1,
            2
          ],
          "description": "Specification grid.",
          "type": "integer"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rd_robustness_table"
  },
  {
    "description": "Covariate balance test at the RD cutoff.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names to test. If None, tests all numeric columns except x.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Manual bandwidth. If None, uses MSE-optimal per covariate.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        }
      },
      "required": [
        "x"
      ],
      "type": "object"
    },
    "name": "rdbalance"
  },
  {
    "description": "MSE-optimal bandwidth selection for the fully interacted RD model.",
    "input_schema": {
      "properties": {
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "z": {
          "description": "Covariate(s) for treatment effect heterogeneity.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x",
        "y",
        "z"
      ],
      "type": "object"
    },
    "name": "rdbwhte"
  },
  {
    "description": "Bandwidth selection for local polynomial RD estimation.",
    "input_schema": {
      "properties": {
        "all": {
          "default": false,
          "description": "If True, compute and return all eight bandwidth types.",
          "type": "boolean"
        },
        "bwselect": {
          "default": "mserd",
          "description": "Bandwidth selection method. One of: - ``'mserd'`` : MSE-optimal common bandwidth (default) - ``'msetwo'`` : MSE-optimal separate left/right bandwidths - ``'msecomb1'`` : min of mserd, mseleft, mseright - ``'msecomb2'`` : median of mserd, mseleft, mseright - ``'cerrd'`` : CER-optimal common bandwidth - ``'certwo'`` : CER-optimal separate left/right - ``'cercomb1'`` : min of cerrd, cerleft, cerright - ``'cercomb2'`` : median of cerrd, cerleft, cerright",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name for cluster-robust variance estimation.",
          "type": "string"
        },
        "covs": {
          "description": "Covariate column names. When provided, the variance estimates used in bandwidth selection account for covariate adjustment, typically yielding narrower bandwidths.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "deriv": {
          "default": 0,
          "description": "Derivative order. 0 = standard RD (jump in level), 1 = regression kink design (change in slope).",
          "type": "integer"
        },
        "fuzzy": {
          "description": "Treatment variable name for fuzzy RD. When provided, bandwidth accounts for first-stage variance in the Wald / IV estimator.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function: 'triangular', 'uniform', or 'epanechnikov'.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order for point estimation (1 = local linear).",
          "type": "integer"
        },
        "q": {
          "description": "Polynomial order for bias correction. Default is p + 1.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable column name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdbwselect"
  },
  {
    "description": "Bandwidth sensitivity analysis for RD estimates.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "bw_grid": {
          "description": "Explicit bandwidth values to evaluate. If None, auto-generates a grid as multiples of the MSE-optimal bandwidth.",
          "type": "number"
        },
        "bw_range": {
          "default": [
            0.5,
            2.0
          ],
          "description": "Range of multipliers for the optimal bandwidth.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fuzzy": {
          "description": "Treatment variable for fuzzy RD.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "n_grid": {
          "default": 15,
          "description": "Number of grid points if bw_grid is None.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order.",
          "type": "integer"
        },
        "x": {
          "description": "Outcome and running variable names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable names.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdbwsensitivity"
  },
  {
    "description": "Sharp / fuzzy RD \u2014 article-friendly alias for :func:`rdrobust`.",
    "input_schema": {
      "properties": {
        "cutoff": {
          "default": 0.0,
          "description": "cutoff parameter (float).",
          "type": "number"
        },
        "fuzzy": {
          "description": "fuzzy parameter (Optional[str]).",
          "type": "string"
        },
        "running": {
          "description": "running parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "running",
        "y"
      ],
      "type": "object"
    },
    "name": "rdd"
  },
  {
    "description": "CJM (2020) density discontinuity test for RD manipulation. Validation: certified parity evidence. Assumptions: Cattaneo-Jansson-Ma (2020) local-polynomial density test: under no manipulation the running-variable density is continuous at the cutoff; Density is estimated from the empirical CDF via local polynomial regression with a data-driven CJM bandwidth and bias-corrected inference (no binning); Sufficient mass on both sides of the cutoff within the bandwidth for the polynomial fit of order p. Pre-conditions: Running variable column + cutoff c for the RD density test; Adequate observations on each side of the cutoff within the chosen bandwidth. Failure modes: Too few points on one side of the cutoff for the local polynomial density fit \u2192 Widen the bandwidth h or lower the polynomial order p to stabilize the local density estimate.; Significant density discontinuity at the cutoff indicating manipulation/sorting \u2192 Inspect the histogram, consider a donut-hole RD, and report the result as a manipulation warning.. Alternatives: sp.mccrary_test, sp.rdrobust, sp.diagnose_result. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff.",
          "type": "number"
        },
        "h": {
          "description": "Bandwidth. Default: automatic (Cattaneo-Jansson-Ma rule).",
          "type": "number"
        },
        "p": {
          "default": 2,
          "description": "Polynomial order for density estimation.",
          "type": "integer"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        }
      },
      "required": [
        "x"
      ],
      "type": "object"
    },
    "name": "rddensity"
  },
  {
    "description": "Estimate conditional average treatment effects (CATE) in RD designs. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "b": {
          "description": "Bandwidth for bias correction. Defaults to h.",
          "type": "number"
        },
        "bwselect": {
          "default": "mserd",
          "description": "Bandwidth selection method: 'mserd' or 'msetwo'.",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name for cluster-robust standard errors.",
          "type": "string"
        },
        "eval_points": {
          "description": "Z values at which to evaluate CATE. Each row is a point in Z-space. If None, n_eval equally spaced quantiles (10th to 90th pctile) are used.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth for estimation. If None, MSE-optimal bandwidth is selected.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function: 'triangular', 'uniform', or 'epanechnikov'.",
          "type": "string"
        },
        "n_eval": {
          "default": 20,
          "description": "Number of evaluation points when eval_points is not provided.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "Polynomial order for the running variable (1 = local linear).",
          "type": "integer"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "z": {
          "description": "Covariate(s) for treatment effect heterogeneity.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x",
        "y",
        "z"
      ],
      "type": "object"
    },
    "name": "rdhte"
  },
  {
    "description": "Compute a weighted linear combination of CATE estimates.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "result": {
          "description": "Result from rdhte().",
          "type": "string"
        },
        "weights": {
          "description": "Linear combination weights. Must have length equal to the number of evaluation points in result.detail.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "weights"
      ],
      "type": "object"
    },
    "name": "rdhte_lincom"
  },
  {
    "description": "Regression Discontinuity in Time (Hausman & Rapson, 2018). Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable for clustered standard errors. If provided, cluster-robust SEs are used instead of HAC.",
          "type": "string"
        },
        "cutoff": {
          "description": "The policy change date / time cutoff.",
          "type": "integer"
        },
        "donut": {
          "default": 0,
          "description": "Donut hole: exclude observations within +/- donut units of the cutoff (in the same numeric time units).",
          "type": "integer"
        },
        "h": {
          "description": "Bandwidth in the same units as the numeric time axis (days if datetime). If None, an MSE-optimal bandwidth is selected automatically.",
          "type": "integer"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function: 'triangular', 'epanechnikov', 'uniform', or 'gaussian'.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "Local polynomial order (1 = local linear).",
          "type": "integer"
        },
        "seasonality": {
          "description": "Deseasonalise before estimation. One of 'month', 'quarter', 'dow' (day-of-week). Regresses Y on seasonal dummies and uses residuals.",
          "type": "string"
        },
        "time": {
          "description": "Time variable name (datetime or numeric).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "cutoff",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "rdit"
  },
  {
    "description": "Multi-cutoff RD design. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth for local polynomial. If None, uses Silverman rule.",
          "type": "number"
        },
        "cutoffs": {
          "description": "Cutoff values.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "pooling": {
          "default": "ivw",
          "description": "Pooling method: 'ivw' (inverse-variance weighted) or 'equal'.",
          "type": "string"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "cutoffs",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdmc"
  },
  {
    "description": "Multi-score / Geographic RD design. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "bandwidth": {
          "description": "Bandwidth used for local smoothing or kernel weighting.",
          "type": "number"
        },
        "cutoff1": {
          "default": 0,
          "description": "Cutoff for x1.",
          "type": "number"
        },
        "cutoff2": {
          "default": 0,
          "description": "Cutoff for x2.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "x1": {
          "description": "First running variable (e.g., latitude distance to boundary).",
          "type": "string"
        },
        "x2": {
          "description": "Second running variable (e.g., longitude distance to boundary).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "x1",
        "x2",
        "y"
      ],
      "type": "object"
    },
    "name": "rdms"
  },
  {
    "description": "Placebo cutoff test for RD validity.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "True cutoff.",
          "type": "number"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fuzzy": {
          "description": "Treatment variable for fuzzy RD.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "n_placebo": {
          "default": 10,
          "description": "Number of placebo cutoffs if auto-generating.",
          "type": "integer"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "placebo_cutoffs": {
          "description": "Explicit placebo cutoff values. If None, auto-generates from the data distribution.",
          "type": "number"
        },
        "side": {
          "default": "both",
          "description": "Which side of the true cutoff to place placebos: 'left', 'right', or 'both'.",
          "type": "string"
        },
        "x": {
          "description": "Outcome and running variable names.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable names.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdplacebo"
  },
  {
    "description": "RD plot: binned scatter with polynomial fit on each side of the cutoff.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "binselect": {
          "default": "esmv",
          "description": "Bin selection method (when nbins=None): - 'es' : IMSE-optimal evenly spaced - 'espr' : IMSE-optimal evenly spaced (mimicking variance) - 'qs' : IMSE-optimal quantile-spaced - 'qspr' : IMSE-optimal quantile-spaced (mimicking variance) - 'esmv' : IMSE-optimal evenly spaced with variance mimicking (default) - 'qsmv' : IMSE-optimal quantile-spaced with variance mimicking",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "ci_level": {
          "default": 0.95,
          "description": "Confidence level for pointwise CI bands.",
          "type": "number"
        },
        "covs": {
          "description": "Covariates to partial out before binning and plotting.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "donut": {
          "default": 0,
          "description": "If > 0, shades the donut region |x - c| <= donut.",
          "type": "number"
        },
        "figsize": {
          "default": [
            10,
            7
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Bandwidth to display.",
          "type": "number"
        },
        "hide_ci": {
          "default": false,
          "description": "If True, suppress CI bands entirely.",
          "type": "boolean"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel for the fitted curve.",
          "type": "string"
        },
        "nbins": {
          "description": "Bins per side. If None, uses data-driven selection via binselect.",
          "type": "integer"
        },
        "p": {
          "default": 4,
          "description": "Polynomial order for the fitted curve.",
          "type": "integer"
        },
        "scatter": {
          "default": true,
          "description": "Show binned scatter points.",
          "type": "boolean"
        },
        "shade_ci": {
          "default": true,
          "description": "Show confidence interval bands around the polynomial fit.",
          "type": "boolean"
        },
        "show_bw": {
          "default": false,
          "description": "If True, shades the bandwidth window.",
          "type": "boolean"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "weights": {
          "description": "Column name for observation weights in polynomial fitting.",
          "type": "string"
        },
        "x": {
          "description": "Outcome and running variable names.",
          "type": "string"
        },
        "x_label": {
          "description": "x_label parameter (Optional[str]).",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable names.",
          "type": "string"
        },
        "y_label": {
          "description": "y_label parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdplot"
  },
  {
    "description": "Boundary-adaptive density discontinuity plot at the RD cutoff.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "ci_level": {
          "default": 0.95,
          "description": "Confidence level for CI bands.",
          "type": "number"
        },
        "figsize": {
          "default": [
            10,
            7
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "h": {
          "description": "Bandwidth. If None, side-specific Silverman pilot.",
          "type": "number"
        },
        "hist": {
          "default": true,
          "description": "Overlay histogram.",
          "type": "boolean"
        },
        "n_grid": {
          "default": 50,
          "description": "Grid points per side for the density curve.",
          "type": "integer"
        },
        "nbins": {
          "default": 30,
          "description": "Number of histogram bins per side.",
          "type": "integer"
        },
        "p": {
          "default": 2,
          "description": "Polynomial order for the CDF regression (p=2 recommended; p=1 is faster but with worse boundary behavior).",
          "type": "integer"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "x": {
          "description": "Running variable.",
          "type": "string"
        }
      },
      "required": [
        "x"
      ],
      "type": "object"
    },
    "name": "rdplotdensity"
  },
  {
    "description": "Power of an RD design given sample size and effect size.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "h_left": {
          "default": 0.5,
          "description": "Bandwidth fractions (proportion of running-variable support used).",
          "type": "number"
        },
        "h_right": {
          "default": 0.5,
          "description": "Bandwidth fractions (proportion of running-variable support used).",
          "type": "number"
        },
        "n_left": {
          "default": 500,
          "description": "Available sample size on each side.",
          "type": "integer"
        },
        "n_right": {
          "default": 500,
          "description": "Available sample size on each side.",
          "type": "integer"
        },
        "target_power": {
          "description": "If set, compute MDE for this target power instead.",
          "type": "number"
        },
        "tau": {
          "description": "Hypothesised treatment effect at the cutoff.",
          "type": "number"
        },
        "var_left": {
          "default": 1.0,
          "description": "Outcome variance on each side of the cutoff.",
          "type": "number"
        },
        "var_right": {
          "default": 1.0,
          "description": "Outcome variance on each side of the cutoff.",
          "type": "number"
        }
      },
      "required": [
        "tau"
      ],
      "type": "object"
    },
    "name": "rdpower"
  },
  {
    "description": "Randomization inference for regression discontinuity designs. Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names to partial out before testing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "fuzzy": {
          "description": "Actual treatment variable for fuzzy RD. The Wald (IV) estimator is computed within the window.",
          "type": "string"
        },
        "kernel": {
          "default": "uniform",
          "description": "Kernel weighting (only 'uniform' currently supported for local randomization).",
          "type": "string"
        },
        "n_perms": {
          "default": 1000,
          "description": "Number of permutations for Fisher randomization test.",
          "type": "integer"
        },
        "p": {
          "default": 0,
          "description": "Polynomial order for adjustment (0 = unadjusted).",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "statistic": {
          "default": "diffmeans",
          "description": "Test statistic: 'diffmeans', 'ksmirnov', 'ranksum', or 'all'.",
          "type": "string"
        },
        "wl": {
          "description": "Window left bound offset from cutoff (typically negative). The left edge of the window is ``c + wl``.",
          "type": "number"
        },
        "wr": {
          "description": "Window right bound offset from cutoff (typically positive). The right edge of the window is ``c + wr``.",
          "type": "number"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdrandinf"
  },
  {
    "description": "Rosenbaum sensitivity bounds for RD under local randomization.",
    "input_schema": {
      "properties": {
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "gamma_list": {
          "description": "Odds ratios to evaluate. Defaults to [1, 1.5, 2, 2.5, 3, 4, 5]. gamma=1 is pure randomization; gamma>1 allows confounding.",
          "type": "number"
        },
        "n_perms": {
          "default": 1000,
          "description": "Number of permutations for p-value computation.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "statistic": {
          "default": "ranksum",
          "description": "Test statistic (ranksum is standard for Rosenbaum bounds).",
          "type": "string"
        },
        "wl": {
          "description": "Window left bound offset (typically negative).",
          "type": "number"
        },
        "wr": {
          "description": "Window right bound offset (typically positive).",
          "type": "number"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdrbounds"
  },
  {
    "description": "Sharp or fuzzy regression-discontinuity with robust bias-corrected CIs (Calonico-Cattaneo-Titiunik 2014). Use fuzzy= for IV-style fuzzy RD.",
    "input_schema": {
      "properties": {
        "c": {
          "default": 0.0,
          "description": "Cutoff value.",
          "type": "number"
        },
        "fuzzy": {
          "description": "Treatment column for fuzzy RD (optional).",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "enum": [
            "triangular",
            "uniform",
            "epanechnikov"
          ],
          "type": "string"
        },
        "x": {
          "description": "Running variable column.",
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y",
        "x"
      ],
      "type": "object"
    },
    "name": "rdrobust"
  },
  {
    "description": "Minimum sample size for a given power in an RD design.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "h_left": {
          "default": 0.5,
          "description": "h_left parameter (float).",
          "type": "number"
        },
        "h_right": {
          "default": 0.5,
          "description": "h_right parameter (float).",
          "type": "number"
        },
        "ratio": {
          "default": 1.0,
          "description": "``n_right / n_left``. Default 1.0 assumes equal allocation.",
          "type": "number"
        },
        "target_power": {
          "default": 0.8,
          "description": "target_power parameter (float).",
          "type": "number"
        },
        "tau": {
          "description": "Quantile level or target treatment-effect index.",
          "type": "number"
        },
        "var_left": {
          "default": 1.0,
          "description": "var_left parameter (float).",
          "type": "number"
        },
        "var_right": {
          "default": 1.0,
          "description": "var_right parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "tau"
      ],
      "type": "object"
    },
    "name": "rdsampsi"
  },
  {
    "description": "Sensitivity of RD estimates across different window widths.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "n_perms": {
          "default": 500,
          "description": "Number of permutations per window.",
          "type": "integer"
        },
        "nwindows": {
          "default": 20,
          "description": "Number of windows when ``wlist`` is None.",
          "type": "integer"
        },
        "p": {
          "default": 0,
          "description": "Polynomial order for adjustment.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "statistic": {
          "default": "diffmeans",
          "description": "Test statistic for inference.",
          "type": "string"
        },
        "wlist": {
          "description": "Symmetric half-window widths to evaluate. If None, an evenly-spaced grid is generated automatically.",
          "type": "number"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdsensitivity"
  },
  {
    "description": "One-stop RD diagnostic battery.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "Cutoff.",
          "type": "number"
        },
        "covs": {
          "description": "Pre-treatment covariates for balance test.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "full": {
          "default": false,
          "description": "Run extended diagnostics (honest CI, power, placebos).",
          "type": "boolean"
        },
        "fuzzy": {
          "description": "Treatment variable for fuzzy RD.",
          "type": "string"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function used for weighting or smoothing.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "p parameter (int).",
          "type": "integer"
        },
        "plot": {
          "default": false,
          "description": "Generate a multi-panel diagnostic plot.",
          "type": "boolean"
        },
        "verbose": {
          "default": true,
          "description": "Print formatted summary to console.",
          "type": "boolean"
        },
        "x": {
          "description": "Outcome and running variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome and running variable.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rdsummary"
  },
  {
    "description": "Data-driven window selection for local randomization RD.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.15,
          "description": "Significance level for balance (lenient by default to be conservative about window selection).",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "RD cutoff value.",
          "type": "number"
        },
        "covs": {
          "description": "Covariate names to test balance for. If None, uses quantiles of the running variable as pseudo-covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "nwindows": {
          "default": 10,
          "description": "Number of windows to evaluate.",
          "type": "integer"
        },
        "p": {
          "default": 0,
          "description": "Polynomial order for adjustment.",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "type": "integer"
        },
        "statistic": {
          "default": "diffmeans",
          "description": "Test statistic for balance testing.",
          "type": "string"
        },
        "wmin": {
          "description": "Minimum half-window width. Defaults to the smallest gap between adjacent observations near the cutoff.",
          "type": "number"
        },
        "wstep": {
          "description": "Window increment. Defaults to ``(max_range - wmin) / nwindows``.",
          "type": "number"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        }
      },
      "required": [
        "x"
      ],
      "type": "object"
    },
    "name": "rdwinselect"
  },
  {
    "description": "Method advisor: given a dataset + research question, recommends a ranked list of estimators with reasoning, precondition checks, and a full suggested workflow.  This is the first call an agent should make if it doesn't know which estimator to run. Supports DAG input, mediator / proxy / principal-strata variables, and optional resampling-stability verification.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cutoff": {
          "description": "RD cutoff value.",
          "type": "number"
        },
        "design": {
          "description": "Override auto-detected design.",
          "enum": [
            "rct",
            "did",
            "rd",
            "iv",
            "observational",
            "panel",
            "cross-section"
          ],
          "type": "string"
        },
        "id": {
          "description": "Unit identifier (panel).",
          "type": "string"
        },
        "instrument": {
          "description": "Instrumental variable.",
          "type": "string"
        },
        "running_var": {
          "description": "Running variable (RD).",
          "type": "string"
        },
        "time": {
          "description": "Time column (panel / DID).",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment / exposure column.",
          "type": "string"
        },
        "verify": {
          "default": false,
          "description": "If True, run resampling-stability checks on top recommendations.",
          "type": "boolean"
        },
        "y": {
          "description": "Outcome column.",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "recommend"
  },
  {
    "description": "Fit an OLS regression with robust (HC1) or clustered SEs. Input is a Wilkinson-style formula like 'y ~ x1 + x2'. Use this for baseline specifications or covariate-adjusted RCT analyses.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Column name for cluster-robust SEs.",
          "type": "string"
        },
        "formula": {
          "description": "R-style formula, e.g. 'y ~ x1 + x2'",
          "type": "string"
        },
        "robust": {
          "default": "hc1",
          "enum": [
            "hc1",
            "hc2",
            "hc3",
            "nonrobust"
          ],
          "type": "string"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "regress"
  },
  {
    "description": "Load a famous dataset and a step-by-step replication guide.",
    "input_schema": {
      "properties": {
        "key": {
          "description": "Replication key (see ``sp.list_replications()``).",
          "type": "string"
        },
        "simulated": {
          "description": "Override the entry's default data source. ``True`` forces a simulated replica; ``False`` forces the bundled real CSV (only valid for entries where ``has_real_data`` is True). Default ``None`` uses whatever the entry declares (currently real for ``card_1995`` and ``abadie_2010``).",
          "type": "boolean"
        }
      },
      "required": [
        "key"
      ],
      "type": "object"
    },
    "name": "replicate"
  },
  {
    "description": "Ramsey RESET test for functional form misspecification.",
    "input_schema": {
      "properties": {
        "powers": {
          "default": 3,
          "description": "Include \u0177\u00b2, \u0177\u00b3, ..., \u0177^powers in the auxiliary regression.",
          "type": "integer"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "reset_test"
  },
  {
    "description": "Randomization inference p-value.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster-level permutation (permute treatment at cluster level).",
          "type": "string"
        },
        "n_perms": {
          "default": 1000,
          "description": "Number of random permutations. Use 10000+ for publications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "stat": {
          "default": "diff_means",
          "description": "Test statistic: - ``'diff_means'``: difference in means (Y_bar_1 - Y_bar_0) - ``'ks'``: Kolmogorov-Smirnov statistic - ``'t'``: t-statistic - A callable ``f(Y, D) -> float`` for custom statistics.",
          "type": "string"
        },
        "treat": {
          "description": "Binary treatment indicator (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "ri_test"
  },
  {
    "description": "RIF Oaxaca-Blinder decomposition (FFL 2009, Section 5). Validation: certified parity evidence. Assumptions: The recentered influence function for the chosen distributional statistic (quantile, variance, Gini, etc.) is a valid first-order approximation, so its expectation recovers the statistic (Firpo-Fortin-Lemieux 2009); The aggregate Oaxaca-Blinder split into explained (endowments) vs unexplained (coefficients) requires no omitted covariates correlated with group and a correctly specified RIF regression; Detailed (per-covariate) decompositions assume path/normalization invariance and, for the unexplained part, an ignorable reference-group choice. Pre-conditions: group is a binary 0/1 indicator and reference in {0,1}; Covariates and the target distributional statistic are well-defined in both groups; Both groups have enough observations to fit the RIF regression at the chosen statistic. Failure modes: RIF for a tail quantile is noisy where the density is near zero, giving unstable shares \u2192 Avoid extreme quantiles or smooth/bootstrap the density estimate underlying the RIF.; Limited covariate overlap between groups makes the explained component unreliable (specification error) \u2192 T\u2026",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "group": {
          "description": "Binary (0/1) group indicator column.",
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "Which group's coefficients to use as the reference (0 or 1).",
          "type": "integer"
        },
        "statistic": {
          "default": "quantile",
          "description": "statistic parameter (StatisticKind).",
          "type": "string"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile level or target treatment-effect index.",
          "type": "number"
        }
      },
      "required": [
        "formula",
        "group"
      ],
      "type": "object"
    },
    "name": "rif_decomposition"
  },
  {
    "description": "RIF regression (Firpo, Fortin & Lemieux 2009).",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "``\"y ~ x1 + x2\"`` style.",
          "type": "string"
        },
        "statistic": {
          "default": "quantile",
          "description": "statistic parameter (StatisticKind).",
          "enum": [
            "quantile",
            "variance",
            "gini"
          ],
          "type": "string"
        },
        "tau": {
          "default": 0.5,
          "description": "Quantile level (default 0.5 = median UQPE).",
          "type": "number"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "rifreg"
  },
  {
    "description": "Regression Kink Design estimator (Card et al., 2015). Assumptions: Conditional expectations of potential outcomes are continuous at the cutoff; Units cannot precisely manipulate the running variable around the cutoff (no sorting); For fuzzy designs: monotonicity of treatment take-up at the cutoff. Pre-conditions: A continuous running/forcing variable with a known cutoff that (sharply or fuzzily) assigns treatment; Enough observations in a neighbourhood of the cutoff to fit a local polynomial. Failure modes: Density of the running variable jumps at the cutoff (manipulation / sorting) \u2192 Run a McCrary / density test (rdplotdensity); if manipulation is present the design is invalid near the cutoff.; Estimate swings with the bandwidth \u2014 results are not robust \u2192 Report a bandwidth-sensitivity curve and use a data-driven MSE-optimal bandwidth.. Alternatives: sp.rdrobust, sp.rdrandinf, sp.rdbwselect. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "c": {
          "default": 0,
          "description": "Kink point (cutoff).",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name for clustered standard errors.",
          "type": "string"
        },
        "h": {
          "description": "Bandwidth. If None, an MSE-optimal bandwidth is selected automatically.",
          "type": "number"
        },
        "kernel": {
          "default": "triangular",
          "description": "Kernel function: 'triangular', 'epanechnikov', or 'uniform'.",
          "type": "string"
        },
        "p": {
          "default": 1,
          "description": "Local polynomial order (1 = local linear, the default and most common choice for RKD).",
          "type": "integer"
        },
        "treatment": {
          "description": "Treatment variable for fuzzy RKD. If None, estimate the reduced-form kink in E[Y|X] (sharp / reduced-form RKD).",
          "type": "string"
        },
        "x": {
          "description": "Running variable name.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable name.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "rkd"
  },
  {
    "description": "Robust / unconstrained Synthetic Control. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "covariates": {
          "description": "Additional covariates to match on.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "intercept": {
          "default": true,
          "description": "Fit an intercept (level shift). Only for unconstrained / elastic_net.",
          "type": "boolean"
        },
        "l1_penalty": {
          "default": 0.0,
          "description": "Lasso (L1) penalty strength.",
          "type": "number"
        },
        "l2_penalty": {
          "default": 0.01,
          "description": "Ridge (L2) penalty strength.",
          "type": "number"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Run in-space placebo inference.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "variant": {
          "default": "unconstrained",
          "description": "* ``'unconstrained'`` \u2014 no sign / sum constraints; optional intercept. * ``'elastic_net'`` \u2014 L1 + L2 penalty, no sign constraints. * ``'penalized'`` \u2014 classic SCM constraints + elastic-net penalty.",
          "enum": [
            "unconstrained",
            "elastic_net",
            "penalized"
          ],
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "robust_synth"
  },
  {
    "description": "Run an automated battery of robustness checks.",
    "input_schema": {
      "properties": {
        "cluster_var": {
          "description": "Column for clustered SE check.",
          "type": "string"
        },
        "drop_controls": {
          "description": "Baseline controls to drop (one-by-one) for sensitivity.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "extra_controls": {
          "description": "Additional controls to add (one-by-one) beyond baseline.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "formula": {
          "description": "Baseline regression formula, e.g. ``\"y ~ x1 + x2 + x3\"``.",
          "type": "string"
        },
        "subsets": {
          "description": "Named boolean masks for subsample checks.",
          "type": "object"
        },
        "trim_pct": {
          "description": "Drop observations beyond this percentile from both tails. E.g. ``0.01`` trims top and bottom 1 %.",
          "type": "number"
        },
        "winsor_levels": {
          "description": "Winsorization percentiles, e.g. ``[0.01, 0.05]``.",
          "type": "number"
        },
        "x": {
          "description": "Key explanatory variable whose estimate stability is assessed.",
          "type": "string"
        }
      },
      "required": [
        "formula",
        "x"
      ],
      "type": "object"
    },
    "name": "robustness_report"
  },
  {
    "description": "Romano-Wolf stepdown adjusted p-values for multiple outcomes.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Nominal significance level (used only for display).",
          "type": "number"
        },
        "cluster": {
          "description": "Column name for cluster-robust standard errors and cluster bootstrap resampling.",
          "type": "string"
        },
        "controls": {
          "description": "Additional control variables included in every regression.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_boot": {
          "default": 1000,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducibility (uses ``np.random.default_rng``).",
          "type": "integer"
        },
        "x": {
          "description": "Treatment / regressor(s) of interest. The adjusted p-value corresponds to the *first* element of ``x``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable names (one regression per outcome).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "romano_wolf"
  },
  {
    "description": "Stable Balancing Weights (Zubizarreta 2015) with optional ATT/ATE Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap \u2014 extreme propensity scores or few acceptable matches \u2192 Trim or restrict to the common-support region and report the discarded units.; Covariate imbalance remains after matching/weighting \u2192 Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences.. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for inference on the outcome.",
          "type": "number"
        },
        "covariates": {
          "description": "Columns whose means must be balanced.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "delta": {
          "default": 0.02,
          "description": "Balance tolerance. With ``tolerance_scale='sd'`` the constraint is ``|mean_T(X_j) - weighted mean_C(X_j)| \u2264 \u03b4_j \u00b7 sd(X_j)``.",
          "type": "number"
        },
        "estimand": {
          "default": "att",
          "description": "``'att'`` reweights controls to match treated means (standard); ``'atc'`` reweights treated to match control means; ``'ate'`` reweights each group to match the pooled means.",
          "enum": [
            "att",
            "ate",
            "atc"
          ],
          "type": "string"
        },
        "include_squares": {
          "default": false,
          "description": "Also balance second-moments (w_j\u00b2 columns).",
          "type": "boolean"
        },
        "objective": {
          "default": "variance",
          "description": "Dispersion objective. ``'variance'`` minimises \u03a3 w_i\u00b2; ``'entropy'`` minimises \u03a3 w_i log(n \u00b7 w_i) (KL from uniform).",
          "enum": [
            "variance",
            "entropy"
          ],
          "type": "string"
        },
        "solver_options": {
          "description": "Passed to ``scipy.optimize.minimize``.",
          "type": "object"
        },
        "tolerance_scale": {
          "default": "sd",
          "description": "Whether ``delta`` is in SD units (standard) or raw units.",
          "enum": [
            "sd",
            "raw"
          ],
          "type": "string"
        },
        "treat": {
          "description": "Binary 0/1 treatment indicator column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome column. If provided, a weighted ATT/ATE estimate with HC-robust SE is attached to the returned :class:`SBWResult`.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treat"
      ],
      "type": "object"
    },
    "name": "sbw"
  },
  {
    "description": "R-style alias: ``synthdid::sc_estimate``. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat_time",
        "treat_unit",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "sc_estimate"
  },
  {
    "description": "Prepare data matrices for synthetic control estimation.",
    "input_schema": {
      "properties": {
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "time": {
          "description": "Time period column name.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column name.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "scdata"
  },
  {
    "description": "Estimate synthetic control weights. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "lasso_lambda": {
          "default": 1.0,
          "description": "L1 penalty (used when ``w_constr='lasso'``).",
          "type": "number"
        },
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "ridge_lambda": {
          "default": 1.0,
          "description": "L2 penalty (used when ``w_constr='ridge'``).",
          "type": "number"
        },
        "time": {
          "description": "Time period column name.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column name.",
          "type": "string"
        },
        "w_constr": {
          "default": "simplex",
          "description": "Weight constraint: - ``'simplex'`` : w >= 0, sum(w) = 1 - ``'lasso'`` : L1-penalised (allows negative, non-summing) - ``'ridge'`` : L2-penalised - ``'ols'`` : ordinary least squares (unconstrained) - ``'ls'`` : least squares (same as 'ols')",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "scest"
  },
  {
    "description": "Prediction intervals for synthetic control methods. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for prediction intervals.",
          "type": "number"
        },
        "cores": {
          "default": 1,
          "description": "Number of cores (reserved for future parallel subsampling).",
          "type": "integer"
        },
        "e_method": {
          "default": "gaussian",
          "description": "Method for estimating out-of-sample uncertainty: - ``'gaussian'`` : sub-Gaussian bound using residual variance - ``'ls'`` : location-scale model (allows heteroskedasticity) - ``'qreg'`` : quantile regression (nonparametric)",
          "type": "string"
        },
        "lasso_lambda": {
          "default": 1.0,
          "description": "L1 penalty (used when ``w_constr='lasso'``).",
          "type": "number"
        },
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "pi_type": {
          "default": "both",
          "description": "Which prediction interval components to include: - ``'in_sample'`` : only in-sample (weight estimation) uncertainty - ``'out_of_sample'``: only out-of-sample (prediction) uncertainty - ``'both'`` : simultaneous interval combining both sources",
          "type": "string"
        },
        "ridge_lambda": {
          "default": 1.0,
          "description": "L2 penalty (used when ``w_constr='ridge'``).",
          "type": "number"
        },
        "seed": {
          "description": "Random seed for reproducibility in subsampling.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column name.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column name.",
          "type": "string"
        },
        "w_constr": {
          "default": "simplex",
          "description": "Weight constraint for SCM estimation: - ``'simplex'`` : w >= 0, sum(w) = 1 - ``'lasso'`` : L1-penalised - ``'ridge'`` : L2-penalised - ``'ols'`` : ordinary least squares (unconstrained) - ``'ls'`` : least squares (same as 'ols')",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "scpi"
  },
  {
    "description": "Synthetic Difference-in-Differences estimator (and SC / DID variants). Assumptions: Parallel trends in the absence of treatment, after the synthetic/DiD weighting; No anticipation and no interference between units (SUTVA); The control pool's outcome process is stable around the intervention. Pre-conditions: Panel with treated and control units and a clear treatment date; Pre-treatment periods available to assess comparability of trends. Failure modes: Weighted pre-treatment trends still diverge between treated and synthetic control \u2192 Inspect the unit/time weights and pre-trend fit; consider event-study DiD with honest bounds.. Alternatives: sp.synth, sp.augsynth, sp.callaway_santanna, sp.gardner_did. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "covariates": {
          "description": "Reserved for future covariate-adjusted extensions.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "sdid",
          "description": "* ``'sdid'`` \u2014 Synthetic DID (unit + time weights) * ``'sc'`` \u2014 Synthetic Control (unit weights only) * ``'did'`` \u2014 DID (uniform weights)",
          "enum": [
            "sdid",
            "sc",
            "did"
          ],
          "type": "string"
        },
        "n_reps": {
          "default": 200,
          "description": "Replications for placebo / bootstrap SE.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable column. Alias ``y=`` accepted for R-style calls.",
          "type": "string"
        },
        "se_method": {
          "default": "placebo",
          "description": "Standard-error method (see Notes).",
          "enum": [
            "placebo",
            "bootstrap",
            "jackknife"
          ],
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Treated unit(s). Alias ``treat_unit=`` accepted.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive). Alias ``treat_time=`` accepted.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "sdid"
  },
  {
    "description": "Score and rank candidate proxies for PCI. Assumptions: The proxies are valid negative controls (relevant to the confounder, excluded from the causal channel); A bridge function exists (completeness conditions hold). Pre-conditions: Treatment-inducing and outcome-inducing proxy variables (negative controls) for the unobserved confounder. Failure modes: Proxies are weak or invalid \u2014 the bridge function is poorly identified \u2192 Test proxy relevance, select stronger proxies, or fall back to sensitivity analysis.. Alternatives: sp.select_pci_proxies, sp.dml. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "candidates": {
          "description": "All variables that could plausibly serve as proxies.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "top_k": {
          "default": 2,
          "description": "Number of top candidates to recommend per side.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "candidates",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "select_pci_proxies"
  },
  {
    "description": "Lee (2009) bounds for ATE under sample selection, optionally Assumptions: Only weak (set-identifying) assumptions are imposed; the result is an interval, not a point; Lee bounds add monotonicity of selection; Oster's delta adds proportional selection on observed vs. unobserved. Pre-conditions: The data needed for the point-identifying analysis, plus the weakest credible identifying restriction; For Lee bounds: a binary selection/attrition indicator. Failure modes: Bounds are too wide to be informative \u2192 Add a credible auxiliary restriction (monotone treatment response, instrument) to tighten the bounds.. Alternatives: sp.oster_delta, sp.lee_bounds, sp.manski_bounds. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "covariates": {
          "description": "Covariates to condition on for tighter (conditional) bounds.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "conditional",
          "description": "- ``'conditional'``: compute Lee bounds within covariate strata and average (tighter). - ``'unconditional'``: standard Lee bounds ignoring covariates.",
          "type": "string"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "selection": {
          "description": "Binary indicator: 1 = outcome observed, 0 = missing.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment (0/1).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable (may have NaN when selection=0).",
          "type": "string"
        }
      },
      "required": [
        "selection",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "selection_bounds"
  },
  {
    "description": "Sensitivity analysis for omitted variable bias (Cinelli & Hazlett 2020). Validation: certified parity evidence. Assumptions: Cinelli-Hazlett (2020) omitted-variable sensitivity in the partial-R-squared framework; the Robustness Value is the minimum partial R-squared a confounder needs with both treatment and outcome to nullify the estimate; Outcome is modeled by OLS of y on treat plus observed controls; benchmarking calibrates confounder strength relative to named observed controls; rv_q is the RV to change the point-estimate sign; rv_qa is the RV to lose significance at alpha. Pre-conditions: A DataFrame with outcome, treatment, and observed control columns; At least one control to anchor the benchmark comparison. Failure modes: Benchmark names a control not present in the controls list or data \u2192 Pass benchmark names that are a subset of the controls actually included in the regression.; Perfectly collinear controls make the partial-R-squared decomposition unstable \u2192 Drop redundant collinear controls before computing the robustness value.. Alternatives: sp.oster_bounds, sp.evalue, sp.unified_sensitivity. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "benchmark_covariates": {
          "description": "Covariates for benchmarking",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "result parameter (EconometricResults).",
          "type": "string"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        }
      },
      "required": [
        "result",
        "treatment"
      ],
      "type": "object"
    },
    "name": "sensemakr"
  },
  {
    "description": "Unified sensitivity analysis for observational causal estimates \u2014 supports Oster (2019) delta/R-max, Cinelli-Hazlett (2020) omitted-variable bias bounds, and E-values (VanderWeele-Ding 2017).  Tells the agent how strong an unobserved confounder would have to be to overturn the result.",
    "input_schema": {
      "properties": {
        "benchmark_covariate": {
          "description": "Covariate used as the benchmark for unobserved-confounder strength.",
          "type": "string"
        },
        "method": {
          "default": "auto",
          "enum": [
            "oster",
            "cinelli_hazlett",
            "evalue",
            "auto"
          ],
          "type": "string"
        },
        "result": {
          "description": "Fitted regression / causal result handle (set by the caller).",
          "type": "string"
        },
        "treatment": {
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "sensitivity"
  },
  {
    "description": "Comprehensive multi-dimensional sensitivity analysis.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "dimensions": {
          "description": "Which dimensions to test. Default: all applicable.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Baseline estimated result.",
          "type": "string"
        },
        "verbose": {
          "default": true,
          "description": "verbose parameter (bool).",
          "type": "boolean"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "sensitivity_dashboard"
  },
  {
    "description": "Run sp.sensitivity / sp.evalue / sp.oster_bounds / sp.sensemakr on a cached result. Pass method='evalue' (default) for the omitted-confounder-strength bound, 'oster' for delta/R-max, 'cinelli_hazlett' for OVB bounds.",
    "input_schema": {
      "properties": {
        "benchmark_covariate": {
          "description": "Cinelli-Hazlett benchmark column (optional).",
          "type": "string"
        },
        "method": {
          "default": "evalue",
          "enum": [
            "evalue",
            "oster",
            "cinelli_hazlett",
            "auto"
          ],
          "type": "string"
        },
        "result_id": {
          "description": "Handle to a fitted causal result.",
          "type": "string"
        }
      },
      "required": [
        "result_id"
      ],
      "type": "object"
    },
    "name": "sensitivity_from_result"
  },
  {
    "description": "Plot Rambachan & Roth (2023) sensitivity analysis.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "breakdown_color": {
          "default": "#E74C3C",
          "description": "Color for the breakdown point marker.",
          "type": "string"
        },
        "color": {
          "default": "#2C3E50",
          "description": "CI band color.",
          "type": "string"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "original_ci": {
          "description": "Original CI (at M=0) for comparison.",
          "type": "number"
        },
        "original_color": {
          "default": "#27AE60",
          "description": "Color for original estimate marker.",
          "type": "string"
        },
        "original_estimate": {
          "description": "Original point estimate.",
          "type": "number"
        },
        "sensitivity": {
          "description": "Output from ``honest_did()``.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        }
      },
      "required": [
        "sensitivity"
      ],
      "type": "object"
    },
    "name": "sensitivity_plot"
  },
  {
    "description": "Rambachan-Roth (2023) honest-DiD sensitivity analysis: computes the largest violation of parallel trends (parametrised by Mbar \u2014 relative magnitude of the post-period violation versus the worst observed pre-period one) under which the post-treatment ATT is still different from zero at level alpha.  Reports both the robust confidence sets and the breakdown Mbar. Assumptions: Pre-period violations bound the magnitude of post-period violations (relative-magnitude family); Post-treatment effects are constant across event time (relax via alternative parameter families in Rambachan-Roth 2023 \u00a73). Pre-conditions: result has at least one pre-period and one post-period coefficient; result carries the variance-covariance matrix of those coefficients. Failure modes: Breakdown Mbar < 1.0 (small parallel-trends violation overturns the sign) \u2192 The result is fragile to plausible pretrends violations; report the breakdown alongside the point estimate.; Confidence set is the entire real line (Mbar grid too coarse) \u2192 Re-run with a finer grid (n_grid=50+) or restrict Mbar to a tighter interval.. Alternatives: sp.honest_did, sp.pretrends_test, sp.breakdown_m. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "Mbar": {
          "description": "Grid of relative-magnitude bounds; default is np.linspace(0, 2, n_grid)",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "method": {
          "default": "C-LF",
          "description": "Identification method",
          "enum": [
            "C-LF"
          ],
          "type": "string"
        },
        "n_grid": {
          "default": 20,
          "description": "Mbar grid size when Mbar=None",
          "type": "integer"
        },
        "result": {
          "description": "Event-study or DiD result with full pre/post coefficients",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "sensitivity_rr"
  },
  {
    "description": "Sequential Synthetic DID for staggered-adoption panels (Arkhangelsky & Samkov 2024): processes cohorts in adoption order using not-yet-treated donors, avoiding TWFE negative weights and SDID overlap failures. Validation: validated. Assumptions: Parallel trends in the absence of treatment, after the synthetic/DiD weighting; No anticipation and no interference between units (SUTVA); The control pool's outcome process is stable around the intervention. Pre-conditions: Panel with treated and control units and a clear treatment date; Pre-treatment periods available to assess comparability of trends. Failure modes: Weighted pre-treatment trends still diverge between treated and synthetic control \u2192 Inspect the unit/time weights and pre-trend fit; consider event-study DiD with honest bounds.. Alternatives: sp.synth, sp.augsynth, sp.callaway_santanna, sp.gardner_did. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "cohort": {
          "description": "First-treated period column; never-treated = 0",
          "type": "string"
        },
        "cohort_weights": {
          "default": "size",
          "description": "cohort_weights parameter (str).",
          "enum": [
            "size",
            "equal"
          ],
          "type": "string"
        },
        "n_reps": {
          "default": 200,
          "description": "Number of reps.",
          "type": "integer"
        },
        "never_treated_value": {
          "default": 0,
          "description": "never_treated_value parameter.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "se_method": {
          "default": "placebo",
          "description": "se_method parameter (str).",
          "enum": [
            "placebo",
            "bootstrap",
            "jackknife"
          ],
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "cohort",
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "sequential_sdid"
  },
  {
    "description": "Set every reachable RNG to a known seed for the duration of the",
    "input_schema": {
      "properties": {
        "jax": {
          "default": true,
          "description": "Yield a fresh JAX ``PRNGKey(seed)`` to the caller via the ``jax_key`` attribute on the yielded session object, when JAX is already imported. Never imports jax on its own. (JAX has no global state so we can't seed it \u2014 agents must thread the key explicitly.)",
          "type": "boolean"
        },
        "pythonhashseed": {
          "default": false,
          "description": "Set ``PYTHONHASHSEED`` for the duration of the block. Most causal-inference numerics don't depend on dict iteration order, but spec-curve enumerators and graph-based DAG search sometimes do. Off by default to avoid surprising downstream callers.",
          "type": "boolean"
        },
        "seed": {
          "description": "Seed value. ``None`` (the default) means \"snapshot current state but don't reseed\" \u2014 useful for opportunistic save / restore around code that you don't want to leak RNG drift.",
          "type": "integer"
        },
        "torch": {
          "default": true,
          "description": "Seed PyTorch (CPU + CUDA) when the library is already imported. Never imports torch on its own.",
          "type": "boolean"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "session"
  },
  {
    "description": "Shorrocks-Shapley decomposition of an inequality index across",
    "input_schema": {
      "properties": {
        "index": {
          "default": "theil_t",
          "description": "index parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "shapley_inequality"
  },
  {
    "description": "Lerman-Yitzhaki (1985) Gini source decomposition.",
    "input_schema": {
      "properties": {
        "sources": {
          "description": "sources parameter (Sequence[str]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        }
      },
      "required": [
        "sources"
      ],
      "type": "object"
    },
    "name": "source_decompose"
  },
  {
    "description": "Specification-curve analysis (Simonsohn et al. 2020): enumerates every combination of model choices the user declares defensible, runs them all, and returns the sign/magnitude distribution.  Use when an agent needs to report robustness across a researcher-degree-of-freedom multiverse.",
    "input_schema": {
      "properties": {
        "covariates": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "model_family": {
          "default": "ols",
          "enum": [
            "ols",
            "did",
            "iv",
            "panel"
          ],
          "type": "string"
        },
        "subsample_vars": {
          "description": "Variables defining subsample splits to include in the curve.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "type": "string"
        },
        "y": {
          "type": "string"
        }
      },
      "required": [
        "y",
        "treatment"
      ],
      "type": "object"
    },
    "name": "spec_curve"
  },
  {
    "description": "Direct + spillover treatment effect estimation under partial interference (within-cluster). Uses the Hudgens-Halloran decomposition with chosen exposure function. Validation: validated. Assumptions: Partial interference: spillover only within cluster, not across; Correct exposure function (fraction / any / count \u2014 sensitivity tested); Overlap: every (treatment \u00d7 exposure) cell has positive probability. Pre-conditions: data has a cluster column defining the interference boundary; treatment varies within clusters; \u2265 30 clusters for cluster-robust inference. Failure modes: No within-cluster variation in treatment \u2192 Assignments are cluster-level \u2014 use sp.cluster_matched_pair or cluster-level ATE.; Exposure function misspecified \u2192 Compare estimates under exposure_fn in {fraction, any, count}.. Alternatives: sp.network_exposure, sp.cluster_matched_pair, sp.peer_effects. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster column (interference boundary)",
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "exposure_fn": {
          "default": "fraction",
          "description": "Exposure function",
          "enum": [
            "fraction",
            "any",
            "count"
          ],
          "type": "string"
        },
        "n_bootstrap": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "spillover"
  },
  {
    "description": "Simultaneous quantile regression at multiple quantiles.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "quantiles": {
          "description": "quantiles parameter (Optional[List[float]]).",
          "type": "number"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "sqreg"
  },
  {
    "description": "Compute stabilized IPTW weights for time-varying treatments. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) \u2192 Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE).. Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "baseline": {
          "description": "Baseline covariates.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit and time identifiers.",
          "type": "string"
        },
        "time": {
          "description": "Unit and time identifiers.",
          "type": "string"
        },
        "time_varying": {
          "description": "Time-varying confounders (already lagged to pre-treatment).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treat": {
          "description": "Treatment column (binary 0/1 or continuous).",
          "type": "string"
        },
        "treat_type": {
          "default": "auto",
          "description": "Overrides auto-detection.",
          "enum": [
            "auto",
            "binary",
            "continuous"
          ],
          "type": "string"
        },
        "trim_per_period": {
          "default": 0.0,
          "description": "If > 0, trim each period's density ratio at the symmetric quantile ``[trim_per_period, 1 - trim_per_period]`` *before* taking the cumulative product. A common value is 0.01. Set to 0 to disable per-period trimming (the default).",
          "type": "number"
        }
      },
      "required": [
        "id",
        "time",
        "time_varying",
        "treat"
      ],
      "type": "object"
    },
    "name": "stabilized_weights"
  },
  {
    "description": "Stacked DiD (Cengiz, Dube, Lindner, Zipperer 2019). For each treatment cohort, constructs a sub-experiment with only that cohort + clean (never-treated or not-yet-treated) controls, then TWFE on the stacked panel. Robust to staggered-adoption contamination at the cost of dropping late-treated units in early sub-experiments. Validation: validated. Assumptions: Parallel trends within each sub-experiment; No anticipation within window; SUTVA. Pre-conditions: staggered adoption with \u2265 2 cohorts; window horizon available per cohort (else dropped). Failure modes: No clean controls for the latest cohort \u2192 Late cohort's sub-experiment is dropped; check coverage in model_info. Consider sp.callaway_santanna.. Alternatives: sp.callaway_santanna, sp.sun_abraham, sp.did_imputation. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "first_treat parameter (str).",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "never_treated_only": {
          "default": false,
          "description": "Use only never-treated as controls (drops late-treated)",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "window": {
          "default": [
            -5,
            5
          ],
          "description": "Event-time (lead, lag) window per sub-experiment",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "stacked_did"
  },
  {
    "description": "Staggered Adoption Synthetic Control. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.. Alternatives: sp.sdid, sp.augsynth, sp.gsynth, sp.callaway_santanna. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "method": {
          "default": "separate",
          "description": "* ``'separate'`` \u2014 fit a separate SCM for each treated unit. * ``'pooled'`` \u2014 partially pool weights across cohorts with the same adoption time.",
          "enum": [
            "separate",
            "pooled"
          ],
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty on donor weights.",
          "type": "number"
        },
        "placebo": {
          "default": true,
          "description": "Run placebo inference.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment indicator (0/1). Units transition from 0 to 1 at their respective adoption times.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treatment",
        "unit"
      ],
      "type": "object"
    },
    "name": "staggered_synth"
  },
  {
    "description": "Stepwise variable selection for OLS regression.",
    "input_schema": {
      "properties": {
        "alpha_in": {
          "default": 0.05,
          "description": "p-value threshold for variable entry (when ``criterion=\"pvalue\"``).",
          "type": "number"
        },
        "alpha_out": {
          "default": 0.1,
          "description": "p-value threshold for variable removal (when ``criterion=\"pvalue\"``).",
          "type": "number"
        },
        "criterion": {
          "default": "bic",
          "description": "Optimisation criterion. Default ``\"bic\"``.",
          "enum": [
            "aic",
            "bic",
            "adjr2",
            "pvalue"
          ],
          "type": "string"
        },
        "method": {
          "default": "both",
          "description": "Selection strategy. Default ``\"both\"`` (bidirectional).",
          "enum": [
            "forward",
            "backward",
            "both"
          ],
          "type": "string"
        },
        "verbose": {
          "default": true,
          "description": "Print step-by-step progress.",
          "type": "boolean"
        },
        "x": {
          "description": "Candidate independent variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Name of the dependent variable column.",
          "type": "string"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "stepwise"
  },
  {
    "description": "Test for stochastic dominance of the treated distribution over the",
    "input_schema": {
      "properties": {
        "order": {
          "default": 1,
          "description": "Order of stochastic dominance. 1 = first-order (CDF dominance). 2 = second-order (integrated CDF dominance).",
          "enum": [
            "1",
            "2"
          ],
          "type": "integer"
        },
        "result": {
          "description": "Output from ``discos()`` or ``qqsynth()``.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "stochastic_dominance"
  },
  {
    "description": "Structural break detection.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "max_breaks": {
          "default": 5,
          "description": "Maximum number of breaks to test.",
          "type": "integer"
        },
        "method": {
          "default": "bai-perron",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "min_segment": {
          "default": 0.15,
          "description": "Minimum segment length as fraction of sample.",
          "type": "number"
        },
        "x": {
          "description": "Regressors. If None, uses constant only (mean shift).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "structural_break"
  },
  {
    "description": "Subcluster wild cluster bootstrap for few-treated-clusters.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Primary cluster column (for SE computation).",
          "type": "string"
        },
        "h0": {
          "default": 0.0,
          "description": "Null value.",
          "type": "number"
        },
        "n_boot": {
          "default": 999,
          "description": "Bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "subcluster": {
          "description": "Finer grouping at which sign-flips occur. If ``None``, every observation is its own subcluster (pure Rademacher at obs level).",
          "type": "string"
        },
        "test_var": {
          "description": "Parameter to test; default last element of ``x``.",
          "type": "string"
        },
        "weight_type": {
          "default": "webb",
          "description": "Distribution of sign flips. ``'webb'`` (6-point) recommended when treatment has <= 5 treated clusters.",
          "enum": [
            "rademacher",
            "webb",
            "mammen"
          ],
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "subcluster_wild_bootstrap"
  },
  {
    "description": "Run subgroup heterogeneity analysis with forest plot.",
    "input_schema": {
      "properties": {
        "by": {
          "description": "Mapping of *display name* \u2192 *column name* for grouping.",
          "type": "object"
        },
        "formula": {
          "description": "Regression formula, e.g. ``\"wage ~ education + experience\"``.",
          "type": "string"
        },
        "robust": {
          "default": "hc1",
          "description": "Standard error type for subgroup regressions.",
          "type": "string"
        },
        "x": {
          "description": "Key explanatory variable.",
          "type": "string"
        }
      },
      "required": [
        "by",
        "formula",
        "x"
      ],
      "type": "object"
    },
    "name": "subgroup_analysis"
  },
  {
    "description": "Subgroup decomposition (between / within) of an inequality index.",
    "input_schema": {
      "properties": {
        "alpha": {
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "by": {
          "description": "by parameter (str).",
          "type": "string"
        },
        "eps": {
          "default": 1.0,
          "description": "eps parameter (float).",
          "type": "number"
        },
        "index": {
          "default": "theil_t",
          "description": "index parameter (str).",
          "type": "string"
        },
        "weights": {
          "description": "Observation weights.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "by",
        "y"
      ],
      "type": "object"
    },
    "name": "subgroup_decompose"
  },
  {
    "description": "Sun-Abraham (2021) interaction-weighted event-study. Fixes the contamination in dynamic event-study TWFE coefficients from other relative-time bins by using cohort-specific interaction weights. Canonical companion to Callaway-Sant'Anna for event studies. Validation: certified parity evidence. Assumptions: Parallel trends across cohorts; No anticipation within event_window lead horizon; SUTVA. Pre-conditions: panel with unit \u00d7 time \u00d7 outcome; g is the first-treatment period (int), 0 / NaN for never-treated; \u2265 2 pre-periods per cohort for event-study leads. Failure modes: No never-treated cohort when control_group='nevertreated' \u2192 Pass control_group='notyettreated' or add never-treated units.; Pre-trend joint test p < 0.05 (or underpowered at 0.10) \u2192 Use sp.sensitivity_rr (Rambachan & Roth honest CI) or switch to sp.callaway_santanna.; Staggered treatment timing with TWFE method \u2192 TWFE can give negative weights; use Callaway-Sant'Anna, Sun-Abraham, or BJS imputation.. Alternatives: sp.callaway_santanna, sp.did_imputation, sp.gardner_did, sp.wooldridge_did. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable (defaults to i)",
          "type": "string"
        },
        "control_group": {
          "default": "nevertreated",
          "description": "Control arm",
          "enum": [
            "nevertreated",
            "notyettreated"
          ],
          "type": "string"
        },
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "event_window": {
          "description": "(lead, lag) window for event-study coefficients",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "g": {
          "description": "First-treatment period (0 = never-treated)",
          "type": "string"
        },
        "i": {
          "description": "Unit identifier",
          "type": "string"
        },
        "t": {
          "description": "Time period column",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "g",
        "i",
        "t",
        "y"
      ],
      "type": "object"
    },
    "name": "sun_abraham"
  },
  {
    "description": "Fit a Super Learner ensemble. Assumptions: Unconfoundedness (sequential exchangeability for longitudinal/LTMLE); Positivity / overlap of treatment given history; At least one nuisance (outcome or treatment) is estimated consistently; the targeting step gives double robustness. Pre-conditions: Covariates, treatment, and outcome (for survival/longitudinal variants: time-to-event and time-varying covariates); Enough data to fit a Super Learner / HAL nuisance library. Failure modes: Near-positivity violations create extreme clever-covariate weights \u2192 Truncate weights, restrict the estimand, or report a positivity diagnostic.. Alternatives: sp.dml, sp.ipw, sp.g_computation. Typical minimum N: 400.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "type": "string"
        },
        "library": {
          "description": "Candidate learners. If None, uses a default library.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "n_folds": {
          "default": 5,
          "description": "Cross-validation folds.",
          "type": "integer"
        },
        "random_state": {
          "default": 42,
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "task": {
          "default": "regression",
          "description": "'regression' or 'classification'.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "y"
      ],
      "type": "object"
    },
    "name": "super_learner"
  },
  {
    "description": "Seemingly Unrelated Regression (SUR).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "equations": {
          "description": "Mapping from equation name to (dep_var, list_of_regressors).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "fgls",
          "description": "'ols' (equation-by-equation), 'fgls' (feasible GLS / SUR), 'iterative' (iterated SUR).",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        }
      },
      "required": [
        "equations"
      ],
      "type": "object"
    },
    "name": "sureg"
  },
  {
    "description": "Nonparametric sensitivity for survival / hazard-ratio outcomes.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "baseline_survival_t": {
          "default": 0.5,
          "description": "Baseline S_0(t) used to report \u0394 survival at time t.",
          "type": "number"
        },
        "gamma_grid": {
          "description": "Gamma (\u2265 1) values. Defaults to ``np.linspace(1.0, 3.0, 21)``.",
          "type": "number"
        },
        "log_hr": {
          "description": "log_hr parameter (float).",
          "type": "number"
        },
        "se_log_hr": {
          "description": "se_log_hr parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "log_hr",
        "se_log_hr"
      ],
      "type": "object"
    },
    "name": "survival_sensitivity"
  },
  {
    "description": "Zhang-Rubin (2003) sharp bounds on the Survivor Average Causal Effect. Assumptions: Sequential exchangeability / no unmeasured confounding at each time point; Positivity: every treatment level is possible given the past; Correct specification of the treatment and/or outcome models. Pre-conditions: Sequentially measured covariates, (time-varying) treatment, and outcome; Models for the treatment process and the outcome (or weights). Failure modes: Stabilized weights have extreme values (positivity near-violation) \u2192 Truncate weights, simplify the treatment model, or use a doubly-robust estimator (TMLE).. Alternatives: sp.tmle, sp.g_computation, sp.ipw. Typical minimum N: 300.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "n_boot": {
          "default": 500,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "survival": {
          "description": "survival parameter (str).",
          "type": "string"
        },
        "treat": {
          "description": "Treatment indicator or first-treatment-period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "survival",
        "treat",
        "y"
      ],
      "type": "object"
    },
    "name": "survivor_average_causal_effect"
  },
  {
    "description": "Parametric survival model (AFT parameterization).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "dist": {
          "default": "weibull",
          "description": "dist parameter (str).",
          "type": "string"
        },
        "duration": {
          "description": "Follow-up time column (or formula LHS).",
          "type": "string"
        },
        "event": {
          "description": "Event indicator column.",
          "type": "string"
        },
        "formula": {
          "description": "Formula ``'duration ~ x1 + x2'``.",
          "type": "string"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "x": {
          "description": "Covariate columns (or formula RHS).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "survreg"
  },
  {
    "description": "Unified synthetic control estimator. method= selects variant: 'classic', 'demeaned', 'detrended', 'unconstrained', 'elastic_net', 'augmented', 'sdid', 'gsynth', 'staggered'. inference= selects: 'placebo', 'conformal', 'bootstrap', 'jackknife'. Validation: certified parity evidence. Assumptions: Treatment effect on the treated is identified by the counterfactual implicit in the donor weights; No spillover from treated unit to donors (SUTVA); Donor pool contains units whose outcomes plausibly track the treated counterfactual. Pre-conditions: panel data in long form (unit \u00d7 time \u00d7 outcome); single treated unit (classic) or a treatment-timing column (staggered); \u2265 10 donor (untreated) units with similar pre-treatment trajectories. Failure modes: Pre-treatment RMSPE > post-treatment effect \u2192 Poor pre-fit \u2014 switch to method='demeaned'/'augmented' or enlarge donor pool.; Placebo p-value \u2265 0.1 despite visible gap \u2192 Use inference='conformal' (valid under weak assumptions) or report ranked placebo statistic.; All weight concentrated on one donor \u2192 Interpolation bias risk \u2014 check method='elastic_net' or augmented SCM.. Alternatives: sp.sdid, sp.did, sp.matrix_completion, sp.causal_impact. Ty\u2026",
    "input_schema": {
      "properties": {
        "inference": {
          "description": "Inference method: placebo/conformal/bootstrap/jackknife",
          "type": "string"
        },
        "method": {
          "default": "classic",
          "description": "SCM variant: classic/demeaned/detrended/unconstrained/elastic_net/augmented/sdid/gsynth/staggered",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Treated unit (not needed for staggered)",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column (staggered only)",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period",
          "type": "integer"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth"
  },
  {
    "description": "Run multiple SCM variants and compare them side by side.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals. Additional keyword arguments forwarded to ``synth()``.",
          "type": "number"
        },
        "methods": {
          "description": "SCM variants to compare. If ``None`` (default), all 20 registered methods are attempted, in ascending complexity order: ``classic, penalized, demeaned, detrended, unconstrained, elastic_net, augmented, sdid, gsynth, mc, discos, scpi, penscm, fdid, sparse, cluster, kernel, kernel_ridge, bayesian, bsts``. Pass an explicit subset to reduce runtime.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "placebo": {
          "default": true,
          "description": "Whether to run placebo inference for each method.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_compare"
  },
  {
    "description": "Donor-pool bootstrap sensitivity for Synthetic Control.",
    "input_schema": {
      "properties": {
        "k": {
          "description": "Donor subset size. Default is ``floor(J * 0.75)`` where *J* is the total number of donors.",
          "type": "integer"
        },
        "n_samples": {
          "default": 100,
          "description": "Number of random donor subsets to draw.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty forwarded to SCM.",
          "type": "number"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_donor_sensitivity"
  },
  {
    "description": "Leave-one-out donor sensitivity for Synthetic Control.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for z-based p-values.",
          "type": "number"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty forwarded to SCM.",
          "type": "number"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_loo"
  },
  {
    "description": "Minimum Detectable Effect for a Synthetic Control design.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the placebo test.",
          "type": "number"
        },
        "n_simulations": {
          "default": 200,
          "description": "Number of simulations per effect size.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "power_target": {
          "default": 0.8,
          "description": "Desired power level.",
          "type": "number"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_mde"
  },
  {
    "description": "Power analysis for Synthetic Control designs.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the placebo test.",
          "type": "number"
        },
        "effect_sizes": {
          "description": "Grid of hypothetical additive effect sizes to evaluate. If ``None``, auto-generates 10 steps from 0 to 3 * pre-treatment SD of the outcome.",
          "type": "number"
        },
        "n_simulations": {
          "default": 200,
          "description": "Number of Monte-Carlo simulations per effect size.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_power"
  },
  {
    "description": "Plot the power curve from :func:`synth_power`.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Axes to plot on. If ``None``, a new figure is created.",
          "type": "string"
        },
        "figsize": {
          "default": [
            9,
            6
          ],
          "description": "Figure size (width, height) in inches.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "power_result": {
          "description": "Output of :func:`synth_power`. Must contain columns ``effect_size``, ``power``, and ``mde_flag``.",
          "type": "string"
        },
        "title": {
          "description": "Custom plot title. Defaults to ``\"SCM Power Curve \u2014 Minimum Detectable Effect\"``.",
          "type": "string"
        }
      },
      "required": [
        "power_result"
      ],
      "type": "object"
    },
    "name": "synth_power_plot"
  },
  {
    "description": "Quickly recommend the best SCM method for the given data.",
    "input_schema": {
      "properties": {
        "outcome": {
          "description": "Outcome variable column name.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive). Additional keyword arguments forwarded to ``synth_compare()``.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_recommend"
  },
  {
    "description": "Generate a comprehensive Synthetic Control analysis report.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for CIs and hypothesis tests. Additional keyword arguments forwarded to ``synth()``.",
          "type": "number"
        },
        "method": {
          "default": "classic",
          "description": "SCM variant passed to ``synth()``.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "output": {
          "default": "text",
          "description": "Output format: ``'text'``, ``'markdown'``, or ``'latex'``.",
          "type": "string"
        },
        "sensitivity": {
          "default": true,
          "description": "Whether to include the sensitivity analysis section.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_report"
  },
  {
    "description": "Generate an SCM report and write it directly to a file.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "filename": {
          "default": "report.md",
          "description": "Output file path. Additional keyword arguments forwarded to ``synth()``.",
          "type": "string"
        },
        "method": {
          "default": "classic",
          "description": "SCM variant passed to ``synth()``.",
          "type": "string"
        },
        "outcome": {
          "description": "Outcome variable name.",
          "type": "string"
        },
        "output": {
          "default": "markdown",
          "description": "Output format: ``'text'``, ``'markdown'``, or ``'latex'``.",
          "type": "string"
        },
        "sensitivity": {
          "default": true,
          "description": "Whether to include the sensitivity analysis section.",
          "type": "boolean"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period (inclusive).",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_report_to_file"
  },
  {
    "description": "Pre-RMSPE-filtered p-value robustness (Abadie et al. 2010).",
    "input_schema": {
      "properties": {
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty.",
          "type": "number"
        },
        "thresholds": {
          "description": "Multiples of treated-unit pre-RMSPE used as cut-offs. Default ``[1, 2, 5, 10, 20, np.inf]``.",
          "type": "number"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_rmspe_filter"
  },
  {
    "description": "Run all SCM sensitivity diagnostics in a single call.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "n_donor_samples": {
          "default": 100,
          "description": "Number of random donor subsets for donor sensitivity.",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty.",
          "type": "number"
        },
        "seed": {
          "description": "Random seed.",
          "type": "integer"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "First treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_sensitivity"
  },
  {
    "description": "Multi-panel sensitivity diagnostic plot.",
    "input_schema": {
      "properties": {
        "figsize": {
          "default": [
            14,
            10
          ],
          "description": "Figure size in inches.",
          "type": "number"
        },
        "sensitivity_result": {
          "description": "Output from :func:`synth_sensitivity`.",
          "type": "object"
        },
        "title": {
          "description": "Super-title for the figure.",
          "type": "string"
        }
      },
      "required": [
        "sensitivity_result"
      ],
      "type": "object"
    },
    "name": "synth_sensitivity_plot"
  },
  {
    "description": "Synthetic Survival Control (Han & Shah 2025, arXiv:2511.14133). Fits a convex combination of donor Kaplan-Meier curves on the complementary log-log scale to match the treated arm's pre-treatment survival, then reports the post-treatment survival gap with placebo UCBs. Validation: validated. Assumptions: A convex (or regularized) combination of donor units reproduces the treated unit's pre-treatment outcome path; No interference: the treatment does not affect the donor units (SUTVA); No anticipation before the treatment date. Pre-conditions: Panel of one or more treated units plus an untreated donor pool, observed over time; Pre-treatment window long enough to fit donor weights (rule of thumb: more pre-periods than donors used); Outcome observed for every unit in every period. Failure modes: Large pre-treatment RMSPE \u2014 the synthetic unit fails to track the treated unit before treatment \u2192 Add donors / predictors, lengthen the pre-period, or use a bias-corrected estimator (sdid, augsynth).; Placebo / permutation inference shows the estimate is not extreme relative to donors \u2192 Report the placebo distribution honestly; the effect may not be distinguishable from noise.; Pre-treatment RM\u2026",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "n_placebos": {
          "default": 100,
          "description": "Number of placebos.",
          "type": "integer"
        },
        "seed": {
          "default": 0,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "survival": {
          "description": "Column with survival probability S_i(t)",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter (float).",
          "type": "number"
        },
        "treated": {
          "description": "Boolean column or name of the single treated unit",
          "type": "string"
        },
        "unit": {
          "description": "Unit/panel-id column",
          "type": "string"
        }
      },
      "required": [
        "survival",
        "time",
        "treat_time",
        "treated",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_survival"
  },
  {
    "description": "Time-placebo (\"backdating\") test for Synthetic Control.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "n_placebo_times": {
          "description": "Max number of placebo treatment times to try. Default is all feasible pre-treatment times (leaving >= 2 pre-periods for each placebo fit).",
          "type": "integer"
        },
        "outcome": {
          "description": "Outcome variable.",
          "type": "string"
        },
        "penalization": {
          "default": 0.0,
          "description": "Ridge penalty forwarded to SCM.",
          "type": "number"
        },
        "time": {
          "description": "Time column.",
          "type": "string"
        },
        "treated_unit": {
          "description": "Identifier of the treated unit.",
          "type": "string"
        },
        "treatment_time": {
          "description": "Real first treatment period.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        }
      },
      "required": [
        "outcome",
        "time",
        "treated_unit",
        "treatment_time",
        "unit"
      ],
      "type": "object"
    },
    "name": "synth_time_placebo"
  },
  {
    "description": "Multi-sheet Excel workbook for synthetic-control results.",
    "input_schema": {
      "properties": {
        "digits": {
          "default": 6,
          "description": "Rounding for floating-point values.",
          "type": "integer"
        },
        "method_names": {
          "description": "Override sheet / column labels.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "obj": {
          "description": "Object to export.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "path": {
          "description": "Destination ``.xlsx`` file path.",
          "type": "string"
        }
      },
      "required": [
        "obj",
        "path"
      ],
      "type": "object"
    },
    "name": "synth_to_excel"
  },
  {
    "description": "Publication-grade LaTeX table for synthetic-control results.",
    "input_schema": {
      "properties": {
        "booktabs": {
          "default": true,
          "description": "If True, use ``\\toprule`` / ``\\midrule`` / ``\\bottomrule`` (requires ``\\usepackage{booktabs}``). Falls back to ``\\hline`` if False.",
          "type": "boolean"
        },
        "caption": {
          "description": "Table caption. Defaults to a sensible auto-generated string.",
          "type": "string"
        },
        "digits": {
          "default": 4,
          "description": "Number of decimal places.",
          "type": "integer"
        },
        "label": {
          "description": "LaTeX label for cross-referencing. Defaults to ``\"tab:synth\"`` (single) or ``\"tab:synth_compare\"`` (multi).",
          "type": "string"
        },
        "method_names": {
          "description": "Override column labels in comparison mode.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "obj": {
          "description": "Object to render. ``SynthComparison`` and lists trigger the side-by-side multi-method layout.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "show_ci": {
          "default": true,
          "description": "Include the confidence-interval row.",
          "type": "boolean"
        },
        "show_weights": {
          "default": false,
          "description": "Append a panel listing the top-N donor weights.",
          "type": "boolean"
        },
        "top_n_weights": {
          "default": 5,
          "description": "How many donors to show per method when ``show_weights=True``.",
          "type": "integer"
        }
      },
      "required": [
        "obj"
      ],
      "type": "object"
    },
    "name": "synth_to_latex"
  },
  {
    "description": "GitHub-flavoured Markdown table for synthetic-control results.",
    "input_schema": {
      "properties": {
        "digits": {
          "default": 4,
          "description": "digits parameter (int).",
          "type": "integer"
        },
        "method_names": {
          "description": "method_names parameter (Optional[Sequence[str]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "obj": {
          "description": "obj parameter (Union[CausalResult, 'SynthComparison', List[CausalResult]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "show_ci": {
          "default": true,
          "description": "show_ci parameter (bool).",
          "type": "boolean"
        },
        "show_weights": {
          "default": false,
          "description": "show_weights parameter (bool).",
          "type": "boolean"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "top_n_weights": {
          "default": 5,
          "description": "top_n_weights parameter (int).",
          "type": "integer"
        }
      },
      "required": [
        "obj"
      ],
      "type": "object"
    },
    "name": "synth_to_markdown"
  },
  {
    "description": "R-style alias: ``synthdid::synthdid_estimate``. Assumptions: Parallel trends in the absence of treatment, after the synthetic/DiD weighting; No anticipation and no interference between units (SUTVA); The control pool's outcome process is stable around the intervention. Pre-conditions: Panel with treated and control units and a clear treatment date; Pre-treatment periods available to assess comparability of trends; panel data in long form (unit \u00d7 time \u00d7 outcome). Failure modes: Weighted pre-treatment trends still diverge between treated and synthetic control \u2192 Inspect the unit/time weights and pre-trend fit; consider event-study DiD with honest bounds.; Pre-treatment RMSPE > post-treatment effect \u2192 Poor pre-fit \u2014 switch to method='demeaned'/'augmented' or enlarge donor pool.; Placebo p-value \u2265 0.1 despite visible gap \u2192 Use inference='conformal' (valid under weak assumptions) or report ranked placebo statistic.. Alternatives: sp.synth, sp.augsynth, sp.callaway_santanna, sp.gardner_did. Typical minimum N: 15.",
    "input_schema": {
      "properties": {
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat_time",
        "treat_unit",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "synthdid_estimate"
  },
  {
    "description": "Run placebo estimates assigning treatment to each control unit.",
    "input_schema": {
      "properties": {
        "method": {
          "default": "sdid",
          "description": "Estimator or algorithm variant to use.",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "treat_time": {
          "description": "treat_time parameter.",
          "type": "string"
        },
        "treat_unit": {
          "description": "treat_unit parameter.",
          "type": "string"
        },
        "unit": {
          "description": "Unit identifier column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "time",
        "treat_time",
        "treat_unit",
        "unit",
        "y"
      ],
      "type": "object"
    },
    "name": "synthdid_placebo"
  },
  {
    "description": "Plot observed vs synthetic trajectory.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "ci_alpha": {
          "default": 0.15,
          "description": "ci_alpha parameter (float).",
          "type": "number"
        },
        "figsize": {
          "default": [
            10,
            6
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Output of :func:`sdid`.",
          "type": "string"
        },
        "synth_color": {
          "default": "#E74C3C",
          "description": "synth_color parameter (str).",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "treated_color": {
          "default": "#2C3E50",
          "description": "treated_color parameter (str).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "synthdid_plot"
  },
  {
    "description": "Pre-treatment RMSE of treated vs synthetic trajectory.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "result parameter (CausalResult).",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "synthdid_rmse_plot"
  },
  {
    "description": "Horizontal bar chart of unit weight contributions.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "figsize": {
          "default": [
            8,
            5
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "result parameter (CausalResult).",
          "type": "string"
        },
        "top_n": {
          "default": 10,
          "description": "Show the top-N donors by weight.",
          "type": "integer"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "synthdid_units_plot"
  },
  {
    "description": "Unified plot function for all Synthetic Control variants.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "Pre-existing axes for single-panel plots.",
          "type": "string"
        },
        "figsize": {
          "description": "Figure size. Auto-selected if None.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "labels": {
          "description": "Labels for ``type='compare'``. Additional arguments passed to individual plotters. Notable: * ``pre_band=True`` \u2014 for ``type='trajectory'`` / ``'gap'`` / ``'both'``: overlay a \u00b11.96 \u00d7 pre-RMSPE noise envelope. * ``pi_band=True`` \u2014 for ``type='trajectory'``: overlay the prediction-interval / conformal CI ribbon around the synthetic counterfactual when the result carries one (``sp.scpi`` / ``sp.conformal_synth``).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "result": {
          "description": "Output of any ``synth()`` variant. Pass a list for ``type='compare'``.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "Override the auto-generated title.",
          "type": "string"
        },
        "top_n": {
          "default": 15,
          "description": "Number of donors to show in weight plots.",
          "type": "integer"
        },
        "type": {
          "default": "trajectory",
          "description": "Plot type: * ``'trajectory'`` \u2014 treated vs synthetic over time. * ``'gap'`` \u2014 effect (gap) over time. * ``'both'`` \u2014 two-panel: trajectory + gap. * ``'weights'`` \u2014 donor weight bar chart. * ``'placebo'`` \u2014 placebo ATT distribution. * ``'placebo_gap'`` \u2014 placebo gap spaghetti plot (Abadie et al. 2010). * ``'rmspe'`` \u2014 post/pre RMSPE ratio histogram (Abadie et al. 2010). * ``'conformal'`` \u2014 period-level effects + conformal CIs. * ``'staggered'`` \u2014 cohort-level ATT comparison. * ``'factors'`` \u2014 latent factor loadings (gsynth only). * ``'compare'`` \u2014 overlay multiple results.",
          "type": "string"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "synthplot"
  },
  {
    "description": "tF adjusted critical value (Lee, McCrary, Moreira & Porter 2022, AER).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "first_stage_F": {
          "description": "first_stage_F parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "first_stage_F"
      ],
      "type": "object"
    },
    "name": "tF_adjustment"
  },
  {
    "description": "Lee\u2013McCrary\u2013Moreira\u2013Porter (2022, AER) tF adjusted critical value.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level. Only ``0.05`` is implemented (the only level for which LMMP publish a complete table).",
          "type": "number"
        },
        "first_stage_F": {
          "description": "Observed first-stage F statistic (or Olea-Pflueger F_eff).",
          "type": "number"
        }
      },
      "required": [
        "first_stage_F"
      ],
      "type": "object"
    },
    "name": "tF_critical_value"
  },
  {
    "description": "Wald test for linear restrictions on coefficients.",
    "input_schema": {
      "properties": {
        "hypothesis": {
          "description": "Hypothesis specification. Examples: - ``\"x1 = 0\"`` \u2014 test if beta_x1 = 0 - ``\"x1 = x2\"`` \u2014 test if beta_x1 = beta_x2 - ``\"x1 = x2 = 0\"`` \u2014 joint test - ``\"x1 + x2 = 1\"`` \u2014 linear restriction",
          "type": "string"
        },
        "result": {
          "description": "Fitted model with ``.params`` and ``.std_errors``.",
          "type": "string"
        }
      },
      "required": [
        "hypothesis",
        "result"
      ],
      "type": "object"
    },
    "name": "test"
  },
  {
    "description": "BLP-of-CATE calibration test (Chernozhukov-Demirer-Duflo-Fernandez-Val 2020). Assumptions: Unconfoundedness given the covariates; Overlap / positivity; Honesty: separate subsamples are used to choose splits and to estimate effects. Pre-conditions: Covariates, treatment, and outcome with enough data to grow an honest forest. Failure modes: Calibration test rejects \u2014 the forest's heterogeneity is not well calibrated \u2192 Increase the sample / number of trees, or fall back to a doubly-robust learner.. Alternatives: sp.dml, sp.auto_cate, sp.tmle. Typical minimum N: 1000.",
    "input_schema": {
      "properties": {
        "T": {
          "description": "If not given, the forest's stored training arrays are used.",
          "type": "string"
        },
        "X": {
          "description": "If not given, the forest's stored training arrays are used.",
          "type": "string"
        },
        "Y": {
          "description": "If not given, the forest's stored training arrays are used.",
          "type": "string"
        },
        "alpha": {
          "default": 0.05,
          "description": "Significance level for reported CIs.",
          "type": "number"
        },
        "forest": {
          "description": "forest parameter ('CausalForest').",
          "type": "string"
        }
      },
      "required": [
        "forest"
      ],
      "type": "object"
    },
    "name": "test_calibration"
  },
  {
    "description": "Three-Stage Least Squares (3SLS).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "equations": {
          "description": "equations parameter (Dict[str, Tuple[str, List[str], List[str]]]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instruments": {
          "description": "Full set of instruments (all exogenous variables in the system).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 100,
          "description": "maxiter parameter (int).",
          "type": "integer"
        }
      },
      "required": [
        "equations"
      ],
      "type": "object"
    },
    "name": "three_sls"
  },
  {
    "description": "Targeted Maximum Likelihood Estimation for ATE/ATT with double-robustness. Validation: validated. Assumptions: Unconfoundedness: Y(d) \u22a5 D | X; Overlap: 0 < P(D=1 | X) < 1 on the estimand support; Consistent estimation of at least one of Q(a, x) = E[Y|A, X] or g(x) = P(A=1|X) (double robustness). Pre-conditions: binary treatment 0/1; covariates comprise the confounding set; n \u2265 500 for asymptotic efficiency. Failure modes: Extreme propensity scores (ATE IF denominator \u2248 0) \u2192 Bound propensity scores away from 0/1 (e.g. 0.025 / 0.975) or trim.; Super-learner cross-validated risk not improving over baseline \u2192 Nuisances not learnable; widen the candidate library or use stronger base learners.. Alternatives: sp.dml, sp.aipw, sp.metalearner, sp.ltmle. Typical minimum N: 500.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariate matrix, DataFrame, or column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "treatment": {
          "description": "Treatment indicator, treatment variable, or treatment array.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment",
        "y"
      ],
      "type": "object"
    },
    "name": "tmle"
  },
  {
    "description": "Tobit model for censored dependent variables. Validation: certified parity evidence. Assumptions: Latent outcome is linear in covariates with normally distributed errors; Censoring threshold is known and exogenous. Pre-conditions: Outcome censoring point and censoring direction are known; Covariates are numeric or properly encoded. Failure modes: MLE fails to converge or sigma is near zero \u2192 Rescale covariates, simplify the model, or compare with censored quantile alternatives.. Alternatives: sp.qreg, sp.regress. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "formula": {
          "description": "Model formula using patsy/R-style syntax.",
          "type": "string"
        },
        "lower": {
          "default": 0.0,
          "description": "Lower censoring point",
          "type": "number"
        },
        "upper": {
          "description": "Upper censoring point",
          "type": "number"
        }
      },
      "required": [
        "formula"
      ],
      "type": "object"
    },
    "name": "tobit"
  },
  {
    "description": "Visualise staggered treatment adoption timing.",
    "input_schema": {
      "properties": {
        "ax": {
          "description": "ax parameter.",
          "type": "string"
        },
        "figsize": {
          "default": [
            12,
            7
          ],
          "description": "figsize parameter (tuple).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unit identifier.",
          "type": "string"
        },
        "never_color": {
          "default": "#BDC3C7",
          "description": "Color for never-treated units.",
          "type": "string"
        },
        "show_cohort_labels": {
          "default": true,
          "description": "Annotate cohort boundaries on the y-axis.",
          "type": "boolean"
        },
        "sort_by": {
          "default": "treat_time",
          "description": "Sort units by: 'treat_time' (earliest first), 'id', or 'random'.",
          "type": "string"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "title": {
          "description": "title parameter (Optional[str]).",
          "type": "string"
        },
        "treat": {
          "description": "First-treatment-period column (0 = never treated), or binary treatment indicator.",
          "type": "string"
        },
        "treated_color": {
          "default": "#E74C3C",
          "description": "Color for treated unit-periods.",
          "type": "string"
        },
        "untreated_color": {
          "default": "#ECF0F1",
          "description": "Color for untreated unit-periods.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "time",
        "treat"
      ],
      "type": "object"
    },
    "name": "treatment_rollout_plot"
  },
  {
    "description": "Trim sample to optimal overlap region. Assumptions: Unconfoundedness: treatment is as-good-as-random given the measured covariates; Overlap / common support: every unit has a non-degenerate probability of each treatment; The covariate set blocks all back-door paths. Pre-conditions: Pre-treatment covariates measured for treated and control units; A binary (or low-cardinality) treatment indicator; Sufficient covariate overlap between treatment arms. Failure modes: Poor overlap \u2014 extreme propensity scores or few acceptable matches \u2192 Trim or restrict to the common-support region and report the discarded units.; Covariate imbalance remains after matching/weighting \u2192 Re-specify the balancing model (CBPS, entropy balancing) and re-check standardized mean differences.. Alternatives: sp.propensity_score, sp.cbps, sp.ebalance, sp.dml. Typical minimum N: 200.",
    "input_schema": {
      "properties": {
        "covariates": {
          "description": "Covariates for PS estimation (if *ps* not supplied).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "default": "crump",
          "description": "``'crump'`` uses Crump et al. (2009) optimal rule. ``'sturmer'`` trims at the fixed [0.1, 0.9] interval.",
          "enum": [
            "crump",
            "sturmer"
          ],
          "type": "string"
        },
        "ps": {
          "description": "Pre-estimated propensity scores. If None, estimated via *ps_method*.",
          "type": "string"
        },
        "ps_method": {
          "default": "logit",
          "description": "Method for PS estimation if *ps* is None.",
          "type": "string"
        },
        "treatment": {
          "description": "Binary treatment column.",
          "type": "string"
        }
      },
      "required": [
        "covariates",
        "treatment"
      ],
      "type": "object"
    },
    "name": "trimming"
  },
  {
    "description": "Truncated regression (MLE).",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "ll": {
          "description": "Lower truncation point. None = no lower truncation.",
          "type": "number"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Robust standard-error or covariance estimator option.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "ul": {
          "description": "Upper truncation point. None = no upper truncation.",
          "type": "number"
        },
        "x": {
          "description": "Regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "truncreg"
  },
  {
    "description": "TWFE decomposition: Goodman-Bacon (2021) + de Chaisemartin\u2013D'Haultfoeuille weights. Assumptions: Conditional parallel trends between treated and comparison groups absent treatment; No anticipation of treatment before its onset; Treatment effects may be heterogeneous across cohorts and time (no homogeneity required). Pre-conditions: Panel or repeated cross-section with a unit (or group) identifier and a time identifier; At least one never-treated or not-yet-treated comparison group; Pre-treatment periods to assess parallel trends. Failure modes: Pre-treatment event-study coefficients are jointly non-zero (pre-trend violation) \u2192 Use honest DiD bounds to quantify robustness to trend violations, or condition on covariates.; Two-way fixed-effects estimate is contaminated by 'forbidden' comparisons / negative weights \u2192 Use a heterogeneity-robust estimator (Callaway-Sant'Anna, Borusyak et al., Gardner two-stage).. Alternatives: sp.callaway_santanna, sp.did, sp.honest_did. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "first_treat": {
          "description": "Treatment timing column (NaN or 0 for never-treated).",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier.",
          "type": "string"
        },
        "time": {
          "description": "Time period variable.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "twfe_decomposition"
  },
  {
    "description": "Compute two-way cluster-robust standard errors.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster1": {
          "description": "Column name for the first clustering dimension.",
          "type": "string"
        },
        "cluster2": {
          "description": "Column name for the second clustering dimension.",
          "type": "string"
        },
        "result": {
          "description": "Fitted OLS result. Must have ``data_info`` containing ``'X'`` (design matrix), ``'y'`` (response), and ``'residuals'``.",
          "type": "string"
        }
      },
      "required": [
        "cluster1",
        "cluster2",
        "result"
      ],
      "type": "object"
    },
    "name": "twoway_cluster"
  },
  {
    "description": "Run every applicable sensitivity analysis in one shot: E-value, Oster delta (when R^2 inputs given), Rosenbaum Gamma (when matched structure exposed), Sensemakr (regression models), and a breakdown-frontier bias estimate. Also available as result.sensitivity(). Validation: validated. Assumptions: Dashboard that dispatches every applicable sensitivity method (E-value, Cinelli-Hazlett RV, Oster's delta) to one fitted result; Oster's delta and rho_max require both short- and long-regression R-squared (r2_treated, r2_controlled); rho_max bounds omitted-to-observed selection; Each component inherits its own identifying assumptions (linear OVB for RV/Oster, RR scale for E-value); the panel does not reconcile conflicting ones. Pre-conditions: A result with point estimate, SE, and CI attributes; r2_treated and r2_controlled for the Oster's-delta component. Failure modes: Result lacks estimate/se/ci, so no sensitivity component can be computed \u2192 Pass a fitted result exposing estimate, se, and ci rather than a raw scalar or DataFrame.; Oster's delta omitted because r2_treated / r2_controlled were not provided \u2192 Pass the short- and long-regression R-squared values to enable the Oster compone\u2026",
    "input_schema": {
      "properties": {
        "include_oster": {
          "default": true,
          "description": "Whether to include oster.",
          "type": "boolean"
        },
        "include_rosenbaum": {
          "default": true,
          "description": "Whether to include rosenbaum.",
          "type": "boolean"
        },
        "include_sensemakr": {
          "default": true,
          "description": "Whether to include sensemakr.",
          "type": "boolean"
        },
        "r2_controlled": {
          "description": "r2_controlled parameter (float).",
          "type": "number"
        },
        "r2_treated": {
          "description": "r2_treated parameter (float).",
          "type": "number"
        },
        "result": {
          "description": "result parameter (CausalResult | EconometricResults).",
          "type": "string"
        },
        "rho_max": {
          "default": 1.0,
          "description": "rho_max parameter (float).",
          "type": "number"
        }
      },
      "required": [
        "result"
      ],
      "type": "object"
    },
    "name": "unified_sensitivity"
  },
  {
    "description": "Estimate a Vector Autoregression (VAR) model. Validation: certified parity evidence. Assumptions: All system variables are jointly covariance-stationary (no unit roots / cointegration left unmodeled); Lag order p is adequate so residuals are white noise; under-fitting biases IRFs and Granger tests; Reduced-form errors are serially uncorrelated; structural IRF identification relies on the chosen ordering/recursive scheme. Pre-conditions: Multivariate (>=2 column) time-ordered DataFrame of comparable-frequency series; Sample length large relative to k*p+trend parameters to estimate each equation. Failure modes: Coefficient covariance is singular or IRFs diverge because k^2*p parameters exceed available observations \u2192 Reduce lags, drop variables, or extend the sample so n is much larger than k*p; Explosive IRFs / non-decaying responses from a non-stationary (unit-root or trending) system \u2192 Difference or detrend the series first, or use trend='ct', and confirm stationarity before fitting. Alternatives: sp.local_projections, sp.arima, sp.iv. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level.",
          "type": "number"
        },
        "lags": {
          "default": 1,
          "description": "Number of lags.",
          "type": "integer"
        },
        "trend": {
          "default": "c",
          "description": "trend parameter (str).",
          "type": "string"
        },
        "variables": {
          "description": "Variable names. If None, uses all numeric columns.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "var"
  },
  {
    "description": "Empirically verify a single recommendation.",
    "input_schema": {
      "properties": {
        "B": {
          "default": 50,
          "description": "Number of bootstrap replications (auto-reduced if over budget).",
          "type": "integer"
        },
        "K_subsample": {
          "default": 5,
          "description": "Number of 50% subsample splits.",
          "type": "integer"
        },
        "budget_s": {
          "default": 30.0,
          "description": "Wall-clock budget per recommendation (seconds).",
          "type": "number"
        },
        "n_placebo": {
          "default": 5,
          "description": "Number of permutation placebo runs.",
          "type": "integer"
        },
        "rec": {
          "description": "A single entry from ``RecommendationResult.recommendations``.",
          "type": "object"
        },
        "seed": {
          "default": 42,
          "description": "RNG seed for reproducibility.",
          "type": "integer"
        }
      },
      "required": [
        "rec"
      ],
      "type": "object"
    },
    "name": "verify"
  },
  {
    "description": "Run verify against built-in DGPs with known true effects.",
    "input_schema": {
      "properties": {
        "n_reps": {
          "default": 3,
          "description": "Number of independent DGP draws per scenario. Each uses a different seed to average out Monte Carlo noise.",
          "type": "integer"
        },
        "scenarios": {
          "description": "Subset of {'rct', 'did', 'staggered_did', 'rd', 'iv', 'observational'}. Defaults to all six.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "seed": {
          "default": 0,
          "description": "Base seed. Per-rep seeds are ``seed + offset``.",
          "type": "integer"
        },
        "verbose": {
          "default": true,
          "description": "Print per-scenario progress.",
          "type": "boolean"
        },
        "verify_B": {
          "default": 30,
          "description": "Bootstrap replications per verification run.",
          "type": "integer"
        },
        "verify_budget_s": {
          "default": 15.0,
          "description": "Wall-clock budget per verification run.",
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "verify_benchmark"
  },
  {
    "description": "Empirically verify a single recommendation.",
    "input_schema": {
      "properties": {
        "B": {
          "default": 50,
          "description": "Number of bootstrap replications (auto-reduced if over budget).",
          "type": "integer"
        },
        "K_subsample": {
          "default": 5,
          "description": "Number of 50% subsample splits.",
          "type": "integer"
        },
        "budget_s": {
          "default": 30.0,
          "description": "Wall-clock budget per recommendation (seconds).",
          "type": "number"
        },
        "n_placebo": {
          "default": 5,
          "description": "Number of permutation placebo runs.",
          "type": "integer"
        },
        "rec": {
          "description": "A single entry from ``RecommendationResult.recommendations``.",
          "type": "object"
        },
        "seed": {
          "default": 42,
          "description": "RNG seed for reproducibility.",
          "type": "integer"
        }
      },
      "required": [
        "rec"
      ],
      "type": "object"
    },
    "name": "verify_recommendation"
  },
  {
    "description": "Variance Inflation Factors for multicollinearity detection.",
    "input_schema": {
      "properties": {
        "x": {
          "description": "Independent variables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "x"
      ],
      "type": "object"
    },
    "name": "vif"
  },
  {
    "description": "Stata-style unified weak-instrument-robust diagnostic panel.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for the robust confidence sets.",
          "type": "number"
        },
        "clr_simulations": {
          "default": 20000,
          "description": "Monte-Carlo draws for the CLR null distribution.",
          "type": "integer"
        },
        "endog": {
          "description": "Outcome and single endogenous regressor (column names in ``data``).",
          "type": "string"
        },
        "exog": {
          "description": "Included exogenous controls. An intercept is always added.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "grid_size": {
          "default": 401,
          "description": "Grid resolution used by AR/CLR/K confidence-set inversion.",
          "type": "integer"
        },
        "h0": {
          "default": 0.0,
          "description": "Null value for the endogenous coefficient. All under-H0 tests (AR, CLR, K) are evaluated at ``\u03b2 = h0``.",
          "type": "number"
        },
        "include_clr": {
          "default": true,
          "description": "Also run the CLR test and invert it for a CLR confidence set.",
          "type": "boolean"
        },
        "include_k": {
          "default": true,
          "description": "Also run the Kleibergen K score test and K confidence set.",
          "type": "boolean"
        },
        "instruments": {
          "description": "Excluded instruments.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "random_state": {
          "description": "Random seed or RandomState for reproducible stochastic steps.",
          "type": "integer"
        },
        "vcov": {
          "default": "HC1",
          "description": "Used by the Olea\u2013Pflueger effective F.",
          "enum": [
            "HC0",
            "HC1",
            "classic"
          ],
          "type": "string"
        },
        "y": {
          "description": "Outcome and single endogenous regressor (column names in ``data``).",
          "type": "string"
        }
      },
      "required": [
        "endog",
        "instruments",
        "y"
      ],
      "type": "object"
    },
    "name": "weakrobust"
  },
  {
    "description": "Wild cluster bootstrap t-test for a single coefficient.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence interval.",
          "type": "number"
        },
        "cluster": {
          "description": "Name of the cluster variable.",
          "type": "string"
        },
        "n_boot": {
          "default": 999,
          "description": "Number of bootstrap replications (use odd number).",
          "type": "integer"
        },
        "result": {
          "description": "A fitted regression result from ``sp.regress()``.",
          "type": "string"
        },
        "seed": {
          "description": "Random seed for reproducibility.",
          "type": "integer"
        },
        "variable": {
          "description": "Name of the coefficient to test (H0: beta = 0).",
          "type": "string"
        },
        "weight_type": {
          "default": "rademacher",
          "description": "Bootstrap weight distribution: - ``'rademacher'``: +/-1 with equal probability. - ``'webb'``: Webb (2014) 6-point distribution for G < 12. - ``'mammen'``: Mammen (1993) 2-point distribution.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "result",
        "variable"
      ],
      "type": "object"
    },
    "name": "wild_cluster_boot"
  },
  {
    "description": "Cameron-Gelbach-Miller (2008) wild cluster bootstrap \u2014 the canonical fix for cluster-robust inference with few clusters (G < 30).  Re-samples cluster-level Rademacher weights to construct a percentile-t reference distribution that has correct size when the standard cluster-robust z-test rejects too often. Validation: validated. Assumptions: Errors are exchangeable within clusters (Rademacher weights are robust to most departures); Number of clusters G >= 5 for finite-sample validity. Pre-conditions: long-format dataset with a cluster identifier present; treatment / test variable varies within at least some clusters; test_var (or first column of x) is the coefficient under test. Failure modes: Multi-way clustering requested \u2192 Multi-way wild cluster bootstrap is not yet supported; see sp.subcluster_wild_bootstrap or use cr2_se for two-way.; G < 5 clusters \u2192 Wild cluster bootstrap is unreliable below ~5 clusters; consider permutation tests (sp.ri_test).. Alternatives: sp.cr2_se, sp.subcluster_wild_bootstrap, sp.ri_test. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier",
          "type": "string"
        },
        "h0": {
          "default": 0.0,
          "description": "Null value of the coefficient",
          "type": "number"
        },
        "n_boot": {
          "default": 999,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "test_var": {
          "description": "Variable being tested; defaults to first in x",
          "type": "string"
        },
        "weight_type": {
          "default": "rademacher",
          "description": "Bootstrap weight distribution",
          "enum": [
            "rademacher",
            "mammen",
            "webb",
            "normal"
          ],
          "type": "string"
        },
        "x": {
          "description": "Right-hand-side variables",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "wild_cluster_bootstrap"
  },
  {
    "description": "Confidence interval via bootstrap p-value inversion.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "grid_size": {
          "default": 41,
          "description": "Number of null-value grid points to evaluate (odd preferred).",
          "type": "integer"
        },
        "grid_span": {
          "default": 6.0,
          "description": "Half-width of the search grid in units of cluster-robust SE.",
          "type": "number"
        },
        "n_boot": {
          "default": 999,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "test_var": {
          "description": "test_var parameter (Optional[str]).",
          "type": "string"
        },
        "weight_type": {
          "default": "webb",
          "description": "weight_type parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "wild_cluster_ci_inv"
  },
  {
    "description": "Wooldridge (2021) extended TWFE (ETWFE). Saturated TWFE regression with cohort \u00d7 post interactions; recovers cohort-specific ATTs. Numerically equivalent to CS / SA / BJS under the saturated specification. Validation: certified parity evidence. Assumptions: Parallel trends per cohort; No anticipation; SUTVA. Pre-conditions: panel with unit \u00d7 time \u00d7 outcome; first_treat cohort column (first period treated, 0 = never). Failure modes: Singleton cohorts with one unit \u2192 Aggregate small cohorts or drop them.. Alternatives: sp.callaway_santanna, sp.sun_abraham, sp.did_imputation, sp.etwfe. Typical minimum N: 50.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster identifier column for clustered standard errors.",
          "type": "string"
        },
        "controls": {
          "description": "Control-variable column names.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "first_treat": {
          "description": "First-treatment period; 0 = never-treated",
          "type": "string"
        },
        "group": {
          "description": "Unit identifier",
          "type": "string"
        },
        "time": {
          "description": "Time period column.",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "first_treat",
        "group",
        "time",
        "y"
      ],
      "type": "object"
    },
    "name": "wooldridge_did"
  },
  {
    "description": "X-Learner CATE \u2014 article alias for :func:`metalearner(learner='x')`.",
    "input_schema": {
      "properties": {
        "X": {
          "description": "Feature matrix or covariate DataFrame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "d": {
          "description": "d parameter (str).",
          "type": "string"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "X",
        "d",
        "y"
      ],
      "type": "object"
    },
    "name": "xlearner"
  },
  {
    "description": "Arellano-Bond / Blundell-Bond GMM for dynamic panels (standalone). Validation: validated. Assumptions: No second-order serial correlation in differenced errors; Internal instruments are valid and not too numerous. Pre-conditions: Panel data include unit, time, outcome, and lagged dependent variable structure; Number of time periods is moderate relative to units. Failure modes: Instrument proliferation or AR(2) test rejects \u2192 Collapse instruments, reduce lag depth, or compare with fixed-effects estimates.. Alternatives: sp.panel, sp.feols. Typical minimum N: 100.",
    "input_schema": {
      "properties": {
        "id": {
          "default": "id",
          "description": "Unit identifier",
          "type": "string"
        },
        "lags": {
          "default": 1,
          "description": "lags parameter (int).",
          "type": "integer"
        },
        "method": {
          "default": "difference",
          "description": "difference or system",
          "enum": [
            "difference",
            "system"
          ],
          "type": "string"
        },
        "time": {
          "default": "time",
          "description": "Time column",
          "type": "string"
        },
        "twostep": {
          "default": false,
          "description": "twostep parameter (bool).",
          "type": "boolean"
        },
        "x": {
          "description": "Exogenous regressors",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable",
          "type": "string"
        }
      },
      "required": [
        "y"
      ],
      "type": "object"
    },
    "name": "xtabond"
  },
  {
    "description": "Fit panel negative-binomial regression. Use model='fe' for explicit entity fixed effects via nbreg, or model='re' for a random-intercept NB-2 GLMM via menbreg. Do not use feols for negative-binomial outcomes.",
    "input_schema": {
      "properties": {
        "cluster": {
          "description": "Cluster variable; defaults to entity for model=\"fe\".",
          "type": "string"
        },
        "entity": {
          "description": "Panel/unit identifier column.",
          "type": "string"
        },
        "exposure": {
          "type": "string"
        },
        "formula": {
          "description": "Formula such as 'count ~ x1 + x2'.",
          "type": "string"
        },
        "irr": {
          "default": false,
          "type": "boolean"
        },
        "model": {
          "default": "fe",
          "enum": [
            "fe",
            "re",
            "pooled"
          ],
          "type": "string"
        },
        "offset": {
          "type": "string"
        },
        "time": {
          "description": "Optional time column.",
          "type": "string"
        },
        "time_effects": {
          "default": false,
          "description": "Include time dummies in the fixed-effects model.",
          "type": "boolean"
        }
      },
      "required": [
        "formula",
        "entity"
      ],
      "type": "object"
    },
    "name": "xtnbreg"
  },
  {
    "description": "Nonparametric causal decomposition of a group disparity.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Two-sided coverage level.",
          "type": "number"
        },
        "cluster": {
          "description": "Column name to use for cluster bootstrap.",
          "type": "string"
        },
        "group": {
          "description": "Binary group indicator (0/1) \u2014 ``1`` = advantaged / index group.",
          "type": "string"
        },
        "inference": {
          "default": "bootstrap",
          "description": "``\"bootstrap\"`` returns SEs and percentile CIs from the non-parametric (cluster-aware) bootstrap. ``\"none\"`` skips inference.",
          "type": "string"
        },
        "method": {
          "default": "plugin",
          "description": "``\"plugin\"`` uses within-cell OLS for outcomes and within-group logit for the propensity and computes plug-in expectations (Yu-Elwert 2025, Section 4.1). ``\"efficient\"`` augments each moment with the doubly-robust correction term \u2014 recommended when nuisance functions might be misspecified.",
          "enum": [
            "plugin",
            "efficient"
          ],
          "type": "string"
        },
        "n_boot": {
          "default": 499,
          "description": "Number of bootstrap replications.",
          "type": "integer"
        },
        "seed": {
          "default": 12345,
          "description": "Random seed for reproducible stochastic steps.",
          "type": "integer"
        },
        "treatment": {
          "description": "Binary treatment indicator (0/1).",
          "type": "string"
        },
        "trim": {
          "default": 0.005,
          "description": "Lower/upper clip for fitted propensities (only used in ``method=\"efficient\"``).",
          "type": "number"
        },
        "x": {
          "description": "Adjustment covariates (used to identify within-group CATEs).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Name of the (continuous) outcome column.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "treatment",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "yu_elwert_decompose"
  },
  {
    "description": "Bauer-Sinning (2008) nonlinear Oaxaca-Blinder decomposition with",
    "input_schema": {
      "properties": {
        "group": {
          "description": "Group or cohort identifier.",
          "type": "string"
        },
        "model": {
          "default": "logit",
          "description": "Model variant or parameterisation to fit.",
          "enum": [
            "logit",
            "probit"
          ],
          "type": "string"
        },
        "reference": {
          "default": 0,
          "description": "reference parameter (int).",
          "enum": [
            "0",
            "1"
          ],
          "type": "integer"
        },
        "variant": {
          "default": "yun",
          "description": "variant parameter (str).",
          "type": "string"
        },
        "x": {
          "description": "Primary running variable, regressor, or feature input for this estimator.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Outcome variable column name or outcome array.",
          "type": "string"
        }
      },
      "required": [
        "group",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "yun_nonlinear"
  },
  {
    "description": "Zero-Inflated Negative Binomial (ZINB) regression via MLE.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals and tests.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name.",
          "type": "string"
        },
        "formula": {
          "description": "Patsy-style formula for the count equation.",
          "type": "string"
        },
        "inflate": {
          "description": "Inflation-equation regressors. Default: same as count regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 200,
          "description": "maxiter parameter (int).",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "Standard error type.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Numerical convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Count-equation regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "zinb"
  },
  {
    "description": "Zero-Inflated Poisson (ZIP) regression via MLE.",
    "input_schema": {
      "properties": {
        "alpha": {
          "default": 0.05,
          "description": "Significance level for confidence intervals.",
          "type": "number"
        },
        "cluster": {
          "description": "Cluster variable name for clustered standard errors.",
          "type": "string"
        },
        "formula": {
          "description": "Patsy-style formula for the count equation, e.g. \"y ~ x1 + x2\".",
          "type": "string"
        },
        "inflate": {
          "description": "Inflation-equation regressors. Default: same as count regressors.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxiter": {
          "default": 200,
          "description": "Maximum iterations for optimizer.",
          "type": "integer"
        },
        "robust": {
          "default": "nonrobust",
          "description": "\"nonrobust\", \"HC0\", \"HC1\", etc.",
          "type": "string"
        },
        "tol": {
          "default": 1e-08,
          "description": "Convergence tolerance.",
          "type": "number"
        },
        "x": {
          "description": "Count-equation regressors (alternative to formula).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "y": {
          "description": "Dependent variable name (alternative to formula).",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    },
    "name": "zip_model"
  }
]
